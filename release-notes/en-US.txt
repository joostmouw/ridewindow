Fix: the app could quietly crash after you restarted your phone. It
pointed at code that did not exist, and that is gone now. Feel free to
reboot once.

Otherwise the same as build 45: your day as one block on Home, shared
rides with several windows to vote on, and the one-time question about
anonymous usage statistics at the second start.
