---
phase: 09-google-calendar-integration
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - test/services/calendar_service_test.dart
  - test/features/detail/ride_detail_screen_calendar_test.dart
autonomous: true
requirements:
  - CAL-02
  - CAL-03
  - PERS-04

must_haves:
  truths:
    - "Unit tests bewijzen dat CalendarService de juiste weersamenvatting opbouwt (CAL-03)"
    - "Unit tests bewijzen dat CalendarService een Exception gooit wanneer signIn() null teruggeeft (gebruiker annuleert)"
    - "Widget test bewijst dat CircularProgressIndicator zichtbaar is terwijl de CalendarService actief is"
    - "PERS-04 privacy test bewijst dat CalendarApi.events.insert NOOIT wordt aangeroepen tenzij gebruiker expliciet getikt heeft"
    - "Alle tests slagen met flutter test"
  artifacts:
    - path: "test/services/calendar_service_test.dart"
      provides: "Unit tests voor CalendarService: weersamenvatting-logica, annuleer-pad, succespad"
      contains: "calendar_service_test"
    - path: "test/features/detail/ride_detail_screen_calendar_test.dart"
      provides: "Widget tests voor 'Toevoegen aan agenda' knop: laadstatus, PERS-04 privacy guard"
      contains: "ride_detail_screen_calendar_test"
  key_links:
    - from: "test/services/calendar_service_test.dart"
      to: "lib/services/calendar_service.dart"
      via: "directe import + mock van GoogleSignIn"
      pattern: "CalendarService"
    - from: "test/features/detail/ride_detail_screen_calendar_test.dart"
      to: "lib/features/detail/ride_detail_screen.dart"
      via: "WidgetTester pump + finder"
      pattern: "RideDetailScreen"
---

<objective>
Voeg unit tests en widget tests toe die de CalendarService-logica en de PERS-04 privacygarantie afdwingen.

Purpose: Bewijzen dat (1) de weersamenvatting correct wordt gebouwd, (2) de annuleer-flow geen CalendarApi aanroepen doet, en (3) de UI de juiste laadstatus toont. De PERS-04 eis — "data verlaat het apparaat niet tenzij de gebruiker expliciet toestemming geeft" — wordt aantoonbaar getest.

Output: test/services/calendar_service_test.dart (unit tests voor CalendarService), test/features/detail/ride_detail_screen_calendar_test.dart (widget tests voor de knop en PERS-04).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-google-calendar-integration/09-01-SUMMARY.md
</context>

<interfaces>
Geextraheerd uit lib/services/calendar_service.dart (aangemaakt in plan 09-01):

  class CalendarService {
    // Privé static GoogleSignIn instantie met CalendarApi.calendarEventsScope
    static final _googleSignIn = GoogleSignIn(scopes: [CalendarApi.calendarEventsScope]);

    // Bouwt één-regel weersamenvatting van forecast-data
    // "~18°C, droog, 12km/u wind" — of "Geen weerdata beschikbaar" als forecasts leeg
    static String buildWeatherSummary(List<HourlyForecast> forecasts);  // eventueel publiek gemaakt voor testbaarheid

    // Hoofdmethode: sign-in → authenticatedClient → CalendarApi → event insert
    Future<void> addRideSlotToCalendar(RideSlot slot, List<HourlyForecast> forecasts);
  }

Geextraheerd uit lib/features/detail/ride_detail_screen.dart (aangepast in plan 09-01):

  class RideDetailScreen extends StatefulWidget {
    final RideSlot slot;
    final List<HourlyForecast> forecasts;
    const RideDetailScreen({super.key, required this.slot, required this.forecasts});
  }

  class _RideDetailScreenState extends State<RideDetailScreen> {
    bool _isLoading = false;
    // onPressed roept CalendarService().addRideSlotToCalendar() aan
  }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: Unit tests voor CalendarService weersamenvatting en foutpaden</name>
  <files>test/services/calendar_service_test.dart</files>
  <read_first>
    - lib/services/calendar_service.dart (volledig lezen — begrijp de exacte methode-signaturen en weersamenvatting-logica)
    - lib/domain/models/hourly_forecast.dart (lezen voor velden: temperatureC, precipitationMm, windspeedKmh)
    - lib/domain/models/ride_slot.dart (lezen voor velden: start, end, tier)
  </read_first>
  <behavior>
    - Test 1 (CAL-03 happy path): twee HourlyForecast objecten met temperatureC 16.0 en 20.0, precipitationMm 0.0 en 0.0, windspeedKmh 10.0 en 14.0 → weersamenvatting bevat "~18°C", "droog", "12km/u wind"
    - Test 2 (CAL-03 neerslag): precipitationMm 0.5 en 1.5 → weersamenvatting bevat "2.0mm" (of "2mm") in plaats van "droog"
    - Test 3 (CAL-03 lege lijst): lege forecasts lijst → weersamenvatting is gelijk aan "Geen weerdata beschikbaar"
    - Test 4 (CAL-03 null-waarden): HourlyForecast met alle null-waarden → weersamenvatting bevat "—" of is "Geen weerdata beschikbaar" (geen crash)
    - Test 5 (PERS-04 annuleer-pad): als buildWeatherSummary bestaat als publieke/testbare methode, verifieer dat de output een String is zonder GoogleSignIn aan te raken
  </behavior>
  <action>
Maak test/services/calendar_service_test.dart aan. Gebruik uitsluitend dart:core, flutter_test, en de eigen package imports (ridewindow/services/calendar_service.dart, ridewindow/domain/models/hourly_forecast.dart, ridewindow/domain/models/ride_slot.dart).

STRATEGIE voor testbaarheid: Als CalendarService een publieke statische methode buildWeatherSummary(List<HourlyForecast>) heeft, test die direct. Als de methode privé is (_buildWeatherSummary of inline logica), extraheer dan de logica naar een testbare publieke helper-methode in lib/services/calendar_service.dart voordat je de tests schrijft. Refactoren van de productiecode is hier correct — dat is het doel van de TDD-stap.

Maak HourlyForecast testfixtures als lokale variabelen in elke test (geen setUp helper nodig voor dit aantal).

Gebruik package:test group() blokken:
- group('CalendarService.buildWeatherSummary', ...) voor de weersamenvatting tests
- group('CalendarService.addRideSlotToCalendar', ...) voor foutpad-tests (mock GoogleSignIn is complex voor unit tests — voeg een TODO-commentaar toe dat integratie-tests dit afdekken, en skip de signIn()-mock test in deze unit-testreeks)

Gebruik expect() met contains() voor string-checks zodat de exacte opmaak licht kan variëren.
  </action>
  <acceptance_criteria>
    - grep -c 'buildWeatherSummary' test/services/calendar_service_test.dart geeft >= 3
    - grep 'droog' test/services/calendar_service_test.dart geeft een resultaat
    - grep 'Geen weerdata' test/services/calendar_service_test.dart geeft een resultaat
    - flutter test test/services/calendar_service_test.dart --reporter=compact slaagt (alle tests groen)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/services/calendar_service_test.dart --reporter=compact</automated>
  </verify>
  <done>Alle weersamenvatting-tests slagen. buildWeatherSummary is publiek en aantoonbaar correct getest voor happy path, neerslag, lege lijst en null-waarden.</done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: Widget tests voor agenda-knop en PERS-04 privacyverificatie</name>
  <files>test/features/detail/ride_detail_screen_calendar_test.dart</files>
  <read_first>
    - lib/features/detail/ride_detail_screen.dart (volledig lezen — begrijp de StatefulWidget structuur, _isLoading, de knop-implementatie)
    - lib/services/calendar_service.dart (lezen voor methode-signatuur addRideSlotToCalendar)
    - lib/domain/models/ride_slot.dart (voor testfixture opbouw)
    - lib/domain/models/hourly_forecast.dart (voor testfixture opbouw)
    - lib/domain/models/ride_tier.dart (voor tier aanmaken in testfixtures)
  </read_first>
  <behavior>
    - Test 1 (laadstatus): Tik op "Toevoegen aan agenda" — CircularProgressIndicator verschijnt terwijl de Future loopt (gebruik een Completer zodat de Future niet direct voltooid)
    - Test 2 (succesmelding): addRideSlotToCalendar voltooit succesvol → SnackBar met tekst 'Rijvenster toegevoegd aan Google Agenda!' verschijnt
    - Test 3 (foutmelding): addRideSlotToCalendar gooit een Exception('test fout') → SnackBar met tekst die 'test fout' bevat verschijnt
    - Test 4 (PERS-04 privacy): CalendarService.addRideSlotToCalendar wordt NIET aangeroepen zonder knoptik — verifieer dat een kale pumpWidget zonder interactie de methode nooit aanroept
  </behavior>
  <action>
Maak test/features/detail/ride_detail_screen_calendar_test.dart aan.

STRATEGIE voor injectie: Om CalendarService te mocken in widget tests zonder complexe dependency injection te introduceren, pas lib/features/detail/ride_detail_screen.dart aan om een optionele calendarServiceFactory parameter te accepteren (default waarde is () => CalendarService()):

  typedef CalendarServiceFactory = CalendarService Function();

  class RideDetailScreen extends StatefulWidget {
    // Voeg toe:
    final CalendarServiceFactory calendarServiceFactory;
    const RideDetailScreen({
      super.key,
      required this.slot,
      required this.forecasts,
      this.calendarServiceFactory = _defaultFactory,
    });
  }

  CalendarService _defaultFactory() => CalendarService();

In de State-klasse: gebruik widget.calendarServiceFactory() in de onPressed callback in plaats van direct CalendarService().

In de tests: gebruik een MockCalendarService of een FakeCalendarService (gebruik mockito of simpele Completer-gebaseerde fake). Geef de factory door via de calendarServiceFactory parameter.

Maak een FakeCalendarService klasse lokaal in de test-file (extends CalendarService of een interface als die bestaat). Override addRideSlotToCalendar om een Completer.future terug te geven zodat je de laadstatus kunt observeren.

Maak een testfixture RideSlot aan met:
- start: DateTime(2026, 6, 10, 9, 0)
- end: DateTime(2026, 6, 10, 13, 0)
- overallScore: 0.85
- tier: Perfect() (importeer ride_tier.dart)
- hours: const []

Gebruik ProviderScope als wrapper alleen als de widget dat vereist. Als RideDetailScreen na de omzetting naar StatefulWidget geen providers meer watchet, is ProviderScope niet nodig.

PERS-04 test: Maak een TrackingFakeCalendarService die bijhoudt of addRideSlotToCalendar is aangeroepen (bool wasCalled). Pump RideDetailScreen, wacht, en verifieer dat wasCalled == false zonder te tikken.
  </action>
  <acceptance_criteria>
    - grep -c 'PERS-04\|privacy\|wasCalled\|calendarServiceFactory' test/features/detail/ride_detail_screen_calendar_test.dart geeft >= 2
    - grep 'CircularProgressIndicator' test/features/detail/ride_detail_screen_calendar_test.dart geeft een resultaat
    - grep 'Rijvenster toegevoegd' test/features/detail/ride_detail_screen_calendar_test.dart geeft een resultaat
    - flutter test test/features/detail/ride_detail_screen_calendar_test.dart --reporter=compact slaagt (alle tests groen)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/features/detail/ride_detail_screen_calendar_test.dart --reporter=compact</automated>
  </verify>
  <done>Alle vier widget tests slagen. PERS-04 privacy test bewijst aantoonbaar dat addRideSlotToCalendar nooit wordt aangeroepen zonder gebruikerstik. Alle tests groen.</done>
</task>

</tasks>

<threat_model>
## Vertrouwensgrenzen

| Grens | Beschrijving |
|-------|-------------|
| Test → CalendarService | Tests gebruiken fakes — geen echte OAuth-tokens of API-aanroepen in de test-suite |
| Test → GoogleSignIn SDK | GoogleSignIn wordt NIET aangeroepen in tests — de factory-injectie vervangt de echte service |

## STRIDE Bedreigingsregister

| Bedreiging-ID | Categorie | Component | Dispositie | Mitigatieplan |
|--------------|-----------|-----------|------------|---------------|
| T-09-02-01 | Information Disclosure | Test-suite lekt echte OAuth-tokens | mitigate | Alle tests gebruiken FakeCalendarService — geen netwerkverkeer; geen echte tokens in CI |
| T-09-02-02 | Tampering | calendarServiceFactory default omzeilt privacy guard | accept | Default factory maakt CalendarService() aan on-demand in onPressed — niet bij app-start; acceptabel risico |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
# Unit tests voor weersamenvatting
flutter test test/services/calendar_service_test.dart --reporter=compact
# Widget tests voor knop + PERS-04
flutter test test/features/detail/ride_detail_screen_calendar_test.dart --reporter=compact
# Alle Phase 9 tests
flutter test test/services/calendar_service_test.dart test/features/detail/ride_detail_screen_calendar_test.dart --reporter=compact
# Geen regressies in het volledige testpakket
flutter test --reporter=compact
```
</verification>

<success_criteria>
- test/services/calendar_service_test.dart bestaat en bevat tests voor CAL-03 weersamenvatting (happy path, neerslag, lege lijst, null-waarden)
- test/features/detail/ride_detail_screen_calendar_test.dart bestaat en bevat tests voor: laadstatus, succesmelding, foutmelding, en PERS-04 privacy guard
- buildWeatherSummary is aantoonbaar getest en correct voor alle randgevallen
- PERS-04: test bewijst dat addRideSlotToCalendar nooit wordt aangeroepen zonder expliciete gebruikerstik
- Alle tests slagen: flutter test --reporter=compact toont geen failures
- dart analyze lib/ meldt geen nieuwe fouten na de calendarServiceFactory aanpassing
</success_criteria>

<output>
Maak `.planning/phases/09-google-calendar-integration/09-02-SUMMARY.md` aan wanneer klaar
</output>
