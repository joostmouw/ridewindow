---
phase: 10-release-internal-track-only
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - lib/features/profile/profile_screen.dart
  - pubspec.yaml
autonomous: false
requirements:
  - REL-03

must_haves:
  truths:
    - "Privacy policy is published at a publicly accessible GitHub Pages URL"
    - "Privacy policy URL appears in the ProfileScreen About section and opens in the device browser"
    - "Privacy policy content correctly describes location data and optional Google Calendar data usage"
    - "A second GitHub repo (ridewindow-privacy) is created with GitHub Pages enabled"
  artifacts:
    - path: "lib/features/profile/profile_screen.dart"
      provides: "OVER section with Privacybeleid ListTile and Versie ListTile"
      contains: "launchUrl"
    - path: "ridewindow-privacy/index.html"
      provides: "Privacy policy HTML page served via GitHub Pages"
      contains: "Privacybeleid"
  key_links:
    - from: "ProfileScreen OVER ListTile onTap"
      to: "GitHub Pages URL"
      via: "launchUrl(Uri.parse(privacyPolicyUrl), mode: LaunchMode.externalApplication)"
      pattern: "launchUrl.*LaunchMode\\.externalApplication"
---

## Phase Goal

**As a** tester, **I want to** access the privacy policy from inside the RideWindow app, **so that** I understand what data the app collects and trust it enough to install it.

<objective>
This plan creates the privacy policy GitHub Pages site (in a separate repo) and adds an OVER section to ProfileScreen with a "Privacybeleid" tile that opens the policy URL in the device browser, plus a "Versie" tile showing 1.0.0.

Purpose: REL-03 requires the policy at a stable public URL linked from both Play Console and the in-app About screen. This plan delivers both.
Output: https://[github-username].github.io/ridewindow-privacy/ serves the policy; ProfileScreen shows it under OVER section.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-release-internal-track-only/10-RESEARCH.md
@.planning/phases/10-release-internal-track-only/10-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Create ridewindow-privacy GitHub repo and publish GitHub Pages</name>
  <read_first>
    .planning/phases/10-release-internal-track-only/10-RESEARCH.md (REL-03 requirement, Pattern "Minimal GitHub Pages Privacy Policy index.html", Open Questions #3 about Dutch/English)
  </read_first>
  <what-built>
    The privacy policy must be at a stable public URL. This cannot be automated — it requires logging into GitHub.com, creating a new repo, and enabling GitHub Pages. Claude cannot authenticate to GitHub.com on your behalf.
  </what-built>
  <how-to-verify>
    Step 1: Go to github.com → New repository
      - Name: ridewindow-privacy
      - Visibility: Public
      - Initialize with: README (so you can immediately add files)

    Step 2: Create index.html in the repo with the Dutch privacy policy from RESEARCH.md "Minimal GitHub Pages Privacy Policy" code example. Fill in:
      - Replace [datum] with today's date: 4 juni 2026
      - Replace [uw e-mailadres] with your contact email address

    Step 3: Enable GitHub Pages:
      - Go to repo Settings → Pages
      - Source: "Deploy from a branch"
      - Branch: main (or master), folder: / (root)
      - Save

    Step 4: Wait 1–2 minutes for GitHub to build and deploy the page.

    Step 5: Visit the published URL:
      https://[your-github-username].github.io/ridewindow-privacy/
      (Replace [your-github-username] with your actual GitHub username)
      Expected: Page loads with "Privacybeleid — RideWindow" heading and Dutch privacy policy text.

    Step 6: Copy the full published URL — you will need it for Task 2 and for Play Console in Plan 04.
  </how-to-verify>
  <resume-signal>Type "policy-live" and paste the full GitHub Pages URL (e.g. https://joostmouw.github.io/ridewindow-privacy/) so Task 2 can use the exact URL</resume-signal>
  <acceptance_criteria>
    - GitHub Pages URL returns HTTP 200 (curl -o /dev/null -s -w "%{http_code}" [URL] returns 200)
    - Page contains "Privacybeleid" text (curl [URL] | grep -c "Privacybeleid" returns >= 1)
    - Page contains "RideWindow" text
    - Page contains section about location data
    - Page contains section about Google Calendar (optional, user-initiated)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add OVER section to ProfileScreen with privacy policy link and version</name>
  <read_first>
    lib/features/profile/profile_screen.dart (full file — to understand the existing _SectionHeader pattern, ListView structure, and where to append the OVER section; existing sections: RIJLENGTE, THEMA, LOCATIE, NOTIFICATIES)
    .planning/phases/10-release-internal-track-only/10-RESEARCH.md (Code Examples "About Screen / Privacy Policy Link in ProfileScreen", url_launcher LaunchMode.externalApplication pattern)
  </read_first>
  <action>
    Modify lib/features/profile/profile_screen.dart:

    1. Add import at the top of the file (after existing imports):
       import 'package:url_launcher/url_launcher.dart';

    2. Add a private constant for the privacy policy URL, immediately after the class-level fields (before _ProfileScreenState build method or as a top-level const in the file):
       const _kPrivacyPolicyUrl = 'PASTE_GITHUB_PAGES_URL_FROM_TASK_1_HERE';
       Replace PASTE_GITHUB_PAGES_URL_FROM_TASK_1_HERE with the exact URL provided in the Task 1 resume-signal (e.g. https://joostmouw.github.io/ridewindow-privacy/).

    3. Add a private method to _ProfileScreenState for launching the URL:
       Future<void> _launchPrivacyPolicy() async {
         final uri = Uri.parse(_kPrivacyPolicyUrl);
         if (await canLaunchUrl(uri)) {
           await launchUrl(uri, mode: LaunchMode.externalApplication);
         }
       }

    4. At the end of the ProfileScreen's ListView children list (after the existing NOTIFICATIES section and before the closing bracket of the children list), append:
       const _SectionHeader('OVER'),
       ListTile(
         title: const Text('Privacybeleid'),
         trailing: const Icon(Icons.open_in_new),
         onTap: _launchPrivacyPolicy,
       ),
       ListTile(
         title: const Text('Versie'),
         trailing: const Text('1.0.0'),
       ),

    Do NOT change any existing ProfileScreen sections (RIJLENGTE, THEMA, LOCATIE, NOTIFICATIES).
    Do NOT add url_launcher to pubspec.yaml — it was already added in Plan 01 Task 3.
    Run `flutter pub get` if pubspec.lock does not yet contain url_launcher (check with grep).
  </action>
  <verify>
    <automated>
      grep -c "url_launcher/url_launcher.dart" /Users/joostmouw/ridewindow/lib/features/profile/profile_screen.dart
      grep -c "_kPrivacyPolicyUrl" /Users/joostmouw/ridewindow/lib/features/profile/profile_screen.dart
      grep -c "launchUrl" /Users/joostmouw/ridewindow/lib/features/profile/profile_screen.dart
      grep -c "Privacybeleid" /Users/joostmouw/ridewindow/lib/features/profile/profile_screen.dart
      grep -c "LaunchMode.externalApplication" /Users/joostmouw/ridewindow/lib/features/profile/profile_screen.dart
      /Users/joostmouw/development/flutter/bin/flutter analyze /Users/joostmouw/ridewindow/lib/features/profile/profile_screen.dart 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - profile_screen.dart contains `import 'package:url_launcher/url_launcher.dart'` (grep -c returns 1)
    - profile_screen.dart contains `_kPrivacyPolicyUrl` (grep -c returns >= 1)
    - profile_screen.dart contains `launchUrl` (grep -c returns >= 1)
    - profile_screen.dart contains `LaunchMode.externalApplication` (grep -c returns 1)
    - profile_screen.dart contains `'Privacybeleid'` text (grep -c returns 1)
    - profile_screen.dart contains `'Versie'` text (grep -c returns 1)
    - profile_screen.dart contains `'1.0.0'` version string (grep -c returns 1)
    - `flutter analyze lib/features/profile/profile_screen.dart` exits 0 with no errors (warnings about existing issues are acceptable, no new errors)
    - _kPrivacyPolicyUrl is NOT the placeholder string "PASTE_GITHUB_PAGES_URL_FROM_TASK_1_HERE"
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → external browser | launchUrl opens an external browser; the URL is a hardcoded constant in the app |
| GitHub Pages → Play Console | Privacy policy URL entered in Play Console must be publicly accessible and stable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-08 | Tampering | Privacy policy URL changes after Play Console submission | mitigate | URL is hardcoded as a const in profile_screen.dart; GitHub Pages URL is stable as long as the repo exists |
| T-10-09 | Information Disclosure | Privacy policy understates data collection | mitigate | Policy explicitly declares location (GPS, app functionality) and Google Calendar (ephemeral, optional, user-initiated only); matches REL-04 Data Safety declarations |
</threat_model>

<verification>
After both tasks complete:
- GitHub Pages URL returns HTTP 200 with Dutch privacy policy content
- ProfileScreen shows OVER section with Privacybeleid and Versie tiles
- `flutter analyze` is clean for profile_screen.dart
- _kPrivacyPolicyUrl contains the real GitHub Pages URL (not placeholder)
</verification>

<success_criteria>
Privacy policy published at a stable GitHub Pages URL; ProfileScreen OVER section links to it via external browser; Versie tile shows 1.0.0.
</success_criteria>

<output>
Create `.planning/phases/10-release-internal-track-only/10-03-SUMMARY.md` when done
</output>
