---
phase: 10-release-internal-track-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - android/app/build.gradle.kts
  - android/key.properties
  - pubspec.yaml
autonomous: false
requirements:
  - REL-01
  - REL-02

must_haves:
  truths:
    - "The keystore file exists at ~/upload-keystore.jks and passwords are stored in a password manager"
    - "android/key.properties exists and is gitignored — it will never be committed"
    - "build.gradle.kts uses the release signingConfig that reads from key.properties"
    - "pubspec.yaml version is 1.0.0+1 (versionCode=1 for first Play Console upload)"
    - "Android SDK is installed and `flutter doctor` shows [✓] Android toolchain"
    - "applicationId is confirmed as the permanent package name before any upload"
  artifacts:
    - path: "android/key.properties"
      provides: "Upload keystore credentials for Gradle signing"
      contains: "storePassword=, keyPassword=, keyAlias=upload, storeFile="
    - path: "android/app/build.gradle.kts"
      provides: "Release signing config referencing key.properties"
      contains: "signingConfigs { create(\"release\")"
    - path: ".gitignore"
      provides: "Prevents keystore and key.properties from being committed"
      contains: "*.jks"
  key_links:
    - from: "android/app/build.gradle.kts"
      to: "android/key.properties"
      via: "keystoreProperties.load(FileInputStream(keystorePropertiesFile))"
      pattern: "keystorePropertiesFile\\.exists"
    - from: "build.gradle.kts release buildType"
      to: "signingConfigs.getByName(\"release\")"
      via: "signingConfig assignment"
      pattern: "signingConfigs\\.getByName.*release"
---

## Phase Goal

**As a** developer, **I want to** have a working signed release build pipeline with the Android SDK installed and the upload keystore created, **so that** I can build a release AAB and APK that Google Play Console will accept.

<objective>
This plan installs the Android SDK (deferred blocker from Phase 1), creates the upload keystore, wires the Gradle signing config, hardens .gitignore against credential exposure, bumps the version to 1.0.0+1, adds url_launcher for the future About screen, and confirms the applicationId.

Purpose: All subsequent plans depend on a working Android toolchain and a valid signed build. Without this foundation, no release AAB can be produced.
Output: Flutter doctor shows [✓] Android toolchain; `flutter build appbundle` succeeds with release signing (not debug).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-release-internal-track-only/10-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Install Android Studio + SDK + accept licenses</name>
  <read_first>
    .planning/phases/10-release-internal-track-only/10-RESEARCH.md (Environment Availability section, Common Pitfalls #3)
  </read_first>
  <what-built>
    Android SDK is not installed on this machine (confirmed by flutter doctor -v showing "[✗] Android toolchain — Unable to locate Android SDK").
    This task cannot be automated — it requires downloading and running the Android Studio GUI installer.
  </what-built>
  <how-to-verify>
    Step 1: Install Android Studio via Homebrew Cask:
      brew install --cask android-studio

    Step 2: Open Android Studio, complete the Setup Wizard:
      - Accept the license agreement
      - Choose "Standard" installation
      - Let the wizard download: Android SDK (API 35), build-tools 35.x, platform-tools (includes adb, keytool)

    Step 3: Accept all Flutter-required Android licenses:
      /Users/joostmouw/development/flutter/bin/flutter doctor --android-licenses
      (Type 'y' and press Enter for each license prompt)

    Step 4: Verify:
      /Users/joostmouw/development/flutter/bin/flutter doctor -v
      Expected output: "[✓] Android toolchain — develop for Android devices"
      Also confirm: `which adb` returns a path (e.g. ~/Library/Android/sdk/platform-tools/adb)
      Also confirm: `which keytool` or `$(dirname $(which java))/keytool` works
  </how-to-verify>
  <resume-signal>Type "android-sdk-ready" once flutter doctor shows [✓] Android toolchain</resume-signal>
  <acceptance_criteria>
    - `flutter doctor -v` output contains "[✓] Android toolchain"
    - `adb --version` exits 0 and prints a version string
    - `keytool -help` exits without "command not found"
  </acceptance_criteria>
</task>

<task type="checkpoint:decision">
  <name>Task 2: Confirm applicationId before first Play Console upload</name>
  <read_first>
    android/app/build.gradle.kts (line 8 and 19 — current applicationId is "com.fanalists.ridewindow.ridewindow")
    .planning/phases/10-release-internal-track-only/10-RESEARCH.md (Common Pitfalls #1, Open Questions #1)
  </read_first>
  <decision>
    The current applicationId is "com.fanalists.ridewindow.ridewindow" — the double "ridewindow" suffix appears to be an auto-generated default from the Flutter scaffold. This identifier is PERMANENT: it cannot be changed after the first upload to Play Console without creating a new app listing and losing all existing installations.

    The namespace in build.gradle.kts (line 8) must match the chosen applicationId.
    The AndroidManifest.xml does not set a package attribute (confirmed by not appearing in grep) so no change needed there.
  </decision>
  <context>
    Options:
    - com.fanalists.ridewindow.ridewindow (current — double suffix, looks auto-generated)
    - com.fanalists.ridewindow (cleaner, same domain owner)
    - dev.joost.ridewindow (personal domain style)

    Any option works technically. The choice only matters for what testers see in their device app list (package name is internal; the display name "RideWindow" is what users see).
  </context>
  <options>
    <option id="keep-current">
      <name>Keep com.fanalists.ridewindow.ridewindow</name>
      <pros>Zero file changes; no risk of introducing a typo</pros>
      <cons>Double "ridewindow" suffix looks auto-generated; mildly awkward if package name ever surfaces</cons>
    </option>
    <option id="shorten">
      <name>Change to com.fanalists.ridewindow</name>
      <pros>Cleaner; matches the domain pattern; no double suffix</pros>
      <cons>Requires updating namespace and applicationId in build.gradle.kts (2 lines)</cons>
    </option>
  </options>
  <resume-signal>Reply "keep-current" or "shorten" — then Task 3 will apply your choice</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Harden .gitignore + apply applicationId decision + wire signing config + bump version + add url_launcher</name>
  <read_first>
    .gitignore (full file — to append signing entries without duplicating existing lines)
    android/app/build.gradle.kts (full file — to replace signingConfig block per RESEARCH.md Pattern 1)
    pubspec.yaml (lines 1-10 — to update version field and add url_launcher dependency)
    .planning/phases/10-release-internal-track-only/10-RESEARCH.md (Pattern 1 Kotlin DSL Signing Config, Pattern 2 key.properties Template, Pattern 5 Version Bump, Anti-Patterns section)
  </read_first>
  <action>
    A. .gitignore — append the following three lines at the end of the file (after existing entries):
       # Signing credentials — NEVER commit
       *.jks
       *.keystore
       android/key.properties

    B. android/app/build.gradle.kts — replace the entire file content with the Pattern 1 template from RESEARCH.md, which:
       - Adds `import java.util.Properties` and `import java.io.FileInputStream` at the top
       - Adds keystoreProperties loading block before `android {`
       - Adds `signingConfigs { create("release") { ... } }` block inside `android {`
       - Changes release buildType to use `signingConfig = signingConfigs.getByName("release")`
       - Uses EITHER `com.fanalists.ridewindow.ridewindow` or `com.fanalists.ridewindow` for both `namespace` and `applicationId` depending on the decision from Task 2
       - Retains `compileSdk = 35`, `ndkVersion = flutter.ndkVersion`, `compileOptions`, `minSdk`, `targetSdk`, `versionCode`, `versionName`, `kotlin { compilerOptions { jvmTarget = JVM_17 } }`, `flutter { source = "../.." }`
       - The storeFile path in the signingConfig reads from keystoreProperties["storeFile"]: `keystoreProperties["storeFile"]?.let { file(it as String) }`

    C. pubspec.yaml — change `version: 0.1.0+1` to `version: 1.0.0+1` on the version line.
       Also add `url_launcher: ^6.3.1` under `dependencies:` (after the last existing dependency line, maintaining 2-space indent).

    D. android/key.properties — create this new file (it will be gitignored by step A):
       storePassword=PLACEHOLDER_STORE_PW
       keyPassword=PLACEHOLDER_KEY_PW
       keyAlias=upload
       storeFile=/Users/joostmouw/upload-keystore.jks
       (Leave PLACEHOLDER values — Task 4 replaces them after keystore creation)

    Do NOT run `flutter pub get` yet — that happens in Plan 02 after the keystore is generated.
  </action>
  <verify>
    <automated>
      grep -c "\.jks" /Users/joostmouw/ridewindow/.gitignore
      grep -c "key\.properties" /Users/joostmouw/ridewindow/.gitignore
      grep -c 'create("release")' /Users/joostmouw/ridewindow/android/app/build.gradle.kts
      grep -c 'signingConfigs.getByName("release")' /Users/joostmouw/ridewindow/android/app/build.gradle.kts
      grep "^version:" /Users/joostmouw/ridewindow/pubspec.yaml
      grep "url_launcher" /Users/joostmouw/ridewindow/pubspec.yaml
    </automated>
  </verify>
  <acceptance_criteria>
    - .gitignore contains "*.jks" (grep -c returns >= 1)
    - .gitignore contains "android/key.properties" (grep -c returns >= 1)
    - android/app/build.gradle.kts contains `create("release")` inside signingConfigs block
    - android/app/build.gradle.kts contains `signingConfigs.getByName("release")` in release buildType
    - android/app/build.gradle.kts does NOT contain `signingConfigs.getByName("debug")` in release buildType
    - pubspec.yaml version line is `version: 1.0.0+1`
    - pubspec.yaml contains `url_launcher:` under dependencies
    - android/key.properties file exists at /Users/joostmouw/ridewindow/android/key.properties
    - `git status` shows android/key.properties as ignored (not as untracked)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 4: Generate upload keystore and back it up to password manager</name>
  <read_first>
    .planning/phases/10-release-internal-track-only/10-RESEARCH.md (Pattern 3 Keystore Generation Command, REL-02 requirement, Anti-Patterns "Losing the keystore" warning)
    android/key.properties (to update PLACEHOLDER values with real passwords after generation)
  </read_first>
  <what-built>
    Task 3 created android/key.properties with PLACEHOLDER passwords. This task generates the actual keystore file and updates key.properties with real passwords. This cannot be automated because it requires you to choose and type a secure password interactively (keytool prompts), and immediately back it up to your password manager — Claude cannot access your password manager.
  </what-built>
  <how-to-verify>
    Step 1: Run keytool to generate the upload keystore:
      keytool -genkey -v \
        -keystore ~/upload-keystore.jks \
        -keyalg RSA \
        -storetype JKS \
        -keysize 2048 \
        -validity 10000 \
        -alias upload
      When prompted:
        - Enter a store password (e.g. 20+ character passphrase) — record it
        - Enter key password (can be the same as store password) — record it
        - Enter your name/org details (can be minimal: CN=RideWindow, OU=App, O=Fanalists, L=NL, ST=NL, C=NL)
        - Confirm with "yes"

    Step 2: Update android/key.properties — replace PLACEHOLDER_STORE_PW and PLACEHOLDER_KEY_PW with the real passwords you chose. Set storeFile to the full path: /Users/joostmouw/upload-keystore.jks

    Step 3: Back up to password manager (Bitwarden/1Password) immediately:
        - Attach ~/upload-keystore.jks as a file attachment
        - Store storePassword, keyPassword, keyAlias (upload), storeFile path as secure notes
        - Title the entry: "RideWindow upload keystore"

    Step 4: Verify backup exists before continuing.

    Step 5: Confirm file exists:
      ls -la ~/upload-keystore.jks
      (Expected: file exists, size ~2.5 KB)
  </how-to-verify>
  <resume-signal>Type "keystore-backed-up" once ~/upload-keystore.jks exists and is saved in your password manager</resume-signal>
  <acceptance_criteria>
    - ~/upload-keystore.jks exists (ls -la shows the file)
    - android/key.properties no longer contains "PLACEHOLDER" (grep -c PLACEHOLDER android/key.properties returns 0)
    - keystore entry exists in password manager (human-verified, not programmatically checkable)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| key.properties → git | Credential file must never cross into version control |
| keystore → backup | Irreplaceable credential must exist in ≥2 locations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | android/key.properties committed to git | mitigate | .gitignore entry for android/key.properties added in Task 3 before file creation; `git status` check in acceptance_criteria |
| T-10-02 | Tampering | *.jks committed to git | mitigate | .gitignore entry for *.jks added in Task 3; keystore lives at ~/upload-keystore.jks (outside project dir) |
| T-10-03 | Denial of Service | Upload keystore lost | mitigate | Immediate Bitwarden/1Password backup with file attachment required in Task 4 before continuing |
| T-10-04 | Tampering | Wrong applicationId after first Play Console upload | mitigate | Task 2 checkpoint forces explicit developer confirmation before any upload |
</threat_model>

<verification>
After all 4 tasks complete:
- `flutter doctor -v` shows "[✓] Android toolchain"
- `git status` shows android/key.properties as ignored (not untracked)
- `git status` shows ~/upload-keystore.jks does not appear (it's outside the project dir)
- `grep -c 'create("release")' android/app/build.gradle.kts` returns 1
- `grep "^version:" pubspec.yaml` shows `version: 1.0.0+1`
- ~/upload-keystore.jks exists on disk
- Password manager entry exists for keystore (human-verified)
</verification>

<success_criteria>
Android SDK installed, flutter doctor green, upload keystore created and backed up, release signing wired into build.gradle.kts, credentials gitignored, version bumped to 1.0.0+1, url_launcher added to pubspec.
</success_criteria>

<output>
Create `.planning/phases/10-release-internal-track-only/10-01-SUMMARY.md` when done
</output>
