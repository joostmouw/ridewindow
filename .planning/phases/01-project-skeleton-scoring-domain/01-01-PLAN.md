---
phase: 01-project-skeleton-scoring-domain
plan: 01
type: execute
wave: 0
depends_on: []
files_modified: []
autonomous: false
requirements: []
requirements_addressed: []
tags: [flutter, environment, blocking-gate, package-legitimacy]
user_setup:
  - service: flutter_sdk
    why: "Flutter 3.x SDK is the BLOCKING runtime for every command in this phase (flutter create, flutter pub get, flutter run, dart test). RESEARCH.md §Environment Availability confirmed `which flutter` returns 'not found' on this machine; without it no other task can execute."
    env_vars: []
    dashboard_config:
      - task: "Install Flutter SDK (3.x stable) for macOS arm64"
        location: "https://docs.flutter.dev/get-started/install/macos"
      - task: "Verify install: `flutter --version` (≥ 3.27.0), `dart --version` (≥ 3.6.0), `flutter doctor` (no blocking Android issues)"
        location: "terminal"

must_haves:
  truths:
    - "User confirms Flutter SDK (≥ 3.27.0) and Dart SDK (≥ 3.6.0) are installed and on PATH"
    - "A trivial `dart test` invocation succeeds inside a freshly-created Flutter package — proving Open Question #2 from RESEARCH.md is resolved"
    - "Dev-dependency package legitimacy is visually confirmed via `flutter pub outdated` (mitigating [ASSUMED] tags from RESEARCH.md §Package Legitimacy Audit)"
  artifacts:
    - path: "test/smoke_test.dart"
      provides: "Minimal `expect(true, isTrue)` proving dart test runs on Flutter package"
      min_lines: 5
  key_links:
    - from: "developer machine"
      to: "flutter / dart binaries"
      via: "PATH"
      pattern: "flutter --version && dart --version"
---

<objective>
Gate the phase on two non-negotiable prerequisites before any code is written:

1. The Flutter SDK + Dart SDK are installed on the developer's machine. RESEARCH.md confirmed they are NOT currently present (`which flutter` returned not found). Every subsequent command in Phase 1 — `flutter create`, `flutter pub get`, `dart test`, `flutter run` — fails immediately without these.
2. A `dart test` invocation actually works on a freshly-scaffolded Flutter package (RESEARCH.md Open Question #2). If `dart test` cannot find `package:test` or otherwise fails on the Flutter package, the entire phase architecture (pure-Dart domain tested via `dart test`) collapses — fallback would be to extract `lib/domain/` to a sub-package, a much larger refactor worth ruling out cheaply upfront.

Purpose: Eliminate environment risk before investing in any plan that assumes a working toolchain.
Output: A green `dart test test/smoke_test.dart` invocation against the freshly bootstrapped project, plus the user's explicit confirmation that Flutter is installed. NOTE: The actual `flutter create` invocation happens in Plan 02; this plan installs nothing, only verifies.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-skeleton-scoring-domain/01-CONTEXT.md
@.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md
@.planning/phases/01-project-skeleton-scoring-domain/01-VALIDATION.md
@/Users/joostmouw/ridewindow/CLAUDE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm Flutter SDK installed and on PATH (BLOCKING)</name>
  <read_first>
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Environment Availability (confirms Flutter is NOT installed; lists all paths checked)
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Open Questions #3
    - /Users/joostmouw/ridewindow/CLAUDE.md (locked tech stack — Flutter 3.x stable, Dart 3.x)
  </read_first>
  <what-built>Nothing — this is a verification gate. The developer must install Flutter SDK before any other task in this phase can execute. RESEARCH.md confirmed `which flutter` returned not found on 2026-06-02; install paths /opt/homebrew/bin, /usr/local/bin, ~/flutter, ~/development/flutter, ~/fvm/default all checked and absent.</what-built>
  <how-to-verify>
    Open a NEW terminal session (so PATH refreshes) and run these commands. Each must exit 0 with sensible output:

    1. `flutter --version` — must show Flutter version ≥ 3.27.0 and Dart version ≥ 3.6.0 (Dart ships bundled with Flutter)
    2. `dart --version` — must show Dart SDK version ≥ 3.6.0
    3. `flutter doctor -v` — must show no "✗" / blocking issues for the "Flutter" and "Android toolchain" sections. (Other sections like "Chrome", "iOS toolchain", "VS Code" may be missing — that is fine; this is an Android-only project per PROJECT.md.)

    If Flutter is NOT installed, follow https://docs.flutter.dev/get-started/install/macos. On Apple Silicon (Darwin arm64 — confirmed via `uname -m`), use the arm64 archive. After install, add Flutter's `bin` to PATH in `~/.zshrc` and re-source.

    The Android toolchain requires Android Studio (or `cmdline-tools`) + accepted SDK licenses (`flutter doctor --android-licenses`). This is needed for `flutter run` in Plan 10 but does NOT block earlier plans (Plans 02-09 use `dart test` exclusively).
  </how-to-verify>
  <acceptance_criteria>
    - User pastes the output of `flutter --version` showing version ≥ 3.27.0
    - User pastes the output of `dart --version` showing version ≥ 3.6.0
    - User pastes the relevant section of `flutter doctor -v` showing Flutter and Android toolchain are green (no ✗)
    - User types "approved" OR describes blocking issues found during install
  </acceptance_criteria>
  <resume-signal>Type "approved" with `flutter --version` output, OR describe issues if install failed</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy audit checkpoint — Phase 1 dev_dependencies</name>
  <read_first>
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Package Legitimacy Audit (all 10 packages tagged [ASSUMED] — slopcheck unavailable during research)
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Bootstrap pubspec.yaml (exact version pins)
    - /Users/joostmouw/ridewindow/CLAUDE.md §Technology Stack (publishers + verification dates)
    - /Users/joostmouw/ridewindow/.planning/research/STACK.md (verified pub.dev 2026-06-01)
  </read_first>
  <what-built>Nothing yet — this gate exists because RESEARCH.md tagged every Phase 1 dev_dependency `[ASSUMED]` (slopcheck CLI was unavailable during research). Per package legitimacy protocol, [ASSUMED] packages require a blocking-human checkpoint before any `flutter pub get` install.</what-built>
  <how-to-verify>
    For each package below, visit the pub.dev page and confirm: (a) publisher matches the column below, (b) the package has > 100 likes OR is verified-publisher, (c) the latest version on pub.dev is ≥ the pinned version, (d) "Repository" link points to a credible GitHub org (flutter/, dart-lang/, google/, rrousselGit/).

    Verify each:
    1. `freezed_annotation` ≥ 3.1.0 — https://pub.dev/packages/freezed_annotation — publisher: dash-overflow.net
    2. `json_annotation` ≥ 4.9.0 — https://pub.dev/packages/json_annotation — publisher: dart.dev
    3. `test` ≥ 1.25.0 — https://pub.dev/packages/test — publisher: dart.dev
    4. `freezed` ≥ 3.2.5 — https://pub.dev/packages/freezed — publisher: dash-overflow.net
    5. `json_serializable` ≥ 6.8.0 — https://pub.dev/packages/json_serializable — publisher: dart.dev
    6. `build_runner` ≥ 2.4.0 — https://pub.dev/packages/build_runner — publisher: dart.dev
    7. `coverage` ≥ 1.10.0 — https://pub.dev/packages/coverage — publisher: dart.dev
    8. `flutter_lints` ≥ 5.0.0 — https://pub.dev/packages/flutter_lints — publisher: flutter.dev

    `flutter` and `flutter_test` come from the Flutter SDK itself (verified in Task 1).

    Type "approved" only after eyeballing each page. Any [SLOP]/[SUS] verdict → STOP, do not type approved; flag the suspicious package for replacement.

    This checkpoint is NOT auto-advanceable; even with `workflow.auto_advance` set, it requires explicit human approval per package legitimacy protocol.
  </how-to-verify>
  <acceptance_criteria>
    - User confirms each of the 8 dev_dependencies above shows the expected publisher on pub.dev
    - User types "approved" — explicitly acknowledging package legitimacy
    - OR: user reports a suspicious package → execution stops, planner reconvened to replace
  </acceptance_criteria>
  <resume-signal>Type "approved" after visiting all 8 pub.dev pages, OR flag a suspicious package</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Spike — prove `dart test` works on a freshly bootstrapped Flutter project (RESEARCH.md Open Question #2)</name>
  <read_first>
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Test Strategy → Test framework choice (`package:test` not `flutter_test`)
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Open Questions #2 (the fallback if dart test fails)
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Bootstrap → `flutter create` incantation
  </read_first>
  <action>
    Perform a one-shot spike to verify `dart test` runs against a freshly bootstrapped Flutter project BEFORE committing to the Phase 1 architecture. This task does NOT bootstrap the real project (that is Plan 02 Task 1); it creates an isolated throwaway directory, runs the spike, then deletes it.

    Steps (run from `/tmp` — do NOT pollute the project root):

    1. Create temp dir: `mkdir -p /tmp/ridewindow-spike && cd /tmp/ridewindow-spike`
    2. Bootstrap a minimal Flutter project: `flutter create --platforms=android --org=com.fanalists.ridewindow --project-name=ridewindow_spike .`
    3. Add `test: ^1.25.0` to the `dev_dependencies` section of `/tmp/ridewindow-spike/pubspec.yaml` (alongside `flutter_test`)
    4. Run `flutter pub get` to install
    5. Replace `/tmp/ridewindow-spike/test/widget_test.dart` with a file `test/smoke_test.dart` containing exactly:
       ```
       import 'package:test/test.dart';
       void main() {
         test('dart test runs on Flutter package', () { expect(1 + 1, equals(2)); });
       }
       ```
       (Delete the old widget_test.dart.)
    6. Run `dart test test/smoke_test.dart` from `/tmp/ridewindow-spike`. EXPECTED OUTCOME: exit 0, test passes (✓ `dart test runs on Flutter package`).
    7. If exit 0 → spike PASSED; proceed to step 8. If non-zero → spike FAILED; capture the full stderr/stdout and STOP — the architecture needs reconsideration (sub-package extraction per RESEARCH.md Open Question #2 fallback).
    8. Copy ONLY the file `/tmp/ridewindow-spike/test/smoke_test.dart` to the project root for reuse in Plan 02: `cp /tmp/ridewindow-spike/test/smoke_test.dart /Users/joostmouw/ridewindow/test/smoke_test.dart` (creating `/Users/joostmouw/ridewindow/test/` if it does not exist via `mkdir -p`). Plan 02 will move this into the bootstrapped scaffold.
    9. Clean up: `rm -rf /tmp/ridewindow-spike`

    Do NOT run `flutter create` on the project root in this task — that is Plan 02's job. Do NOT add packages to the project root pubspec — there is no pubspec yet.
  </action>
  <verify>
    <automated>cd /tmp/ridewindow-spike 2>/dev/null && dart test test/smoke_test.dart || (test -f /Users/joostmouw/ridewindow/test/smoke_test.dart && echo "SPIKE PASSED — smoke_test.dart copied to project root")</automated>
  </verify>
  <acceptance_criteria>
    - `dart test test/smoke_test.dart` in `/tmp/ridewindow-spike` exits 0 with passing test (proves RESEARCH.md Open Question #2: dart test works on Flutter package)
    - File `/Users/joostmouw/ridewindow/test/smoke_test.dart` exists after task completes
    - Temp dir `/tmp/ridewindow-spike` is cleaned up (no leftover artifacts)
    - If spike FAILS: task stops with a clear error message and a pointer to RESEARCH.md Open Question #2 fallback
  </acceptance_criteria>
  <done>
    Two conditions both true: (1) the spike command `dart test test/smoke_test.dart` exited 0 in the temp dir; (2) the smoke test file is staged at `/Users/joostmouw/ridewindow/test/smoke_test.dart` for Plan 02 to incorporate into the real bootstrap.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine ↔ pub.dev | dev_dependencies installed via `flutter pub get` execute arbitrary code at build time (build_runner, freezed, json_serializable, coverage). Untrusted package contents would compromise the dev machine. |
| developer machine ↔ Flutter SDK installer | Flutter SDK install runs platform code; trust boundary crossed at install time. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-SC | Tampering | dev_dependencies on pub.dev (freezed, build_runner, json_serializable, coverage, flutter_lints, test, freezed_annotation, json_annotation) | mitigate | Task 2 is a blocking-human checkpoint per package legitimacy protocol; verifier eyeballs publisher + likes + repo link for each [ASSUMED] package on pub.dev before any `flutter pub get` runs |
| T-1-ENV | DoS | Missing Flutter SDK | mitigate | Task 1 is a blocking checkpoint; if Flutter is absent, execution stops before any dependent task wastes context |
| T-1-SPIKE | DoS (architectural) | `dart test` may not work on Flutter package (RESEARCH.md Open Q#2) | mitigate | Task 3 is a 5-min throwaway spike that proves the assumption before the entire phase architecture commits to it |
</threat_model>

<verification>
After all 3 tasks complete:

- `flutter --version` exits 0 with version ≥ 3.27.0 (manual)
- `dart --version` exits 0 with version ≥ 3.6.0 (manual)
- User has typed "approved" twice (Task 1 env, Task 2 packages)
- File `/Users/joostmouw/ridewindow/test/smoke_test.dart` exists with the spike test body (automated)
- `/tmp/ridewindow-spike/` does NOT exist (cleanup verified) (automated)
</verification>

<success_criteria>
Plan 01 succeeds iff: (1) developer has confirmed Flutter ≥ 3.27.0 and Dart ≥ 3.6.0 installed; (2) developer has approved the package legitimacy audit for 8 dev_dependencies; (3) `dart test` has been proven to work on a Flutter-scaffolded package via the spike, with the smoke_test.dart file staged at the project root for Plan 02 to reuse.
</success_criteria>

<output>
Create `.planning/phases/01-project-skeleton-scoring-domain/01-01-SUMMARY.md` documenting: Flutter version installed, Dart version installed, output of `flutter doctor -v` (Flutter + Android sections), spike result (PASS/FAIL), and confirmation of package legitimacy approval.
</output>
