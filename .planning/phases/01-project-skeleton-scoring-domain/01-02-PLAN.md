---
phase: 01-project-skeleton-scoring-domain
plan: 02
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - "pubspec.yaml"
  - "analysis_options.yaml"
  - ".gitignore"
  - "lib/main.dart"
  - "lib/core/.gitkeep"
  - "lib/data/.gitkeep"
  - "lib/features/.gitkeep"
  - "lib/platform/.gitkeep"
  - "lib/domain/models/.gitkeep"
  - "lib/domain/services/.gitkeep"
  - "test/smoke_test.dart"
  - "android/**"
  - "README.md"
  - ".metadata"
  - "pubspec.lock"
autonomous: true
requirements: []
requirements_addressed: []
tags: [flutter, bootstrap, scaffold, pubspec]
must_haves:
  truths:
    - "`flutter pub get` completes successfully with zero warnings about missing/conflicting packages"
    - "`flutter run` boots an empty Material 3 Scaffold showing 'RideWindow — domain ready' text (manual verify deferred to Plan 10)"
    - "The full `lib/{core,domain,data,features,platform}/` tree exists, with `domain/{models,services}/` subdirs ready for code"
    - "Only Android platform is scaffolded — no `ios/`, `web/`, `linux/`, `macos/`, `windows/` directories exist"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Locked package versions for Phase 1 + reserved deps for Phases 2-10 commented out"
      contains: "name: ridewindow"
    - path: "analysis_options.yaml"
      provides: "flutter_lints baseline + generated file exclusions + Phase 1 lint rules"
      contains: "include: package:flutter_lints/flutter_lints.yaml"
    - path: "lib/main.dart"
      provides: "Minimal empty MaterialApp boot so `flutter run` succeeds"
      contains: "class RideWindowApp"
    - path: "lib/domain/models"
      provides: "Empty directory reserved for Plan 04/05 Freezed models"
    - path: "lib/domain/services"
      provides: "Empty directory reserved for Plans 06-09 scoring/slot services"
  key_links:
    - from: "lib/main.dart"
      to: "package:flutter/material.dart"
      via: "import (allowed — main.dart is OUTSIDE lib/domain/)"
      pattern: "import\\s+['\"]package:flutter/material"
---

<objective>
Bootstrap the Flutter project on the real project root. After Plan 01 verified Flutter SDK is installed and `dart test` works on a Flutter package, this plan runs `flutter create` against `/Users/joostmouw/ridewindow/`, writes the locked `pubspec.yaml` + `analysis_options.yaml` from RESEARCH.md, scaffolds the canonical `lib/` tree (per ARCHITECTURE.md), and writes a minimal `lib/main.dart` that boots an empty Material 3 `Scaffold`.

Purpose: Lay the architectural foundation that Phases 2-10 build on without renegotiating the project structure. Per CONTEXT.md Claude's Discretion "Skeleton scope", scaffold the full `lib/{core,domain,data,features,platform}/` tree but populate only `lib/domain/` with real code in subsequent plans.

Output: A bootstrapped Flutter project where `flutter pub get` succeeds and the canonical directory tree exists, ready for Plan 03 (structural test infrastructure) and Plans 04-09 (domain code).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-project-skeleton-scoring-domain/01-CONTEXT.md
@.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@/Users/joostmouw/ridewindow/CLAUDE.md
@.planning/phases/01-project-skeleton-scoring-domain/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Flutter project via `flutter create` (Android-only, fanalists org)</name>
  <read_first>
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Bootstrap → `flutter create` incantation (exact flags + rationale)
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Open Questions #4 (non-empty directory behavior)
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Bootstrap → Folders to leave alone
  </read_first>
  <action>
    The project root `/Users/joostmouw/ridewindow/` already contains `CLAUDE.md`, `mockup.html`, `.planning/`, `.git`, and `test/smoke_test.dart` (staged in Plan 01 Task 3). `flutter create` should additively scaffold the missing files without clobbering existing ones — but verify by working from a tempdir if the in-place invocation refuses (per RESEARCH.md Open Question #4 fallback).

    Steps:

    1. Try in-place: `cd /Users/joostmouw/ridewindow && flutter create --platforms=android --org=com.fanalists.ridewindow --description="RideWindow — cyclist-specific weather windows" --project-name=ridewindow .`
       - Per RESEARCH.md §Bootstrap, this should add `android/`, `lib/main.dart`, `test/widget_test.dart`, `pubspec.yaml`, `analysis_options.yaml`, `.metadata`, `pubspec.lock`, `README.md`, `.gitignore` WITHOUT touching `.planning/`, `CLAUDE.md`, `mockup.html`, `.git`, or `test/smoke_test.dart`.
    2. If `flutter create` REFUSES due to non-empty dir: fall back to the temp-dir approach — run `flutter create` in `/tmp/ridewindow-bootstrap/`, then `rsync -a --ignore-existing /tmp/ridewindow-bootstrap/ /Users/joostmouw/ridewindow/`.
    3. Verify ONLY `android/` was scaffolded as platform — NOT `ios/`, `web/`, `linux/`, `macos/`, `windows/`. If any of those appeared, `rm -rf` them.
    4. Delete the generated `test/widget_test.dart` (it tests the Flutter counter app, irrelevant here). The `test/smoke_test.dart` from Plan 01 Task 3 MUST remain.
    5. Verify `test/smoke_test.dart` (from Plan 01) still exists and was not overwritten.
    6. Do NOT yet run `flutter pub get` — that happens in Task 3 after pubspec is overwritten in Task 2.
  </action>
  <verify>
    <automated>test -d /Users/joostmouw/ridewindow/android && test -f /Users/joostmouw/ridewindow/lib/main.dart && test -f /Users/joostmouw/ridewindow/pubspec.yaml && test -f /Users/joostmouw/ridewindow/test/smoke_test.dart && ! test -d /Users/joostmouw/ridewindow/ios && ! test -d /Users/joostmouw/ridewindow/web && ! test -d /Users/joostmouw/ridewindow/linux && ! test -d /Users/joostmouw/ridewindow/macos && ! test -d /Users/joostmouw/ridewindow/windows && ! test -f /Users/joostmouw/ridewindow/test/widget_test.dart && echo "BOOTSTRAP OK"</automated>
  </verify>
  <acceptance_criteria>
    - `/Users/joostmouw/ridewindow/android/` directory exists (Android platform scaffolded)
    - `/Users/joostmouw/ridewindow/lib/main.dart` exists (will be overwritten in Task 4)
    - `/Users/joostmouw/ridewindow/pubspec.yaml` exists (will be overwritten in Task 2)
    - `/Users/joostmouw/ridewindow/test/smoke_test.dart` still exists (preserved from Plan 01)
    - `/Users/joostmouw/ridewindow/test/widget_test.dart` does NOT exist (deleted)
    - None of `ios/`, `web/`, `linux/`, `macos/`, `windows/` directories exist at the project root
    - Pre-existing files preserved: `CLAUDE.md`, `mockup.html`, `.planning/`, `.git`
  </acceptance_criteria>
  <done>The Android-only Flutter scaffold exists at the project root; widget_test.dart is gone; smoke_test.dart from Plan 01 is preserved; no unexpected platform directories were created.</done>
</task>

<task type="auto">
  <name>Task 2: Overwrite `pubspec.yaml` and `analysis_options.yaml` with Phase 1 locked configuration</name>
  <read_first>
    - /Users/joostmouw/ridewindow/pubspec.yaml (current state after `flutter create` — must Read to see what fields exist before overwriting)
    - /Users/joostmouw/ridewindow/analysis_options.yaml (current state)
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Bootstrap → pubspec.yaml (Phase 1) — EXACT versions to pin
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Bootstrap → analysis_options.yaml (Phase 1)
    - /Users/joostmouw/ridewindow/CLAUDE.md §Technology Stack (verified versions cross-check)
  </read_first>
  <action>
    Replace `pubspec.yaml` with the Phase 1 locked layout per RESEARCH.md §Bootstrap. Fields required (in this order):

    - `name: ridewindow`
    - `description: "RideWindow — cyclist-specific weather windows"`
    - `publish_to: 'none'`
    - `version: 0.1.0+1`
    - `environment:` block with `sdk: ">=3.6.0 <4.0.0"` and `flutter: ">=3.27.0"`
    - `dependencies:` — Phase 1 ONLY: `flutter` (SDK), `freezed_annotation: ^3.1.0`, `json_annotation: ^4.9.0`. Do NOT add Phase 2+ deps (drift, http, riverpod, etc.); those arrive in their respective phases.
    - `dev_dependencies:` — `flutter_test` (SDK), `test: ^1.25.0`, `freezed: ^3.2.5`, `json_serializable: ^6.8.0`, `build_runner: ^2.4.0`, `coverage: ^1.10.0`, `flutter_lints: ^5.0.0`
    - `flutter:` block with `uses-material-design: true` ONLY (no assets / fonts in Phase 1)

    Replace `analysis_options.yaml` per RESEARCH.md §Bootstrap → analysis_options.yaml:
    - `include: package:flutter_lints/flutter_lints.yaml`
    - `analyzer:` block with `errors: always_declare_return_types: error` and `exclude: ["**/*.freezed.dart", "**/*.g.dart"]`
    - `linter: rules:` block with `prefer_const_constructors: true`, `prefer_const_literals_to_create_immutables: true`, `avoid_print: true`, `require_trailing_commas: true`

    Append to `.gitignore` (do NOT delete the existing Flutter-default entries — only append):
    - A comment `# Phase 1 — coverage artifacts`
    - `coverage/`
    - DO NOT add `*.freezed.dart` or `*.g.dart` to .gitignore — generated files are committed per CONTEXT.md decisions and RESEARCH.md §Bootstrap rationale.

    Use the Edit tool (not Write) for `.gitignore` — preserve existing flutter-generated entries.
    Use the Write tool for `pubspec.yaml` and `analysis_options.yaml` — they are wholesale replacements with structure per RESEARCH.md.
  </action>
  <verify>
    <automated>grep -q 'name: ridewindow' /Users/joostmouw/ridewindow/pubspec.yaml && grep -q 'freezed_annotation: \^3' /Users/joostmouw/ridewindow/pubspec.yaml && grep -q 'test: \^1' /Users/joostmouw/ridewindow/pubspec.yaml && grep -q 'coverage: \^1' /Users/joostmouw/ridewindow/pubspec.yaml && grep -q 'flutter_lints: \^5' /Users/joostmouw/ridewindow/pubspec.yaml && grep -q 'flutter_lints/flutter_lints.yaml' /Users/joostmouw/ridewindow/analysis_options.yaml && grep -q '\*\*/\*.freezed.dart' /Users/joostmouw/ridewindow/analysis_options.yaml && grep -q '^coverage/$' /Users/joostmouw/ridewindow/.gitignore && echo "CONFIG OK"</automated>
  </verify>
  <acceptance_criteria>
    - `pubspec.yaml` contains exact name `ridewindow`, all 8 packages from RESEARCH.md §Bootstrap pinned to the documented versions, and DOES NOT contain Phase 2+ packages (drift, http, flutter_riverpod, geolocator, workmanager, flutter_local_notifications, go_router, google_sign_in, googleapis)
    - `analysis_options.yaml` includes `flutter_lints/flutter_lints.yaml` and excludes `*.freezed.dart`/`*.g.dart`
    - `.gitignore` has `coverage/` line appended and DOES NOT contain `*.freezed.dart` or `*.g.dart`
    - All three files are valid YAML / well-formed (no syntax errors)
  </acceptance_criteria>
  <done>The three config files reflect the Phase 1 locked stack from RESEARCH.md exactly; Phase 2+ deps are absent; generated files are committed not gitignored.</done>
</task>

<task type="auto">
  <name>Task 3: Run `flutter pub get` and verify package legitimacy via `flutter pub outdated`</name>
  <read_first>
    - /Users/joostmouw/ridewindow/pubspec.yaml (just written in Task 2)
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Package Legitimacy Audit (the [ASSUMED] tags + recommended outdated check)
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Assumptions Log A1
  </read_first>
  <action>
    Install dependencies and verify the [ASSUMED] packages from RESEARCH.md §Package Legitimacy Audit resolve to legitimate versions on pub.dev:

    1. From project root: `flutter pub get`. EXPECTED: all packages resolve, `pubspec.lock` is created, exit 0. If any package fails to resolve, STOP and report — likely a typo'd version in pubspec.
    2. `flutter pub outdated --no-dev-dependencies` — confirms runtime deps. EXPECTED: `freezed_annotation` and `json_annotation` show current versions matching what was pinned. No "discontinued" markers.
    3. `flutter pub outdated` (with dev deps) — confirms `freezed`, `json_serializable`, `build_runner`, `coverage`, `flutter_lints`, `test` are all resolvable and not discontinued.
    4. Read `pubspec.lock` and grep for each Phase 1 package to confirm it actually got installed at a sensible version (≥ the pinned version, < major+1).

    Mitigation per RESEARCH.md A1: if `flutter pub outdated` shows any package as "discontinued" or wildly newer (suggesting the pinned version is stale by years), pause and flag — the package may have been deprecated since the 2026-06-01 STACK.md verification.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter pub get && test -f pubspec.lock && grep -q 'freezed_annotation' pubspec.lock && grep -q 'test:' pubspec.lock && grep -q 'coverage' pubspec.lock && grep -q 'flutter_lints' pubspec.lock && echo "PUB GET OK"</automated>
  </verify>
  <acceptance_criteria>
    - `flutter pub get` exits 0
    - `pubspec.lock` exists and contains entries for all 8 Phase 1 packages (freezed_annotation, json_annotation, test, freezed, json_serializable, build_runner, coverage, flutter_lints) plus their transitive deps
    - `flutter pub outdated` reports no "discontinued" packages
    - No resolution errors in stderr
  </acceptance_criteria>
  <done>Phase 1 dev environment is fully installed; pubspec.lock captures the resolution; package legitimacy gate (Plan 01 Task 2 + this outdated check) is complete.</done>
</task>

<task type="auto">
  <name>Task 4: Scaffold `lib/{core,domain,data,features,platform}/` tree + write minimal `lib/main.dart`</name>
  <read_first>
    - /Users/joostmouw/ridewindow/lib/main.dart (current state — flutter-create generated counter app)
    - /Users/joostmouw/ridewindow/.planning/research/ARCHITECTURE.md §Recommended Folder Structure (canonical lib/ tree)
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-CONTEXT.md `<decisions>` Claude's Discretion → Skeleton scope (scaffold full tree, populate only domain)
    - /Users/joostmouw/ridewindow/.planning/phases/01-project-skeleton-scoring-domain/01-RESEARCH.md §Bootstrap → Minimal `lib/main.dart`
    - /Users/joostmouw/ridewindow/.planning/research/STACK.md (Material 3 default; ColorScheme.fromSeed cycling green 0xFF2E7D32)
  </read_first>
  <action>
    Create the canonical directory tree per ARCHITECTURE.md §Recommended Folder Structure, populated only under `lib/domain/` (Phase 1 scope per CONTEXT.md Claude's Discretion). Phases 2-10 will fill the other folders.

    Steps:

    1. Create directories: `mkdir -p lib/core lib/data lib/features lib/platform lib/domain/models lib/domain/services`
    2. Add `.gitkeep` files so empty dirs are preserved by git: `touch lib/core/.gitkeep lib/data/.gitkeep lib/features/.gitkeep lib/platform/.gitkeep lib/domain/models/.gitkeep lib/domain/services/.gitkeep`
    3. Overwrite `lib/main.dart` with the minimal empty Material 3 `Scaffold` per RESEARCH.md §Bootstrap → Minimal `lib/main.dart`. Required identifiers:
       - `import 'package:flutter/material.dart';`
       - `void main() { runApp(const RideWindowApp()); }`
       - `class RideWindowApp extends StatelessWidget` with `const RideWindowApp({super.key});` constructor
       - `build()` returns a `MaterialApp` with:
         - `title: 'RideWindow'`
         - `theme: ThemeData(colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xFF2E7D32)))` — cycling green per STACK.md
         - `home: const Scaffold(body: Center(child: Text('RideWindow — domain ready')))`
       - DO NOT include `useMaterial3: true` (deprecated since Flutter 3.16 per STACK.md — Material 3 is default).
       - DO NOT use `BottomNavigationBar` (deprecated per STACK.md — Phase 4 uses `NavigationBar`).
       - Add a top-of-file doc comment: `/// Phase 1: minimal MaterialApp boot. Real UI arrives in Phase 4 (Onboarding/Home/Welcome).`
    4. Do NOT add any imports beyond `package:flutter/material.dart` (no go_router, no flutter_riverpod — those arrive in Phase 3/4).

    Per CONTEXT.md decisions: `lib/main.dart` IS allowed to import `package:flutter` (it is in `lib/`, NOT `lib/domain/`). The structural test in Plan 03 only scans `lib/domain/`.
  </action>
  <verify>
    <automated>test -d /Users/joostmouw/ridewindow/lib/core && test -d /Users/joostmouw/ridewindow/lib/data && test -d /Users/joostmouw/ridewindow/lib/features && test -d /Users/joostmouw/ridewindow/lib/platform && test -d /Users/joostmouw/ridewindow/lib/domain/models && test -d /Users/joostmouw/ridewindow/lib/domain/services && grep -q 'class RideWindowApp' /Users/joostmouw/ridewindow/lib/main.dart && grep -q 'Phase 1: minimal MaterialApp boot' /Users/joostmouw/ridewindow/lib/main.dart && grep -q '0xFF2E7D32' /Users/joostmouw/ridewindow/lib/main.dart && ! grep -q 'useMaterial3' /Users/joostmouw/ridewindow/lib/main.dart && cd /Users/joostmouw/ridewindow && dart analyze --no-fatal-warnings lib/main.dart && echo "TREE OK"</automated>
  </verify>
  <acceptance_criteria>
    - All 6 directories exist: `lib/core/`, `lib/data/`, `lib/features/`, `lib/platform/`, `lib/domain/models/`, `lib/domain/services/`
    - Each empty directory has a `.gitkeep` to be preserved by git
    - `lib/main.dart` defines `RideWindowApp` extending `StatelessWidget`, uses `ColorScheme.fromSeed(seedColor: Color(0xFF2E7D32))`, contains the phase 1 doc comment, and does NOT contain `useMaterial3` (it is implicit per Flutter 3.16+)
    - `dart analyze lib/main.dart` exits 0 with no warnings
    - Smoke test from Plan 01 (`test/smoke_test.dart`) still exists and `dart test test/smoke_test.dart` passes
  </acceptance_criteria>
  <done>The canonical `lib/` tree is in place, `lib/main.dart` is a minimal empty Material 3 app, `dart analyze` is clean, and the Plan 01 smoke test still passes from this real project root.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project root ↔ Flutter SDK scaffolder | `flutter create` writes platform code (android/) we will ship; trust boundary crossed |
| pubspec.yaml ↔ pub.dev resolver | `flutter pub get` downloads and runs build-time code from pub.dev for build_runner et al. |
| analysis_options.yaml ↔ analyzer | Lint config controls what `dart analyze` enforces; misconfiguration silently weakens defenses |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Tampering | lib/domain/ accidentally importing Flutter/IO | mitigate | Structural test in Plan 03 enforces; this plan establishes the tree where the test will run |
| T-1-02 | Tampering | dev_dependencies executing arbitrary code at `flutter pub get` (build_runner, freezed) | mitigate | Plan 01 Task 2 blocking checkpoint verified each package on pub.dev; pubspec pins minor versions with `^`; Task 3 verifies no "discontinued" markers |
| T-1-PIN | Tampering | Stale pinned versions resolving to a yanked/compromised version | mitigate | Task 3 runs `flutter pub outdated` to surface stale pins |
| T-1-PLAT | EoP | Unwanted ios/web/linux/macos/windows scaffolds expanding attack surface | mitigate | Task 1 verifies only `android/` was created and removes any rogue platform dirs |
</threat_model>

<verification>
After all 4 tasks complete:

- `cd /Users/joostmouw/ridewindow && flutter pub get` exits 0
- `dart analyze lib/main.dart` exits 0 with no warnings
- `dart test test/smoke_test.dart` exits 0 (smoke test from Plan 01 still passes on the real project)
- `ls lib/` shows exactly: `core/`, `data/`, `domain/`, `features/`, `main.dart`, `platform/`
- `ls lib/domain/` shows: `models/`, `services/`
- No `ios/`, `web/`, `linux/`, `macos/`, `windows/` directories at project root
- `pubspec.lock` contains all 8 Phase 1 packages
</verification>

<success_criteria>
Plan 02 succeeds iff: (1) Android-only Flutter scaffold exists at project root with the locked pubspec/analysis config; (2) the canonical `lib/{core,domain,data,features,platform}/` tree exists with `.gitkeep` placeholders; (3) `lib/main.dart` is a minimal empty Material 3 Scaffold using cycling-green seed color and no deprecated APIs; (4) `flutter pub get` resolves cleanly; (5) `dart test test/smoke_test.dart` still passes (proving the spike conclusion holds on the real project).
</success_criteria>

<output>
Create `.planning/phases/01-project-skeleton-scoring-domain/01-02-SUMMARY.md` documenting: bootstrap command used, list of platform directories created (should be android/ only), list of Phase 1 packages installed (from pubspec.lock), confirmation that smoke_test.dart still passes, and the SHA-1 of pubspec.lock for future drift detection.
</output>
