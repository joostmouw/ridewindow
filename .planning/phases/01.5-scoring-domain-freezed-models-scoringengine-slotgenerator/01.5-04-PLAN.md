---
phase: "1.5"
plan: "01.5-04"
wave: 3
depends_on:
  - "01.5-02"
  - "01.5-03"
files_modified:
  - dart_test.yaml
  - test/domain/models/hourly_forecast_test.dart
  - test/domain/models/hourly_score_test.dart
  - test/domain/models/ride_slot_test.dart
  - test/domain/models/weather_tolerances_test.dart
  - test/domain/models/ride_tier_test.dart
  - test/domain/services/scoring_engine_test.dart
  - test/domain/services/slot_generator_test.dart
  - test/domain/services/availability_filter_test.dart
autonomous: true
requirements:
  - SCOR-01
  - SCOR-02
  - SCOR-04
  - SCOR-05
  - SLOT-01
  - SLOT-02
  - SLOT-03
  - SLOT-04
---

# Plan 01.5-04: Coverage verification — 100% line coverage of lib/domain/

<objective>
Run `dart test --coverage=coverage` for the full `test/domain/` suite, generate an LCOV report, and verify 100% line coverage of `lib/domain/` (excluding generated `*.freezed.dart` and `*.g.dart` files). Fix any uncovered lines discovered during this pass. This is the final gate for Phase 1.5.
</objective>

<tasks>

<task id="01.5-04-T01">
<title>Run full domain test suite and confirm all tests pass</title>
<type>execute</type>
<read_first>
- /Users/joostmouw/ridewindow/dart_test.yaml — check if test paths or tags are already configured
- /Users/joostmouw/ridewindow/pubspec.yaml — confirm coverage ^1.10.0 is in dev_dependencies
</read_first>
<action>
From /Users/joostmouw/ridewindow/, run:
  dart test test/domain/

All tests must pass. If any test fails, diagnose the root cause by reading the failing test file and the corresponding source file, fix the implementation (not the test), and re-run until green.

Also run the existing tests to ensure nothing in Phase 1 regressed:
  dart test test/

All tests (smoke, structural, domain) must exit 0.
</action>
<acceptance_criteria>
- `dart test test/domain/` exits 0 with no failures
- `dart test test/` exits 0 with no failures (includes smoke_test.dart and structural test from Phase 1)
</acceptance_criteria>
</task>

<task id="01.5-04-T02">
<title>Generate coverage report and measure lib/domain/ line coverage</title>
<type>execute</type>
<read_first>
- /Users/joostmouw/ridewindow/pubspec.yaml — confirm coverage package present
- /Users/joostmouw/ridewindow/dart_test.yaml — check existing configuration
</read_first>
<action>
From /Users/joostmouw/ridewindow/, run:
  dart test --coverage=coverage test/domain/

Then convert to LCOV format:
  dart run coverage:format_coverage --lcov --in=coverage --out=coverage/lcov.info --report-on=lib/domain

Then examine which lines are uncovered by inspecting coverage/lcov.info. Lines with DA:N,0 (hit count 0) in files under lib/domain/ that are NOT *.freezed.dart and NOT *.g.dart files indicate gaps.

To summarize coverage, run (if lcov is installed):
  lcov --summary coverage/lcov.info 2>/dev/null || grep -E "^(SF|DA)" coverage/lcov.info | grep -v ".freezed.dart" | grep -v ".g.dart" | grep "DA:" | grep ",0$" | head -30

Identify every uncovered line in lib/domain/ (excluding generated files). Document each uncovered line by its file path and line number before proceeding to T03.
</action>
<acceptance_criteria>
- coverage/lcov.info exists after running the commands
- The list of uncovered lines in lib/domain/ (excluding generated files) is identified (even if empty)
</acceptance_criteria>
</task>

<task id="01.5-04-T03">
<title>Fix coverage gaps — add tests for any uncovered lines</title>
<type>execute</type>
<read_first>
- coverage/lcov.info — the uncovered lines identified in T02
- The source file containing each uncovered line — read the actual source to understand what case is missing
- The corresponding test file — read before adding new cases to avoid duplicating existing tests
</read_first>
<action>
For each uncovered line identified in T02:
1. Read the source file to understand what code path is on that line.
2. Read the corresponding test file to understand what is already tested.
3. Add the minimal test case(s) needed to cover the uncovered line(s) to the existing test file.
4. Re-run `dart test test/domain/` to confirm the new test passes.
5. Re-run the coverage command from T02 to confirm the line is now covered.

Common gaps to check proactively (add tests now if not already present from earlier plans):
- ScoringEngine: the apparent temperature drag path when apparentTemperatureC is null (only temperatureC used)
- ScoringEngine: temperature exactly at tempMinIdealC and tempMaxIdealC (boundary = 100.0)
- SlotGenerator: the branch where scores.length < d (returns no slots for that duration)
- AvailabilityFilter: the apply() method called with an empty slots list
- RideTier: rideTierFromScore with a score of exactly 50.0 (→ Acceptable boundary)
- RideTier: rideTierFromScore with a score of exactly 85.0 (→ Perfect boundary)

Do NOT modify source files to remove code paths — only add tests. If a code path is unreachable, that is a source design issue, not a test issue; in that case remove the dead code from the source.

If no coverage gaps are found in T02, mark this task as no-op and proceed to T04.
</action>
<acceptance_criteria>
- All previously uncovered lines now have a corresponding test
- `dart test test/domain/` exits 0 after any additions
- No source files were changed to remove code paths (only test files were modified, or dead code was legitimately pruned from source with an explanation)
</acceptance_criteria>
</task>

<task id="01.5-04-T04">
<title>Final coverage verification — 100% line coverage of lib/domain/ (excluding generated files)</title>
<type>execute</type>
<read_first>
- coverage/lcov.info — regenerate after T03 to get the post-fix report
</read_first>
<action>
Re-run the full coverage pipeline from /Users/joostmouw/ridewindow/:
  dart test --coverage=coverage test/domain/ && dart run coverage:format_coverage --lcov --in=coverage --out=coverage/lcov.info --report-on=lib/domain

Verify 100% line coverage by checking that no DA lines with a 0 hit count exist in lcov.info for files that are not *.freezed.dart or *.g.dart:

Run this check to surface any remaining gaps:
  grep -v ".freezed.dart" coverage/lcov.info | grep -v ".g.dart" | grep "^DA:" | grep ",0$"

If this grep returns no output, coverage is 100% (no uncovered lines remain).
If it returns output, identify the file and line, return to T03 to fix, then re-run this verification.

Also run a final full-suite check to ensure Phase 1 tests still pass:
  dart test test/
  dart analyze lib/domain/
</action>
<acceptance_criteria>
- `grep -v ".freezed.dart" coverage/lcov.info | grep -v ".g.dart" | grep "^DA:" | grep ",0$"` returns no output
- `dart test test/` exits 0 (all tests including Phase 1 structural + smoke tests)
- `dart analyze lib/domain/` exits 0
- Phase 1.5 success criterion 7 is met: 100% line coverage of lib/domain/ excluding generated files
</acceptance_criteria>
</task>

<task id="01.5-04-T05">
<title>Verify all Phase 1.5 success criteria are demonstrably met</title>
<type>execute</type>
<read_first>
- /Users/joostmouw/ridewindow/.planning/ROADMAP.md — Phase 1.5 success criteria (7 items)
- /Users/joostmouw/ridewindow/lib/domain/models/ — confirm all five models and generated files exist
- /Users/joostmouw/ridewindow/lib/domain/services/ — confirm ScoringEngine, SlotGenerator, AvailabilityFilter exist
</read_first>
<action>
Run each verification command listed below and confirm the result matches the expected output. Document any that fail and fix before completing the plan.

1. Confirm model files exist (run ls):
   ls /Users/joostmouw/ridewindow/lib/domain/models/
   Expected: hourly_forecast.dart, hourly_forecast.freezed.dart, hourly_forecast.g.dart, hourly_score.dart, hourly_score.freezed.dart, ride_slot.dart, ride_slot.freezed.dart, ride_tier.dart, weather_tolerances.dart, weather_tolerances.freezed.dart, weather_tolerances.g.dart

2. Confirm service files exist:
   ls /Users/joostmouw/ridewindow/lib/domain/services/
   Expected: scoring_engine.dart, slot_generator.dart, availability_filter.dart

3. Confirm no Flutter imports in domain/:
   grep -r "package:flutter" /Users/joostmouw/ridewindow/lib/domain/
   Expected: no output

4. Full test suite:
   dart test test/
   Expected: exits 0

5. Static analysis:
   dart analyze lib/domain/
   Expected: exits 0

6. Coverage gate (from T04):
   grep -v ".freezed.dart" coverage/lcov.info | grep -v ".g.dart" | grep "^DA:" | grep ",0$"
   Expected: no output (0 uncovered lines)

7. Tier thresholds (spot-check via test output grep):
   dart test test/domain/models/ride_tier_test.dart --reporter=expanded
   Expected: 8 passing tests, all boundary values covered
</action>
<acceptance_criteria>
- All 7 verification commands above return expected output
- All 7 Phase 1.5 success criteria from ROADMAP.md are demonstrably met
- Phase 1.5 can be marked complete
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `dart test test/` exits 0 (full suite including Phase 1 and Phase 1.5 tests)
2. `dart analyze lib/domain/` exits 0
3. `grep -v ".freezed.dart" coverage/lcov.info | grep -v ".g.dart" | grep "^DA:" | grep ",0$"` returns no output
4. `grep -r "package:flutter" lib/domain/` returns no output
5. All five model files have corresponding *.freezed.dart files present on disk
6. HourlyForecast and WeatherTolerances have *.g.dart files (JSON serialization)
7. ROADMAP.md Phase 1.5 success criteria 1–7 are all demonstrably true
</verification>

<must_haves>
- 100% line coverage of lib/domain/ is verified by an LCOV grep showing zero DA:N,0 lines for non-generated files
- *.freezed.dart and *.g.dart files are present on disk (generated and committed, not gitignored)
- No Flutter imports exist anywhere under lib/domain/ — verified by grep
- The full test suite (including Phase 1 smoke + structural tests) passes without regression
- dart analyze lib/domain/ exits 0 with no warnings or errors
- Phase 1.5 is not marked complete until all seven ROADMAP success criteria are verifiably met
</must_haves>
