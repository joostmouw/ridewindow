---
phase: "1.5"
plan: "01.5-01"
wave: 1
depends_on: []
files_modified:
  - lib/domain/models/hourly_forecast.dart
  - lib/domain/models/hourly_forecast.freezed.dart
  - lib/domain/models/hourly_forecast.g.dart
  - lib/domain/models/hourly_score.dart
  - lib/domain/models/hourly_score.freezed.dart
  - lib/domain/models/ride_slot.dart
  - lib/domain/models/ride_slot.freezed.dart
  - lib/domain/models/weather_tolerances.dart
  - lib/domain/models/weather_tolerances.freezed.dart
  - lib/domain/models/weather_tolerances.g.dart
  - lib/domain/models/ride_tier.dart
  - test/domain/models/hourly_forecast_test.dart
  - test/domain/models/hourly_score_test.dart
  - test/domain/models/ride_slot_test.dart
  - test/domain/models/weather_tolerances_test.dart
  - test/domain/models/ride_tier_test.dart
autonomous: true
requirements:
  - SCOR-04
  - SLOT-04
---

# Plan 01.5-01: Domain models — Freezed models + build_runner setup

<objective>
Create all five domain model types under `lib/domain/models/` using Freezed, run build_runner to generate `*.freezed.dart` and `*.g.dart` files, and write unit tests covering equality, copyWith, and JSON round-trip. This is the foundation all other Phase 1.5 plans depend on.
</objective>

<tasks>

<task id="01.5-01-T01">
<title>Create HourlyForecast Freezed model with JSON serialization</title>
<type>execute</type>
<read_first>
- /Users/joostmouw/ridewindow/pubspec.yaml — confirm freezed ^3.2.5, freezed_annotation ^3.1.0, json_annotation ^4.9.0, json_serializable ^6.8.0 are present
- /Users/joostmouw/ridewindow/lib/domain/models/ — confirm directory is empty, no prior files to conflict
</read_first>
<action>
Create /Users/joostmouw/ridewindow/lib/domain/models/hourly_forecast.dart.

The file must:
- Import 'package:freezed_annotation/freezed_annotation.dart'
- Include the two part directives: `part 'hourly_forecast.freezed.dart';` and `part 'hourly_forecast.g.dart';`
- Define a @freezed class HourlyForecast with factory HourlyForecast({...})
- All six weather fields modeled as nullable doubles matching the Open-Meteo field names used as @JsonKey names:
  - @JsonKey(name: 'temperature_2m') required double? temperatureC
  - @JsonKey(name: 'apparent_temperature') required double? apparentTemperatureC
  - @JsonKey(name: 'precipitation') required double? precipitationMm
  - @JsonKey(name: 'precipitation_probability') required double? precipitationProbability
  - @JsonKey(name: 'windspeed_10m') required double? windspeedKmh
  - @JsonKey(name: 'winddirection_10m') required double? winddirectionDeg
  - required DateTime time (non-nullable; represents the forecast hour)
- Include factory HourlyForecast.fromJson(Map<String, dynamic> json) => _$HourlyForecastFromJson(json);
</action>
<acceptance_criteria>
- /Users/joostmouw/ridewindow/lib/domain/models/hourly_forecast.dart exists and contains `@freezed`, all seven fields, and `fromJson` factory
- `dart analyze lib/domain/models/hourly_forecast.dart` exits 0 after generation (run after T05)
</acceptance_criteria>
</task>

<task id="01.5-01-T02">
<title>Create HourlyScore Freezed model</title>
<type>execute</type>
<read_first>
- /Users/joostmouw/ridewindow/lib/domain/models/hourly_forecast.dart — understand pattern to replicate
</read_first>
<action>
Create /Users/joostmouw/ridewindow/lib/domain/models/hourly_score.dart.

The file must:
- Import 'package:freezed_annotation/freezed_annotation.dart'
- Include the part directive: `part 'hourly_score.freezed.dart';` (no JSON part — HourlyScore is computed, not deserialized)
- Define a @freezed class HourlyScore with factory HourlyScore({...}):
  - required double overall (clamped 0–100 by the engine, not the model)
  - required double temperatureScore
  - required double rainScore
  - required double windScore
  - required DateTime time (the hour this score applies to)
- No fromJson factory needed.
</action>
<acceptance_criteria>
- /Users/joostmouw/ridewindow/lib/domain/models/hourly_score.dart exists with `@freezed` and all five fields
- No `part '*.g.dart'` line present (no JSON serialization)
</acceptance_criteria>
</task>

<task id="01.5-01-T03">
<title>Create WeatherTolerances Freezed model with JSON serialization and defaults</title>
<type>execute</type>
<read_first>
- /Users/joostmouw/ridewindow/lib/domain/models/hourly_forecast.dart — pattern reference
- /Users/joostmouw/.planning/REQUIREMENTS.md — SCOR-02 defaults: temp 12–26°C ideal, wind <15 km/h ideal, rain <0.5mm ideal
</read_first>
<action>
Create /Users/joostmouw/ridewindow/lib/domain/models/weather_tolerances.dart.

The file must:
- Import 'package:freezed_annotation/freezed_annotation.dart'
- Include part directives: `part 'weather_tolerances.freezed.dart';` and `part 'weather_tolerances.g.dart';`
- Define a @freezed class WeatherTolerances with factory WeatherTolerances({...}):
  - @Default(12.0) double tempMinIdealC — lower bound of ideal temperature range
  - @Default(26.0) double tempMaxIdealC — upper bound of ideal temperature range
  - @Default(15.0) double windMaxIdealKmh — max ideal wind speed
  - @Default(0.5) double rainMaxIdealMm — max ideal precipitation per hour
- Include factory WeatherTolerances.fromJson(Map<String, dynamic> json) => _$WeatherTolerancesFromJson(json);
- Include a const factory WeatherTolerances() (or rely on defaults via the standard Freezed default constructor).
</action>
<acceptance_criteria>
- /Users/joostmouw/ridewindow/lib/domain/models/weather_tolerances.dart exists with `@freezed`, four fields with @Default values, and `fromJson` factory
</acceptance_criteria>
</task>

<task id="01.5-01-T04">
<title>Create RideSlot Freezed model and sealed RideTier class</title>
<type>execute</type>
<read_first>
- /Users/joostmouw/ridewindow/lib/domain/models/hourly_score.dart — pattern reference
- /Users/joostmouw/.planning/REQUIREMENTS.md — SLOT-04: Perfect ≥85, Great 70–84, Acceptable 50–69, Poor <50
- /Users/joostmouw/CLAUDE.md — Dart 3 sealed class pattern: sealed class + final class subclasses
</read_first>
<action>
Create two files:

FILE 1: /Users/joostmouw/ridewindow/lib/domain/models/ride_tier.dart

Define the sealed class hierarchy using Dart 3 sealed classes (no Freezed, no annotations needed):
  sealed class RideTier {}
  final class Perfect extends RideTier { const Perfect(); }
  final class Great extends RideTier { const Great(); }
  final class Acceptable extends RideTier { const Acceptable(); }
  final class Poor extends RideTier { const Poor(); }

Also add a top-level function to convert a score to a tier:
  RideTier rideTierFromScore(double score) {
    if (score >= 85) return const Perfect();
    if (score >= 70) return const Great();
    if (score >= 50) return const Acceptable();
    return const Poor();
  }

FILE 2: /Users/joostmouw/ridewindow/lib/domain/models/ride_slot.dart

The file must:
- Import 'package:freezed_annotation/freezed_annotation.dart'
- Import 'ride_tier.dart'
- Include part directive: `part 'ride_slot.freezed.dart';` (no JSON part)
- Define a @freezed class RideSlot with factory RideSlot({...}):
  - required DateTime start — inclusive start of slot
  - required DateTime end — exclusive end of slot (the [start, end) convention)
  - required double overallScore — average score across the slot's hours
  - required RideTier tier — computed tier for the slot
  - required List<HourlyScore> hours — the individual hourly scores making up this slot
- Import hourly_score.dart for the List<HourlyScore> field.
- No fromJson factory needed.
</action>
<acceptance_criteria>
- /Users/joostmouw/ridewindow/lib/domain/models/ride_tier.dart exists with `sealed class RideTier`, all four subclasses, and `rideTierFromScore` function
- /Users/joostmouw/ridewindow/lib/domain/models/ride_slot.dart exists with `@freezed`, `start`, `end`, `overallScore`, `tier`, and `hours` fields
</acceptance_criteria>
</task>

<task id="01.5-01-T05">
<title>Run build_runner to generate *.freezed.dart and *.g.dart files</title>
<type>execute</type>
<read_first>
- /Users/joostmouw/ridewindow/pubspec.yaml — verify build_runner ^2.4.0 is in dev_dependencies
- /Users/joostmouw/ridewindow/lib/domain/models/ — all four source files from T01–T04 must exist before running
</read_first>
<action>
From the project root /Users/joostmouw/ridewindow/, run:
  dart run build_runner build --delete-conflicting-outputs

This must generate:
  - lib/domain/models/hourly_forecast.freezed.dart
  - lib/domain/models/hourly_forecast.g.dart
  - lib/domain/models/hourly_score.freezed.dart
  - lib/domain/models/weather_tolerances.freezed.dart
  - lib/domain/models/weather_tolerances.g.dart
  - lib/domain/models/ride_slot.freezed.dart

After generation, run `dart analyze lib/domain/` and confirm it exits 0 with no errors.
</action>
<acceptance_criteria>
- All six generated files exist in /Users/joostmouw/ridewindow/lib/domain/models/
- `dart analyze lib/domain/` exits 0
</acceptance_criteria>
</task>

<task id="01.5-01-T06">
<title>Write unit tests for all five domain models</title>
<type>execute</type>
<read_first>
- /Users/joostmouw/ridewindow/lib/domain/models/hourly_forecast.dart and its generated files — understand available API (copyWith, ==, fromJson/toJson)
- /Users/joostmouw/ridewindow/lib/domain/models/weather_tolerances.dart — confirm @Default values for testing
- /Users/joostmouw/ridewindow/lib/domain/models/ride_tier.dart — rideTierFromScore boundaries
- /Users/joostmouw/ridewindow/dart_test.yaml — check test configuration
</read_first>
<action>
Create the test directory /Users/joostmouw/ridewindow/test/domain/models/ and write five test files.

FILE: test/domain/models/hourly_forecast_test.dart
Tests:
- equality: two HourlyForecast instances with identical fields are equal (==)
- copyWith: copyWith(precipitationMm: 1.0) returns a new instance with only that field changed
- JSON round-trip: HourlyForecast.fromJson(instance.toJson()) equals the original instance; use a fixture where all fields are non-null and one where all nullable fields are null (time must always be non-null)

FILE: test/domain/models/hourly_score_test.dart
Tests:
- equality: two HourlyScore instances with identical fields are equal
- copyWith: copyWith(overall: 75.0) returns a new instance with updated overall

FILE: test/domain/models/weather_tolerances_test.dart
Tests:
- defaults: WeatherTolerances() has tempMinIdealC == 12.0, tempMaxIdealC == 26.0, windMaxIdealKmh == 15.0, rainMaxIdealMm == 0.5
- equality: two WeatherTolerances() instances are equal
- JSON round-trip: WeatherTolerances.fromJson(instance.toJson()) equals the original

FILE: test/domain/models/ride_tier_test.dart
Tests (for rideTierFromScore):
- score 100.0 → Perfect
- score 85.0 → Perfect (boundary, inclusive)
- score 84.9 → Great
- score 70.0 → Great (boundary, inclusive)
- score 69.9 → Acceptable
- score 50.0 → Acceptable (boundary, inclusive)
- score 49.9 → Poor
- score 0.0 → Poor

FILE: test/domain/models/ride_slot_test.dart
Tests:
- equality: two RideSlot instances with identical fields are equal
- copyWith: copyWith(overallScore: 90.0) returns updated instance
- tier assignment: a RideSlot with overallScore 86.0 has tier that is Perfect (use is Perfect pattern match)
- end exclusivity: end == start + duration means start is included and end is not (document this as a comment in the test explaining the [start, end) convention)
</action>
<acceptance_criteria>
- /Users/joostmouw/ridewindow/test/domain/models/ contains five test files
- `dart test test/domain/models/` exits 0 with all tests passing
- `dart analyze test/domain/models/` exits 0
</acceptance_criteria>
</task>

</tasks>

<verification>
1. `dart analyze lib/domain/models/` exits 0
2. `dart test test/domain/models/` exits 0
3. All six generated files exist: hourly_forecast.freezed.dart, hourly_forecast.g.dart, hourly_score.freezed.dart, weather_tolerances.freezed.dart, weather_tolerances.g.dart, ride_slot.freezed.dart
4. rideTierFromScore boundary tests cover all four tier thresholds (85, 70, 50) with inclusive lower bound assertions
5. JSON round-trip tests pass for HourlyForecast and WeatherTolerances
</verification>

<must_haves>
- HourlyForecast has all seven fields (six nullable weather doubles + DateTime time) and a fromJson factory
- HourlyScore has five fields (overall, temperatureScore, rainScore, windScore, time) and no JSON serialization
- WeatherTolerances defaults are exactly: tempMinIdealC=12.0, tempMaxIdealC=26.0, windMaxIdealKmh=15.0, rainMaxIdealMm=0.5
- RideTier is a sealed class with four final subclasses: Perfect, Great, Acceptable, Poor
- rideTierFromScore uses inclusive thresholds: ≥85 Perfect, ≥70 Great, ≥50 Acceptable, else Poor
- RideSlot uses the [start, end) exclusive-end convention documented in comments
- All *.freezed.dart and *.g.dart files are committed (generated, not gitignored)
- `dart test test/domain/models/` exits 0
</must_haves>
