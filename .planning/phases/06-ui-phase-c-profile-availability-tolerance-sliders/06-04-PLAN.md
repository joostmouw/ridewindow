---
phase: 06-ui-phase-c-profile-availability-tolerance-sliders
plan: "04"
type: execute
wave: 4
depends_on: [06-02, 06-03]
files_modified:
  - test/features/profile_screen_test.dart
  - test/features/availability_screen_test.dart
autonomous: true
requirements: [PROF-01, PROF-02, PROF-04, AVAIL-01, AVAIL-02, AVAIL-03]

must_haves:
  truths:
    - "ProfileScreen widget-tests bewijzen aanwezigheid van vier sliders, drie chips en de SegmentedButton"
    - "AvailabilityScreen widget-tests bewijzen drie celstaten en de tap-tap-guard voor werk-blokken"
    - "Alle nieuwe tests bestaan als deel van de volledige suite (flutter test exit 0)"
  artifacts:
    - path: test/features/profile_screen_test.dart
      provides: "Widget-tests voor ProfileScreen (sliders, chips, thema)"
    - path: test/features/availability_screen_test.dart
      provides: "Widget-tests voor AvailabilityScreen (celstaten, tap-guard)"
  key_links:
    - from: test/features/profile_screen_test.dart
      to: lib/features/profile/profile_screen.dart
      via: "ProviderScope override van profileProvider"
      pattern: "profileProvider.overrideWith"
    - from: test/features/availability_screen_test.dart
      to: lib/features/availability/availability_screen.dart
      via: "ProviderScope override van availabilityProvider"
      pattern: "availabilityProvider.overrideWith"
---

<objective>
Widget-tests schrijven voor ProfileScreen (sliders, chips, thema-selector) en
AvailabilityScreen (drie celstaten, werk-tap-guard).

Purpose: Regressiebescherming voor alle Phase 6 success criteria — bewijst dat de UI
weergeeft wat de providers bevatten en dat interactie de juiste mutators aanroept.

Output: Twee nieuwe test-bestanden; volledige testsuites groen (≥145 + nieuwe tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-CONTEXT.md
@.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-PATTERNS.md
@.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-02-SUMMARY.md
@.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-03-SUMMARY.md

<interfaces>
<!-- Testpatronen gevestigd in Phase 4/5 — gebruik exact deze patronen. -->

setUp-patroon (alle Phase 4/5 tests):
```dart
setUp(() => SharedPreferences.setMockInitialValues({}));
```

FakeNotifier-patroon (extends concrete klasse, NIET de _$-abstracte):
```dart
class FakeProfileNotifier extends ProfileNotifier {
  final UserProfile fakeProfile;
  FakeProfileNotifier(this.fakeProfile);
  @override
  Future<UserProfile> build() async => fakeProfile;
}

class FakeAvailabilityNotifier extends AvailabilityNotifier {
  final Map<DateTime, BlockType> fakeMap;
  FakeAvailabilityNotifier(this.fakeMap);
  @override
  Future<Map<DateTime, BlockType>> build() async => fakeMap;
}
```

ProviderScope override-patroon:
```dart
await tester.pumpWidget(
  ProviderScope(
    overrides: [
      profileProvider.overrideWith(() => FakeProfileNotifier(testProfile)),
    ],
    child: MaterialApp(home: const ProfileScreen()),
  ),
);
await tester.pump();
await tester.pump(const Duration(milliseconds: 100));
```

Pump-patroon: NOOIT pumpAndSettle — gebruik pump() + pump(Duration(milliseconds: 100))

TestProfile-fixture voor ProfileScreen tests:
```dart
const testProfile = UserProfile(
  tolerances: WeatherTolerances(
    tempMinIdealC: 12.0, tempMaxIdealC: 26.0,
    windMaxIdealKmh: 15.0, rainMaxIdealMm: 0.5,
  ),
  allowedDurations: [2, 3, 5],
  theme: 'system',
  notifEveningBefore: false,
  notifMorningOf: false,
  notifWeeklyDigest: false,
);
```

TestMap-fixture voor AvailabilityScreen tests:
```dart
// Huidige week maandag (weekday-arithmetic)
final now = DateTime.now();
final weekStart = now.subtract(Duration(days: now.weekday - DateTime.monday));
final freeMap = <DateTime, BlockType>{};
final mapWithWork = {
  DateTime.utc(weekStart.year, weekStart.month, weekStart.day, 9): BlockType.work,
};
final mapWithCustom = {
  DateTime.utc(weekStart.year, weekStart.month, weekStart.day, 10): BlockType.custom,
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: Widget-tests voor ProfileScreen</name>
  <files>test/features/profile_screen_test.dart</files>
  <behavior>
    - Test 1: ProfileScreen toont vier Slider-widgets bij een geladen profiel
    - Test 2: Sectiekopteksten 'TOLERANTIES' en 'RIJLENGTE' en 'THEMA' aanwezig
    - Test 3: Drie FilterChip-widgets aanwezig; chip '2u' is selected (testProfile.allowedDurations bevat 2)
    - Test 4: SegmentedButton aanwezig; segment 'Systeem' is geselecteerd (testProfile.theme == 'system')
    - Test 5: Knop/tegel 'Mijn schema bewerken' aanwezig
  </behavior>
  <action>
    Maak `test/features/profile_screen_test.dart` aan. Volg het testpatroon uit
    `test/features/home_screen_test.dart` en `test/features/ride_detail_screen_test.dart`.

    Imports die nodig zijn:
    - flutter_test
    - flutter/material.dart
    - flutter_riverpod
    - shared_preferences
    - package:ridewindow/features/profile/profile_screen.dart
    - package:ridewindow/providers/profile_notifier.dart
    - package:ridewindow/domain/models/weather_tolerances.dart

    Definieer `const testProfile` (zoals in de interface-sectie boven).
    Definieer `FakeProfileNotifier` (extends ProfileNotifier).

    Schrijf `testWidgets` voor de vijf gedragspunten boven.
    Gebruik `ProviderScope(overrides: [profileProvider.overrideWith(...)], child: MaterialApp(home: const ProfileScreen()))`.
    Pump-patroon: `pump()` + `pump(const Duration(milliseconds: 100))`.

    Test 1: `expect(find.byType(Slider), findsNWidgets(4))`.
    Test 2: `expect(find.text('TOLERANTIES'), findsOneWidget)` etc.
    Test 3: Zoek FilterChip-widgets: `find.byType(FilterChip)` → `findsNWidgets(3)`.
             Zoek specifiek op geselecteerde chip: `find.byWidgetPredicate((w) => w is FilterChip && w.label is Text && (w.label as Text).data == '2u' && w.selected == true)`.
    Test 4: `expect(find.byType(SegmentedButton<String>), findsOneWidget)`.
    Test 5: `expect(find.text('Mijn schema bewerken'), findsOneWidget)`.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/features/profile_screen_test.dart --no-pub 2>&1 | tail -5</automated>
  </verify>
  <done>
    test/features/profile_screen_test.dart bestaat met ≥5 testWidgets,
    alle tests slagen, geen failures of errors.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: Widget-tests voor AvailabilityScreen</name>
  <files>test/features/availability_screen_test.dart</files>
  <behavior>
    - Test 1: AvailabilityScreen toont AppBar met tekst 'Mijn schema'
    - Test 2: Dag-kopteksten 'Ma' t/m 'Zo' (of equivalent) aanwezig in het rooster
    - Test 3: Met een kaart met één werk-blok: dat cel heeft de werk-kleur (0xFFB0BEC5)
    - Test 4: Met een kaart met één custom-blok: dat cel heeft de custom-kleur (0xFFFF9800)
    - Test 5: Tappen op een werk-cel: toggleCustomHour wordt NIET aangeroepen
              (verificatie: cel-kleur verandert niet na tap)
  </behavior>
  <action>
    Maak `test/features/availability_screen_test.dart` aan. Gebruik hetzelfde patroon
    als profile_screen_test.dart maar met availabilityProvider.

    Imports die nodig zijn:
    - flutter_test
    - flutter/material.dart
    - flutter_riverpod
    - shared_preferences
    - package:ridewindow/features/availability/availability_screen.dart
    - package:ridewindow/providers/availability_notifier.dart

    Definieer `FakeAvailabilityNotifier` (extends AvailabilityNotifier).
    Bereken `weekStart` in setUp of per test.

    Test 1: Pump met lege map → `expect(find.text('Mijn schema'), findsOneWidget)`.

    Test 2: Pump met lege map → `expect(find.text('Ma'), findsOneWidget)`.
             (of welk dagindicatoren de implementatie ook gebruikt — pas aan op implementatie)

    Test 3: Pump met mapWithWork (uur 9 van dag 0 is werk) →
             Zoek Container met backgroundColor == const Color(0xFFB0BEC5) via
             `find.byWidgetPredicate((w) => w is Container && (w.decoration as BoxDecoration?)?.color == const Color(0xFFB0BEC5))`.
             `expect(..., findsWidgets)` (minstens één werk-gekleurde cel aanwezig).

    Test 4: Pump met mapWithCustom (uur 10 van dag 0 is custom) →
             Zoek Container met backgroundColor == const Color(0xFFFF9800).
             `expect(..., findsWidgets)`.

    Test 5: Werk-tap-guard: pump met mapWithWork. Tik op de werk-cel.
             Na `pump()`, verifieer dat de cel nog steeds werk-kleur heeft:
             `expect(find.byWidgetPredicate((w) => w is Container && (w.decoration as BoxDecoration?)?.color == const Color(0xFFB0BEC5)), findsWidgets)`.
             De tap zou geen kleurwijziging mogen veroorzaken.

    Pump-patroon voor alle tests: `pump()` + `pump(const Duration(milliseconds: 100))`.

    Opmerking: Het rooster bevat 168 cellen (7×24). Voor cel-zoeken op kleur gebruik
    `findsWidgets` (niet `findsOneWidget`) vanwege meerdere overeenkomsten. Voor werk-
    en custom-testen is het voldoende om ≥1 cel met de verwachte kleur te bevestigen.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/features/availability_screen_test.dart --no-pub 2>&1 | tail -5</automated>
  </verify>
  <done>
    test/features/availability_screen_test.dart bestaat met ≥5 testWidgets,
    alle tests slagen, geen failures of errors.
    Volledige suite groen: `flutter test --no-pub` exit code 0.
  </done>
</task>

</tasks>

<threat_model>
## Vertrouwensgrenzen

| Grens | Omschrijving |
|-------|-------------|
| Test-fixtures → FakeNotifier | Fake-data is hardcoded in tests — geen externe invoer |

## STRIDE Dreigingsregister

| Dreiging-ID | Categorie | Component | Dispositie | Mitigatieplan |
|-------------|-----------|-----------|------------|---------------|
| T-06-09 | Spoofing | FakeNotifier in tests | accept | Fake-notifiers overschrijven alleen build(); state-mutators worden getest via integratie, niet via unit-mocks |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Geen nieuwe pakketten in Wave 4 |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow

# 1. ProfileScreen tests individueel
flutter test test/features/profile_screen_test.dart --no-pub 2>&1 | tail -5

# 2. AvailabilityScreen tests individueel
flutter test test/features/availability_screen_test.dart --no-pub 2>&1 | tail -5

# 3. Volledige suite — alle tests groen (verwacht: >= 155 tests, 0 failures)
flutter test --no-pub 2>&1 | tail -3

# 4. Correcte testbestanden aangemaakt
ls test/features/profile_screen_test.dart test/features/availability_screen_test.dart
```
</verification>

<success_criteria>
1. `test/features/profile_screen_test.dart` bestaat met ≥5 testWidgets die allemaal slagen
2. `test/features/availability_screen_test.dart` bestaat met ≥5 testWidgets die allemaal slagen
3. Volledige testsuite slaagt (`flutter test --no-pub` exit code 0) met ≥155 tests
4. SC-5 (werk-tap-guard) expliciet bewezen via een test die celkleur controleert na tap
</success_criteria>

<output>
Maak `.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-04-SUMMARY.md`
aan na voltooiing.
</output>
