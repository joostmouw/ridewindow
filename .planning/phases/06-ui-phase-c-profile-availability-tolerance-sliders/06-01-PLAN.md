---
phase: 06-ui-phase-c-profile-availability-tolerance-sliders
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/providers/theme_mode_provider.dart
  - lib/app/router.dart
  - lib/features/home/home_screen.dart
  - lib/main.dart
  - lib/features/profile/profile_screen.dart
autonomous: true
requirements: [PROF-04]

must_haves:
  truths:
    - "/profile route bestaat en is bereikbaar via de bottom-navigatiebalk"
    - "Het tappen van 'Profiel' in de NavigationBar navigeert naar ProfileScreen"
    - "themeModeProvider geeft ThemeMode.light/dark/system terug op basis van profile.theme"
    - "MaterialApp.router reageert direct op themeModeProvider-wijzigingen"
    - "ProfileScreen toont een AppBar 'Profiel' en een skeleton-structuur met secties"
  artifacts:
    - path: lib/providers/theme_mode_provider.dart
      provides: "ThemeMode-provider die profile.theme String omzet"
      exports: [themeModeProvider]
    - path: lib/features/profile/profile_screen.dart
      provides: "ProfileScreen skeleton (ConsumerStatefulWidget)"
      contains: "class ProfileScreen"
    - path: lib/providers/theme_mode_provider.g.dart
      provides: "Gegenereerde Riverpod code voor themeModeProvider"
  key_links:
    - from: lib/main.dart
      to: lib/providers/theme_mode_provider.dart
      via: "ref.watch(themeModeProvider)"
      pattern: "themeModeProvider"
    - from: lib/features/home/home_screen.dart
      to: lib/features/profile/profile_screen.dart
      via: "context.go('/profile')"
      pattern: "go.*profile"
    - from: lib/app/router.dart
      to: lib/features/profile/profile_screen.dart
      via: "GoRoute path: '/profile'"
      pattern: "path.*profile"
---

<objective>
ThemeModeProvider aanmaken, /profile route toevoegen aan de router, de bottom-navigatiebalk
in HomeScreen verbinden, en een ProfileScreen-skeleton leveren.

Purpose: Legt de infrastructuur voor Phase 6 — navigatie naar Profiel werkt, het thema
reageert op profiel-wijzigingen, de ProfileScreen is zichtbaar en gereed voor Wave 2.

Output: theme_mode_provider.dart (+ gegenereerd .g.dart), profilescreen-skeleton,
/profile route in router.dart, HomeScreen bottomNav wired, main.dart themeMode.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-CONTEXT.md
@.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-PATTERNS.md

<interfaces>
<!-- Bestaande providers en klassen die de uitvoerder nodig heeft. -->

Uit lib/providers/profile_notifier.dart:
```dart
class UserProfile {
  final WeatherTolerances tolerances;
  final List<int> allowedDurations;  // waarden: [2, 3, 5]
  final String theme;                // 'system' | 'light' | 'dark'
  // ...
}

// Provider-naam: profileProvider (gegenereerd door @riverpod class ProfileNotifier)
// Mutators: updateTolerances(), toggleDuration(), setTheme()
```

Uit lib/providers/location_provider.dart (functioneel provider-patroon):
```dart
@riverpod
LocationData location(Ref ref) {
  return const LocationData(...);
}
// part 'location_provider.g.dart'; is vereist
```

Uit lib/app/router.dart (bestaande routes, te wijzigen):
```dart
routes: [
  GoRoute(path: '/welcome', ...),
  GoRoute(path: '/onboard', ...),
  GoRoute(path: '/home', ...),
  GoRoute(path: '/availability', ...),
  GoRoute(path: '/detail', ...),
  // TOEVOEGEN: GoRoute(path: '/profile', ...)
],
```

Uit lib/features/home/home_screen.dart (_buildBottomNav, te wijzigen):
```dart
NavigationBar(
  selectedIndex: 0,
  onDestinationSelected: (i) {
    // Profiel-navigatie komt in Phase 6.  ← DIT VERVANGEN
  },
  ...
)
```

Uit lib/main.dart (te wijzigen):
```dart
return MaterialApp.router(
  title: 'RideWindow',
  theme: ThemeData(colorScheme: ColorScheme.fromSeed(seedColor: const Color(0xFF2E7D32))),
  routerConfig: router,
  // themeMode en darkTheme TOEVOEGEN
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Taak 1: ThemeModeProvider aanmaken en main.dart uitbreiden met themeMode</name>
  <files>lib/providers/theme_mode_provider.dart, lib/main.dart</files>
  <action>
    Maak `lib/providers/theme_mode_provider.dart` aan. Volg het functionele provider-patroon
    uit location_provider.dart. Voeg `part 'theme_mode_provider.g.dart';` toe.
    De provider kijkt naar `profileProvider` via `ref.watch`. Als profileProvider nog laadt of
    een fout heeft (`valueOrNull == null`), val terug op `ThemeMode.system`.
    Map de theme-string: `'light'` → `ThemeMode.light`, `'dark'` → `ThemeMode.dark`,
    alles andere → `ThemeMode.system`. Importeer `package:flutter/material.dart` voor ThemeMode.

    Pas daarna `lib/main.dart` aan: voeg `themeMode: ref.watch(themeModeProvider)` toe aan
    `MaterialApp.router`. Voeg ook `darkTheme` toe met dezelfde seedkleur maar
    `brightness: Brightness.dark`. Voeg de import voor `theme_mode_provider.dart` toe.

    Run na aanmaken: `flutter pub run build_runner build --delete-conflicting-outputs`
    om `theme_mode_provider.g.dart` te genereren.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter analyze lib/providers/theme_mode_provider.dart lib/main.dart --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <done>
    theme_mode_provider.dart bestaat, theme_mode_provider.g.dart is gegenereerd,
    main.dart bevat themeMode: ref.watch(themeModeProvider) en een darkTheme-definitie,
    flutter analyze toont geen fouten op de twee bestanden.
  </done>
</task>

<task type="auto">
  <name>Taak 2: /profile route + HomeScreen bottomNav + ProfileScreen skeleton</name>
  <files>lib/app/router.dart, lib/features/home/home_screen.dart, lib/features/profile/profile_screen.dart</files>
  <action>
    Stap A — Maak `lib/features/profile/profile_screen.dart` aan als `ConsumerStatefulWidget`
    (per D-06-03). De `_ProfileScreenState` declareert vier `late double` velden voor de
    slider-waarden. In `initState` initialiseer deze uit `ref.read(profileProvider).valueOrNull`
    met fallback-defaults (tempMin=12, tempMax=26, rainMax=0.5, windMax=15). In `build` kijk
    naar `ref.watch(profileProvider).valueOrNull`. Als null: geef een `Scaffold` terug met
    een gecentreerde `CircularProgressIndicator`. Anders: geef een `Scaffold` met
    `AppBar(title: Text('Profiel'))` en een `ListView` met vier secties:
    - Sectiekoptekst 'THEMA' (placeholder Text — ingevuld in Wave 2)
    - Sectiekoptekst 'TOLERANTIES' (placeholder Text — ingevuld in Wave 2)
    - Sectiekoptekst 'RIJLENGTE' (placeholder Text — ingevuld in Wave 2)
    - ListTile met titel 'Mijn schema bewerken' en een trailing pijlpictogram;
      onTap: `context.go('/availability')` (per D-06-08)
    Zorg voor de juiste imports: go_router, flutter_riverpod, profile_notifier.

    Stap B — Voeg in `lib/app/router.dart` een GoRoute toe voor `/profile` met
    `builder: (context, state) => const ProfileScreen()`. Voeg de import toe voor ProfileScreen.
    Commentaar boven de route: `// Phase 6: ProfileScreen — toleranties, rijlengte, thema`.

    Stap C — Pas in `lib/features/home/home_screen.dart` de `_buildBottomNav` methode aan.
    Vervang de placeholder-commentaar in `onDestinationSelected` door:
    `if (i == 1) context.go('/profile');`
    De `selectedIndex` blijft 0 (HomeScreen is altijd de home-tab; geen cross-tab state nodig).
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter analyze lib/app/router.dart lib/features/home/home_screen.dart lib/features/profile/profile_screen.dart --no-fatal-infos 2>&1 | tail -5</automated>
  </verify>
  <done>
    profile_screen.dart bestaat met ConsumerStatefulWidget en vier secties,
    router.dart bevat een /profile GoRoute met ProfileScreen import,
    home_screen.dart _buildBottomNav roept context.go('/profile') aan bij index 1,
    flutter analyze toont geen fouten.
  </done>
</task>

</tasks>

<threat_model>
## Vertrouwensgrenzen

| Grens | Omschrijving |
|-------|-------------|
| Gebruikerstap → provider-mutator | ProfileScreen roept setTheme() aan — altijd String uit {system,light,dark} |

## STRIDE Dreigingsregister

| Dreiging-ID | Categorie | Component | Dispositie | Mitigatieplan |
|-------------|-----------|-----------|------------|---------------|
| T-06-01 | Tampering | theme_mode_provider | mitigate | Onbekende theme-strings vallen terug op ThemeMode.system (switch default) |
| T-06-02 | Spoofing | /profile route | accept | App-interne route; geen externe invoer; go_router biedt al type-safe state.extra |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Geen nieuwe pakketten in Wave 1 — alleen nieuwe Dart-bestanden |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow

# 1. Statische analyse
flutter analyze lib/providers/theme_mode_provider.dart lib/main.dart \
  lib/app/router.dart lib/features/home/home_screen.dart \
  lib/features/profile/profile_screen.dart --no-fatal-infos

# 2. Gegenereerde bestanden aanwezig
ls lib/providers/theme_mode_provider.g.dart

# 3. /profile route aanwezig in router
grep -c "path.*'/profile'" lib/app/router.dart

# 4. bottomNav wiring aanwezig
grep -c "go.*profile" lib/features/home/home_screen.dart

# 5. themeMode aanwezig in main.dart
grep -c "themeModeProvider" lib/main.dart

# 6. Volledige testsuites nog steeds groen
flutter test --no-pub 2>&1 | tail -3
```
</verification>

<success_criteria>
1. `lib/providers/theme_mode_provider.dart` bestaat en exporteert `themeModeProvider`
2. `lib/providers/theme_mode_provider.g.dart` is gegenereerd door build_runner
3. `lib/main.dart` bevat `themeMode: ref.watch(themeModeProvider)` en een `darkTheme`
4. `lib/app/router.dart` bevat een GoRoute voor `/profile`
5. `lib/features/home/home_screen.dart` navigeert naar `/profile` bij bottomNav-index 1
6. `lib/features/profile/profile_screen.dart` bestaat als ConsumerStatefulWidget met
   vier secties (Thema, Toleranties, Rijlengte, schema-navigatie)
7. `flutter analyze` toont geen fouten; bestaande testsuites blijven groen
</success_criteria>

<output>
Maak `.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-01-SUMMARY.md`
aan na voltooiing.
</output>
