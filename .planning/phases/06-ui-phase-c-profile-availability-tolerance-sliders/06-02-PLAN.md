---
phase: 06-ui-phase-c-profile-availability-tolerance-sliders
plan: "02"
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - lib/features/profile/profile_screen.dart
autonomous: true
requirements: [PROF-01, PROF-02, PROF-04]

must_haves:
  truths:
    - "Vier tolerantie-sliders zijn zichtbaar en interactief (tempMin, tempMax, regen, wind)"
    - "Sliders persisteren alleen bij loslaten (onChangeEnd), niet bij iedere beweging"
    - "Rijlengte-chips (2u / 3u / 4-5u) zijn togglebaar; de laatste actieve chip reageert niet"
    - "Thema-SegmentedButton wisselt direct van ThemeMode na selectie"
    - "De knop 'Mijn schema bewerken' navigeert naar /availability"
  artifacts:
    - path: lib/features/profile/profile_screen.dart
      provides: "Volledig ProfileScreen met sliders, chips, thema-selector"
      contains: "onChangeEnd"
      min_lines: 150
  key_links:
    - from: lib/features/profile/profile_screen.dart
      to: lib/providers/profile_notifier.dart
      via: "ref.read(profileProvider.notifier).updateTolerances()"
      pattern: "profileProvider.notifier"
    - from: lib/features/profile/profile_screen.dart
      to: lib/providers/profile_notifier.dart
      via: "ref.read(profileProvider.notifier).toggleDuration()"
      pattern: "toggleDuration"
    - from: lib/features/profile/profile_screen.dart
      to: lib/providers/profile_notifier.dart
      via: "ref.read(profileProvider.notifier).setTheme()"
      pattern: "setTheme"
---

<objective>
ProfileScreen vullen met de drie tolerantie-sliders, rijlengte-chips en de thema-SegmentedButton.

Purpose: Levert de kern van PROF-01, PROF-02 en PROF-04 — gebruikers kunnen hun toleranties
en rijlengte-voorkeuren aanpassen en het thema wisselen; Riverpod-reactiviteit zorgt dat
slotherberekening automatisch volgt.

Output: Volledig werkende ProfileScreen met gedebounced sliders, chip-guard en thema-selector.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-CONTEXT.md
@.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-PATTERNS.md
@.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-01-SUMMARY.md

<interfaces>
<!-- Bestaande providers en mutators die de uitvoerder direct nodig heeft. -->

Uit lib/providers/profile_notifier.dart:
```dart
class UserProfile {
  final WeatherTolerances tolerances;
  final List<int> allowedDurations;  // waarden: [2, 3, 5]
  final String theme;                // 'system' | 'light' | 'dark'
}

// Mutators (alle async — gebruik ref.read(...notifier)):
Future<void> updateTolerances(WeatherTolerances tolerances);
Future<void> toggleDuration(int duration);   // last-chip-guard is al aanwezig
Future<void> setTheme(String theme);
```

Uit lib/domain/models/weather_tolerances.dart:
```dart
@freezed
abstract class WeatherTolerances with _$WeatherTolerances {
  const factory WeatherTolerances({
    @Default(12.0) double tempMinIdealC,
    @Default(26.0) double tempMaxIdealC,
    @Default(15.0) double windMaxIdealKmh,
    @Default(0.5) double rainMaxIdealMm,
  }) = _WeatherTolerances;
}
// copyWith is gegenereerd door Freezed
```

Sliderwaarden (per D-06-10):
- tempMin:  min=0,  max=20,  divisions=20, label='${v.round()}°C'
- tempMax:  min=15, max=35,  divisions=20, label='${v.round()}°C'
- rainMax:  min=0,  max=5,   divisions=50, label='${v.toStringAsFixed(1)}mm'
- windMax:  min=0,  max=50,  divisions=50, label='${v.round()}km/u'

Rijlengte-chips: waarden [2, 3, 5] (5 = "4-5u")
Chip-labels: {2: '2u', 3: '3u', 5: '4-5u'}

Thema-SegmentedButton waarden: {'system': 'Systeem', 'light': 'Licht', 'dark': 'Donker'}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: Tolerantie-sliders implementeren in ProfileScreen</name>
  <files>lib/features/profile/profile_screen.dart</files>
  <behavior>
    - Vier Slider-widgets aanwezig met de juiste min/max/divisions/label-waarden
    - onChanged update lokale _tempMin/_tempMax/_rainMax/_windMax setState (directe UI-feedback)
    - onChangeEnd roept updateTolerances() aan met tolerances.copyWith(...) voor de gewijzigde waarde
    - Slider-initwaarden worden geladen uit profileProvider in initState
    - Sectiekoptekst 'TOLERANTIES' zichtbaar boven de sliders
  </behavior>
  <action>
    Vervang de 'TOLERANTIES'-sectie-placeholder in ProfileScreen door de vier sliders.
    Gebruik voor elke slider een `Column` met een `Row` voor label+waarde en een `Slider`
    eronder. Patroon voor elke slider:

    ```
    Row(children: [
      Text('Min. temperatuur'),
      Spacer(),
      Text('${_tempMin.round()}°C'),
    ])
    Slider(
      value: _tempMin,
      min: 0, max: 20, divisions: 20,
      label: '${_tempMin.round()}°C',
      onChanged: (v) => setState(() => _tempMin = v),
      onChangeEnd: (v) => ref.read(profileProvider.notifier)
          .updateTolerances(profile.tolerances.copyWith(tempMinIdealC: v)),
    )
    ```

    Dezelfde structuur voor tempMax (15–35°C), rainMax (0–5mm, 1 decimaal), windMax (0–50km/u).
    Voeg een `Divider` toe tussen elke slider voor leesbaarheid.
    Importeer `package:ridewindow/domain/models/weather_tolerances.dart` voor `copyWith`.
    Voeg commentaar toe: `// D-06-02: onChangeEnd persisteert, onChanged update lokale state`
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter analyze lib/features/profile/profile_screen.dart --no-fatal-infos 2>&1 | tail -3 && grep -c "onChangeEnd" lib/features/profile/profile_screen.dart</automated>
  </verify>
  <done>
    ProfileScreen bevat vier Slider-widgets met onChanged+onChangeEnd, sectiekoptekst
    'TOLERANTIES' aanwezig, flutter analyze toont geen fouten,
    grep onChangeEnd ≥ 4 (één per slider).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: Rijlengte-chips en thema-SegmentedButton implementeren</name>
  <files>lib/features/profile/profile_screen.dart</files>
  <behavior>
    - Drie FilterChip-widgets (2u / 3u / 4-5u) aanwezig met juiste selected-state
    - Tappen op een actieve chip roept toggleDuration() aan; last-chip-guard zit in notifier
    - SegmentedButton<String> met drie segmenten (Systeem / Licht / Donker)
    - Selecteren van een thema roept setTheme() aan en verandert de ThemeMode direct
    - Secties 'RIJLENGTE' en 'THEMA' zijn zichtbaar boven de respectievelijke widgets
  </behavior>
  <action>
    Vervang de 'RIJLENGTE'-sectie-placeholder door drie `FilterChip`-widgets in een `Wrap`:
    ```
    Wrap(
      spacing: 8,
      children: [
        for (final (value, label) in [(2,'2u'), (3,'3u'), (5,'4-5u')])
          FilterChip(
            label: Text(label),
            selected: profile.allowedDurations.contains(value),
            onSelected: (_) => ref.read(profileProvider.notifier).toggleDuration(value),
          ),
      ],
    )
    ```
    Voeg commentaar toe: `// PROF-02: last-chip-guard zit in ProfileNotifier.toggleDuration()`

    Vervang de 'THEMA'-sectie-placeholder door een `SegmentedButton<String>`:
    ```
    SegmentedButton<String>(
      segments: const [
        ButtonSegment(value: 'system', label: Text('Systeem')),
        ButtonSegment(value: 'light',  label: Text('Licht')),
        ButtonSegment(value: 'dark',   label: Text('Donker')),
      ],
      selected: {profile.theme},
      onSelectionChanged: (s) => ref.read(profileProvider.notifier).setTheme(s.first),
    )
    ```
    Voeg commentaar toe: `// D-06-09: SegmentedButton, PROF-04`

    Zorg dat de `profile`-variabele in `build` verwijst naar `ref.watch(profileProvider).valueOrNull`.
    Guard aan het begin van `build`: als `profile == null` return loading-scaffold.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter analyze lib/features/profile/profile_screen.dart --no-fatal-infos 2>&1 | tail -3 && grep -c "FilterChip" lib/features/profile/profile_screen.dart && grep -c "SegmentedButton" lib/features/profile/profile_screen.dart</automated>
  </verify>
  <done>
    ProfileScreen bevat FilterChip (3×) en SegmentedButton (1×), secties 'RIJLENGTE' en
    'THEMA' aanwezig, flutter analyze toont geen fouten.
    Volledige testsuites blijven groen: `flutter test --no-pub` exit code 0.
  </done>
</task>

</tasks>

<threat_model>
## Vertrouwensgrenzen

| Grens | Omschrijving |
|-------|-------------|
| Sliderinteractie → SharedPreferences | onChangeEnd schrijft double naar SharedPreferences |
| Chip-tap → SharedPreferences | toggleDuration schrijft List<String> naar SharedPreferences |

## STRIDE Dreigingsregister

| Dreiging-ID | Categorie | Component | Dispositie | Mitigatieplan |
|-------------|-----------|-----------|------------|---------------|
| T-06-03 | Tampering | Slider.onChangeEnd | mitigate | Waarden worden geclamped door Slider.min/max — ProfileNotifier ontvangt altijd een geldige double binnen bereik |
| T-06-04 | Denial of Service | Slider.onChanged | accept | onChanged update alleen lokale setState, geen I/O; onChangeEnd limiteert SharedPreferences-schrijfacties tot één per swipe-einde |
| T-06-05 | Tampering | toggleDuration last-chip | accept | Guard al aanwezig in ProfileNotifier.toggleDuration(): verwijdert niet als length == 1 |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | Geen nieuwe pakketten in Wave 2 |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow

# 1. Statische analyse
flutter analyze lib/features/profile/profile_screen.dart --no-fatal-infos

# 2. Slider-debouncing aanwezig
grep -c "onChangeEnd" lib/features/profile/profile_screen.dart
# verwacht: 4

# 3. Chip-implementatie
grep -c "FilterChip" lib/features/profile/profile_screen.dart
# verwacht: 3

# 4. Thema-selector
grep -c "SegmentedButton" lib/features/profile/profile_screen.dart
# verwacht: 1

# 5. Volledige testsuites groen
flutter test --no-pub 2>&1 | tail -3
```
</verification>

<success_criteria>
1. ProfileScreen bevat vier gedebounced tolerantie-sliders (tempMin, tempMax, rain, wind)
   met correcte min/max/divisions-waarden per D-06-10
2. Drie FilterChip-widgets voor rijlengte (2u/3u/4-5u) togglebaar; last-chip-guard in notifier
3. SegmentedButton voor thema met drie opties (Systeem/Licht/Donker)
4. Navigatieknop 'Mijn schema bewerken' aanwezig en navigeert naar /availability
5. `flutter analyze` toont geen fouten; bestaande testsuites blijven groen
</success_criteria>

<output>
Maak `.planning/phases/06-ui-phase-c-profile-availability-tolerance-sliders/06-02-SUMMARY.md`
aan na voltooiing.
</output>
