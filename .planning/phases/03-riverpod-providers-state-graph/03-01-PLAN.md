---
phase: 03-riverpod-providers-state-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/providers/app_database_provider.dart
  - lib/providers/weather_notifier.dart
  - lib/providers/weather_notifier.g.dart
  - test/providers/weather_notifier_test.dart
  - test/providers/weather_notifier_test.mocks.dart
autonomous: true
requirements:
  - PROF-03
  - SLOT-05

must_haves:
  truths:
    - "WeatherNotifier exposeert AsyncValue<List<HourlyForecast>> via @riverpod code-gen"
    - "Een ProviderContainer-test bevestigt de transitie loading → data met een mock WeatherRepository"
    - "WeatherNotifier is onafhankelijk van BuildContext en volledig testbaar in isolatie"
    - "AppDatabase en OpenMeteoClient zijn als aparte providers beschikbaar voor injectie"
  artifacts:
    - path: "lib/providers/app_database_provider.dart"
      provides: "appDatabaseProvider en openMeteoClientProvider"
      exports: ["appDatabaseProvider", "openMeteoClientProvider"]
    - path: "lib/providers/weather_notifier.dart"
      provides: "WeatherNotifier als AsyncNotifier<List<HourlyForecast>>"
      contains: "@riverpod"
    - path: "lib/providers/weather_notifier.g.dart"
      provides: "Gegenereerde provider-registratie via build_runner"
    - path: "test/providers/weather_notifier_test.dart"
      provides: "ProviderContainer-test: loading→data transitie met MockWeatherRepository"
  key_links:
    - from: "lib/providers/weather_notifier.dart"
      to: "lib/data/repositories/weather_repository.dart"
      via: "ref.watch(weatherRepositoryProvider)"
      pattern: "weatherRepositoryProvider"
    - from: "test/providers/weather_notifier_test.dart"
      to: "lib/providers/weather_notifier.dart"
      via: "ProviderContainer + overrides"
      pattern: "ProviderContainer"
---

<objective>
Voeg flutter_riverpod, riverpod_annotation en riverpod_generator toe aan pubspec.yaml, maak infrastructuur-providers aan voor AppDatabase en OpenMeteoClient, en implementeer WeatherNotifier als een @riverpod AsyncNotifier die WeatherRepository.getForecast() aanroept. Verifieer de loading→data transitie via een ProviderContainer-test met een mock repository.

Purpose: Dit is de fundering van de Riverpod provider-keten. Alle verdere providers (SlotsNotifier, ProfileNotifier) zijn downstream hiervan. Zonder een testbare, context-loze WeatherNotifier kunnen de reactieve recomputation-tests in plan 03-03 niet worden geschreven.

Output: lib/providers/weather_notifier.dart + .g.dart, lib/providers/app_database_provider.dart, test/providers/weather_notifier_test.dart + .mocks.dart. Alle bestaande 78 tests blijven groen.
</objective>

<execution_context>
@/Users/joostmouw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostmouw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joostmouw/ridewindow/.planning/PROJECT.md
@/Users/joostmouw/ridewindow/.planning/ROADMAP.md
@/Users/joostmouw/ridewindow/.planning/STATE.md
@/Users/joostmouw/ridewindow/.planning/phases/02-data-layer-drift-open-meteo/02-03-SUMMARY.md

<interfaces>
<!-- Bestaande contracten die WeatherNotifier consumeert. Geen codebase-exploratie nodig. -->

Van lib/data/repositories/weather_repository.dart:
```dart
class WeatherRepository {
  WeatherRepository({required AppDatabase db, required OpenMeteoClient client});
  Future<List<HourlyForecast>> getForecast() async { ... }
}
```

Van lib/domain/models/hourly_forecast.dart:
```dart
@freezed
abstract class HourlyForecast with _$HourlyForecast {
  const factory HourlyForecast({
    required double? temperatureC,
    required double? apparentTemperatureC,
    required double? precipitationMm,
    required double? precipitationProbability,
    required double? windspeedKmh,
    required double? winddirectionDeg,
    required DateTime time,
  }) = _HourlyForecast;
}
```

Van lib/data/database/app_database.dart (uit Plan 02-01 + 02-02):
```dart
@DriftDatabase(tables: [ForecastCacheEntries, HourlyForecastEntries], daos: [ForecastDao])
class AppDatabase extends _$AppDatabase {
  AppDatabase([QueryExecutor? e]) : super(e ?? driftDatabase(name: 'ridewindow'));
  @override ForecastDao get forecastDao => ForecastDao(this);
}
```

Van lib/data/remote/open_meteo_client.dart:
```dart
class OpenMeteoClient {
  OpenMeteoClient({http.Client? client});
  Future<List<HourlyForecast>> fetch(double lat, double lon) async { ... }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Voeg Riverpod-pakketten toe aan pubspec.yaml en maak infrastructuur-providers</name>
  <files>pubspec.yaml, lib/providers/app_database_provider.dart</files>
  <behavior>
    - Na `flutter pub get` lost flutter_riverpod 3.3.1, riverpod_annotation 4.0.2 op zonder conflicten
    - appDatabaseProvider levert een AppDatabase-instantie terug (lazy singleton via keepAlive)
    - openMeteoClientProvider levert een OpenMeteoClient-instantie terug
    - weatherRepositoryProvider levert een WeatherRepository terug die appDatabaseProvider en openMeteoClientProvider gebruikt
    - `dart run build_runner build --delete-conflicting-outputs` genereert app_database_provider.g.dart foutloos
  </behavior>
  <action>
1. Voeg toe aan pubspec.yaml onder dependencies (na de bestaande Phase 2 entries):
   - flutter_riverpod: ^3.3.1
   - riverpod_annotation: ^4.0.2
   Voeg toe aan dev_dependencies:
   - riverpod_generator: ^2.6.5
   Voeg toe aan het einde van pubspec.yaml (of bestaande flutter_gen sectie): geen extra Flutter-sectie nodig.
   Voer daarna `flutter pub get` uit en controleer op conflicten.

2. Maak lib/providers/app_database_provider.dart aan. Gebruik de @riverpod annotatie (niet @Riverpod — lowercase).
   - Importeer: package:riverpod_annotation/riverpod_annotation.dart, dart:io, package:drift_flutter/drift_flutter.dart, de AppDatabase en OpenMeteoClient classes.
   - appDatabaseProvider: annoteer met @Riverpod(keepAlive: true). Retourneer AppDatabase() met de productie driftDatabase. Gebruik NIET AutoDispose hier — de database moet de app-levensduur overleven.
   - openMeteoClientProvider: annoteer met @riverpod (lowercase = AutoDispose is prima voor stateless client). Retourneer OpenMeteoClient().
   - weatherRepositoryProvider: annoteer met @riverpod. Lees ref.watch(appDatabaseProvider) en ref.watch(openMeteoClientProvider). Retourneer WeatherRepository(db: ..., client: ...).
   - Voeg de part directive toe: `part 'app_database_provider.g.dart';`

3. Voer uit: `dart run build_runner build --delete-conflicting-outputs`
   Verifieer dat app_database_provider.g.dart aangemaakt is en dart analyze lib/providers/ schoon is.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter pub get && dart run build_runner build --delete-conflicting-outputs && dart analyze lib/providers/app_database_provider.dart</automated>
  </verify>
  <done>pubspec.yaml bevat flutter_riverpod 3.3.1 + riverpod_annotation 4.0.2 + riverpod_generator. app_database_provider.g.dart bestaat. dart analyze lib/providers/ meldt geen issues.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: WeatherNotifier implementeren + ProviderContainer-test (TDD RED→GREEN)</name>
  <files>
    lib/providers/weather_notifier.dart,
    lib/providers/weather_notifier.g.dart,
    test/providers/weather_notifier_test.dart,
    test/providers/weather_notifier_test.mocks.dart
  </files>
  <behavior>
    - WeatherNotifier extends AsyncNotifier&lt;List&lt;HourlyForecast&gt;&gt; met @riverpod annotatie
    - build() roept ref.watch(weatherRepositoryProvider).getForecast() aan en retourneert het resultaat
    - Test "loading state": ProviderContainer met een mock die compleet na een Completer — state is AsyncLoading
    - Test "data state": MockWeatherRepository.getForecast() retourneert Future.value([fixture]) — state is AsyncData met de fixture
    - Test "error state": mock gooit een Exception — state is AsyncError
    - Geen enkele test importeert package:flutter of gebruikt een Widget-boom
    - flutter test test/providers/weather_notifier_test.dart passeert
  </behavior>
  <action>
1. RED: Maak test/providers/weather_notifier_test.dart aan VOOR de implementatie.
   - Gebruik @GenerateMocks([WeatherRepository]) bovenaan.
   - Schrijf drie tests in een groep 'WeatherNotifier':
     a. 'starts in loading state' — maak een ProviderContainer aan met override voor weatherRepositoryProvider (mock waarvan getForecast() een Completer().future retourneert). Lees container.read(weatherNotifierProvider). Expect: isA&lt;AsyncLoading&gt;().
     b. 'transitions to data on success' — mock retourneert Future.value([testForecast]). Gebruik await container.read(weatherNotifierProvider.future). Expect: resultaat is een lijst met lengte 1 en time overeenkomt.
     c. 'transitions to error on failure' — mock gooit Exception('network'). Gebruik container.read(weatherNotifierProvider). Wacht via addListener op een AsyncError-state. Expect: value is AsyncError.
   - Voer `dart run build_runner build --delete-conflicting-outputs` uit voor het genereren van mocks.
   - Voer `flutter test test/providers/weather_notifier_test.dart` uit — verwacht 3 FAILING (NotifierProvider nog niet aangemaakt).

2. GREEN: Maak lib/providers/weather_notifier.dart aan.
   - Import: riverpod_annotation, de WeatherRepository, HourlyForecast, en app_database_provider.dart.
   - Declareer: `part 'weather_notifier.g.dart';`
   - Klasse: `class WeatherNotifier extends _$WeatherNotifier` met `@riverpod` annotatie.
   - build(): `return ref.watch(weatherRepositoryProvider).getForecast();`
   - Noteer: build() is een Future&lt;List&lt;HourlyForecast&gt;&gt; — Riverpod wikkelt dit automatisch in AsyncValue.
   - Riverpod 3.0: gebruik NIET StateNotifierProvider of AutoDisposeNotifier — die zijn legacy.
   - Voer `dart run build_runner build --delete-conflicting-outputs` opnieuw uit.
   - Voer `flutter test test/providers/weather_notifier_test.dart` — verwacht 3 PASSING.

3. Voer de volledige suite uit: `flutter test --reporter=compact` — verwacht 81+ tests groen (78 bestaand + 3 nieuw).
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart run build_runner build --delete-conflicting-outputs && flutter test test/providers/weather_notifier_test.dart --reporter=compact && flutter test --reporter=compact</automated>
  </verify>
  <done>weather_notifier.dart + .g.dart bestaan. Drie WeatherNotifier-tests slagen (loading, data, error). Volledige suite: 81+ tests groen. dart analyze lib/providers/ schoon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschrijving |
|----------|--------------|
| Provider → Repository | WeatherNotifier roept WeatherRepository.getForecast() aan; repository valideert cache-beleid |
| shared_preferences | Nog niet in dit plan — gaat in 03-02 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | appDatabaseProvider (keepAlive) | accept | Database bevat geen PII — alleen weerdata en Amsterdam-coördinaten (placeholder) |
| T-03-02 | Denial of Service | WeatherNotifier.build() zonder timeout | accept | HTTP-timeout is een Phase 8-concern; cache beperkt het aantal echte fetches |
| T-03-03 | Tampering | ProviderContainer override in tests | mitigate | Overrides alleen in testcode; productie-providers kennen geen override-punt |
| T-03-SC | Tampering | riverpod_annotation / riverpod_generator npm/pub installs | accept | Publisher: dash-overflow.net (Remi Rousselet, Riverpod-auteur) — geverifieerd in CLAUDE.md |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow

# 1. Packages opgelost
flutter pub get

# 2. Code-gen slaagt
dart run build_runner build --delete-conflicting-outputs

# 3. Gegenereerde bestanden aanwezig
ls lib/providers/app_database_provider.g.dart
ls lib/providers/weather_notifier.g.dart

# 4. Statische analyse schoon
dart analyze lib/providers/

# 5. WeatherNotifier-tests slagen
flutter test test/providers/weather_notifier_test.dart --reporter=compact

# 6. Volledige regressie
flutter test --reporter=compact
```
</verification>

<success_criteria>
1. pubspec.yaml bevat flutter_riverpod ^3.3.1, riverpod_annotation ^4.0.2, riverpod_generator ^2.6.5 in de juiste secties
2. lib/providers/app_database_provider.dart bestaat met appDatabaseProvider (keepAlive), openMeteoClientProvider, weatherRepositoryProvider
3. lib/providers/weather_notifier.dart bestaat als @riverpod AsyncNotifier&lt;List&lt;HourlyForecast&gt;&gt;
4. Drie WeatherNotifier ProviderContainer-tests slagen: loading, data, error
5. Volledige test-suite: minimaal 81 tests groen (78 bestaand + 3 nieuw)
6. dart analyze lib/ meldt geen issues
7. Geen gebruik van StateProvider, StateNotifierProvider, of AutoDisposeNotifier (deprecated in Riverpod 3.0)
</success_criteria>

<output>
Maak .planning/phases/03-riverpod-providers-state-graph/03-01-SUMMARY.md aan wanneer klaar.
</output>
