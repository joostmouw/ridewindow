---
phase: 03-riverpod-providers-state-graph
plan: 04
type: execute
wave: 4
depends_on:
  - 03-03
files_modified:
  - test/providers/integration_test.dart
  - lib/main.dart
autonomous: true
requirements:
  - PROF-03
  - AVAIL-04
  - SLOT-05
  - PERS-01

must_haves:
  truths:
    - "Alle vijf Phase 3 success criteria zijn aantoonbaar vervuld door geautomatiseerde tests"
    - "lib/main.dart gebruikt ProviderScope als root widget zodat de app klaar is voor Phase 4 UI"
    - "dart analyze lib/ en flutter test --reporter=compact beide slagen zonder warnings"
    - "Een end-to-end ProviderContainer-test bevestigt de volledige keten: weather mock → profiel defaults → lege beschikbaarheid → SlotsLoaded"
  artifacts:
    - path: "test/providers/integration_test.dart"
      provides: "End-to-end keten-test die alle vijf Phase 3 success criteria verifieert"
    - path: "lib/main.dart"
      provides: "ProviderScope-wrapped app als root voor Phase 4"
  key_links:
    - from: "test/providers/integration_test.dart"
      to: "lib/providers/weather_notifier.dart"
      via: "ProviderContainer override"
      pattern: "weatherNotifierProvider.overrideWith"
    - from: "test/providers/integration_test.dart"
      to: "lib/providers/slots_notifier.dart"
      via: "container.read(slotsNotifierProvider)"
      pattern: "slotsNotifierProvider"
    - from: "lib/main.dart"
      to: "flutter_riverpod ProviderScope"
      via: "runApp(ProviderScope(child: MyApp()))"
      pattern: "ProviderScope"
---

<objective>
Afrondingsplan voor Phase 3: schrijf een end-to-end ProviderContainer-integratietest die de volledige provider-keten in één test doorloopt, wrap lib/main.dart in ProviderScope, en voer de complete test-suite gate uit. Dit plan bewijst aantoonbaar dat alle vijf Phase 3 success criteria zijn vervuld.

Purpose: Individuele provider-tests (plannen 03-01 t/m 03-03) verifiëren elk component in isolatie. Dit plan verifieert dat de keten als geheel werkt en dat de app klaar is om in Phase 4 een UI op te zetten. De ProviderScope in main.dart is een vereiste voor elk Flutter-widget dat providers gebruikt.

Output: test/providers/integration_test.dart met een volledige ketentest, lib/main.dart bijgewerkt met ProviderScope. Final dart analyze + flutter test gate: alle tests groen.
</objective>

<execution_context>
@/Users/joostmouw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostmouw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joostmouw/ridewindow/.planning/PROJECT.md
@/Users/joostmouw/ridewindow/.planning/ROADMAP.md
@/Users/joostmouw/ridewindow/.planning/STATE.md
@/Users/joostmouw/ridewindow/.planning/phases/03-riverpod-providers-state-graph/03-01-SUMMARY.md
@/Users/joostmouw/ridewindow/.planning/phases/03-riverpod-providers-state-graph/03-02-SUMMARY.md
@/Users/joostmouw/ridewindow/.planning/phases/03-riverpod-providers-state-graph/03-03-SUMMARY.md

<interfaces>
<!-- Alle provider-namen uit vorige plannen. Executor gebruikt deze — geen exploratie nodig. -->

Uit 03-01 (WeatherNotifier):
- weatherNotifierProvider — AsyncValue&lt;List&lt;HourlyForecast&gt;&gt;
- weatherRepositoryProvider — WeatherRepository (override voor tests)
- appDatabaseProvider — AppDatabase (keepAlive)
- openMeteoClientProvider — OpenMeteoClient

Uit 03-02 (ProfileNotifier):
- profileNotifierProvider — AsyncValue&lt;UserProfile&gt;
- UserProfile.tolerances — WeatherTolerances
- UserProfile.allowedDurations — List&lt;int&gt;
- UserProfile.theme — String
- ProfileNotifier.updateTolerances(WeatherTolerances) — async void
- ProfileNotifier.toggleDuration(int) — async void

Uit 03-03 (SlotsNotifier + AvailabilityNotifier):
- slotsNotifierProvider — SlotsState (Notifier, niet async)
- availabilityNotifierProvider — AsyncValue&lt;Set&lt;DateTime&gt;&gt;
- AvailabilityNotifier.toggleHour(DateTime) — async void
- SlotsLoaded.slots — List&lt;RideSlot&gt;
- SlotsLoaded.reason — SlotsEmptyReason? (badWeather | allBlocked)
- SlotsEmptyReason enum

Riverpod 3.0 ProviderContainer test-patroon:
```dart
final container = ProviderContainer(overrides: [
  weatherNotifierProvider.overrideWith(() => FakeWeatherNotifier()),
]);
addTearDown(container.dispose);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: End-to-end keten-integratietest</name>
  <files>test/providers/integration_test.dart</files>
  <behavior>
    - Eén testbestand met vijf testgevallen — elk test één van de vijf Phase 3 success criteria
    - Alle vijf tests gebruiken ProviderContainer (geen WidgetTester, geen Flutter UI)
    - Test 1 "weather loading→data": WeatherNotifier transitie via mock repository (criterion 1)
    - Test 2 "slots recompute on weather change": verander mock-weerdata → SlotsNotifier geeft nieuwe slots (criterion 2, weather-tak)
    - Test 3 "slots recompute on profile change": wijzig ProfileNotifier.updateTolerances → SlotsNotifier recomputed (criterion 2, profile-tak)
    - Test 4 "availability toggle triggers slot change": AvailabilityNotifier.toggleHour blokkert alle uren → SlotsLoaded.reason == allBlocked (criterion 3)
    - Test 5 "dispose and recreate preserves settings": UserProfile instellingen overleven ProviderContainer dispose/re-create (criterion 5)
    - SharedPreferences.setMockInitialValues({}) in setUp voor geïsoleerde state
  </behavior>
  <action>
1. Maak test/providers/integration_test.dart aan.
   - Importeer: flutter_test, flutter_riverpod, shared_preferences, plus alle provider-bestanden uit lib/providers/.
   - Elke test: maak een fresh ProviderContainer aan, voeg addTearDown(container.dispose) toe, gebruik SharedPreferences.setMockInitialValues({}).
   - Voor weather-overrides: gebruik FakeWeatherNotifier classes (inline in de test, extends _$WeatherNotifier) die build() een Future.value([fixtures]) laten retourneren.
   - Fixture voor "slechte scores": maak HourlyForecast-instanties aan met precipitationMm=50.0 (zwaar regenweer) zodat ScoringEngine alle uren &lt;50 scoort.
   - Fixture voor "goede scores": HourlyForecast met temperatureC=20.0, precipitationMm=0.0, windspeedKmh=10.0.
   - Test 4 "availability toggle": gebruik een goede-weer fixture maar roep AvailabilityNotifier.toggleHour() aan voor elk uur in de 7-daagse forecast (of een representatieve subset van 5 uur om de test snel te houden). Assert SlotsLoaded.reason == SlotsEmptyReason.allBlocked.
   - Test 5: schrijf een ProfileNotifier-instelling via updateTolerances, dispose de container, maak een nieuwe container aan (zonder overrides — echte SharedPreferences mock), en bevestig dat de waarden hersteld zijn.

2. Voer `flutter test test/providers/integration_test.dart --reporter=expanded` uit.
   Alle vijf tests moeten slagen.

3. Controleer dat de uitvoer geen deprecated-API-waarschuwingen bevat:
   `dart analyze lib/providers/ --fatal-infos`
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/providers/integration_test.dart --reporter=expanded</automated>
  </verify>
  <done>Vijf integratietests slagen. Elke Phase 3 success criterion is gedekt door minstens één test. dart analyze lib/providers/ --fatal-infos meldt geen issues.</done>
</task>

<task type="auto">
  <name>Task 2: ProviderScope in main.dart + volledige suite gate</name>
  <files>lib/main.dart</files>
  <action>
1. Lees lib/main.dart (huidige inhoud uit Phase 1 — minimale Material 3 boot).

2. Wrap de root widget in ProviderScope:
   - Verander `runApp(MyApp())` naar `runApp(const ProviderScope(child: MyApp()))`.
   - Voeg import toe: `import 'package:flutter_riverpod/flutter_riverpod.dart';`
   - Laat alle andere inhoud van main.dart ongewijzigd (geen andere changes).

3. Voer dart analyze lib/main.dart uit — verwacht: No issues found.

4. Voer de volledige test-suite gate uit:
   `flutter test --reporter=compact`
   Verwacht: alle tests groen (minimaal 98 — 78 bestaand + 3 weather + 4 profile + 4 availability + 4 slots + 5 integratie = 98).

5. Voer analyze uit over de gehele lib/:
   `dart analyze lib/`
   Verwacht: No issues found.

6. Controleer dat er geen legacy Riverpod API's gebruikt worden:
   `grep -rn "StateProvider\|StateNotifierProvider\|AutoDisposeNotifier\|ChangeNotifierProvider" lib/providers/`
   Verwacht: geen uitvoer (grep vindt niets).
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart analyze lib/main.dart && flutter test --reporter=compact && dart analyze lib/ && grep -rn "StateProvider\|StateNotifierProvider\|ChangeNotifierProvider" lib/providers/ | grep -v ".g.dart" | wc -l</automated>
  </verify>
  <done>lib/main.dart gebruikt ProviderScope als root. Volledige suite: minimaal 98 tests groen, nul legacy Riverpod API's. dart analyze lib/ meldt No issues found.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschrijving |
|----------|--------------|
| ProviderScope → Widget-boom | Alle Riverpod-providers zijn uitsluitend toegankelijk binnen ProviderScope; geen directe widget-state buiten |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Elevation of Privilege | ProviderScope in main.dart wraps gehele app | accept | Geen gevolgen — ProviderScope is een Flutter-infrastructuur-widget zonder beveiligingsimplicaties |
| T-03-13 | Tampering | Integratietest gebruikt SharedPreferences mock — kan productie-state vervuilen | mitigate | setMockInitialValues({}) in setUp + addTearDown(container.dispose) garandeert geïsoleerde state per test |
| T-03-14 | Information Disclosure | Test-fixtures bevatten geen echte locatie- of gebruikersdata | accept | Alle fixtures zijn hard-coded anonieme testwaarden |
| T-03-SC | Tampering | Geen nieuwe pub-installs in dit plan | n/a | Alle pakketten aanwezig na 03-01/02 |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow

# 1. Integratietests slagen
flutter test test/providers/integration_test.dart --reporter=expanded

# 2. Alle vijf criteria gedekt (check test-namen)
grep -c "test(" test/providers/integration_test.dart
# verwacht: 5

# 3. ProviderScope aanwezig in main.dart
grep -c "ProviderScope" lib/main.dart
# verwacht: 1

# 4. Volledige suite gate
flutter test --reporter=compact

# 5. Gehele lib/ analyse
dart analyze lib/

# 6. Geen legacy Riverpod API's
grep -rn "StateProvider\|StateNotifierProvider\|ChangeNotifierProvider" lib/providers/ | grep -v "\.g\.dart"
# verwacht: geen uitvoer

# 7. Alle Phase 3 provider-bestanden aanwezig
ls lib/providers/weather_notifier.dart \
   lib/providers/profile_notifier.dart \
   lib/providers/slots_notifier.dart \
   lib/providers/availability_notifier.dart \
   lib/providers/app_database_provider.dart
```
</verification>

<success_criteria>
1. test/providers/integration_test.dart bestaat met vijf tests — elk correspondeert met één Phase 3 success criterion
2. Alle vijf integratietests slagen
3. lib/main.dart gebruikt ProviderScope als root widget
4. Volledige test-suite: minimaal 98 tests groen
5. dart analyze lib/ meldt No issues found
6. Geen legacy Riverpod API's (StateProvider / StateNotifierProvider / ChangeNotifierProvider) in lib/providers/
7. Alle vijf provider-bestanden aanwezig: weather_notifier, profile_notifier, slots_notifier, availability_notifier, app_database_provider

## Multi-Source Coverage Audit

| Bron | Item | Gedekt door |
|------|------|-------------|
| GOAL | Volledige provider-keten getest in ProviderContainer | 03-01 (weather), 03-02 (profile), 03-03 (slots+avail), 03-04 (e2e) |
| GOAL | Automatische recomputation zonder handmatige refresh | 03-03 Task 2 (ref.watch reactief model) |
| REQ PROF-03 | Tolerantie/rijlengte wijzigingen triggeren slot recomputation | 03-03 + 03-04 |
| REQ AVAIL-04 | Beschikbaarheid wijzigen triggert slot recomputation | 03-03 Task 1 + 03-04 Test 4 |
| REQ SLOT-05 | Expliciete empty state wanneer geen slots kwalificeren | 03-03 Task 2 (SlotsEmptyReason) |
| REQ PERS-01 | Scalaire instellingen persisteren in shared_preferences | 03-02 + 03-04 Test 5 |
</success_criteria>

<output>
Maak .planning/phases/03-riverpod-providers-state-graph/03-04-SUMMARY.md aan wanneer klaar.
</output>
