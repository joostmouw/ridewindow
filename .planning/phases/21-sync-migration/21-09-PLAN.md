---
phase: 21-sync-migration
plan: 09
type: execute
wave: 8
depends_on: [21-08]
files_modified:
  - .planning/phases/21-sync-migration/REGRESSION-CHECKLIST-21.md
  - .planning/phases/21-sync-migration/MANUAL-VERIFICATION-21.md
  - docs/ACCOUNTS-OPERATIONS.md
autonomous: false
requirements: [REG-03]

must_haves:
  truths:
    - "The full automated suite and flutter build apk --release both pass before any manual verification begins — a red baseline invalidates a manual pass/fail judgement"
    - "Web cold-start is re-measured using the exact same methodology Phase 19's REGRESSION-CHECKLIST.md defined (device, connection type, measurement method, conditions), so the comparison to the Phase 19 baseline is meaningful (D-19, 19-CONTEXT.md)"
    - "If Phase 19's own baseline measurement (plan 19-07) was never actually completed, this plan surfaces that as a blocker rather than silently comparing against nothing"
    - "A regression past the 2-second budget is treated as blocking, not noted-and-shipped"
  artifacts:
    - path: ".planning/phases/21-sync-migration/REGRESSION-CHECKLIST-21.md"
      provides: "Reused cold-start methodology from Phase 19, plus SYNC-04 (cross-device propagation) and SYNC-11 (multi-tab) real-device/browser checks specific to this phase"
      contains: "cold-start"
  key_links:
    - from: ".planning/phases/21-sync-migration/REGRESSION-CHECKLIST-21.md"
      to: ".planning/phases/19-auth/REGRESSION-CHECKLIST.md"
      via: "identical cold-start measurement method, so the two numbers are comparable"
      pattern: "cold-start"
---

<objective>
Close Phase 21 the same way Phase 19 closed: Claude prepares everything checkable and buildable (this plan's `auto` task), then hands off exactly what remains genuinely manual — physical devices, real network conditions, a stopwatch — as one short, ordered checklist (this plan's checkpoint task), per the D-17/D-19 split precedent already established in `.planning/phases/19-auth/19-CONTEXT.md`.

**REG-03's whole point is a comparison, not a number in isolation:** `.planning/phases/19-auth/REGRESSION-CHECKLIST.md` §3 measured a pre-cloud-reads web cold-start baseline using a specific, written-down method (device, connection type, exact measurement technique, conditions). This plan reuses that *identical* method for the post-cloud-reads measurement — a different method would make the "before vs after" comparison meaningless, which is exactly the trap D-19 was written to avoid. **Before assuming a baseline exists, verify it does** — `.planning/STATE.md`'s Phase 19 status notes plan 19-07 (the plan that was supposed to fill in that baseline) was still open/manual/pending as of this phase's start; if the baseline fields in `19-auth/REGRESSION-CHECKLIST.md` §3 are still blank placeholders, that is a real gap this plan must surface to the user rather than silently comparing today's number against nothing.

Purpose: prove SYNC-07/REG-03's "still fast, still no network wait on cold start" claim was not just true in isolated unit tests but true of the shipped app on real hardware, and give SYNC-04/SYNC-11 (cross-device propagation, multi-tab safety) their one genuinely-needs-a-human proof, since neither is fully provable from `flutter test` alone.

Output: `REGRESSION-CHECKLIST-21.md`, a confirmed-green full suite + release build, and the final manual verification record for the whole phase.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/21-sync-migration/21-CONTEXT.md
@.planning/phases/19-auth/REGRESSION-CHECKLIST.md
@.planning/phases/21-sync-migration/MANUAL-VERIFICATION-21.md
@docs/ACCOUNTS-OPERATIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write REGRESSION-CHECKLIST-21.md, confirm green baseline, build release artifact</name>
  <files>.planning/phases/21-sync-migration/REGRESSION-CHECKLIST-21.md, docs/ACCOUNTS-OPERATIONS.md</files>
  <read_first>.planning/phases/19-auth/REGRESSION-CHECKLIST.md in full (the exact cold-start methodology section to reuse verbatim, and to check whether its baseline fields were actually filled in or are still blank placeholders), .planning/phases/21-sync-migration/MANUAL-VERIFICATION-21.md (plans 21-02/21-08's checkpoint records so far)</read_first>
  <action>
Run `flutter test` (full suite) and confirm the result matches the phase's documented running floor (at least 348 passed, only the pre-existing documented `notification_service_test.dart` date-boundary failure, no new failures). Run `flutter test test/structure/background_task_no_supabase_test.dart` explicitly and confirm it still passes (REG-05's final proof for this phase). Run `flutter build apk --release` and confirm it exits 0.

Create `.planning/phases/21-sync-migration/REGRESSION-CHECKLIST-21.md` with: (1) a section reproducing `19-auth/REGRESSION-CHECKLIST.md` §3's exact cold-start measurement method (device/connection-type/measurement-method/conditions fields, blank for the human to fill in) plus an explicit note of whatever baseline number that file's §3 already recorded — or, if those fields are still blank placeholders, an explicit `**BLOCKER:**` line stating Phase 19's baseline was never actually measured and this plan's re-measurement will have nothing authoritative to compare against (surface this, do not silently proceed as if a baseline exists); (2) a SYNC-04 cross-device check (edit a profile setting on one platform/device, background, foreground on a second platform/device signed into the same account, confirm the change appears); (3) a SYNC-11 multi-tab check (open the deployed PWA in two browser tabs signed into the same account, edit availability in one tab, foreground the second tab, confirm no data loss/stale overwrite); (4) an AUTH-09 checkpoint cross-reference back to plan 21-08's already-completed dashboard verification (do not re-run it, just confirm it is recorded in `MANUAL-VERIFICATION-21.md`).

Append a short final section to `docs/ACCOUNTS-OPERATIONS.md` (its Runbook section) noting that sync is now live and pointing at `REGRESSION-CHECKLIST-21.md` for the manual verification record, alongside the existing "sync stopped working" entry.
  </action>
  <acceptance_criteria>
    <automated>flutter test 2>&1 | tail -5</automated>
    - `flutter test` output shows at least 348 passed, and any failure count matches exactly the pre-existing documented bug (no more, no fewer)
    - `flutter test test/structure/background_task_no_supabase_test.dart` exits 0
    - `flutter build apk --release` exits 0
    - `.planning/phases/21-sync-migration/REGRESSION-CHECKLIST-21.md` exists and contains all four sections described above
    - If `19-auth/REGRESSION-CHECKLIST.md` §3's fields are still unfilled placeholders, `grep -c "BLOCKER" .planning/phases/21-sync-migration/REGRESSION-CHECKLIST-21.md` is at least 1
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Execute REGRESSION-CHECKLIST-21.md — cold-start, SYNC-04, SYNC-11</name>
  <files>.planning/phases/21-sync-migration/REGRESSION-CHECKLIST-21.md, .planning/phases/21-sync-migration/MANUAL-VERIFICATION-21.md</files>
  <read_first>Task 1's output in this same plan (REGRESSION-CHECKLIST-21.md, written by Claude, executed here)</read_first>
  <action>
Work through `.planning/phases/21-sync-migration/REGRESSION-CHECKLIST-21.md` in order:
1. Web cold-start: follow the reproduced Phase 19 methodology exactly (same device/connection-type/measurement-method if at all possible — a different device than Phase 19 used is acceptable if Phase 19's own device is unavailable, but note the change explicitly rather than silently swapping it). Record the measured value. If it exceeds 2 seconds, this is a blocking regression — do not mark this phase complete; report the number back instead of proceeding.
2. SYNC-04: make a profile or availability change on one platform, bring the app to the foreground on a second signed-in device/platform, confirm the change appears.
3. SYNC-11: two browser tabs of the deployed PWA, same signed-in account, edit in one, foreground the other, confirm no data loss.
4. Confirm AUTH-09's dashboard check from plan 21-08 is recorded in `MANUAL-VERIFICATION-21.md`.
Fill in every checklist field as you go — this file is the phase's permanent verification record.
  </action>
  <acceptance_criteria>
    - Web cold-start measured and recorded, at or under the 2-second budget (or explicitly reported as a blocking regression, not silently accepted)
    - SYNC-04 cross-device propagation confirmed
    - SYNC-11 multi-tab safety confirmed
    - `MANUAL-VERIFICATION-21.md` contains the final, complete record for the whole phase
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Full suite, structure test, and release build are all green before the manual checklist is handed off
- REGRESSION-CHECKLIST-21.md reuses Phase 19's exact cold-start method, and explicitly flags if no valid baseline exists to compare against
- The checkpoint records real pass/fail evidence for REG-03, SYNC-04, and SYNC-11 — the three requirements this phase cannot fully close from `flutter test` alone
</verification>

<success_criteria>
- Phase 21's five ROADMAP.md success criteria are all demonstrably true: signed-in sync works across platforms with a working outbox and visible sync status; the 2-second cold-start budget holds after cloud reads are real; first-login/second-device/divergence resolve correctly; migration is atomic and never destructive; and RLS-proven data isolation plus a verified account-deletion path are both real
</success_criteria>

<output>
Create `.planning/phases/21-sync-migration/21-09-SUMMARY.md` when done. Append the checkpoint's final outcome to `.planning/phases/21-sync-migration/MANUAL-VERIFICATION-21.md`.
</output>
