---
phase: 19-auth
plan: 07
type: execute
wave: 5
depends_on: [19-06]
files_modified:
  - .planning/phases/19-auth/REGRESSION-CHECKLIST.md
autonomous: false
requirements: [AUTH-10, REG-01, REG-02, REG-04]

must_haves:
  truths:
    - "Sign-in is proven working from a real release build installed from the Play Store internal testing track, not a debug build and not a local APK — the only way AUTH-10 is actually true (D-16)"
    - "The Android release build, the PWA, and 'Add to calendar' on both platforms all still work after supabase_flutter and serverClientId landed"
    - "A web cold-start number is recorded with its measurement method, ready for Phase 21 to repeat and compare (REG-03 baseline)"
  artifacts:
    - path: ".planning/phases/19-auth/REGRESSION-CHECKLIST.md"
      provides: "Every checkbox ticked with an observed outcome, or recorded as blocked with a reason — this file becomes the phase's proof record"
      contains: "- [x]"
  key_links:
    - from: "Play Console internal testing track"
      to: "the Play App Signing SHA-1 registered in Cloud Console (Phase 18, docs/CONSOLE-SETUP-CHECKLIST.md)"
      via: "installing from the Store link, not a locally built APK — this is the SHA-1 that actually matters for real users"
      pattern: "internal testing"
---

<objective>
Do the device work. Everything in this plan is a manual action Claude cannot perform: uploading a build artifact to Play Console, installing and tapping through a real Android device from a real Store link, installing and tapping through a real iPhone PWA, and timing a real cold start. The value of writing it as a plan is that it is one short ordered checklist (D-17), not steps scattered through five other plans, and every observation gets recorded in `REGRESSION-CHECKLIST.md` rather than remembered.

This is the phase's actual proof, not a formality: this project has already shipped one Google integration (Calendar, Phase 9) broken for exactly the class of reason AUTH-10 exists to catch — a feature that works perfectly from a debug build and fails for every real Play Store user because only the wrong SHA-1 was registered. A local release APK proves the wrong half of that story; only a Play-Store-installed build proves the right half.

Output: `app-release.aab` uploaded and live on the internal testing track, and `REGRESSION-CHECKLIST.md` fully ticked (or explicitly recorded as blocked with a reason).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth/19-CONTEXT.md
@.planning/phases/19-auth/REGRESSION-CHECKLIST.md
@docs/CONSOLE-SETUP-CHECKLIST.md
@docs/ACCOUNTS-OPERATIONS.md
</context>

<interfaces>
**This plan is `autonomous: false`.** Every task is performed by Joost, on real devices, in an authenticated Play Console session. Claude's role is to present each step, wait for the observed outcome, and record it — not to claim completion on the user's behalf. If a step cannot be completed or fails, record it as blocked/failed with the reason; do not tick it.

**The AAB to upload:** `build/app/outputs/bundle/release/app-release.aab`, built by Plan 19-06, Task 2.

**Play Console coordinates:** `https://play.google.com/console/u/0/developers/4716806605533867534/app/4975932698287804363` (from `docs/CONSOLE-SETUP-CHECKLIST.md`'s app-signing page reference) — navigate to Release → Testing → Internal testing to upload.

**If AUTH-10's sign-in step fails with an audience-mismatch-shaped error:** re-check `docs/CONSOLE-SETUP-CHECKLIST.md` §2/§3 — this is the exact failure shape `PITFALLS.md` #3 predicts when the wrong OAuth client ID is configured somewhere in the chain (Supabase's Google provider, `serverClientId` in the app, or a missing SHA-1). Do not guess; compare the observed error against that finding before re-trying blindly.
</interfaces>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Upload app-release.aab to the internal testing track</name>
  <files>none — Play Console action</files>
  <read_first>docs/CONSOLE-SETUP-CHECKLIST.md (coordinates)</read_first>
  <action>
In Play Console → the app → Release → Testing → Internal testing, create a new release, upload `build/app/outputs/bundle/release/app-release.aab`, add release notes (e.g. "Fase 19: inloggen met Google"), and roll it out to the internal testing track. Confirm the release is processed (not stuck "Processing") and the internal testing link is live before moving to Task 2.
  </action>
  <acceptance_criteria>
    - The AAB is uploaded and the release shows a non-"Processing" status in Play Console
    - The internal testing opt-in/install link is confirmed reachable
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Execute the Android-release section of REGRESSION-CHECKLIST.md (AUTH-10 hard gate)</name>
  <files>.planning/phases/19-auth/REGRESSION-CHECKLIST.md</files>
  <read_first>.planning/phases/19-auth/REGRESSION-CHECKLIST.md ("Android release" section)</read_first>
  <action>
On a real Android device, install the app from the internal testing track's Play Store link (not `adb install`, not a local APK — this is the entire point of AUTH-10). Walk through every step in `REGRESSION-CHECKLIST.md`'s "Android release" section: start the app, sign in with Google, open a Ride Detail and tap "Voeg toe aan agenda" and confirm the event lands in the real Google Calendar, sign out via the confirm dialog, fully close and restart the app and confirm the session survived (no sign-in prompt, no flicker to signed-out).

Tick each step in the file as it is confirmed. If any step fails, record the exact observed error text (not a paraphrase) next to that step — this is the diagnostic trail if a later fix is needed.
  </action>
  <acceptance_criteria>
    - Every step in the "Android release" section of `REGRESSION-CHECKLIST.md` is either ticked `- [x]` with an observed-good result, or left unticked with the exact failure recorded
    - Sign-in specifically confirmed from the Play-Store-installed build (AUTH-10) — not from a debug run
    - "Voeg toe aan agenda" confirmed working end-to-end (REG-04)
    - Restart-survives-sign-in confirmed (AUTH-04's real-device proof)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 3: Execute the iPhone-PWA section and the web cold-start measurement</name>
  <files>.planning/phases/19-auth/REGRESSION-CHECKLIST.md</files>
  <read_first>.planning/phases/19-auth/REGRESSION-CHECKLIST.md ("iPhone PWA" and "Web cold-start" sections)</read_first>
  <action>
On a real iPhone, install the PWA via Safari's "Zet op beginscherm" / "Add to Home Screen", open it standalone from the home screen icon (confirm no Safari chrome is visible), navigate between Home/Ride Detail/Profiel, sign in with Google via the rendered button, and confirm "Voeg toe aan agenda" works end-to-end.

Then take the web cold-start measurement using the exact method `REGRESSION-CHECKLIST.md` specifies (device, connection type, start/end instants) — this is the REG-03 baseline Phase 21 will repeat and compare against, so measure it precisely rather than approximately.

Tick each step as confirmed; record the measured cold-start number and the exact conditions next to it.
  </action>
  <acceptance_criteria>
    - Every step in the "iPhone PWA" section is ticked with an observed-good result, or recorded as blocked with the reason
    - PWA installs, launches standalone, and navigates correctly (REG-02)
    - Sign-in via the rendered web button confirmed working (AUTH-06 real-device proof)
    - "Voeg toe aan agenda" confirmed working on web (REG-04's web half)
    - A cold-start number is recorded with its measurement method (device, connection, timing method) — not a bare number with no context
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `REGRESSION-CHECKLIST.md` has every checkbox either ticked with an observed outcome or explicitly recorded as blocked with a reason — no checkbox is ticked on the strength of an assumption
- AUTH-10 is proven from the Play Store internal testing install, not a debug build
- REG-01, REG-02, and REG-04 are each traced to an observed device outcome
- Phase 19 is complete when this plan closes
</verification>

<success_criteria>
Every claim this phase makes about working on real devices — sign-in, Calendar, restart-survival, PWA install — is backed by an observation recorded in `REGRESSION-CHECKLIST.md`, not an assumption. AUTH-10 specifically is proven from the artifact that actually matters: a Play-Store-installed build signed with Google's own key.
</success_criteria>

<output>
Create `.planning/phases/19-auth/19-07-SUMMARY.md` when done
</output>
