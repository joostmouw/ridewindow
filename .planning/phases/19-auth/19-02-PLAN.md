---
phase: 19-auth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/services/calendar_service.dart
autonomous: true
requirements: [AUTH-05, REG-04]

must_haves:
  truths:
    - "GoogleSignIn.instance.initialize() is called from exactly one place in the codebase — the existing memoized _sharedInitialize() gate — now carrying serverClientId, so auth and Calendar can never race into a double-init crash"
    - "Add to Google Calendar still works end to end after this change (REG-04) — the shared gate's existing behavior (memoized in-flight future, lazy, lock re-entrant) is preserved exactly, only the initialize() call gains one named argument"
  artifacts:
    - path: "lib/services/calendar_service.dart"
      provides: "_sharedInitialize() now passes serverClientId to GoogleSignIn.instance.initialize(); a new public static helper other files can call to trigger the same shared init without duplicating it"
      contains: "serverClientId:"
  key_links:
    - from: "lib/services/calendar_service.dart _sharedInitialize()"
      to: "GoogleSignIn.instance.initialize(serverClientId: ...)"
      via: "the single memoized _initFuture gate, unchanged in shape"
      pattern: "serverClientId"
---

<objective>
Make the one-line change that sits directly on top of the working "Add to calendar" path: add `serverClientId` to the existing shared `GoogleSignIn.instance.initialize()` call, so Supabase's auth server can verify the ID token's audience (PITFALLS.md #3). Also expose a public entry point to that same shared init gate, since Plan 19-03's account UI needs to trigger it too and the constraint is that `initialize()` may only ever be called from this one file.

This is deliberately its own plan, isolated from all UI work, because REG-04 makes it the sharpest regression gate in the phase — a mistake here breaks a feature this project has already shipped broken once for exactly this class of reason (three OAuth client IDs, easy to conflate).

Purpose: unblock every other plan in this phase from needing to touch `calendar_service.dart` again, and prove the shared-init change is safe in isolation before any new UI depends on it.

Output: `_sharedInitialize()` passes `serverClientId`; a new `CalendarService.ensureGoogleSignInReady()` static method other files call instead of reaching into the private gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth/19-CONTEXT.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md
@docs/CONSOLE-SETUP-CHECKLIST.md
@lib/services/calendar_service.dart
</context>

<interfaces>
**The web client ID (from `docs/CONSOLE-SETUP-CHECKLIST.md` §2, already live in this codebase):**

`300023366326-ddo399qf5lavv48njbfpm7rg0mc8cnno.apps.googleusercontent.com`

Cross-checked: this exact value is already wired into `web/index.html:47` as `<meta name="google-signin-client_id">`. Use the identical string — do not re-derive or guess it. This is the value Supabase's auth server checks the ID token's `aud` claim against (PITFALLS.md #3); the wrong client ID here produces "Invalid audience", a generic-looking error with no clue pointing back at this line.

**Why this file and no other may call `GoogleSignIn.instance.initialize()` (AUTH-05, hard constraint):** a second concurrent call throws `Bad state: init() has already been called` — a bug this project has already fixed once (see the file's own header comment, "Rule 1 bugfix, CAL-06 follow-up"). `_sharedInitialize()`'s memoized `_initFuture` is the only thing preventing that. Plan 19-03's account UI must call the new public wrapper this task adds, never `GoogleSignIn.instance.initialize()` directly.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add serverClientId to _sharedInitialize() and expose a public entry point</name>
  <files>lib/services/calendar_service.dart</files>
  <read_first>lib/services/calendar_service.dart (full file — the memoized gate at lines 33-44 and its doc comments), .planning/research/PITFALLS.md #1 and #3, .planning/phases/19-auth/19-CONTEXT.md (canonical_refs, "Code die deze fase raakt")</read_first>
  <action>
In `_sharedInitialize()`, change `GoogleSignIn.instance.initialize()` to `GoogleSignIn.instance.initialize(serverClientId: '300023366326-ddo399qf5lavv48njbfpm7rg0mc8cnno.apps.googleusercontent.com')`. Define the client ID as a private top-level `const String _kGoogleWebClientId` near the top of the file (next to the existing `_initialized`/`_initFuture` static fields) rather than inline, and reference it from the `initialize()` call — this keeps the one magic string in one place and matches the file's existing style of named static fields for shared state.

Add a Dutch doc comment above the constant explaining why it exists: Supabase's `signInWithIdToken` verifies the ID token's audience against this exact web client ID (PITFALLS.md #3); it is the same value already embedded in `web/index.html`'s `google-signin-client_id` meta tag.

Add a new public static method `Future<void> ensureGoogleSignInReady()` that simply calls `_sharedInitialize()`. This is the entry point Plan 19-03's account UI (and Plan 19-05's mismatch check) must call to trigger the shared init from outside this file — `_sharedInitialize()` itself stays private, `_initFuture`/`_initialized` stay private, only this one wrapper becomes public. Update the file's header comment (lines 1-10) to note that auth now shares this same gate (AUTH-05), alongside the existing Calendar-only description.

Do not touch `addRideSlotToCalendar()`, `getEvents()`, `isCalendarConnected()`, or `disconnectCalendar()` — none of them need to change; they already call `_ensureInitialized()`, which already delegates to `_sharedInitialize()`.
  </action>
  <acceptance_criteria>
    - `lib/services/calendar_service.dart` contains `serverClientId: _kGoogleWebClientId` (or the literal value inline via the constant) inside the `GoogleSignIn.instance.initialize(...)` call
    - The constant's value is exactly `300023366326-ddo399qf5lavv48njbfpm7rg0mc8cnno.apps.googleusercontent.com` (matches `web/index.html:47` byte-for-byte — verify with `grep -o` on both files and compare)
    - A new `static Future<void> ensureGoogleSignInReady()` method exists and its body calls `_sharedInitialize()`
    - `grep -c "GoogleSignIn.instance.initialize(" lib/services/calendar_service.dart` returns exactly `1` — still only one call site in the whole codebase
    - `addRideSlotToCalendar`, `getEvents`, `isCalendarConnected`, `disconnectCalendar` are textually unchanged (diff shows zero changes inside those method bodies)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Confirm no regression in existing Calendar tests and coverage</name>
  <files>test/services/calendar_service_test.dart, test/features/profile_screen_calendar_test.dart</files>
  <read_first>test/services/calendar_service_test.dart, test/features/profile_screen_calendar_test.dart</read_first>
  <action>
Run both existing Calendar test files and the full suite to confirm the `serverClientId` addition introduces no regression. Neither existing test file mocks `GoogleSignIn.instance.initialize()`'s arguments directly (they rely on the established "no platform channel handler in tests → MissingPluginException → graceful degrade to 'not connected'" pattern documented in `profile_screen_calendar_test.dart`'s header comment), so no test code changes are expected here — this task is a verification pass, not new test authorship. If either file's expectations no longer hold, fix the test to match the still-correct graceful-degradation behavior, not the source.
  </action>
  <acceptance_criteria>
    - `flutter test test/services/calendar_service_test.dart` exits 0
    - `flutter test test/features/profile_screen_calendar_test.dart` exits 0
    - `flutter test` (full suite) still reports the pre-existing 306+/0 baseline — zero new failures attributable to this plan
    - `flutter analyze lib/services/calendar_service.dart` introduces no new errors
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Exactly one call site for `GoogleSignIn.instance.initialize()` in the codebase, now carrying `serverClientId`
- The web client ID matches `web/index.html`'s existing meta tag exactly
- `CalendarService.ensureGoogleSignInReady()` exists as the public entry point for Plan 19-03/19-05
- Full test suite still 306+/0
</verification>

<success_criteria>
Supabase can verify Google ID tokens against the correct audience, the shared init gate has exactly one caller as before, and Calendar's existing four methods are provably untouched — REG-04's sharpest risk is closed before any new UI is built on top of it.
</success_criteria>

<output>
Create `.planning/phases/19-auth/19-02-SUMMARY.md` when done
</output>
