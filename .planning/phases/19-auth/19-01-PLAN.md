---
phase: 19-auth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/core/supabase_config.dart
  - lib/main.dart
  - lib/providers/auth_notifier.dart
  - lib/providers/auth_notifier.g.dart
autonomous: false
requirements: [AUTH-04]
user_setup:
  - service: supabase
    why: "Supabase.initialize() and every subsequent auth call need the project's public anon key. Claude has no dashboard session and no CLI (no `supabase` binary is installed in this environment) to read it, so a human must copy it once."
    dashboard_config:
      - task: "Copy the 'anon public' key for project hcdrydlgqpnmumfupgcx (West EU / Paris)"
        location: "https://supabase.com/dashboard/project/hcdrydlgqpnmumfupgcx/settings/api"

must_haves:
  truths:
    - "A cold app start (Android or web) shows a previously signed-in user as signed in, with no one-frame 'signed out' flicker before the real state resolves"
    - "Supabase.initialize() completes before any provider can read authStateProvider — no race between cold-start init and the first widget build"
    - "No existing build or deploy command changes — flutter build apk --release, flutter build web --release and the Firebase deploy all still run with zero added flags"
  artifacts:
    - path: "lib/core/supabase_config.dart"
      provides: "Supabase URL + anon key as code constants, with a comment explaining the anon key is public by design"
      contains: "const supabaseUrl ="
    - path: "lib/providers/auth_notifier.dart"
      provides: "authStateProvider (seeded Stream<User?>, keepAlive) and currentUserIdProvider"
      exports: ["authStateProvider", "currentUserIdProvider"]
  key_links:
    - from: "lib/main.dart"
      to: "lib/core/supabase_config.dart"
      via: "Supabase.initialize(url:, anonKey:) awaited before runApp()"
      pattern: "Supabase\\.initialize"
    - from: "lib/providers/auth_notifier.dart"
      to: "Supabase.instance.client.auth.currentSession"
      via: "seeded as the stream's first value, before merging onAuthStateChange"
      pattern: "currentSession"
---

<objective>
Add `supabase_flutter` to the app and stand up the two pieces every later plan in this phase depends on: the URL/anon-key config file and `authStateProvider` (the single source of truth for "who is signed in", read everywhere downstream).

This plan does not touch UI, does not touch `calendar_service.dart`, and writes no database rows — it is pure bootstrap, per the phase boundary in `19-CONTEXT.md`.

Purpose: every other plan in this phase (account UI, switch detection, calendar-mismatch warning, regression proof) needs `authStateProvider` to exist and needs Supabase to actually be initialized before it can be read. Getting the seeding right here (D from ARCHITECTURE.md §1: `onAuthStateChange` does not replay a synchronous initial value) is what makes AUTH-04 true instead of flaky.

Output: `supabase_flutter` in `pubspec.yaml`, `lib/core/supabase_config.dart`, `Supabase.initialize()` wired into `lib/main.dart`'s existing parallel-init block, and `lib/providers/auth_notifier.dart` exporting `authStateProvider` + `currentUserIdProvider`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth/19-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@docs/CONSOLE-SETUP-CHECKLIST.md
@docs/ACCOUNTS-OPERATIONS.md
@lib/main.dart
@pubspec.yaml
</context>

<interfaces>
**Known coordinates (from `docs/CONSOLE-SETUP-CHECKLIST.md` §1):**

| Item | Value |
|---|---|
| Supabase project ref | `hcdrydlgqpnmumfupgcx` |
| Supabase API URL | `https://hcdrydlgqpnmumfupgcx.supabase.co` |
| Region | West EU (Paris), `eu-west-3` |
| Package version (ARCHITECTURE.md §6, verified live 2026-07-25) | `supabase_flutter: ^2.16.0` |

**Anon key:** not present in any file Claude can read — `CONSOLE-SETUP-CHECKLIST.md` deliberately excludes it ("Contains no secrets"). It ships inside the app bundle by design (D-13/D-14/D-15 in `19-CONTEXT.md`), so pasting it into a tracked source file is correct, not a leak — but Claude cannot fetch it itself (no `supabase` CLI is installed in this environment, confirmed by `command -v supabase` failing). Task 1 gets it from the user.

**Why constants, not `--dart-define` (D-14, locked):** every build/deploy command (`flutter build apk --release`, `flutter build web --release`, the Firebase deploy) would need the flag, and one forgotten flag silently ships a build where sign-in is broken — exactly the failure class AUTH-10 exists to catch. Do not introduce `--dart-define` or `.env` anywhere in this phase.
</interfaces>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Get the Supabase anon key and write lib/core/supabase_config.dart</name>
  <files>lib/core/supabase_config.dart</files>
  <read_first>.planning/phases/19-auth/19-CONTEXT.md (D-13, D-14, D-15), docs/CONSOLE-SETUP-CHECKLIST.md §1</read_first>
  <action>
Ask the user to open https://supabase.com/dashboard/project/hcdrydlgqpnmumfupgcx/settings/api, copy the value labelled "anon" / "public" under Project API keys (a JWT starting with `eyJ`), and paste it back.

Once received, create `lib/core/supabase_config.dart` with two top-level `const String` declarations: `supabaseUrl = 'https://hcdrydlgqpnmumfupgcx.supabase.co'` and `supabaseAnonKey = '<pasted value>'`. Precede them with a Dutch comment block explaining, per D-15: this key is public by design — it ships readable inside every APK and every web bundle; Postgres row-level security (Phase 21), not secrecy, is what protects the data; do not "fix" this later by moving it to `--dart-define` or `.env` (D-14 explains why that is the worse option, not a safer one). Reference `18-CONTEXT.md` D-13/D-19 as the precedent (the same reasoning already applied to the keep-warm job's anon key).

Do not touch `pubspec.yaml` or `main.dart` in this task — that is Task 2, kept separate so a mistyped key is a one-file fix.
  </action>
  <acceptance_criteria>
    - `lib/core/supabase_config.dart` exists
    - Contains `const supabaseUrl = 'https://hcdrydlgqpnmumfupgcx.supabase.co';` (or equivalent exact URL match)
    - Contains `const supabaseAnonKey = '...'` where the value starts with `eyJ` (a real JWT, not a placeholder like `TODO` or `YOUR_ANON_KEY`)
    - A Dutch comment block above the constants states the key is public by design and explains why (references RLS as the real protection, not secrecy)
    - No other file changed in this task
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add supabase_flutter and wire Supabase.initialize() into main.dart's cold-start block</name>
  <files>pubspec.yaml, lib/main.dart</files>
  <read_first>lib/main.dart, pubspec.yaml, lib/core/supabase_config.dart (from Task 1), .planning/research/ARCHITECTURE.md §6 (modified-files table, main.dart row), .planning/research/PITFALLS.md #10 (cold-start risk)</read_first>
  <action>
Add `supabase_flutter: ^2.16.0` to `pubspec.yaml` under a new `# Phase 19 — Supabase Auth` comment, matching the existing phase-comment convention already used for every other dependency block in this file. Run `flutter pub get`.

In `lib/main.dart`, import `package:supabase_flutter/supabase_flutter.dart` and `package:ridewindow/core/supabase_config.dart`. Immediately after the existing `final prefsFuture = SharedPreferences.getInstance();` line, start `final supabaseFuture = Supabase.initialize(url: supabaseUrl, anonKey: supabaseAnonKey);` — started in parallel with `tzFuture`/`prefsFuture`, not chained after them. Before the existing `runApp(...)` call, add `await supabaseFuture;` so initialization is guaranteed complete before `ProviderScope` builds `RideWindowApp` (and therefore before anything can read `authStateProvider`) — this satisfies ARCHITECTURE.md §7 step 3's ordering requirement.

Do not modify the `kIsWeb` branches (`WorkManager` init, `CalendarService.warmUpForWeb()`) — those run after `runApp()` today and must keep doing so unchanged, per the phase's hard constraint that `lib/platform/background_task.dart` and the WorkManager isolate stay untouched and Supabase-free.

As a REG-01 smoke check before this phase goes further, run `flutter build apk --release` after the dependency is added and confirm it still exits 0 — this catches a Gradle/manifest regression from the new dependency (`app_links`, transitively) immediately in Wave 1 rather than waiting for the phase's final build gate.
  </action>
  <acceptance_criteria>
    - `pubspec.yaml` contains `supabase_flutter: ^2.16.0`
    - `flutter pub get` exits 0
    - `lib/main.dart` contains `Supabase.initialize(` and the call is `await`-ed before the `runApp(` line (grep the file: the line number of `Supabase.initialize` is lower than the line number of `runApp(`)
    - The existing `if (kIsWeb) { ... CalendarService.warmUpForWeb(); }` and `if (!kIsWeb) { ... Workmanager()... }` blocks are byte-identical to before this task (diff shows no changes inside those two `if` blocks)
    - `flutter build apk --release` exits 0
    - `flutter analyze` introduces no new errors in `lib/main.dart` compared to the pre-task baseline
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create authStateProvider + currentUserIdProvider (seeded, keepAlive)</name>
  <files>lib/providers/auth_notifier.dart, lib/providers/auth_notifier.g.dart</files>
  <read_first>.planning/research/ARCHITECTURE.md §1 (exact seeding requirement and both Supabase traps), lib/providers/location_provider.dart (reactive @riverpod function-provider pattern), lib/providers/clock_provider.dart (keepAlive pattern reference)</read_first>
  <behavior>
    - authStateProvider's very first emitted value must equal `Supabase.instance.client.auth.currentSession?.user` (synchronous seed) — not wait for the first `onAuthStateChange` event, which never replays an initial value on its own.
    - After the seed, authStateProvider continues to emit on every subsequent `onAuthStateChange` event, mapped from `AuthState.session?.user` to `User?`.
    - currentUserIdProvider derives `ref.watch(authStateProvider).value?.id` — returns null while loading or when signed out, and the Supabase UUID (not a Google account id) when signed in.
  </behavior>
  <action>
Create `lib/providers/auth_notifier.dart` with a `@Riverpod(keepAlive: true)` function provider named `authState` (generated name: `authStateProvider`) returning `Stream<User?>`. Implement it as an `async*` generator: `yield` the current session's user first (`Supabase.instance.client.auth.currentSession?.user`), then `yield*` `Supabase.instance.client.auth.onAuthStateChange.map((state) => state.session?.user)`. `keepAlive: true` because every downstream provider that reacts to sign-in/out (Phase 20's repositories) depends on this never disposing between listener drops.

Add a second `@riverpod` (auto-dispose is fine here — it's a cheap derived value) function `currentUserId` returning `String?` = `ref.watch(authStateProvider).value?.id`. Note the ARCHITECTURE.md §1 warning in a code comment: this `id` is a Supabase UUID, not the Google account id — AUTH-07's Calendar-mismatch comparison (Plan 19-05) must use `.email`, not this id.

Run `dart run build_runner build --delete-conflicting-outputs` to generate `auth_notifier.g.dart`.
  </action>
  <acceptance_criteria>
    - `lib/providers/auth_notifier.g.dart` exists and defines `authStateProvider` and `currentUserIdProvider`
    - `lib/providers/auth_notifier.dart` contains `currentSession` (the seed) textually before `onAuthStateChange` (grep line-number check)
    - `flutter analyze` introduces no new errors in the two new files
    - `flutter test` still reports the pre-existing 306+/0 baseline (no test yet exercises this provider, so this confirms no compile-time regression elsewhere)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `lib/core/supabase_config.dart` holds the real anon key (starts `eyJ`), not a placeholder
- `Supabase.initialize()` is awaited before `runApp()` in `lib/main.dart`, and the two existing `kIsWeb` branches are unchanged
- `authStateProvider` seeds from `currentSession` before merging the change stream (AUTH-04's actual mechanism)
- `flutter build apk --release` and `flutter pub get` both exit 0; `flutter test` still shows the pre-existing 306+/0 baseline
- No `--dart-define`, no `.env`, no change to any existing build/deploy command (D-14)
</verification>

<success_criteria>
`supabase_flutter` is a working dependency, Supabase is initialized before the app's first frame, and `authStateProvider`/`currentUserIdProvider` exist and are ready to be watched by Plan 19-03's account UI — all without a single database row, without touching `calendar_service.dart`, and without changing how the app is built or deployed.
</success_criteria>

<output>
Create `.planning/phases/19-auth/19-01-SUMMARY.md` when done
</output>
