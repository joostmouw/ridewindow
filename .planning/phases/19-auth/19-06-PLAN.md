---
phase: 19-auth
plan: 06
type: execute
wave: 4
depends_on: [19-04, 19-05]
files_modified:
  - .planning/phases/19-auth/REGRESSION-CHECKLIST.md
autonomous: true
requirements: [AUTH-10, REG-01]

must_haves:
  truths:
    - "A checkable, per-platform regression checklist exists and describes exactly what Plan 19-07's human tester must do — nothing is left to memory or improvisation"
    - "The web cold-start measurement methodology is written down precisely enough that Phase 21's re-measurement (REG-03) is comparable, not just another number"
    - "A release AAB exists locally, built after every code change in this phase, ready for Plan 19-07's Play Console upload"
    - "The automated test suite is still green before any manual verification begins — a red suite is not a valid starting point for a release gate"
  artifacts:
    - path: ".planning/phases/19-auth/REGRESSION-CHECKLIST.md"
      provides: "Checkable Android-release and iPhone-PWA steps, plus a web cold-start measurement method — reusable as-is by Phase 21 (D-18)"
      contains: "cold-start"
  key_links:
    - from: ".planning/phases/19-auth/REGRESSION-CHECKLIST.md"
      to: "Plan 19-07"
      via: "the document this plan writes is what 19-07's checkpoint tasks execute against"
      pattern: "REGRESSION-CHECKLIST"
---

<objective>
Everything in this plan is something Claude can do without a human's browser session: write the regression checklist itself, build the release artifact, and confirm the suite is green. This is deliberately separated from Plan 19-07 (D-17) — every task here is `auto`, every task there is a human action Claude cannot perform (uploading to Play Console, tapping through a real device, timing a real cold start).

Purpose: hand Plan 19-07 everything it needs — a document to follow and an AAB to upload — so the manual plan is purely execution, not also authorship.

Output: `.planning/phases/19-auth/REGRESSION-CHECKLIST.md`, a built `app-release.aab`, and a confirmed-green test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-auth/19-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/research/PITFALLS.md
@docs/CONSOLE-SETUP-CHECKLIST.md
</context>

<interfaces>
**D-18's exact required checklist shape (from `19-CONTEXT.md`, locked):**

- **Android release** section: opstarten (start the app), inloggen (sign in), "Voeg toe aan agenda" (add a ride to Calendar), uitloggen (sign out), herstart → nog ingelogd (restart → still signed in).
- **iPhone PWA** section: installeren (install to home screen), standalone openen (open standalone, not in Safari chrome), navigeren (navigate between screens), inloggen (sign in), "Voeg toe aan agenda".

**D-19's methodology requirement:** the web cold-start measurement needs its *method* written down, not just a target number — which device, which connection (e.g. throttled "Fast 3G" via Chrome DevTools Network tab, or a real mobile connection with the type noted), and exactly how the start/end instants are captured (e.g. tap-to-open timestamp to first meaningful paint of the Home screen's slot list). Phase 21 re-measures against this exact method to decide REG-03's pass/fail — an incomparable method makes that re-measurement meaningless.

**This is a Dutch-language checklist** (the tester is Joost), matching the project's established documentation language — mirror `docs/CONSOLE-SETUP-CHECKLIST.md` and `18-04-PLAN.md`'s checkbox style (`- [ ] step`).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Write REGRESSION-CHECKLIST.md</name>
  <files>.planning/phases/19-auth/REGRESSION-CHECKLIST.md</files>
  <read_first>.planning/phases/19-auth/19-CONTEXT.md (D-16, D-18, D-19), .planning/research/PITFALLS.md #10, docs/CONSOLE-SETUP-CHECKLIST.md (for the exact Play Console app URL / internal testing track path if recorded, otherwise note it needs to be looked up during 19-07)</read_first>
  <action>
Write `.planning/phases/19-auth/REGRESSION-CHECKLIST.md` in Dutch, with three checkbox sections:

1. **Android release** (source: install from the Play Store internal testing track, not a local APK — D-16, since AUTH-10 is specifically about the Play App Signing key, not the local upload key): open de app na installatie vanaf de Play Store-link; log in met Google; open een Ride Detail en tik "Voeg toe aan agenda", bevestig dat het event in de echte Google Agenda verschijnt; log uit via de bevestigingsdialoog; sluit de app volledig af en start opnieuw, bevestig dat de gebruiker nog ingelogd is (geen flikkering naar uitgelogd).
2. **iPhone PWA**: installeer de PWA via "Zet op beginscherm" in Safari; open de app vanaf het beginscherm (standalone-modus, geen Safari-chrome zichtbaar); navigeer tussen Home, Ride Detail en Profiel; log in met Google via de gerenderde knop; tik "Voeg toe aan agenda" en bevestig het event verschijnt.
3. **Web cold-start meting (D-19, voedt REG-03 in fase 21)**: specificeer het exacte toestel (bv. "iPhone [model], Safari"), het verbindingstype (bv. "4G, niet gethrottled" of "Chrome DevTools Fast 3G-throttling" als op desktop gemeten), en de exacte meetmethode (bv. "tijd tussen tik-op-de-PWA-icoon en het eerste zichtbare ritvenster op Home, gestopt met een stopwatch / de systeemklok"). Noteer het gemeten getal en de meetomstandigheden zodat fase 21 exact dezelfde methode kan herhalen.

Add a short header note (2-3 sentences) stating this file is reusable as-is by Phase 21 per D-18, and that every checkbox must be ticked with an observed outcome, not assumed.
  </action>
  <acceptance_criteria>
    - `.planning/phases/19-auth/REGRESSION-CHECKLIST.md` exists
    - Contains an "Android release" section with all five steps from D-18 (opstarten, inloggen, Voeg toe aan agenda, uitloggen, herstart → nog ingelogd)
    - Contains an "iPhone PWA" section with all five steps from D-18 (installeren, standalone openen, navigeren, inloggen, Voeg toe aan agenda)
    - Contains a web cold-start section naming device, connection type, and measurement method explicitly (not just a target number)
    - All checklist items use `- [ ]` markdown checkbox syntax
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build the release AAB</name>
  <files>build/app/outputs/bundle/release/app-release.aab</files>
  <read_first>android/app/build.gradle.kts (confirm the signing config still points at the upload keystore, unchanged by this phase)</read_first>
  <action>
Run `flutter build appbundle --release`. This is the artifact Plan 19-07 uploads to the Play Console internal testing track — Claude can produce the file, but cannot upload it (no Play Developer API credentials are configured in this environment). Confirm the output path and file size look sane (a multi-megabyte `.aab`, not a near-empty file indicating a partial build).
  </action>
  <acceptance_criteria>
    - `flutter build appbundle --release` exits 0
    - `build/app/outputs/bundle/release/app-release.aab` exists and is larger than 1 MB
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Confirm the full suite is green before handing off to manual verification</name>
  <files>none — verification only</files>
  <read_first>none</read_first>
  <action>
Run `flutter test` (full suite) and `flutter analyze`. This is the phase's internal gate before Plan 19-07's real-device verification begins — a red suite is not a valid starting point for AUTH-10's release-build proof. If any test newly fails (beyond the pre-existing, already-documented baseline gaps unrelated to this phase — see `STATE.md`'s note on `flutter test` variance), stop and report it rather than proceeding to Plan 19-07; this phase's earlier plans (19-01 through 19-05) are the ones to fix, not this plan.
  </action>
  <acceptance_criteria>
    - `flutter test` exits with the running total from Plans 19-03/19-04/19-05's new tests, and 0 new failures beyond the pre-existing documented baseline
    - `flutter analyze` shows 0 new errors introduced across this phase's plans (pre-existing warnings/info-level lints, as already documented in `STATE.md`, are not this phase's regression)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- `REGRESSION-CHECKLIST.md` exists with all three sections, matching D-18/D-19 exactly
- `app-release.aab` built and ready for upload
- Full suite green (net of the pre-existing documented baseline gap) before Plan 19-07 begins
</verification>

<success_criteria>
Plan 19-07 has everything it needs to execute without also having to author anything: a checklist to follow and an artifact to upload.
</success_criteria>

<output>
Create `.planning/phases/19-auth/19-06-SUMMARY.md` when done
</output>
