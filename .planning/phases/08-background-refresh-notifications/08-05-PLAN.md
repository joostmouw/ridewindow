---
phase: 08-background-refresh-notifications
plan: 05
type: execute
wave: 5
depends_on:
  - 08-04
files_modified:
  - test/platform/notification_service_test.dart
  - test/providers/last_refreshed_provider_test.dart
  - test/features/profile_screen_notif_test.dart
  - test/features/home_screen_refresh_test.dart
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02
  - NOTIF-03
  - NOTIF-04
  - NOTIF-05
  - NOTIF-06

must_haves:
  truths:
    - "NotificationService unit-tests bevestigen tijdberekeningen voor alle drie notificatie-typen"
    - "LastRefreshedNotifier provider-tests bevestigen read-from-SharedPreferences en refresh()-gedrag"
    - "ProfileScreen widget-tests bevestigen dat de NOTIFICATIES sectie met 3 SwitchListTile widgets zichtbaar is"
    - "HomeScreen widget-tests bevestigen lastRefreshed header-weergave ('Bijgewerkt: HH:mm' en 'Bijgewerkt: —')"
    - "Volledige flutter test suite slaagt groen zonder regressies"
  artifacts:
    - path: "test/platform/notification_service_test.dart"
      provides: "Unit-tests voor NotificationService tijdberekeningen (mocked plugin)"
    - path: "test/providers/last_refreshed_provider_test.dart"
      provides: "ProviderContainer-tests voor LastRefreshedNotifier"
    - path: "test/features/profile_screen_notif_test.dart"
      provides: "Widget-tests voor ProfileScreen NOTIFICATIES sectie (3 toggles)"
    - path: "test/features/home_screen_refresh_test.dart"
      provides: "Widget-tests voor HomeScreen lastRefreshed header"
  key_links:
    - from: "test/platform/notification_service_test.dart"
      to: "lib/platform/notification_service.dart NotificationService"
      via: "injecteerbare FlutterLocalNotificationsPlugin mock"
      pattern: "NotificationService"
    - from: "test/features/profile_screen_notif_test.dart"
      to: "lib/features/profile/profile_screen.dart"
      via: "FakeProfileNotifier + FakeNotificationService overrides"
      pattern: "SwitchListTile"
    - from: "test/features/home_screen_refresh_test.dart"
      to: "lib/providers/last_refreshed_provider.dart"
      via: "FakeLastRefreshedNotifier override"
      pattern: "Bijgewerkt"
---

<objective>
Schrijf unit- en widget-tests voor alle Phase 8 componenten en draai de volledige test suite.

Purpose: Alle zes NOTIF-requirements moeten aantoonbaar gedekt zijn door geautomatiseerde tests. Regressies in bestaande Phases 1–7 moeten vroegtijdig worden ontdekt.
Output: vier nieuwe testbestanden, volledige suite groen.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-background-refresh-notifications/08-CONTEXT.md
@.planning/phases/08-background-refresh-notifications/08-PATTERNS.md
@lib/platform/notification_service.dart
@lib/providers/last_refreshed_provider.dart
@lib/features/profile/profile_screen.dart
@lib/features/home/home_screen.dart

<interfaces>
<!-- Testpatronen overgenomen uit Phase 6 en 7. -->

FakeNotifier patroon (STATE.md beslissing 03-03, 04-05):
```dart
class FakeLastRefreshedNotifier extends LastRefreshedNotifier {
  final DateTime? fakeTime;
  FakeLastRefreshedNotifier(this.fakeTime);
  @override
  Future<DateTime?> build() async => fakeTime;
}
```

ProviderScope override patroon (Phase 7):
```dart
ProviderScope(
  overrides: [
    lastRefreshedProvider.overrideWith(() => FakeLastRefreshedNotifier(DateTime(2026, 6, 3, 14, 30))),
    profileProvider.overrideWith(() => FakeProfileNotifier(testProfile)),
  ],
  child: MaterialApp(home: const HomeScreen()),
)
```

SharedPreferences mock in tests:
```dart
TestWidgetsFlutterBinding.ensureInitialized();
SharedPreferences.setMockInitialValues({'weather.lastRefreshed': 1748952600000});
```

FlutterLocalNotificationsPlugin mocken:
```dart
// Gebruik mockito of een fake implementatie
// NotificationService accepteert FlutterLocalNotificationsPlugin via constructor
class FakeFlutterLocalNotificationsPlugin extends Fake
    implements FlutterLocalNotificationsPlugin {
  List<String> calls = [];
  @override
  Future<bool?> initialize(InitializationSettings ...) async { ... }
  // etc.
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Taak 1: Unit-tests voor NotificationService en LastRefreshedNotifier</name>
  <files>
    test/platform/notification_service_test.dart,
    test/providers/last_refreshed_provider_test.dart
  </files>
  <action>
**test/platform/notification_service_test.dart**

Test de tijdberekeningen in NotificationService. Gebruik een `FakeFlutterLocalNotificationsPlugin` (implements Fake) die methode-aanroepen registreert. Injecteer via de constructor.

Schrijf 4 tests:

1. **scheduleEveningBefore — tijdberekening**: Geef een `slotDay` van morgen; bevestig dat de geplande tijd 19:00 GISTEREN is ten opzichte van slotDay (d.w.z. de dag vóór slotDay om 19:00). Hint: vergelijk TZDateTime-jaar/maand/dag/uur/minuut met verwachte waarden.

2. **scheduleEveningBefore — skip als verleden**: Geef een `slotDay` van gisteren; bevestig dat `zonedSchedule()` NIET aangeroepen wordt (de notificatie zou in het verleden vallen).

3. **scheduleMorningOf — tijdberekening**: Geef een `slotStart` van morgen 10:00; bevestig dat de geplande tijd morgen 08:00 is (slotStart − 2h).

4. **scheduleWeeklyDigest — eerstvolgende zondag**: Bevestig dat de geplande tijd een zondag is en het uur 19 is. Gebruik `scheduledDate.weekday == DateTime.sunday` en `scheduledDate.hour == 19`.

**test/providers/last_refreshed_provider_test.dart**

Gebruik `ProviderContainer` zonder mocks (SharedPreferences mock via `setMockInitialValues`).

Schrijf 3 tests:

1. **build — null als geen timestamp opgeslagen**: `setMockInitialValues({})` → `lastRefreshedProvider` geeft `null` terug.

2. **build — leest timestamp uit SharedPreferences**: `setMockInitialValues({'weather.lastRefreshed': 1748952600000})` → provider geeft `DateTime.fromMillisecondsSinceEpoch(1748952600000)` terug.

3. **refresh() — herlaadt state**: Maak een container met lege prefs, lees provider (null), schrijf dan een waarde naar prefs, roep `refresh()` aan, bevestig dat de provider nu de nieuwe waarde retourneert.

Gebruik `TestWidgetsFlutterBinding.ensureInitialized()` en `SharedPreferences.setMockInitialValues()` voor alle SharedPreferences-tests.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/platform/notification_service_test.dart test/providers/last_refreshed_provider_test.dart --reporter compact</automated>
  </verify>
  <done>
- test/platform/notification_service_test.dart: 4 tests slagen
- test/providers/last_refreshed_provider_test.dart: 3 tests slagen
- flutter test meldt geen fouten voor beide bestanden
  </done>
</task>

<task type="auto">
  <name>Taak 2: Widget-tests voor ProfileScreen notificaties + HomeScreen lastRefreshed + volledige suite gate</name>
  <files>
    test/features/profile_screen_notif_test.dart,
    test/features/home_screen_refresh_test.dart
  </files>
  <action>
**test/features/profile_screen_notif_test.dart**

Gebruik het FakeNotifier-patroon (per STATE.md 03-03, D-08-14). Definieer lokaal:
- `FakeProfileNotifier extends ProfileNotifier` — retourneert een UserProfile met alle notif-toggles false (standaard) of true (per test)
- Een `testProfile` constante/factory

Schrijf 5 widget-tests:

1. **Toont NOTIFICATIES sectie**: Bevestig dat `find.text('NOTIFICATIES')` en `find.byType(SwitchListTile)` (drie instanties, gebruik `findsNWidgets(3)`) aanwezig zijn. Opmerking: gebruik `skipOffstage: false` als de sectie buiten het initiële viewport valt.

2. **Avond-van-tevoren toggle zichtbaar met label**: `find.text('Avond van tevoren')` is aanwezig.

3. **Ochtend-van-de-dag toggle zichtbaar met label**: `find.text('Ochtend van de dag')` is aanwezig.

4. **Wekelijks overzicht toggle zichtbaar met label**: `find.text('Wekelijks overzicht')` is aanwezig.

5. **Toggle-aan verandert provider state**: Gebruik een `FakeProfileNotifier` die aanroepen registreert. Tap de eerste SwitchListTile. Bevestig dat `setNotifEveningBefore(true)` aangeroepen is. Opmerking: de POST_NOTIFICATIONS permissie-flow wordt gemockt via een FakeNotificationService of wordt overgeslagen als het systeem-platform-kanaal niet beschikbaar is in tests.

Benodigde provider overrides in `ProviderScope`:
- `profileProvider.overrideWith(() => FakeProfileNotifier(testProfile))`
- Voeg ook `weatherProvider`, `slotsProvider`, `locationProvider`, `gpsPermissionProvider`, `lastRefreshedProvider` overrides toe als ProfileScreen die watches (check de imports)

**test/features/home_screen_refresh_test.dart**

Schrijf 3 widget-tests:

1. **Toont 'Bijgewerkt: 14:30' bij bekende timestamp**: Gebruik `FakeLastRefreshedNotifier(DateTime(2026, 6, 3, 14, 30))`. Bevestig `find.text('Bijgewerkt: 14:30')`.

2. **Toont 'Bijgewerkt: —' bij null timestamp**: Gebruik `FakeLastRefreshedNotifier(null)`. Bevestig `find.text('Bijgewerkt: —')`.

3. **Toont 'Bijgewerkt: —' bij loading state**: Gebruik een `FakeLastRefreshedNotifier` die `Completer<DateTime?>().future` retourneert (permanente loading). Bevestig `find.text('Bijgewerkt: —')`.

**Na beide testbestanden: draai de volledige test suite:**
```
flutter test --reporter compact
```
Verwacht: alle tests slagen inclusief de 173 bestaande tests van Phases 1–7.

**Fysiek apparaat verificatie (aantekening):**
Het vuren van de "Avond van tevoren" notificatie wordt NIET geautomatiseerd getest — dit vereist een fysiek apparaat met een echte WorkManager-run. De success criteria in ROADMAP.md benoemen dit expliciet als handmatige verificatie. De executor hoeft dit NIET te simuleren — de unit-tests voor tijdberekeningen zijn voldoende voor de geautomatiseerde gate.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test --reporter compact</automated>
  </verify>
  <done>
- test/features/profile_screen_notif_test.dart: 5 tests slagen
- test/features/home_screen_refresh_test.dart: 3 tests slagen
- Volledige flutter test suite slaagt (inclusief alle bestaande tests)
- Geen regressies in Phases 1–7
  </done>
</task>

</tasks>

<threat_model>
## Vertrouwensgrenzen

| Grens | Beschrijving |
|-------|-------------|
| Test-suite → productie-code | Tests bevestigen contracten maar dekken geen OS-specifiek gedrag (WorkManager OEM-variaties) |

## STRIDE Bedreigingsregister

| Bedreiging-ID | Categorie | Component | Dispositie | Mitigatieplan |
|--------------|-----------|-----------|------------|---------------|
| T-08-05-01 | Denial of Service | FakeNotifier mist provider override | mitigate | Alle benodigde provider overrides in ProviderScope vermeld in taakinstructie; ProfileScreen vereist meerdere overrides (zie check-imports opmerking) |
| T-08-05-02 | Tampering | TZDateTime-tijdberekening in verleden | mitigate | Test 2 van notification_service_test.dart verifieert expliciet dat verleden-scheduling overgeslagen wordt |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
# Draai alleen de nieuwe Phase 8 tests
flutter test test/platform/ test/providers/last_refreshed_provider_test.dart test/features/profile_screen_notif_test.dart test/features/home_screen_refresh_test.dart --reporter compact
# Draai de volledige suite
flutter test --reporter compact
```
</verification>

<success_criteria>
- test/platform/notification_service_test.dart: 4 tests groen
- test/providers/last_refreshed_provider_test.dart: 3 tests groen
- test/features/profile_screen_notif_test.dart: 5 tests groen (inclusief 3×SwitchListTile)
- test/features/home_screen_refresh_test.dart: 3 tests groen
- Volledige flutter test suite slaagt zonder regressies
- Totaal testsaantal: 173 (bestaand) + 15 (Phase 8) = minimaal 188 tests groen

Fysiek apparaat verificatie (buiten geautomatiseerde gate):
- Op een Android-apparaat (Samsung of Xiaomi aanbevolen vanwege OEM WorkManager-variaties): schakel "Avond van tevoren" toggle in, wacht tot 19:00 de vorige dag van een toekomstig slot, bevestig dat de notificatie verschijnt
</success_criteria>

<output>
Maak `.planning/phases/08-background-refresh-notifications/08-05-SUMMARY.md` aan wanneer klaar
</output>
