---
phase: 08-background-refresh-notifications
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - android/app/src/main/AndroidManifest.xml
autonomous: true
requirements:
  - NOTIF-04
  - NOTIF-05
  - NOTIF-06

must_haves:
  truths:
    - "flutter pub get lost geen dependency-fouten op na toevoegen van workmanager, flutter_local_notifications, timezone en flutter_timezone"
    - "AndroidManifest.xml bevat alle permissies en WorkManager service/receiver entries die vereist zijn voor achtergrondtaken en exacte alarmen"
    - "dart analyze rapporteert geen nieuwe fouten na de manifest-wijzigingen"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Vier nieuwe runtime-deps: workmanager, flutter_local_notifications, timezone, flutter_timezone"
      contains: "workmanager"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Alle benodigde Android-permissies en WorkManager-registraties"
      contains: "SCHEDULE_EXACT_ALARM"
  key_links:
    - from: "pubspec.yaml workmanager dep"
      to: "android/app/src/main/AndroidManifest.xml WorkManager service"
      via: "WorkManager plugin-registratie"
      pattern: "workmanager"
    - from: "pubspec.yaml flutter_local_notifications dep"
      to: "android/app/src/main/AndroidManifest.xml notification channel"
      via: "flutter_local_notifications plugin-setup"
      pattern: "flutter_local_notifications"
---

<objective>
Voeg de vier notificatie/achtergrond-runtime-afhankelijkheden toe aan pubspec.yaml en voeg alle benodigde Android-permissies en WorkManager-registraties toe aan AndroidManifest.xml.

Purpose: Fase 8 kan niet starten zonder de correcte pakketten en manifest-entries. Dit is de fundering waarop alle volgende plannen bouwen.
Output: bijgewerkte pubspec.yaml (vier nieuwe deps), bijgewerkt AndroidManifest.xml (permissies + WorkManager service/receiver).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-background-refresh-notifications/08-CONTEXT.md
@.planning/phases/08-background-refresh-notifications/08-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Taak 1: Voeg vier runtime-deps toe aan pubspec.yaml</name>
  <files>pubspec.yaml</files>
  <action>
Voeg de vier Phase 8 runtime-afhankelijkheden toe aan het `dependencies:` blok in pubspec.yaml, onder een `# Phase 8 — Achtergrond + Notificaties` commentaar (per D-08-01, D-08-02, D-08-03):

  workmanager: ^0.9.0+3
  flutter_local_notifications: ^21.0.0
  timezone: ^0.11.0
  flutter_timezone: ^5.1.0

Voeg GEEN dev_dependencies toe in dit plan. Plaatsing: na de Phase 7 entries (`geolocator`, `permission_handler`), vóór het afsluitende commentaar over Phase 2+ deps.

Draai daarna `flutter pub get` om de lock file bij te werken en te bevestigen dat er geen versieconflicten zijn.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && grep -c 'workmanager\|flutter_local_notifications\|timezone\|flutter_timezone' pubspec.yaml</automated>
  </verify>
  <done>pubspec.yaml bevat alle vier de deps, `flutter pub get` slaagt zonder fouten, pubspec.lock bijgewerkt.</done>
</task>

<task type="auto">
  <name>Taak 2: Update AndroidManifest.xml met permissies en WorkManager-entries</name>
  <files>android/app/src/main/AndroidManifest.xml</files>
  <action>
Voeg de volgende wijzigingen toe aan android/app/src/main/AndroidManifest.xml:

**Permissies** (toevoegen na de bestaande ACCESS_FINE_LOCATION en ACCESS_COARSE_LOCATION uses-permission elementen, per D-08-04, NOTIF-04, NOTIF-05):

```xml
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
<uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.WAKE_LOCK" />
```

**WorkManager service** (toevoegen binnen `<application>` element, na de `<activity>` block en vóór de flutterEmbedding meta-data, per D-08-01):

```xml
<!-- WorkManager achtergrondtaak service -->
<service
    android:name="androidx.work.impl.foreground.SystemForegroundService"
    android:foregroundServiceType="dataSync"
    android:exported="false" />

<!-- WorkManager boot-completed receiver voor herscheduling na herstart -->
<receiver
    android:name="be.tramckrijte.workmanager.RescheduleOnBootReceiver"
    android:directBootAware="false"
    android:exported="true"
    android:enabled="true">
    <intent-filter>
        <action android:name="android.intent.action.BOOT_COMPLETED" />
    </intent-filter>
</receiver>
```

**Notificatiekanaal meta-data** (toevoegen in `<application>` element, per D-08-07):

```xml
<!-- flutter_local_notifications: standaard notificatiekanaal -->
<meta-data
    android:name="com.dexterous.flutterlocalnotifications.default_notification_channel_id"
    android:value="ride_alerts" />
```

Verifieer daarna `dart analyze` om te bevestigen dat er geen Dart-analyse-fouten zijn ingevoerd.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && grep -c 'SCHEDULE_EXACT_ALARM\|RECEIVE_BOOT_COMPLETED\|POST_NOTIFICATIONS\|RescheduleOnBootReceiver' android/app/src/main/AndroidManifest.xml</automated>
  </verify>
  <done>AndroidManifest.xml bevat alle vijf permissies en de WorkManager service + receiver entries. `dart analyze` meldt geen nieuwe fouten.</done>
</task>

</tasks>

<threat_model>
## Vertrouwensgrenzen

| Grens | Beschrijving |
|-------|-------------|
| pubspec.yaml → pub.dev | Pakketversies worden opgehaald van pub.dev; versieconflicten kunnen build breken |
| AndroidManifest → OS | Android OS interpreteert permissies; verkeerde entries = falen bij runtime |

## STRIDE Bedreigingsregister

| Bedreiging-ID | Categorie | Component | Dispositie | Mitigatieplan |
|--------------|-----------|-----------|------------|---------------|
| T-08-01-01 | Tampering | pubspec.yaml dep-versies | mitigate | Pinned versies (^0.9.0+3 etc.) beperken onverwachte upgrades; pub.dev publisher-verificatie uitgevoerd in CLAUDE.md |
| T-08-01-02 | Elevation of Privilege | SCHEDULE_EXACT_ALARM permissie | mitigate | Android 12+ vereist expliciet gebruikerstoestemming via systeeminstellingen (NOTIF-05); permissie is gedeclareerd maar niet automatisch verleend |
| T-08-01-SC | Tampering | npm/pip/cargo/pub installs | mitigate | Alle pakketten zijn geverifieerd in CLAUDE.md package-legitimiteitsaudit; workmanager (fluttercommunity.dev), flutter_local_notifications (dexterx.dev) zijn bekende uitgevers |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
# Controleer alle vier deps aanwezig in pubspec.yaml
grep 'workmanager\|flutter_local_notifications\|^  timezone\|flutter_timezone' pubspec.yaml
# Controleer kritische AndroidManifest entries
grep 'SCHEDULE_EXACT_ALARM\|POST_NOTIFICATIONS\|RECEIVE_BOOT_COMPLETED\|RescheduleOnBootReceiver\|ride_alerts' android/app/src/main/AndroidManifest.xml
# Geen nieuwe analyse-fouten
flutter pub get && dart analyze lib/
```
</verification>

<success_criteria>
- pubspec.yaml bevat workmanager ^0.9.0+3, flutter_local_notifications ^21.0.0, timezone ^0.11.0, flutter_timezone ^5.1.0
- `flutter pub get` slaagt zonder versieconflicten
- AndroidManifest.xml bevat SCHEDULE_EXACT_ALARM, POST_NOTIFICATIONS, RECEIVE_BOOT_COMPLETED permissies
- AndroidManifest.xml bevat WorkManager SystemForegroundService en RescheduleOnBootReceiver
- `dart analyze lib/` meldt geen nieuwe fouten
</success_criteria>

<output>
Maak `.planning/phases/08-background-refresh-notifications/08-01-SUMMARY.md` aan wanneer klaar
</output>
