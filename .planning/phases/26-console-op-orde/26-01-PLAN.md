---
phase: 26-console-op-orde
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [docs/testers/changelog.md]
autonomous: true
requirements: [CON-06, PROOF-01]

must_haves:
  truths:
    - "PWA, the published privacy policy, and GitHub main all report version 1.0.31 build 42 and the name Ridewindow"
    - "docs/testers/changelog.md exists and documents builds 41 and 42, including Ingrid's daylight feedback and its backlog link (#68)"
  artifacts:
    - path: "docs/testers/changelog.md"
      provides: "Per-build tester changelog starting at build 41 (PROOF-01)"
      contains: "Build 41"
  key_links:
    - from: "docs/testers/changelog.md"
      to: "git commit history"
      via: "referenced short hashes for each build's content"
      pattern: "3de37e6|36b56f0|9414a9c|a28886f|059e352|7f29940"
---

<objective>
Verify CON-06's "same version everywhere" claim is actually true right now, and create the tester-facing changelog PROOF-01 requires, starting at build 41.

Purpose: CON-06 and PROOF-01 have no Play Console dependency — provable/producible entirely from this machine, in parallel with the manual Console work in 26-02/26-03/26-04.
Output: A verification record confirming CON-06, and `docs/testers/changelog.md` satisfying PROOF-01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-console-op-orde/26-RESEARCH.md
@docs/privacy-policy.html
@pubspec.yaml
@lib/core/app_version.dart
@.planning/TESTERS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify CON-06 — version/name consistency across PWA, privacy policy, GitHub main</name>
  <files>none (read-only verification)</files>
  <read_first>.planning/STATE.md "Stand na 2026-09-10 avond"; docs/privacy-policy.html; pubspec.yaml; lib/core/app_version.dart; 26-RESEARCH.md Pitfall 5</read_first>
  <action>
    Run and record these four read-only checks; do not assume STATE.md's claim without checking.
    1. `curl -s https://my-project-joost.web.app/version.json` — expect `"version":"1.0.31"` and `"build_number":"42"`.
    2. `diff &lt;(curl -s https://joostmouw.github.io/ridewindow/privacy-policy.html) docs/privacy-policy.html` — expect empty output, and confirm the live page contains "Ridewindow".
    3. `git show origin/main:pubspec.yaml | grep '^version:'` — expect `version: 1.0.31+42`; `git show origin/main:lib/core/app_version.dart` — expect `kAppVersionName = '1.0.31'` and `kAppBuildNumber = '42'`. Confirms pushed GitHub main, not just local main.
    4. Per Pitfall 5 this is verification, not build: if all pass, do not redeploy/re-push anything. If any fails, report the mismatch as a finding — do not silently patch it.
  </action>
  <verify>
    <automated>curl -s https://my-project-joost.web.app/version.json | grep -q '"version":"1.0.31"' &amp;&amp; curl -s https://my-project-joost.web.app/version.json | grep -q '"build_number":"42"'</automated>
  </verify>
  <done>All four checks run and recorded; CON-06 confirmed true, or a specific mismatch reported.</done>
  <acceptance_criteria>
    - version.json contains "1.0.31" and "42"
    - diff between live and repo privacy policy is empty; live page contains "Ridewindow"
    - `git show origin/main:pubspec.yaml` shows `version: 1.0.31+42`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create docs/testers/changelog.md (PROOF-01)</name>
  <files>docs/testers/changelog.md</files>
  <read_first>26-RESEARCH.md "Code Examples"; .planning/TESTERS.md; git log for 3de37e6, 36b56f0, 9414a9c, a28886f, 059e352, 7f29940</read_first>
  <action>
    Create `docs/testers/changelog.md` (make `docs/testers/` if needed). Heading `# Testers changelog — Ridewindow`; intro blockquote noting one entry per build from 41 onward, kept live not reconstructed (ref `.planning/TESTERS.md`); one `## Build N — &lt;version&gt;+&lt;build&gt;` section per build with **Datum**, **Track**, **Inhoud** bullets, **Feedback opgelost**.

    Build 41 (1.0.30+41): Datum 2026-09-10, Track "Closed testing — Alpha". Inhoud: daylight now counts in the score, a window an hour after sunset no longer scores perfect (was hardcoded 06:00–22:00), commit `9414a9c`; the four verdict tiers named Toprit/Fijne rit/Te doen/Binnenblijver; Peloton introduction now reaches whoever needs it, commit `36b56f0`. Feedback opgelost: Ingrid (tester, 2026-09-09) asked whether sunset timing is accounted for — daylight scoring added in response, first tester feedback leading to a fix (backlog #68).

    Build 42 (1.0.31+42): Datum 2026-09-10, Track "Internal testing (live) → Closed testing — Alpha (status: fill in from Plan 26-02 Task 3's finding — Approved/In review/Rejected, do not leave generic)". Inhoud: app named Ridewindow everywhere a user sees it, commit `a28886f`; opening intro twice as short — 2.47s instead of 4.94s, commit `059e352`. Feedback opgelost: none — own decision (brand name, faster start).

    Cross-check every commit hash against `git log --oneline --all | grep &lt;hash&gt;` before finalizing.
  </action>
  <verify>
    <automated>test -f docs/testers/changelog.md &amp;&amp; grep -q "Build 41" docs/testers/changelog.md &amp;&amp; grep -q "Build 42" docs/testers/changelog.md &amp;&amp; grep -q "Ingrid" docs/testers/changelog.md &amp;&amp; grep -q "#68" docs/testers/changelog.md</automated>
  </verify>
  <done>File exists with both build entries, Ingrid's feedback linked to #68, verified against git history.</done>
  <acceptance_criteria>
    - `test -f docs/testers/changelog.md` succeeds
    - Contains "Build 41" and "Build 42" headings, "Ingrid", "#68"
    - Every referenced commit hash resolves via `git log --oneline --all | grep`
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local machine to public HTTPS endpoints | Read-only GET to Firebase Hosting and GitHub Pages, no credentials |
| Repo to published changelog | docs/testers/changelog.md becomes public-facing; names a real tester (Ingrid) by first name |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Information Disclosure | docs/testers/changelog.md | accept | First-name-only reference to an already-participating tester, consistent with existing TESTERS.md style |
| T-26-02 | Tampering | curl responses from Firebase/GitHub Pages | accept | HTTPS-only, read-only, no state change based on result beyond human review |
</threat_model>

<verification>
Run Task 1's four checks and Task 2's automated grep in sequence. All must pass. If Task 1 finds a mismatch, report it as a blocking finding for the phase rather than resolving it inside this plan.
</verification>

<success_criteria>
- CON-06 confirmed true via four independent read-only checks
- docs/testers/changelog.md exists, documents build 41 and 42 with verified commit references, links Ingrid's feedback to #68
</success_criteria>

<output>
Create `.planning/phases/26-console-op-orde/26-01-SUMMARY.md` when done
</output>
