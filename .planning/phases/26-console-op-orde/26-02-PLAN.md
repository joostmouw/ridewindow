---
phase: 26-console-op-orde
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [CON-01]

must_haves:
  truths:
    - "Build 42's closed-testing review outcome is confirmed (not assumed) before any promotion happens"
    - "The Oppo has 1.0.31 (42) installed from the Play Store (installerPackageName=com.android.vending), not a sideload"
    - "The short (~2.47s) intro has been observed on a fresh launch of the Play-installed build"
    - "Build 42 is the Active release on Closed testing — Alpha, promoted via Add from library, never a fresh upload"
  artifacts: []
  key_links:
    - from: "Internal testing track (build 42 already live)"
      to: "Closed testing — Alpha track"
      via: "Create release -> Add from library (reuses the exact tested bundle, never a fresh flutter build appbundle)"
      pattern: "Add from library"
---

<objective>
Prove CON-01: every build goes to internal testing first, is tested from a genuine Play-installed build on the Oppo, and only then is promoted to closed testing — never straight to closed, never re-uploaded as a fresh bundle for the promotion step.

Purpose: this is the phase's hard gate per this project's own history (Google Calendar previously shipped broken specifically because it was verified from a sideload, not a Play install — see STATE.md 2026-09-02).
Output: A confirmed review status for build 42, a clean Play-sourced install on the Oppo, and build 42 Active on Closed testing — Alpha.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/HANDOFF.json
@.planning/phases/26-console-op-orde/26-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:decision">
  <name>Task 1: Confirm OK to wipe the Oppo's sideloaded install</name>
  <files>none — this task changes no repo files</files>
  <read_first>.planning/HANDOFF.json "remaining_before_phase_26_planning" (Oppo entry); .planning/STATE.md section "Als Play weigert te installeren: kijk eerst in het kloonprofiel" (the clone-profile ghost-record precedent and exact recovery commands)</read_first>
  <decision>Wipe the Oppo's current sideloaded install of Ridewindow (1.0.31/42) so a genuine Play Store install can be tested, or skip this device verification for now?</decision>
  <context>
    CON-01 requires proving build 42 was tested from an actual Play install (installerPackageName=com.android.vending), not the current sideload. The Oppo has had 1.0.31(42) sideloaded since 22:50 on 2026-09-10 (installed over 31 with data kept), plus a past ghost-record risk in the Android App Clone profile (user 10) from a prior 2026-08 incident. Wiping is low-risk: the signed-in account means profile/availability/planned rides restore from Supabase on next sign-in (proven in Phase 21's MIG-02) — only the forecast cache and other local-only state are lost, and those rebuild automatically.
  </context>
  <options>
    <option id="option-a">
      <name>Wipe and reinstall from Play (recommended)</name>
      <pros>Produces the Play-store proof CON-01 requires; low risk since account data is cloud-backed</pros>
      <cons>Requires reopening the app afterward and confirming the cloud restore looks right</cons>
    </option>
    <option id="option-b">
      <name>Skip for now</name>
      <pros>No device changes today</pros>
      <cons>CON-01 stays unproven; blocks promotion to closed testing until done in a later session</cons>
    </option>
  </options>
  <action>Present this decision to Joost with the context and options above, then wait for his resume-signal before Task 2 proceeds.</action>
  <verify><manual>Joost's choice (option-a or option-b) is explicitly recorded in the session before Task 2 runs</manual></verify>
  <done>Joost has typed option-a or option-b.</done>
  <resume-signal>Type option-a to proceed with the wipe, or option-b to defer</resume-signal>
  <acceptance_criteria>A choice is recorded before Task 2 runs; if option-b, Task 2 and the install/promote half of Task 3 are explicitly reported as deferred, not silently skipped.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wipe the sideloaded install via adb (only if Task 1 = option-a)</name>
  <files>none (device state only, no repo files)</files>
  <read_first>.planning/STATE.md section "Als Play weigert te installeren: kijk eerst in het kloonprofiel" (exact commands that worked for this ghost-record class of problem)</read_first>
  <action>
    Confirm exactly one device with `adb devices` before running anything device-specific. Run `adb shell pm uninstall --user 0 ridewindow.joost.amsterdam` to remove the sideload from the primary profile. Check the Android App Clone profile with `adb shell pm list packages --user 10 | grep ridewindow`; if present, remove it with `adb shell pm uninstall --user 10 ridewindow.joost.amsterdam` (plain `adb uninstall` fails here with `DELETE_FAILED_INTERNAL_ERROR` on a clone-profile ghost record — this is the command that worked in the 2026-08 incident). Confirm with `adb shell pm list packages | grep ridewindow` returning nothing.
  </action>
  <verify>
    <automated>adb shell pm list packages | grep ridewindow | wc -l | grep -qx 0</automated>
  </verify>
  <done>Package ridewindow.joost.amsterdam is absent from both user 0 and user 10 on the Oppo.</done>
  <acceptance_criteria>`adb shell pm list packages` (all users) returns no line containing "ridewindow" after the uninstall sequence.</acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Play-sourced install, intro check, and promotion to closed testing</name>
  <files>none — this task changes no repo files</files>
  <read_first>26-RESEARCH.md Pitfall 1 (Add from library, never a fresh upload) and the Architecture Patterns diagram; .planning/HANDOFF.json live_on_play block</read_first>
  <what-built>Claude cleared the Oppo's sideloaded install (Task 2, if approved) so a genuine Play install can be observed. Claude cannot open the Play Store app or tap Install itself — that step is yours.</what-built>
  <action>Present the how-to-verify steps below to Joost and wait for his report per resume-signal before this plan is considered complete.</action>
  <how-to-verify>
    1. In Play Console: Release -> Testing -> Closed testing -> Alpha -> Releases. Confirm build 42's (1.0.31) review status: Live/Approved, still In review, or Rejected.
    2. On the Oppo, open the internal-testing opt-in link (already opted in) and install Ridewindow from the Play Store. Confirm the source with `adb shell dumpsys package ridewindow.joost.amsterdam | grep installerPackageName` — expect `com.android.vending`.
    3. Force-stop and cold-launch the app. Confirm the short intro (~2.47s) plays, and that your signed-in profile/week/rides restore from the cloud.
    4. If step 1 shows build 42 approved/live on internal but not yet Active on Closed testing -- Alpha, promote it: Closed testing -> Alpha -> Create new release -> **Add from library** -> select the 1.0.31 (42) bundle already live on internal -> Save -> Review release -> Start rollout. Do NOT run a fresh `flutter build appbundle` or upload a new file for this step (Pitfall 1) — reuse the exact tested bundle.
    5. If step 1 shows build 42 was rejected, stop and report the rejection reason instead of promoting.
  </how-to-verify>
  <verify><manual>Joost reports the review status, installerPackageName, intro result, and promotion outcome</manual></verify>
  <done>Joost has reported back all four items per resume-signal.</done>
  <resume-signal>Report the review status from step 1, the installerPackageName from step 2, whether the intro played, and whether promotion (step 4) completed or was blocked by a rejection.</resume-signal>
  <acceptance_criteria>installerPackageName=com.android.vending confirmed on-device; intro observed on a fresh launch; build 42 is the Active release on Closed testing — Alpha (or the rejection reason is explicitly reported instead of a silent skip).</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adb access to Joost's physical Oppo | Local device with real personal data (signed-in account, own ride history) |
| Play Console track promotion | Determines which build every closed-test tester receives |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-03 | Tampering | adb uninstall targeting the wrong device/package | mitigate | `adb devices` single-device confirmation before any uninstall; exact package name (`ridewindow.joost.amsterdam`) used in every command, never a wildcard |
| T-26-04 | Repudiation | Promoting a build that was never actually tested from a Play install | mitigate | Task ordering forces the Play-install+intro check (Task 3, steps 1-3) before promotion (step 4); promotion is explicitly gated on step 1-3 succeeding |
| T-26-05 | Tampering | Promoting via a fresh upload instead of Add from library, producing a build that was never internal-tested | mitigate | Task 3 step 4 explicitly forbids a fresh `flutter build appbundle`/upload; requires Add from library |
</threat_model>

<verification>
Task 2's automated package-list check must show zero matches before Task 3 begins. Task 3's manual verification (installerPackageName, intro, promotion) is the phase's hard gate for CON-01 and cannot be automated — it is reported back by Joost.
</verification>

<success_criteria>
- Build 42's closed-testing review status is known, not assumed
- 1.0.31 (42) is running on the Oppo from a genuine Play Store install
- The short intro is confirmed on a fresh launch
- Build 42 is Active on Closed testing — Alpha, promoted via Add from library
</success_criteria>

<output>
Create `.planning/phases/26-console-op-orde/26-02-SUMMARY.md` when done
</output>
