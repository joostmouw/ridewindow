---
phase: 04-ui-phase-a-onboarding-home-welcome
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/providers/availability_notifier.dart
  - lib/providers/availability_notifier.g.dart
  - lib/domain/services/availability_filter.dart
  - lib/providers/slots_notifier.dart
  - test/providers/availability_notifier_test.dart
autonomous: true
requirements:
  - AVAIL-02

must_haves:
  truths:
    - "AvailabilityNotifier.state is AsyncValue<Map<DateTime, BlockType>> (niet Set<DateTime>)"
    - "BlockType enum met work en custom waarden bestaat in availability_notifier.dart"
    - "toggleCustomHour voegt een BlockType.custom entry toe of verwijdert hem"
    - "seedPreset vervangt de volledige map met een new preset"
    - "AvailabilityFilter.apply accepteert Map<DateTime, BlockType> en blokkeert alle uren ongeacht BlockType"
    - "SlotsNotifier compileert zonder type-fouten na de AvailabilityFilter-aanpassing"
    - "flutter test --reporter=compact slaagt zonder regressies"
  artifacts:
    - path: "lib/providers/availability_notifier.dart"
      provides: "BlockType enum + AvailabilityNotifier met Map<DateTime, BlockType>"
      contains: "enum BlockType"
    - path: "lib/domain/services/availability_filter.dart"
      provides: "AvailabilityFilter.apply met Map<DateTime, BlockType> signatuur"
    - path: "test/providers/availability_notifier_test.dart"
      provides: "4 bijgewerkte tests voor Map<DateTime, BlockType> API"
  key_links:
    - from: "lib/providers/slots_notifier.dart"
      to: "lib/domain/services/availability_filter.dart"
      via: "_filter.apply(allSlots, blockedHours)"
      pattern: "_filter\\.apply"
    - from: "lib/providers/availability_notifier.dart"
      to: "shared_preferences"
      via: "_persist — ISO8601|work of ISO8601|custom serialisatie"
      pattern: "toIso8601String"
---

<objective>
Upgrade AvailabilityNotifier van Set<DateTime> naar Map<DateTime, BlockType> zodat Phase 6 de drie visuele celtoestand (free / work / custom-blocked) correct kan renderen. Pas AvailabilityFilter en SlotsNotifier aan zodat ze de nieuwe signatuur accepteren. Update de 03-03 tests.

Purpose: Breaking change die nu moet plaatsvinden — wachten tot Phase 6 vereist een data-migratie. Door Map<DateTime, BlockType> nu te introduceren is Phase 4 onboarding in staat `BlockType.work` te seeden.

Output: availability_notifier.dart met BlockType enum en nieuwe API; availability_filter.dart met Map-signatuur; bijgewerkte tests; groene flutter test suite.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-phase-a-onboarding-home-welcome/04-CONTEXT.md
@.planning/phases/04-ui-phase-a-onboarding-home-welcome/04-PATTERNS.md
</context>

<interfaces>
<!-- Huidige AvailabilityNotifier (lib/providers/availability_notifier.dart) -->
<!-- State type wordt gewijzigd: Set<DateTime> → Map<DateTime, BlockType> -->

Huidig (te vervangen):
```dart
@riverpod
class AvailabilityNotifier extends _$AvailabilityNotifier {
  static const _key = 'availability.blockedHours';

  @override
  Future<Set<DateTime>> build() async { ... }

  Future<void> toggleHour(DateTime hour) async { ... }
  Future<void> clearAll() async { ... }
  Future<void> _persist(Set<DateTime> hours) async { ... }
}
```

Nieuw (doelopstelling na dit plan):
```dart
enum BlockType { work, custom }

@riverpod
class AvailabilityNotifier extends _$AvailabilityNotifier {
  static const _key = 'availability.blockedHours';

  @override
  Future<Map<DateTime, BlockType>> build() async { ... }
  // Deserialiseert "ISO8601|work" / "ISO8601|custom" strings

  Future<void> toggleCustomHour(DateTime hour) async { ... }
  // Voegt BlockType.custom toe of verwijdert de entry

  Future<void> seedPreset(Map<DateTime, BlockType> preset) async { ... }
  // Vervangt volledige map, persist, emits AsyncData(preset)

  Future<void> clearAll() async { ... }
  // Wist de map: state = const AsyncData(<DateTime, BlockType>{})

  Future<void> _persist(Map<DateTime, BlockType> hours) async { ... }
  // Serialiseert naar "ISO8601|work" / "ISO8601|custom"
}
```

Huidig AvailabilityFilter (lib/domain/services/availability_filter.dart):
```dart
List<RideSlot> apply(List<RideSlot> slots, Set<DateTime> blockedHours)
List<RideSlot> removeBlocked(List<RideSlot> slots, Set<DateTime> blockedHours)
bool _overlapsBlocked(RideSlot slot, Set<DateTime> blockedHours)
```

Nieuw na dit plan:
```dart
// Set<DateTime> → Iterable<DateTime> (keys van de map)
// apply: accepteert Map<DateTime, BlockType> — blokkeert alle entries ongeacht type
List<RideSlot> apply(List<RideSlot> slots, Map<DateTime, BlockType> blockedHours)
List<RideSlot> removeBlocked(List<RideSlot> slots, Map<DateTime, BlockType> blockedHours)
bool _overlapsBlocked(RideSlot slot, Map<DateTime, BlockType> blockedHours)
// Intern: gebruik blockedHours.containsKey(current) i.p.v. blockedHours.contains(current)
```

SlotsNotifier lijn 108 (lib/providers/slots_notifier.dart):
```dart
// Was: Set<DateTime> blockedHours = availValue.requireValue;
// Wordt: Map<DateTime, BlockType> blockedHours = availValue.requireValue;
// De aanroep _filter.apply(allSlots, blockedHours) blijft ongewijzigd
// Maar _determineReason(allSlots, blockedHours) signatuur wijzigt mee
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BlockType enum + AvailabilityNotifier upgrade naar Map<DateTime, BlockType></name>
  <files>lib/providers/availability_notifier.dart, lib/providers/availability_notifier.g.dart, test/providers/availability_notifier_test.dart</files>
  <behavior>
    - Test 1: cold start levert lege Map<DateTime, BlockType> op (niet Set)
    - Test 2: toggleCustomHour voegt BlockType.custom entry toe
    - Test 3: toggleCustomHour tweemaal verwijdert de entry (vrij maken)
    - Test 4: seedPreset vervangt de map — na seedPreset met {dt: BlockType.work} is dt aanwezig met type work
    - Test 5: clearAll wist de volledige map
    - Test 6: persistentie — na toggle + dispose + nieuw container is de entry nog aanwezig met het juiste BlockType
  </behavior>
  <action>
Stap 1 — Update test/providers/availability_notifier_test.dart:
- Vervang alle `Set<DateTime>`-typeaanduidingen door `Map<DateTime, BlockType>`
- Test 1 ("cold start"): verwacht `isEmpty` — blijft werken, lege map is ook isEmpty
- Test 2 ("toggle adds"): roep `toggleCustomHour(dt)` aan (was `toggleHour`); verwacht `blocked.containsKey(dt)` en `blocked[dt] == BlockType.custom`
- Test 3 ("toggle removes"): roep `toggleCustomHour(dt)` tweemaal aan; verwacht `!blocked.containsKey(dt)`
- Voeg Test 4 toe ("seedPreset"): maak preset `{dt: BlockType.work}`; roep `seedPreset(preset)`; controleer `blocked[dt] == BlockType.work`
- Voeg Test 5 toe ("clearAll"): seed dan clearAll; verwacht `blocked.isEmpty`
- Test 6 ("persists"): gebruik `toggleCustomHour`, dispose, nieuw container; verwacht `blocked.containsKey(dt)` en `blocked[dt] == BlockType.custom`

Stap 2 — Voer `flutter test test/providers/availability_notifier_test.dart` uit — MOET ROOD zijn (compile-error of type-fout).

Stap 3 — Update lib/providers/availability_notifier.dart:
a. Voeg `enum BlockType { work, custom }` toe BOVEN de class, BUITEN elke andere declaration.
b. Wijzig class header: `_$AvailabilityNotifier` blijft gelijk (code-gen repareer later).
c. Wijzig `Future<Set<DateTime>> build()` naar `Future<Map<DateTime, BlockType>> build()`.
   Deserialisatie: split elke string op `'|'`; `DateTime.parse(parts[0])` en `BlockType.values.byName(parts[1])`.
d. Hernoem `toggleHour` naar `toggleCustomHour`. Interne logica: als `current.containsKey(hour)` en `current[hour] == BlockType.custom`, verwijder hem; anders zet `next[hour] = BlockType.custom`.
e. Voeg `seedPreset(Map<DateTime, BlockType> preset)` toe: `await _persist(preset); state = AsyncData(preset)`.
f. Wijzig `clearAll()`: `await _persist(const {}); state = const AsyncData(<DateTime, BlockType>{})`.
g. Wijzig `_persist` signatuur naar `Map<DateTime, BlockType> hours` en serialiseer als `'${e.key.toIso8601String()}|${e.value.name}'`.

Stap 4 — Voer `dart run build_runner build --delete-conflicting-outputs` uit zodat availability_notifier.g.dart opnieuw gegenereerd wordt.

Stap 5 — Voer `flutter test test/providers/availability_notifier_test.dart` uit — MOET GROEN zijn.

Stap 6 — Commit: `feat(04-01): upgrade AvailabilityNotifier to Map<DateTime, BlockType>`
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/providers/availability_notifier_test.dart --reporter=compact</automated>
  </verify>
  <done>Alle 6 AvailabilityNotifier-tests slagen; availability_notifier.dart bevat `enum BlockType`; state type is `Map<DateTime, BlockType>`</done>
</task>

<task type="auto">
  <name>Task 2: AvailabilityFilter + SlotsNotifier aanpassen aan Map<DateTime, BlockType></name>
  <files>lib/domain/services/availability_filter.dart, lib/providers/slots_notifier.dart</files>
  <action>
Stap 1 — Update lib/domain/services/availability_filter.dart:
a. Wijzig signatuur `removeBlocked(List<RideSlot> slots, Set<DateTime> blockedHours)` naar `removeBlocked(List<RideSlot> slots, Map<DateTime, BlockType> blockedHours)`.
b. Wijzig signatuur `apply(List<RideSlot> slots, Set<DateTime> blockedHours)` naar `apply(List<RideSlot> slots, Map<DateTime, BlockType> blockedHours)`.
c. Wijzig `_overlapsBlocked` parameter en interne check: `blockedHours.contains(current)` → `blockedHours.containsKey(current)`.
d. Voeg import toe voor BlockType: `import 'package:ridewindow/providers/availability_notifier.dart';` — ja, domain→providers richting; dit is per PATTERNS.md acceptabel en wordt in Phase 6 gerefactored.

Stap 2 — Update lib/providers/slots_notifier.dart:
a. Wijzig `final blockedHours = availValue.requireValue;` — geen expliciete type-annotatie nodig, type-inferentie werkt.
b. Wijzig `_determineReason` signatuur: `SlotsEmptyReason? _determineReason(List<RideSlot> allSlots, Map<DateTime, BlockType> blockedHours)`.
c. Controleer of `_filter.apply(allSlots, blockedHours)` en `_filter.removeHiddenPoor(allSlots)` nog correct zijn — die hoeven niet te wijzigen.

Stap 3 — Voer `dart analyze lib/` uit — verwacht: No issues found.

Stap 4 — Voer `flutter test --reporter=compact` uit — VOLLEDIGE suite moet slagen (inclusief de 03-03 en 03-04 integratietests).

Stap 5 — Commit: `fix(04-01): update AvailabilityFilter + SlotsNotifier for Map<DateTime, BlockType>`
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart analyze lib/ && flutter test --reporter=compact</automated>
  </verify>
  <done>dart analyze lib/ meldt No issues found; alle bestaande tests + nieuwe AvailabilityNotifier-tests slagen; geen regressies in slots_notifier_test of integration_test</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschrijving |
|----------|-------------|
| SharedPreferences → AvailabilityNotifier | Serialized strings worden geparsed via `split('|')` en `BlockType.values.byName` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | SharedPreferences deserialisatie (`BlockType.values.byName`) | mitigate | `byName` gooit `ArgumentError` bij onbekende naam — wrap `build()` deserialisatie in try-catch; sla corrupte entries over (log en skip) |
| T-04-02 | Information Disclosure | BlockType in SharedPreferences | accept | Beschikbaarheidspatroon is geen gevoelige PII; opslag client-side |
| T-04-SC | Tampering | Geen nieuwe npm/pip/cargo installs in dit plan | accept | Geen nieuwe package-installaties; bestaande deps ongewijzigd |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
dart analyze lib/
flutter test --reporter=compact
grep -c "enum BlockType" lib/providers/availability_notifier.dart
grep -c "Map<DateTime, BlockType>" lib/providers/availability_notifier.dart
grep -c "Map<DateTime, BlockType>" lib/domain/services/availability_filter.dart
```

Verwachte output:
- dart analyze: No issues found
- flutter test: all tests passed
- grep BlockType enum: 1
- grep Map<DateTime, BlockType> in notifier: >= 3 (build, persist, seedPreset)
- grep Map<DateTime, BlockType> in filter: >= 2 (removeBlocked, apply)
</verification>

<success_criteria>
1. `enum BlockType { work, custom }` bestaat in lib/providers/availability_notifier.dart
2. `AvailabilityNotifier.build()` retourneert `Future<Map<DateTime, BlockType>>`
3. `toggleCustomHour`, `seedPreset` en `clearAll` zijn aanwezig met correcte signaturen
4. `AvailabilityFilter.apply` accepteert `Map<DateTime, BlockType>`
5. `dart analyze lib/` meldt No issues found
6. `flutter test --reporter=compact` slaagt zonder regressies (minimaal 104 tests, was 98 + 6 nieuwe)
</success_criteria>

<output>
Maak `.planning/phases/04-ui-phase-a-onboarding-home-welcome/04-01-SUMMARY.md` aan na voltooiing.
</output>
