---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/data/database/tables/forecast_cache_entries.dart
  - lib/data/database/tables/hourly_forecast_entries.dart
  - lib/data/database/tables/availability_grid_entries.dart
  - lib/data/database/app_database.dart
  - lib/data/database/app_database.g.dart
autonomous: true
requirements:
  - PERS-02
  - PERS-03

must_haves:
  truths:
    - "Drift tables compile with nullable real columns for all six weather fields"
    - "AppDatabase uses schemaVersion=1 with MigrationStrategy scaffolded"
    - "build_runner generates app_database.g.dart without errors"
    - "Availability grid table is scaffolded in the schema (no rows written yet)"
    - "AppDatabase accepts an optional QueryExecutor so tests can inject NativeDatabase.memory()"
  artifacts:
    - path: "lib/data/database/tables/forecast_cache_entries.dart"
      provides: "ForecastCacheEntries Drift table"
      contains: "class ForecastCacheEntries extends Table"
    - path: "lib/data/database/tables/hourly_forecast_entries.dart"
      provides: "HourlyForecastEntries Drift table with nullable real columns"
      contains: "real().nullable()"
    - path: "lib/data/database/tables/availability_grid_entries.dart"
      provides: "AvailabilityGridEntries Drift table scaffold"
      contains: "class AvailabilityGridEntries extends Table"
    - path: "lib/data/database/app_database.dart"
      provides: "AppDatabase DriftDatabase class"
      contains: "schemaVersion => 1"
    - path: "lib/data/database/app_database.g.dart"
      provides: "Generated Drift database code"
  key_links:
    - from: "lib/data/database/app_database.dart"
      to: "lib/data/database/tables/forecast_cache_entries.dart"
      via: "@DriftDatabase(tables: [...])"
      pattern: "ForecastCacheEntries"
    - from: "lib/data/database/tables/hourly_forecast_entries.dart"
      to: "lib/data/database/tables/forecast_cache_entries.dart"
      via: "foreign key reference"
      pattern: "references(ForecastCacheEntries"
---

<objective>
Add Drift to the project and define the v1 database schema: two forecast tables and an availability grid scaffold. Establish the migration baseline (schemaVersion=1) before any data is written. AppDatabase accepts an optional QueryExecutor so test code can inject NativeDatabase.memory() without triggering Flutter-specific executors.

Purpose: All subsequent plans depend on the schema being code-genned and the AppDatabase constructor being injectable.
Output: pubspec.yaml updated, three Drift table files, AppDatabase class, generated app_database.g.dart committed.
</objective>

<execution_context>
@/Users/joostmouw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostmouw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joostmouw/ridewindow/.planning/ROADMAP.md
@/Users/joostmouw/ridewindow/.planning/REQUIREMENTS.md
@/Users/joostmouw/ridewindow/.planning/phases/02-data-layer-drift-open-meteo/02-RESEARCH.md
@/Users/joostmouw/ridewindow/pubspec.yaml
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Drift and mockito to pubspec.yaml</name>
  <files>pubspec.yaml</files>
  <read_first>
    - pubspec.yaml — read current dependencies before editing; must not remove existing Phase 1 entries
    - .planning/phases/02-data-layer-drift-open-meteo/02-RESEARCH.md — Standard Stack section for exact versions
  </read_first>
  <action>
    Add to pubspec.yaml dependencies block:
      drift: ^2.33.0
      drift_flutter: ^0.3.0

    Add to pubspec.yaml dev_dependencies block:
      drift_dev: ^2.33.0
      mockito: ^5.7.0

    Do NOT add sqlite3, sqlite3_flutter_libs, or path_provider separately — drift_flutter brings them transitively.
    After editing, run: flutter pub get
    Verify resolution succeeds with no discontinued markers.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter pub get 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains "drift: ^2.33.0" under dependencies
    - pubspec.yaml contains "drift_flutter: ^0.3.0" under dependencies
    - pubspec.yaml contains "drift_dev: ^2.33.0" under dev_dependencies
    - pubspec.yaml contains "mockito: ^5.7.0" under dev_dependencies
    - "flutter pub get" exits 0 with "Got dependencies!" or equivalent success message
    - pubspec.lock resolves drift to >=2.33.0, drift_flutter to >=0.3.0
  </acceptance_criteria>
  <done>flutter pub get succeeds with all four new packages resolved in pubspec.lock</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Define Drift tables and AppDatabase</name>
  <files>
    lib/data/database/tables/forecast_cache_entries.dart,
    lib/data/database/tables/hourly_forecast_entries.dart,
    lib/data/database/tables/availability_grid_entries.dart,
    lib/data/database/app_database.dart
  </files>
  <read_first>
    - .planning/phases/02-data-layer-drift-open-meteo/02-RESEARCH.md — Pattern 1 (Drift table with nullable real columns), Pattern 4 (AppDatabase migration scaffolding)
    - lib/domain/models/hourly_forecast.dart — field names (temperatureC, apparentTemperatureC, precipitationMm, precipitationProbability, windspeedKmh, winddirectionDeg) to align column naming
  </read_first>
  <action>
    Create lib/data/database/tables/forecast_cache_entries.dart:
      Import drift/drift.dart.
      Define class ForecastCacheEntries extends Table with columns:
        IntColumn get id => integer().autoIncrement()();
        RealColumn get lat => real()();
        RealColumn get lon => real()();
        DateTimeColumn get fetchedAt => dateTime()();
      Add part directive: part 'forecast_cache_entries.g.dart'; — Drift requires this for code-gen.

    Create lib/data/database/tables/hourly_forecast_entries.dart:
      Import drift/drift.dart.
      Add part directive: part 'hourly_forecast_entries.g.dart';
      Define class HourlyForecastEntries extends Table with columns:
        IntColumn get id => integer().autoIncrement()();
        IntColumn get cacheId => integer().references(ForecastCacheEntries, #id)();
        IntColumn get time => integer()();   // unixtime; store as int, convert to DateTime in domain layer
        RealColumn get temperatureC => real().nullable()();
        RealColumn get apparentTemperatureC => real().nullable()();
        RealColumn get precipitationMm => real().nullable()();
        RealColumn get precipitationProbability => real().nullable()();
        RealColumn get windspeedKmh => real().nullable()();
        RealColumn get winddirectionDeg => real().nullable()();

    Create lib/data/database/tables/availability_grid_entries.dart:
      Import drift/drift.dart.
      Add part directive: part 'availability_grid_entries.g.dart';
      Define class AvailabilityGridEntries extends Table with columns:
        IntColumn get id => integer().autoIncrement()();
        IntColumn get dayOfWeek => integer()();   // 1=Monday … 7=Sunday
        IntColumn get hour => integer()();        // 0–23
        TextColumn get state => text()();         // 'free' | 'blocked' | 'work'
      Comment: "// Phase 6 writes rows; schema scaffolded here to complete PERS-03 v1 baseline"

    Create lib/data/database/app_database.dart:
      Import drift/drift.dart and drift_flutter/drift_flutter.dart and path_provider/path_provider.dart.
      Add part directive: part 'app_database.g.dart';
      Import the three table files via relative imports.
      Annotate: @DriftDatabase(tables: [ForecastCacheEntries, HourlyForecastEntries, AvailabilityGridEntries])
      Define class AppDatabase extends _$AppDatabase:
        Constructor: AppDatabase([QueryExecutor? executor]) : super(executor ?? _openConnection());
        Override int get schemaVersion => 1;
        Add block comment: "// MIGRATION RULE: columns are append-only. Never remove or rename a column; always add new columns in a new schemaVersion block."
        Override MigrationStrategy get migration => MigrationStrategy(
          onCreate: (m) => m.createAll(),
          onUpgrade: (m, from, to) async {
            // v1 → future: add columns here
          },
        );
        Static method _openConnection() returns driftDatabase(name: 'ridewindow', native: DriftNativeOptions(databaseDirectory: getApplicationSupportDirectory));
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - lib/data/database/app_database.dart contains "@DriftDatabase(tables: [ForecastCacheEntries, HourlyForecastEntries, AvailabilityGridEntries])"
    - lib/data/database/app_database.dart contains "int get schemaVersion => 1"
    - lib/data/database/app_database.dart contains "AppDatabase([QueryExecutor? executor])"
    - lib/data/database/tables/hourly_forecast_entries.dart contains "real().nullable()" at least 6 times (one per weather field)
    - lib/data/database/tables/availability_grid_entries.dart contains "class AvailabilityGridEntries extends Table"
    - build_runner exits 0 with no errors (warnings about part files OK)
    - lib/data/database/app_database.g.dart exists and is non-empty
    - dart analyze lib/data/database/ exits 0 with no issues
  </acceptance_criteria>
  <done>All four table/database files exist, build_runner generates app_database.g.dart without errors, dart analyze is clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pubspec.yaml → pub.dev | Package resolution fetches from pub.dev; resolved against CLAUDE.md verified-publisher table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | pubspec.yaml dependency resolution | mitigate | All four packages are in CLAUDE.md verified-publisher table (drift = simonbinder.eu, mockito = dart.dev); versions pinned with ^ constraint |
| T-02-SC | Tampering | drift/drift_flutter/drift_dev/mockito installs | accept | Packages already vetted in RESEARCH.md Package Legitimacy Audit; CLAUDE.md table confirms publisher identity |
</threat_model>

<verification>
After both tasks complete:

1. Run dart analyze lib/data/ — must exit 0 with no issues
2. Run dart run build_runner build --delete-conflicting-outputs — must exit 0
3. Confirm lib/data/database/app_database.g.dart exists and contains class _$AppDatabase
4. Confirm HourlyForecastEntries has 6 nullable real columns (grep -c "real().nullable()" lib/data/database/tables/hourly_forecast_entries.dart returns 6)
5. Confirm AppDatabase constructor accepts optional QueryExecutor (grep "QueryExecutor? executor" lib/data/database/app_database.dart)
</verification>

<success_criteria>
- flutter pub get resolves drift 2.33.0, drift_flutter 0.3.0, drift_dev 2.33.0, mockito 5.7.0
- build_runner generates app_database.g.dart without errors
- AppDatabase([QueryExecutor? executor]) constructor is injectable for test isolation
- schemaVersion=1, MigrationStrategy with onCreate + onUpgrade scaffolded with append-only comment
- AvailabilityGridEntries table exists in schema (PERS-02 grid scaffold, PERS-03 versioned baseline)
- dart analyze lib/data/ is clean
</success_criteria>

<output>
Create .planning/phases/02-data-layer-drift-open-meteo/02-01-SUMMARY.md when done
</output>
