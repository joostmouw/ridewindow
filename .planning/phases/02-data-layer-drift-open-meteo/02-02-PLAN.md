---
phase: "02"
plan: "02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - lib/data/database/daos/forecast_dao.dart
  - lib/data/database/app_database.dart
  - lib/data/remote/open_meteo_client.dart
  - test/data/remote/open_meteo_client_test.dart
  - test/data/remote/open_meteo_client_test.mocks.dart
autonomous: true
requirements:
  - FORE-01
  - FORE-02
  - FORE-03
  - FORE-05

must_haves:
  truths:
    - "OpenMeteoClient.fetch() returns a typed List<HourlyForecast> for given lat/lon"
    - "timezone=auto and timeformat=unixtime are baked into the base URL, not passed at call sites"
    - "All six weather fields are requested in the hourly= query parameter"
    - "A null field in the API response is preserved as null in HourlyForecast, not coerced to 0 or defaulted"
    - "OpenMeteoClient zips the parallel arrays returned by Open-Meteo (not per-hour objects)"
    - "ForecastDao exposes latestCache(), hourlyForecasts(), and replaceAll() methods"
  artifacts:
    - path: "lib/data/remote/open_meteo_client.dart"
      provides: "HTTP client for Open-Meteo with baked-in params"
      contains: "timezone=auto"
    - path: "lib/data/database/daos/forecast_dao.dart"
      provides: "Type-safe Drift DAO for forecast tables"
      exports: ["latestCache", "hourlyForecasts", "replaceAll"]
    - path: "test/data/remote/open_meteo_client_test.dart"
      provides: "Unit tests for OpenMeteoClient"
      contains: "isNull"
  key_links:
    - from: "lib/data/remote/open_meteo_client.dart"
      to: "lib/domain/models/hourly_forecast.dart"
      via: "manual array-zip in _parseResponse"
      pattern: "HourlyForecast("
    - from: "lib/data/database/daos/forecast_dao.dart"
      to: "lib/data/database/app_database.dart"
      via: "@DriftAccessor annotation"
      pattern: "@DriftAccessor"
---

<objective>
Build the OpenMeteoClient (HTTP fetch, array-zip parsing, null preservation) and ForecastDao (type-safe Drift queries), with unit tests confirming the null-preservation contract (FORE-05) and correct URL construction (FORE-02).

Purpose: Delivers the network → domain model slice. WeatherRepository in Plan 02-03 depends on both OpenMeteoClient and ForecastDao being available.
Output: lib/data/remote/open_meteo_client.dart, lib/data/database/daos/forecast_dao.dart, passing unit tests.
</objective>

<execution_context>
@/Users/joostmouw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostmouw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joostmouw/ridewindow/.planning/ROADMAP.md
@/Users/joostmouw/ridewindow/.planning/REQUIREMENTS.md
@/Users/joostmouw/ridewindow/.planning/phases/02-data-layer-drift-open-meteo/02-RESEARCH.md
@/Users/joostmouw/ridewindow/lib/domain/models/hourly_forecast.dart

<interfaces>
From lib/domain/models/hourly_forecast.dart:
  @freezed abstract class HourlyForecast with _$HourlyForecast
  Fields (all double? nullable):
    temperatureC      (@JsonKey(name: 'temperature_2m'))
    apparentTemperatureC  (@JsonKey(name: 'apparent_temperature'))
    precipitationMm   (@JsonKey(name: 'precipitation'))
    precipitationProbability (@JsonKey(name: 'precipitation_probability'))
    windspeedKmh      (@JsonKey(name: 'windspeed_10m'))
    winddirectionDeg  (@JsonKey(name: 'winddirection_10m'))
    time              (DateTime, required)
  factory HourlyForecast.fromJson(Map<String, dynamic> json)

Note: fromJson() is designed for a flat per-hour map. Open-Meteo returns parallel arrays.
OpenMeteoClient MUST zip arrays manually — do NOT call fromJson() on the API response directly.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create OpenMeteoClient with array-zip parsing and null preservation</name>
  <files>
    lib/data/remote/open_meteo_client.dart,
    test/data/remote/open_meteo_client_test.dart,
    test/data/remote/open_meteo_client_test.mocks.dart
  </files>
  <read_first>
    - lib/domain/models/hourly_forecast.dart — field names and types (all double? nullable, time as DateTime)
    - .planning/phases/02-data-layer-drift-open-meteo/02-RESEARCH.md — Pattern 2 (OpenMeteoClient with baked-in params), Pattern 5 (unit test with @GenerateMocks), Pitfall 1 (arrays not per-hour objects), Pitfall 2 (null coercion)
  </read_first>
  <behavior>
    Test 1 — fetch() returns HourlyForecast with all six fields when all present:
      Stub MockHttpClient.get(any) to return Response(validJson, 200) where validJson has arrays of length 1 with all six fields non-null.
      Expect: result.length == 1, result.first.windspeedKmh == 10.1

    Test 2 — fetch() preserves null when a field array contains null at index i:
      Stub MockHttpClient.get(any) to return Response(jsonWithNullWindspeed, 200) where windspeed_10m is [null].
      Expect: result.first.windspeedKmh == null   (not 0.0, not throws)

    Test 3 — fetch() preserves null when a field array is entirely absent from response:
      Stub MockHttpClient.get(any) to return Response(jsonMissingWinddirection, 200) where winddirection_10m key is absent.
      Expect: result.first.winddirectionDeg == null

    Test 4 — fetch() throws when HTTP status != 200:
      Stub MockHttpClient.get(any) to return Response('', 503).
      Expect: throws Exception with message containing '503'

    Test 5 — URL contains timezone=auto and timeformat=unixtime:
      Capture the Uri passed to MockHttpClient.get(). Use verify(mockHttp.get(argThat(predicate((Uri u) => ...)))).called(1).
      Expect: uri.queryParameters['timezone'] == 'auto' and uri.queryParameters['timeformat'] == 'unixtime'

    Test 6 — URL contains all six hourly field names:
      Capture Uri from MockHttpClient.get().
      Expect: uri.queryParameters['hourly'] contains 'temperature_2m', 'apparent_temperature', 'precipitation', 'precipitation_probability', 'windspeed_10m', 'winddirection_10m'
  </behavior>
  <action>
    Write tests first (RED), then implement to pass (GREEN).

    Test file structure (test/data/remote/open_meteo_client_test.dart):
      Import http/http.dart as http, package:mockito/annotations.dart, package:mockito/mockito.dart, open_meteo_client.dart, hourly_forecast.dart.
      Annotate: @GenerateMocks([], customMocks: [MockSpec<http.Client>(as: #MockHttpClient)])
      Define validJson, jsonWithNullWindspeed, jsonMissingWinddirection as const String constants in the test file body (outside main). These are single-hour arrays; see RESEARCH.md Code Examples for the Open-Meteo response structure.
      Run dart run build_runner build --delete-conflicting-outputs to generate open_meteo_client_test.mocks.dart.

    Implementation (lib/data/remote/open_meteo_client.dart):
      Import dart:convert, package:http/http.dart as http, lib/domain/models/hourly_forecast.dart.
      Class OpenMeteoClient with:
        static const String _baseUrl = 'https://api.open-meteo.com/v1/forecast'
        static const List<String> _hourlyFields = ['temperature_2m', 'apparent_temperature', 'precipitation', 'precipitation_probability', 'windspeed_10m', 'winddirection_10m']
        final http.Client _client field
        Constructor: OpenMeteoClient({http.Client? client}) : _client = client ?? http.Client()
        Future<List<HourlyForecast>> fetch(double lat, double lon) builds Uri with queryParameters map including timezone: 'auto', timeformat: 'unixtime', hourly: _hourlyFields.join(','), latitude: lat.toString(), longitude: lon.toString()
        Throws Exception('Open-Meteo error: ${response.statusCode}') for status != 200
        Private _parseResponse(String body) method:
          jsonDecode body to Map<String, dynamic>
          Extract hourly sub-map
          Extract times as List<int> from hourly['time']
          Define local helper double? _get(String key, int i): returns null if hourly[key] == null OR (hourly[key] as List)[i] == null; otherwise (((hourly[key] as List)[i]) as num).toDouble()
          Return List.generate(times.length, (i) => HourlyForecast(time: DateTime.fromMillisecondsSinceEpoch(times[i] * 1000), temperatureC: _get('temperature_2m', i), apparentTemperatureC: _get('apparent_temperature', i), precipitationMm: _get('precipitation', i), precipitationProbability: _get('precipitation_probability', i), windspeedKmh: _get('windspeed_10m', i), winddirectionDeg: _get('winddirection_10m', i)))

    After implementation: run dart test test/data/remote/open_meteo_client_test.dart — all 6 tests must pass.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart test test/data/remote/open_meteo_client_test.dart --reporter=compact 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - dart test test/data/remote/open_meteo_client_test.dart exits 0 with all 6 tests passing
    - lib/data/remote/open_meteo_client.dart contains "timezone=auto" and "timeformat=unixtime" within the _baseUrl or queryParameters — not in fetch() call parameters
    - lib/data/remote/open_meteo_client.dart contains the _get helper that returns null when list is null or element is null (grep for "return null" in _get body, at least 2 occurrences)
    - No occurrence of "(val ?? 0" or "?? 0.0" in lib/data/remote/open_meteo_client.dart
    - dart analyze lib/data/remote/ exits 0 with no issues
  </acceptance_criteria>
  <done>All 6 OpenMeteoClient tests pass; null fields are preserved as null through parsing</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create ForecastDao and wire into AppDatabase</name>
  <files>
    lib/data/database/daos/forecast_dao.dart,
    lib/data/database/app_database.dart
  </files>
  <read_first>
    - lib/data/database/app_database.dart — current state after Plan 02-01; must add forecastDao getter and include ForecastDao in @DriftDatabase annotation
    - lib/data/database/tables/forecast_cache_entries.dart — column names and types for query construction
    - lib/data/database/tables/hourly_forecast_entries.dart — column names and types for query construction
    - lib/domain/models/hourly_forecast.dart — field names for mapping Drift row → domain model
    - .planning/phases/02-data-layer-drift-open-meteo/02-RESEARCH.md — Pattern 3 (WeatherRepository) for the DAO methods it calls: latestCache(), hourlyForecasts(), replaceAll()
  </read_first>
  <action>
    Create lib/data/database/daos/forecast_dao.dart:
      Import drift/drift.dart, the two forecast table files, and HourlyForecast domain model.
      Add part directive: part 'forecast_dao.g.dart';
      Annotate: @DriftAccessor(tables: [ForecastCacheEntries, HourlyForecastEntries])
      Define class ForecastDao extends DatabaseAccessor<AppDatabase> with _$ForecastDaoMixin:
        Constructor: ForecastDao(super.db);
        Future<ForecastCacheEntry?> latestCache({required double lat, required double lon}):
          Queries ForecastCacheEntries ordered by fetchedAt DESC, limit 1, filtered by lat and lon.
        Future<List<HourlyForecast>> hourlyForecasts({required int cacheId}):
          Queries HourlyForecastEntries filtered by cacheId, ordered by time ASC.
          Maps each row to HourlyForecast: time = DateTime.fromMillisecondsSinceEpoch(row.time * 1000), all weather fields passed through directly (already double? nullable).
        Future<void> replaceAll({required double lat, required double lon, required List<HourlyForecast> forecasts}):
          Runs in a transaction:
            Delete HourlyForecastEntries rows for any existing cache matching lat/lon.
            Delete ForecastCacheEntries rows for lat/lon.
            Insert new ForecastCacheEntries row with lat, lon, fetchedAt: DateTime.now(); capture the inserted id.
            Insert HourlyForecastEntries rows for each forecast using the new cacheId; time stored as forecast.time.millisecondsSinceEpoch ~/ 1000 (back to unixtime int).

    Update lib/data/database/app_database.dart:
      Add ForecastDao to the daos list in @DriftDatabase(tables: [...], daos: [ForecastDao]).
      Add getter: ForecastDao get forecastDao => ForecastDao(this);
      Add import for forecast_dao.dart.

    Run: dart run build_runner build --delete-conflicting-outputs
    Then: dart analyze lib/data/database/ — must exit 0.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5 && dart analyze lib/data/database/ 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - lib/data/database/daos/forecast_dao.dart contains "latestCache", "hourlyForecasts", "replaceAll" method definitions
    - lib/data/database/daos/forecast_dao.dart contains "transaction(" (uses Drift transaction for replaceAll)
    - lib/data/database/app_database.dart contains "daos: [ForecastDao]" in the @DriftDatabase annotation
    - lib/data/database/app_database.dart contains "ForecastDao get forecastDao"
    - build_runner exits 0 after update
    - dart analyze lib/data/database/ exits 0 with no issues
    - lib/data/database/daos/forecast_dao.g.dart exists (generated)
  </acceptance_criteria>
  <done>ForecastDao is wired into AppDatabase, build_runner regenerates cleanly, dart analyze is clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OpenMeteoClient → Open-Meteo API | External HTTP GET; response body parsed as JSON |
| JSON response → HourlyForecast | Untrusted numeric values coerced via (num).toDouble() |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Information Disclosure | open_meteo_client.dart — URL contains lat/lon | accept | Amsterdam hardcoded for dev; no user PII; Phase 7 adds real GPS with explicit permission |
| T-02-03 | Denial of Service | OpenMeteoClient — no timeout on http.Client.get() | accept | Open-Meteo is free public API; Dart's default http.Client has no timeout; WorkManager retry handles failures; timeout added in Phase 8 if needed |
| T-02-04 | Tampering | JSON parsing — cast to (num).toDouble() | mitigate | _get() helper null-checks before cast; throws cast error for non-numeric type which surfaces as Exception rather than silent corruption |
</threat_model>

<verification>
After both tasks complete:

1. dart test test/data/remote/open_meteo_client_test.dart — all 6 tests pass
2. dart analyze lib/data/ — exits 0
3. dart run build_runner build --delete-conflicting-outputs — exits 0
4. grep "timezone=auto" lib/data/remote/open_meteo_client.dart — finds the constant (not in fetch() call)
5. grep -c "real().nullable()" lib/data/database/tables/hourly_forecast_entries.dart — returns 6
</verification>

<success_criteria>
- OpenMeteoClient.fetch() returns List<HourlyForecast> with null preserved for missing/null fields (FORE-05 verified by test)
- timezone=auto and timeformat=unixtime are baked in at client construction (FORE-02)
- All six field names requested in hourly= parameter (FORE-03)
- ForecastDao exposes latestCache, hourlyForecasts, replaceAll ready for WeatherRepository
- All unit tests pass; dart analyze is clean
</success_criteria>

<output>
Create .planning/phases/02-data-layer-drift-open-meteo/02-02-SUMMARY.md when done
</output>
