---
phase: "02"
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - lib/data/repositories/weather_repository.dart
  - test/data/repositories/weather_repository_test.dart
  - test/data/repositories/weather_repository_test.mocks.dart
autonomous: true
requirements:
  - FORE-01
  - FORE-04
  - FORE-05

must_haves:
  truths:
    - "WeatherRepository.getForecast() returns cached data without HTTP call when cache is < 1 hour old"
    - "WeatherRepository.getForecast() calls OpenMeteoClient.fetch() and persists new data when cache is > 1 hour old"
    - "WeatherRepository.getForecast() calls OpenMeteoClient.fetch() on cold start (no cache rows)"
    - "A null field received from the API is preserved as null through repository → domain model (end-to-end)"
    - "Amsterdam coordinates (lat=52.3676, lon=4.9041) are hardcoded as static consts; a TODO comment marks them for Phase 7 replacement"
  artifacts:
    - path: "lib/data/repositories/weather_repository.dart"
      provides: "Cache-policy orchestrator wiring ForecastDao + OpenMeteoClient"
      contains: "_amsterdamLat"
    - path: "test/data/repositories/weather_repository_test.dart"
      provides: "Unit tests for cache policy and null preservation"
      contains: "NativeDatabase.memory()"
  key_links:
    - from: "lib/data/repositories/weather_repository.dart"
      to: "lib/data/database/daos/forecast_dao.dart"
      via: "_db.forecastDao.latestCache()"
      pattern: "forecastDao.latestCache"
    - from: "lib/data/repositories/weather_repository.dart"
      to: "lib/data/remote/open_meteo_client.dart"
      via: "_client.fetch()"
      pattern: "_client.fetch"
---

<objective>
Build WeatherRepository — the cache-policy orchestrator that wires ForecastDao and OpenMeteoClient — and verify its cache contract with unit tests using an in-memory Drift database and a mock HTTP client.

Purpose: Completes the full data layer slice: network → Drift → domain model. Phase 3 Riverpod providers call WeatherRepository.getForecast() directly.
Output: lib/data/repositories/weather_repository.dart with passing unit tests covering fresh cache, stale cache, cold start, and null field preservation.
</objective>

<execution_context>
@/Users/joostmouw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostmouw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joostmouw/ridewindow/.planning/ROADMAP.md
@/Users/joostmouw/ridewindow/.planning/REQUIREMENTS.md
@/Users/joostmouw/ridewindow/.planning/phases/02-data-layer-drift-open-meteo/02-RESEARCH.md
@/Users/joostmouw/ridewindow/lib/data/database/daos/forecast_dao.dart
@/Users/joostmouw/ridewindow/lib/data/remote/open_meteo_client.dart

<interfaces>
From lib/data/remote/open_meteo_client.dart:
  class OpenMeteoClient
    OpenMeteoClient({http.Client? client})
    Future<List<HourlyForecast>> fetch(double lat, double lon)

From lib/data/database/daos/forecast_dao.dart:
  class ForecastDao extends DatabaseAccessor<AppDatabase>
    Future<ForecastCacheEntry?> latestCache({required double lat, required double lon})
    Future<List<HourlyForecast>> hourlyForecasts({required int cacheId})
    Future<void> replaceAll({required double lat, required double lon, required List<HourlyForecast> forecasts})

From lib/data/database/app_database.dart:
  class AppDatabase extends _$AppDatabase
    AppDatabase([QueryExecutor? executor])   // injectable; use NativeDatabase.memory() in tests
    ForecastDao get forecastDao
    int get schemaVersion => 1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build WeatherRepository with cache policy and unit tests</name>
  <files>
    lib/data/repositories/weather_repository.dart,
    test/data/repositories/weather_repository_test.dart,
    test/data/repositories/weather_repository_test.mocks.dart
  </files>
  <read_first>
    - lib/data/remote/open_meteo_client.dart — constructor signature (takes http.Client?); fetch() signature
    - lib/data/database/app_database.dart — constructor AppDatabase([QueryExecutor? executor]); forecastDao getter
    - lib/data/database/daos/forecast_dao.dart — latestCache, hourlyForecasts, replaceAll signatures
    - .planning/phases/02-data-layer-drift-open-meteo/02-RESEARCH.md — Pattern 3 (WeatherRepository cache policy), Pattern 5 (unit test with NativeDatabase.memory() + MockHttpClient), anti-pattern "Putting cache policy in the provider"
  </read_first>
  <behavior>
    Test 1 — cold start (no cache): getForecast() calls client.fetch(), persists to DB, returns forecasts:
      db = AppDatabase(DatabaseConnection(NativeDatabase.memory()))
      Stub MockHttpClient.get(any) to return validJson Response(200).
      Call repo.getForecast().
      Expect: result is non-empty List<HourlyForecast>.
      Expect: verify(mockHttp.get(any)).called(1).

    Test 2 — fresh cache (< 1 hour): getForecast() returns cached data without HTTP call:
      Seed DB by calling repo.getForecast() once (cold start with mock HTTP).
      Reset mock: clearInteractions(mockHttp).
      Call repo.getForecast() a second time.
      Expect: verifyNever(mockHttp.get(any)) — no HTTP request issued.
      Expect: result is non-empty.

    Test 3 — stale cache (> 1 hour): getForecast() re-fetches and returns fresh data:
      Directly insert a ForecastCacheEntries row with fetchedAt = DateTime.now().subtract(Duration(hours: 2)) via db.into(db.forecastCacheEntries).insert(...).
      Insert at least one HourlyForecastEntries row for that cacheId.
      Stub MockHttpClient.get(any) to return validJson Response(200).
      Call repo.getForecast().
      Expect: verify(mockHttp.get(any)).called(1).

    Test 4 — null field preserved end-to-end:
      Stub MockHttpClient.get(any) to return jsonWithNullWindspeed Response(200) where windspeed_10m array contains null.
      Call repo.getForecast().
      Expect: result.first.windspeedKmh == null.

    Test 5 — getForecast() uses Amsterdam coordinates:
      Capture the Uri passed to MockHttpClient.get().
      Expect: uri.queryParameters['latitude'] == '52.3676' and uri.queryParameters['longitude'] == '4.9041'.
  </behavior>
  <action>
    Write tests first (RED), then implement (GREEN).

    Test file (test/data/repositories/weather_repository_test.dart):
      Import drift/native.dart, package:mockito/annotations.dart, package:mockito/mockito.dart, http/http.dart as http.
      Import AppDatabase, OpenMeteoClient, WeatherRepository.
      Annotate: @GenerateMocks([], customMocks: [MockSpec<http.Client>(as: #MockHttpClient)])
      Define validJson and jsonWithNullWindspeed as const String top-level constants (single-hour arrays matching Open-Meteo format).
      In setUp: db = AppDatabase(DatabaseConnection(NativeDatabase.memory())), mockHttp = MockHttpClient(), repo = WeatherRepository(db: db, client: OpenMeteoClient(client: mockHttp)).
      In tearDown: db.close().
      Run dart run build_runner build --delete-conflicting-outputs to generate mocks.

    Implementation (lib/data/repositories/weather_repository.dart):
      Import the database, dao, remote client, and domain model.
      Class WeatherRepository:
        static const double _amsterdamLat = 52.3676;
        static const double _amsterdamLon = 4.9041;
        // TODO Phase 7: replace with LocationService.currentLatLon()
        static const Duration _cacheDuration = Duration(hours: 1);
        final AppDatabase _db;
        final OpenMeteoClient _client;
        Constructor: WeatherRepository({required AppDatabase db, required OpenMeteoClient client}) : _db = db, _client = client;
        Future<List<HourlyForecast>> getForecast():
          final cache = await _db.forecastDao.latestCache(lat: _amsterdamLat, lon: _amsterdamLon);
          if (cache != null && DateTime.now().difference(cache.fetchedAt) < _cacheDuration):
            return _db.forecastDao.hourlyForecasts(cacheId: cache.id);
          final fresh = await _client.fetch(_amsterdamLat, _amsterdamLon);
          await _db.forecastDao.replaceAll(lat: _amsterdamLat, lon: _amsterdamLon, forecasts: fresh);
          return fresh;

    After implementation: dart test test/data/repositories/weather_repository_test.dart — all 5 tests must pass.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart test test/data/repositories/weather_repository_test.dart --reporter=compact 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - dart test test/data/repositories/weather_repository_test.dart exits 0 with all 5 tests passing
    - lib/data/repositories/weather_repository.dart contains "static const double _amsterdamLat = 52.3676"
    - lib/data/repositories/weather_repository.dart contains "static const double _amsterdamLon = 4.9041"
    - lib/data/repositories/weather_repository.dart contains "// TODO Phase 7"
    - lib/data/repositories/weather_repository.dart contains "Duration(hours: 1)" (cache duration constant)
    - lib/data/repositories/weather_repository.dart contains "forecastDao.latestCache"
    - lib/data/repositories/weather_repository.dart contains "forecastDao.replaceAll"
    - No occurrence of "http.get(" (static call) in lib/data/repositories/weather_repository.dart — client is injected
    - dart analyze lib/data/repositories/ exits 0 with no issues
  </acceptance_criteria>
  <done>All 5 WeatherRepository tests pass; cache policy (fresh/stale/cold-start/null-preservation/coordinates) is verified</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run full test suite and phase gate verification</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md — Phase 2 success criteria (5 numbered items)
    - test/ directory structure — confirm all test files discovered
  </read_first>
  <action>
    Run the full dart test suite to confirm Phase 2 work does not break Phase 1.5 domain tests:
      dart test --reporter=compact

    Verify Phase 2 success criteria from ROADMAP.md:
      1. OpenMeteoClient.fetch() returns typed List<HourlyForecast>; all six fields are double? — confirmed by client tests.
      2. Every HTTP request includes timezone=auto and timeformat=unixtime enforced at client level — confirmed by URL test.
      3. WeatherRepository returns cached data when fetched_at is within 1h; re-fetches when stale — confirmed by repository tests.
      4. Null field not coerced to 0 — confirmed by null-preservation tests in both client and repository.
      5. Drift schema has migration scaffolding comment and append-only convention — confirmed by grep.

    Confirm no regression in Phase 1.5 tests:
      dart test test/domain/ — must exit 0 with 67 tests passing (same count as Phase 1.5 final).

    Confirm dart analyze passes across all new files:
      dart analyze lib/data/ -- must exit 0 with no issues.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart test --reporter=compact 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - dart test exits 0 with all tests passing (no failures, no errors)
    - Test count is greater than 67 (at least 11 new tests added: 6 client + 5 repository)
    - dart analyze lib/data/ exits 0
    - grep "MIGRATION RULE: columns are append-only" lib/data/database/app_database.dart — matches
    - grep "_amsterdamLat" lib/data/repositories/weather_repository.dart — matches
  </acceptance_criteria>
  <done>Full test suite is green; Phase 2 success criteria are all verifiable via tests and grep gates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WeatherRepository → ForecastDao | Internal Dart layer; types enforced by Drift code-gen |
| WeatherRepository → OpenMeteoClient | Internal injection; mocked in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Tampering | replaceAll transaction — partial write on crash | mitigate | Drift transaction wraps delete+insert; either all rows committed or none; no partial state |
| T-02-06 | Denial of Service | getForecast() — concurrent calls both see stale cache and trigger two fetches | accept | Phase 3 routes all callers through a single Riverpod provider, which Riverpod serialises; acceptable for Phase 2 |
| T-02-07 | Information Disclosure | Amsterdam hardcoded lat/lon in repository | accept | Dev placeholder, no user PII; Phase 7 replaces with actual GPS with explicit permission; TODO comment present |
</threat_model>

<verification>
After both tasks complete:

1. dart test — all tests pass (domain suite unchanged + 11 new data layer tests)
2. dart analyze lib/data/ — exits 0
3. ROADMAP Phase 2 success criterion 3: WeatherRepository cache test confirms fresh/stale behavior
4. ROADMAP Phase 2 success criterion 4: null preservation test confirms windspeedKmh == null end-to-end
5. ROADMAP Phase 2 success criterion 5: grep "MIGRATION RULE" lib/data/database/app_database.dart — found
</verification>

<success_criteria>
- WeatherRepository serves cached data within 1h (FORE-04)
- Re-fetches on stale cache (FORE-04)
- Null field preserved null through repository (FORE-05 end-to-end)
- Amsterdam hardcoded with TODO Phase 7 comment (FORE-01 dev baseline)
- Full dart test suite green (no regressions)
- dart analyze lib/data/ clean
- All 5 Phase 2 ROADMAP success criteria verifiable
</success_criteria>

<output>
Create .planning/phases/02-data-layer-drift-open-meteo/02-03-SUMMARY.md when done
</output>
