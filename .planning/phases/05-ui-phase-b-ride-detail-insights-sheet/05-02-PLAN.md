---
phase: 05-ui-phase-b-ride-detail-insights-sheet
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - lib/features/detail/ride_detail_screen.dart
autonomous: true
requirements: [UI-DETAIL-01]

must_haves:
  truths:
    - "RideDetailScreen toont de start/eindtijd en duur van het slot in de AppBar"
    - "Een ScoreBadge matching de Home card is zichtbaar in het detailscherm"
    - "De uurtabel toont elke HourlyRow met tijd, temperatuur, gevoelstemperatuur, neerslag en windsnelheid"
    - "De 'Waarom deze score?' knop is zichtbaar en tikt naar InsightsSheet"
  artifacts:
    - path: lib/features/detail/ride_detail_screen.dart
      provides: "Volledig RideDetailScreen (geen stub meer)"
      contains: "HourlyRow"
  key_links:
    - from: lib/features/detail/ride_detail_screen.dart
      to: lib/features/detail/insights_sheet.dart
      via: "showModalBottomSheet"
      pattern: "showModalBottomSheet"
    - from: lib/features/detail/ride_detail_screen.dart
      to: lib/features/shared/score_badge.dart
      via: "ScoreBadge(tier: slot.tier)"
      pattern: "ScoreBadge"
---

<objective>
Bouw het volledige RideDetailScreen dat de uitvoer van Wave 1 (DetailArgs, HourlyRow, ScoreBadge)
gebruikt.

Doel: Een gebruiker kan de volledige breakdown van een slot zien — start/eindtijd,
overall score met badge, een overzichtskaart "Weer" (gemiddeld), een uurlijkse tabel
met gevoelstemperatuur, en een "Waarom deze score?" knop die InsightsSheet opent.

Uitvoer:
- `lib/features/detail/ride_detail_screen.dart` — volledig scherm (vervangt Wave 1-stub)
</objective>

<execution_context>
@/Users/joostmouw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostmouw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joostmouw/ridewindow/.planning/PROJECT.md
@/Users/joostmouw/ridewindow/.planning/ROADMAP.md
@/Users/joostmouw/ridewindow/.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-PATTERNS.md
@/Users/joostmouw/ridewindow/.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-CONTEXT.md
@/Users/joostmouw/ridewindow/.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-01-SUMMARY.md

<interfaces>
<!-- Contracten die Wave 2 gebruikt, geproduceerd door Wave 1. -->

Van lib/domain/models/hourly_row.dart (Wave 1):
```dart
class HourlyRow {
  final DateTime time;
  final double? temperatureC;
  final double? apparentTemperatureC;
  final double? precipitationMm;
  final double? windspeedKmh;
  final double overallScore;
  final double temperatureScore;
  final double rainScore;
  final double windScore;
  const HourlyRow({ ... });
}

List<HourlyRow> buildHourlyRows(RideSlot slot, List<HourlyForecast> forecasts);
```

Van lib/features/detail/detail_args.dart (Wave 1):
```dart
class DetailArgs {
  final RideSlot slot;
  final List<HourlyForecast> forecasts;
  const DetailArgs({required this.slot, required this.forecasts});
}
```

Van lib/features/shared/score_badge.dart (Wave 1):
```dart
class ScoreBadge extends StatelessWidget {
  final RideTier tier;
  const ScoreBadge({super.key, required this.tier});
  // toont "Perfect"/"Goed"/"Acceptabel"/"Slecht" met tier-kleuren
}
```

Van lib/domain/models/ride_tier.dart:
```dart
sealed class RideTier { const RideTier(); }
final class Perfect extends RideTier {}
final class Great extends RideTier {}
final class Acceptable extends RideTier {}
final class Poor extends RideTier {}
```

Mockup visueel contract (mockup.html .score-banner):
- Achtergrond groen (#E8F5E9 voor Perfect/Great) of amber (#FFF3E0 voor Acceptable)
- Tier-emoji (🟢 Perfect/Great, 🟡 Acceptable, ⚪ Poor) + tier-tekst + "i" knop
- Beschrijving "Perfect — het beste venster deze week"

Mockup visueel contract (.info-card):
- Afgeronde kaart (border-radius 18px), witte achtergrond, schaduw
- Sectie-titel: "WEER" / "UURLIJKS" (10px bold uppercase #999)
- Rijen gescheiden door een dunne lijn (#F0F0F0)

Mockup visueel contract (.hourly-row):
- Kolommen: tijd (48px breed, 13px #999), temp (46px, 13px bold), gevoelstemp (flex), neerslag, wind
- Gevoelstemperatuur tonen als "voelt als 21°C" of weglaten als null

InsightsSheet stub (wordt gebouwd in Wave 3):
```dart
class InsightsSheet extends StatelessWidget {
  final RideSlot slot;
  const InsightsSheet({super.key, required this.slot});
  // Wave 3 vult de inhoud in
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: RideDetailScreen volledig scherm</name>
  <files>lib/features/detail/ride_detail_screen.dart</files>
  <behavior>
    - RideDetailScreen is een StatelessWidget met constructor (slot: RideSlot, forecasts: List&lt;HourlyForecast&gt;)
    - AppBar toont "{dag} {startTijd} – {eindTijd}" als titel en "{N}u · {tier-label} omstandigheden" als ondertitel
    - Score-banner toont tier-emoji + "{tier-label} — {beschrijving}" + ronde "i"-knop
    - Tikken op de "i"-knop opent InsightsSheet via showModalBottomSheet
    - Info-kaart "Weer" toont gemiddelde temp, totale neerslag, gemiddelde wind over het slot
    - Info-kaart "Uurlijks" toont één rij per HourlyRow: tijd / temp / gevoelstemp / neerslag / wind
    - Gevoelstemperatuur wordt overgeslagen als apparentTemperatureC null is
    - Twee placeholder-knoppen onderaan: "Toevoegen aan agenda" (Phase 9) + "Herinner me" (Phase 8)
  </behavior>
  <action>
    Vervang de Wave 1-stub door de volledige implementatie van `lib/features/detail/ride_detail_screen.dart`.

    Structuur van het scherm:
    ```
    Scaffold
    └── SafeArea
        └── Column
            ├── AppBar (aangepast — terug naar /home)
            ├── Score-banner Container
            ├── Expanded(child: SingleChildScrollView)
            │   ├── Info-kaart "Weer" (3 rijen: temp, neerslag, wind)
            │   └── Info-kaart "Uurlijks" (buildHourlyRows-resultaat)
            └── Detail-acties (2 placeholder-knoppen)
    ```

    Score-banner kleur per tier:
    - Perfect/Great: achtergrond #E8F5E9, tekst #1B5E20
    - Acceptable: achtergrond #FFF3E0, tekst #E65100
    - Poor: achtergrond #F5F5F5, tekst #757575

    Score-banner emoji per tier:
    - Perfect/Great: "🟢"
    - Acceptable: "🟡"
    - Poor: "⚪"

    Score-banner beschrijvingstekst per tier:
    - Perfect: "Perfect — het beste venster deze week"
    - Great: "Goed — prettige rijomstandigheden"
    - Acceptable: "Acceptabel — te doen, pak een extra laag"
    - Poor: "Slecht — niet ideaal, maar mogelijk"

    Info-kaart "Weer" waarden (berekend uit `forecasts` die binnen [slot.start, slot.end) vallen):
    - Temperatuur: gemiddelde van niet-null temperatureC, getoond als "22°C, voelt als 21°C"
      (gevoelstemperatuur weergegeven als gemiddelde apparentTemperatureC, weglaten als null)
    - Neerslag: som van precipitationMm, getoond als "0.0mm" of "Droog" als som == 0.0
    - Wind: gemiddelde van windspeedKmh, getoond als "10km/u"
    - Gebruik "—" als alle uren null zijn

    Info-kaart "Uurlijks" rij per HourlyRow:
    - Bouw rijen via `buildHourlyRows(slot, forecasts)` (geïmporteerd uit hourly_row.dart)
    - Kolommen: tijd ("09:00"), temp ("22°C"), gevoelstemp ("v.a. 21°C" of leeg), neerslag ("🌧 droog" of "🌧 0.5mm"), wind ("💨 10km/u" of "💨 —")
    - Elke rij: padding 9px verticaal, 16px horizontaal; dunne bovenkant lijn #F0F0F0

    Placeholder-acties onderaan:
    - "Toevoegen aan agenda" knop: ElevatedButton primary groen (#2E7D32), onPressed toont SnackBar
      "Google Agenda integratie komt in een volgende update."
    - "Herinner me de avond ervoor" knop: ElevatedButton.styleFrom backgroundColor #F5F5F5,
      foregroundColor #1A1A1A, onPressed toont SnackBar "Notificaties komen in een volgende update."

    Maak ook een stub `InsightsSheet` aan in `lib/features/detail/insights_sheet.dart` als die
    nog niet bestaat (Wave 3 vult de inhoud in):
    ```dart
    class InsightsSheet extends StatelessWidget {
      final RideSlot slot;
      const InsightsSheet({super.key, required this.slot});
      @override
      Widget build(BuildContext context) {
        return const SizedBox(height: 200);  // Wave 3 vult dit in
      }
    }
    ```

    NavigationBar onderaan identiek aan HomeScreen (Home + Profiel tabs).
    Terug-navigatie: automatische back-button van AppBar (context.pop()) — geen custom logica nodig.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart analyze lib/ && flutter test --reporter=compact</automated>
  </verify>
  <done>
    - lib/features/detail/ride_detail_screen.dart is geen stub meer — bevat score-banner, info-kaarten en uurlijkse tabel
    - lib/features/detail/insights_sheet.dart bestaat (als stub of voltooide wave 3-versie)
    - Alle placeholder-knoppen sturen een SnackBar
    - dart analyze lib/ — geen issues
    - flutter test — alle bestaande tests slagen
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschrijving |
|----------|-------------|
| DetailArgs → RideDetailScreen | slot en forecasts komen uit de provider-state — betrouwbaar |
| showModalBottomSheet | widget-context vereist; geen cross-origin data |

## STRIDE Threat Register

| Threat ID | Categorie | Component | Dispositie | Mitigatieplan |
|-----------|-----------|-----------|------------|---------------|
| T-05-02-01 | Denial of Service | buildHourlyRows O(n×m) in build() | accepteer | Max 7 uren per slot × 168 forecasts — verwaarloosbaar |
| T-05-02-02 | Tampering | slot.hours leeg (nul HourlyScore items) | mitigeer | Controleer op lege lijst voordat gemiddelden worden berekend; toon "—" als fallback |
| T-05-02-03 | Information Disclosure | slot-data in widget tree | accepteer | Geen PII — weersdata en scores zijn niet privacygevoelig |
| T-05-SC | Tampering | nieuwe pakketinstallaties | n.v.t. | Geen nieuwe pakketinstallaties in Wave 2 |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
dart analyze lib/
flutter test --reporter=compact
grep -c "buildHourlyRows" lib/features/detail/ride_detail_screen.dart
grep -c "showModalBottomSheet" lib/features/detail/ride_detail_screen.dart
grep -c "ScoreBadge" lib/features/detail/ride_detail_screen.dart
```
Verwachte uitvoer: geen analyze-fouten, alle tests groen, telwaarden > 0.
</verification>

<success_criteria>
1. RideDetailScreen toont: AppBar met slot-tijdreeks + duur, score-banner met tier-emoji en label
2. Info-kaart "Uurlijks" toont één rij per uur met tijd, temp, gevoelstemp, neerslag, wind
3. "Waarom deze score?" knop (i) opent InsightsSheet via showModalBottomSheet
4. Twee placeholder-knoppen tonen elk een SnackBar
5. `dart analyze lib/` — geen issues
6. `flutter test` — alle bestaande tests slagen
</success_criteria>

<output>
Maak `.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-02-SUMMARY.md` aan na voltooiing.
</output>
