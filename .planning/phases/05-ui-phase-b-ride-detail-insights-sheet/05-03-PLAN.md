---
phase: 05-ui-phase-b-ride-detail-insights-sheet
plan: 03
type: execute
wave: 3
depends_on: [05-02]
files_modified:
  - lib/features/detail/insights_sheet.dart
autonomous: true
requirements: [UI-DETAIL-01]

must_haves:
  truths:
    - "InsightsSheet toont drie LinearProgressIndicator balken (temp, regen, wind)"
    - "Elke balk weerspiegelt de gemiddelde sub-score van de slot-uren"
    - "Elke factor heeft een score-label (bijv. '95 · Ideaal') en een één-regel uitleg"
    - "De totale score wordt onderaan de sheet getoond"
    - "Een 'Begrijpen' knop sluit de sheet"
  artifacts:
    - path: lib/features/detail/insights_sheet.dart
      provides: "Volledige InsightsSheet widget (vervangt Wave 2-stub)"
      contains: "LinearProgressIndicator"
  key_links:
    - from: lib/features/detail/insights_sheet.dart
      to: lib/domain/models/ride_slot.dart
      via: "slot.hours.map().reduce() voor gemiddelde sub-scores"
      pattern: "temperatureScore|rainScore|windScore"
---

<objective>
Bouw de volledige InsightsSheet bottom-sheet widget die de sub-scores van ScoringEngine
visualiseert als drie `LinearProgressIndicator` balken met uitleg.

Doel: Tikken op "Waarom deze score?" opent een bottom-sheet met drie gekwantificeerde factoren
(temperatuur, neerslag, wind), elk met een kleurgecodeerde voortgangsbalk, een score-label
en een automatisch gegenereerde één-regel uitleg.

Uitvoer:
- `lib/features/detail/insights_sheet.dart` — volledig (vervangt Wave 2-stub)
</objective>

<execution_context>
@/Users/joostmouw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostmouw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joostmouw/ridewindow/.planning/PROJECT.md
@/Users/joostmouw/ridewindow/.planning/ROADMAP.md
@/Users/joostmouw/ridewindow/.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-PATTERNS.md
@/Users/joostmouw/ridewindow/.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-CONTEXT.md
@/Users/joostmouw/ridewindow/.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-02-SUMMARY.md

<interfaces>
<!-- Contracten die Wave 3 gebruikt. -->

Van lib/domain/models/ride_slot.dart:
```dart
// slot.hours: List<HourlyScore>
// Elke HourlyScore heeft: temperatureScore, rainScore, windScore (0.0–100.0)
// InsightsSheet berekent intern: avg(temperatureScore), avg(rainScore), avg(windScore)
```

Van lib/domain/models/hourly_score.dart:
```dart
const factory HourlyScore({
  required double overall,
  required double temperatureScore,
  required double rainScore,
  required double windScore,
  required DateTime time,
}) = _HourlyScore;
```

Mockup visueel contract (mockup.html .insight-row):
```
Sheet-titel: "Waarom 'Perfect' — 93/100"
Meta: "zaterdag 09:00 – 13:00 · 4u"
Per factor:
  Label  [score · label-tekst]
  [███████████░░░] ← LinearProgressIndicator (8px hoog)
  Één-regel uitleg (12.5px, #666)
Totaal: "Overall score    93/100"
Knop: "Begrijpen"
```

Score-kleurschema (per D-05-05):
- score ≥ 80 → groen Color(0xFF2E7D32)
- score ≥ 60 → oranje Color(0xFFE65100)
- score < 60 → rood Color(0xFFC62828)

Uitleg per factor (per D-05-04):
Temperatuur:
- score ≥ 80 → "Ideale temperatuur — comfortabel rijden"
- score ≥ 60 → "Acceptabele temperatuur — pak een extra laag"
- score < 60 → "Buiten het ideale bereik — kleding aanpassen"
Neerslag:
- score ≥ 80 → "Droog — geen neerslag verwacht"
- score ≥ 60 → "Lichte neerslag verwacht — spatborden handig"
- score < 60 → "Neerslag verwacht — overweeg een regenjas"
Wind:
- score ≥ 80 → "Lichte wind — nauwelijks merkbaar"
- score ≥ 60 → "Matige wind — verwacht wat weerstand"
- score < 60 → "Sterke wind — plan de route strategisch"

Score-label per factor:
Temperatuur: score ≥ 80 → "Ideaal", score ≥ 60 → "Acceptabel", anders → "Koud/Warm"
Neerslag:    score ≥ 80 → "Droog",  score ≥ 60 → "Licht",      anders → "Nat"
Wind:        score ≥ 80 → "Rustig", score ≥ 60 → "Matig",      anders → "Sterk"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: InsightsSheet volledige bottom-sheet widget</name>
  <files>lib/features/detail/insights_sheet.dart</files>
  <behavior>
    - InsightsSheet ontvangt één parameter: `RideSlot slot`
    - Gemiddelde sub-scores worden intern berekend via `slot.hours`
    - avgTempScore = gemiddelde van hours[i].temperatureScore over alle uren
    - avgRainScore = gemiddelde van hours[i].rainScore over alle uren
    - avgWindScore = gemiddelde van hours[i].windScore over alle uren
    - Bij lege hours-lijst: alle gemiddelden = 50.0 (onzeker)
    - Drie factorrijen: elk heeft label, score-tekst, LinearProgressIndicator, uitleg
    - LinearProgressIndicator value = avgScore / 100.0 (range 0.0–1.0)
    - Kleur van LinearProgressIndicator per D-05-05
    - Uitleg per factor per D-05-04
    - Sheet-titel: "Waarom '${tierLabel}' — ${slot.overallScore.round()}/100"
    - Meta-tekst: "${dag} ${startTijd} – ${eindTijd} · ${duurUren}u"
    - Totaal-rij: "Overall score" links, "${slot.overallScore.round()}/100" rechts
    - "Begrijpen" TextButton sluit de sheet via Navigator.pop(context)
  </behavior>
  <action>
    Vervang de Wave 2-stub door de volledige implementatie van `lib/features/detail/insights_sheet.dart`.

    De widget is een `StatelessWidget` die via `showModalBottomSheet` wordt aangeroepen.

    Layout van de sheet (DraggableScrollableSheet of gewone Column in een Container):
    ```
    Container (padding: 20px horizontaal, 16px boven)
    └── Column
        ├── Handle (32×4px, afgerond, grijs #E0E0E0, gecentreerd, mb 16px)
        ├── Titel (18px bold): "Waarom '${tierLabel}' — ${score}/100"
        ├── Meta (13px #666): "${dag} ${startTijd} – ${eindTijd} · ${N}u"
        ├── SizedBox(16)
        ├── _FactorRow(emoji: "🌡", label: "Temperatuur", score: avgTemp, ...)
        ├── SizedBox(18)
        ├── _FactorRow(emoji: "🌧", label: "Neerslag", score: avgRain, ...)
        ├── SizedBox(18)
        ├── _FactorRow(emoji: "💨", label: "Wind", score: avgWind, ...)
        ├── SizedBox(16)
        ├── Totaal-Container (achtergrond #F5F5F5, border-radius 12, padding 12×16)
        │   └── Row: "Overall score" + "${score}/100" (groen/oranje/rood)
        └── TextButton "Begrijpen" (groen #2E7D32, onPressed: Navigator.pop)
    ```

    `_FactorRow` als private widget of private bouwmethode:
    - Rij-label links (14px bold) + score-tekst rechts (13px #666): "95 · Ideaal"
    - LinearProgressIndicator: minHeight 8, borderRadius circular(4),
      backgroundColor #ECEFF1, valueColor AlwaysStoppedAnimation(_scoreColor(score))
    - Uitleg-tekst (12.5px #666, lineHeight 1.45)

    Hulpfuncties (private):
    - `double _avg(List<HourlyScore> hours, double Function(HourlyScore) selector)`:
      geeft 50.0 terug bij lege lijst; anders gemiddelde.
    - `Color _scoreColor(double score)`: per D-05-05.
    - `String _tempLabel(double score)`, `_rainLabel`, `_windLabel`: score-label per factor.
    - `String _tempNote(double score)`, `_rainNote`, `_windNote`: uitleg per D-05-04.
    - `String _tierLabel(RideTier tier)`: "Perfect"/"Goed"/"Acceptabel"/"Slecht".
    - `String _formatTime(DateTime dt)`: "09:00".
    - `String _dayName(DateTime dt)`: Nederlandse dagnaam (maandag/dinsdag/...).

    Geen Riverpod — geen provider-toegang nodig. Alle data komt uit `slot`.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart analyze lib/ && flutter test --reporter=compact</automated>
  </verify>
  <done>
    - lib/features/detail/insights_sheet.dart is geen stub meer — bevat drie LinearProgressIndicator balken
    - Alle drie factoren (temp, regen, wind) hebben label, score-tekst, balk en uitleg
    - Totaal-score rij zichtbaar onderaan
    - "Begrijpen" knop aanwezig
    - dart analyze lib/ — geen issues
    - flutter test — alle bestaande tests slagen
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschrijving |
|----------|-------------|
| slot.hours → gemiddelde berekening | Lege lijst veroorzaakt deling door nul — afgevangen |

## STRIDE Threat Register

| Threat ID | Categorie | Component | Dispositie | Mitigatieplan |
|-----------|-----------|-----------|------------|---------------|
| T-05-03-01 | Denial of Service | gemiddelde berekening op lege uren | mitigeer | `_avg` functie geeft 50.0 terug bij lege lijst — nul-deling niet mogelijk |
| T-05-03-02 | Tampering | sub-scores buiten [0,100] bereik | mitigeer | ScoringEngine garandeert .clamp(0.0, 100.0) — waarden zijn veilig; gebruik toch `.clamp(0.0, 100.0)` in `value: score / 100.0` voor LinearProgressIndicator als extra zekerheid |
| T-05-03-03 | Information Disclosure | slot.overallScore.round() in titel | accepteer | Score is afgeleid van weersdata — niet privacygevoelig |
| T-05-SC | Tampering | nieuwe pakketinstallaties | n.v.t. | Geen nieuwe pakketinstallaties in Wave 3 |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
dart analyze lib/
flutter test --reporter=compact
grep -c "LinearProgressIndicator" lib/features/detail/insights_sheet.dart
grep -c "temperatureScore" lib/features/detail/insights_sheet.dart
grep -c "rainScore" lib/features/detail/insights_sheet.dart
grep -c "windScore" lib/features/detail/insights_sheet.dart
```
Verwachte uitvoer: geen analyze-fouten, alle tests groen, LinearProgressIndicator ≥ 3,
elke sub-score-naam ≥ 1.
</verification>

<success_criteria>
1. InsightsSheet toont drie `LinearProgressIndicator` balken (temp, regen, wind)
2. Elke balk heeft een waarde die overeenkomt met de gemiddelde sub-score van `slot.hours`
3. Elke factor heeft een score-label (bijv. "95 · Ideaal") en een één-regel uitleg
4. De totale score van het slot is zichtbaar onderaan
5. "Begrijpen" knop sluit de sheet via `Navigator.pop`
6. `dart analyze lib/` — geen issues
7. `flutter test` — alle bestaande tests slagen
</success_criteria>

<output>
Maak `.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-03-SUMMARY.md` aan na voltooiing.
</output>
