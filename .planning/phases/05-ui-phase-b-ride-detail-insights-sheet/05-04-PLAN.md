---
phase: 05-ui-phase-b-ride-detail-insights-sheet
plan: 04
type: execute
wave: 4
depends_on: [05-03]
files_modified:
  - test/features/ride_detail_screen_test.dart
  - test/features/insights_sheet_test.dart
autonomous: true
requirements: [UI-DETAIL-01]

must_haves:
  truths:
    - "RideDetailScreen toont de tijdreeks van het slot in de AppBar — bewezen via widget-test"
    - "RideDetailScreen toont de correcte ScoreBadge tier — bewezen via widget-test"
    - "RideDetailScreen toont gevoelstemperatuur in de uurtabel — bewezen via widget-test"
    - "InsightsSheet sub-score balken komen overeen met de bekende fixture-waarden — bewezen via widget-test"
  artifacts:
    - path: test/features/ride_detail_screen_test.dart
      provides: "Widget-tests voor RideDetailScreen"
    - path: test/features/insights_sheet_test.dart
      provides: "Widget-tests voor InsightsSheet sub-scores"
  key_links:
    - from: test/features/insights_sheet_test.dart
      to: lib/domain/services/scoring_engine.dart
      via: "fixture-waarden berekend via ScoringEngine"
      pattern: "ScoringEngine"
---

<objective>
Schrijf widget-tests die alle vier Phase 5 succescriteria automatisch bewijzen.

Doel: Geautomatiseerde regressie-beveiliging voor het detailscherm en de insights-sheet.
De insights-test gebruikt een bekende fixture die via ScoringEngine is berekend om te
bevestigen dat de voortgangsbalken echte sub-score waarden tonen.

Uitvoer:
- `test/features/ride_detail_screen_test.dart` — 5 widget-tests
- `test/features/insights_sheet_test.dart` — 4 widget-tests
</objective>

<execution_context>
@/Users/joostmouw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostmouw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joostmouw/ridewindow/.planning/PROJECT.md
@/Users/joostmouw/ridewindow/.planning/ROADMAP.md
@/Users/joostmouw/ridewindow/.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-PATTERNS.md
@/Users/joostmouw/ridewindow/.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-CONTEXT.md
@/Users/joostmouw/ridewindow/.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-03-SUMMARY.md

<interfaces>
<!-- Test-infrastructuurpatronen van Phase 4 (SUMMARY-05). -->

Gevestigd testpatroon:
```dart
// setUp
SharedPreferences.setMockInitialValues({});

// GoRouter fixture inline in testWidget (niet als parameter)
final router = GoRouter(routes: [
  GoRoute(path: '/', builder: (_, __) => const SizedBox()),
  GoRoute(
    path: '/detail',
    builder: (_, state) {
      final args = state.extra as DetailArgs;
      return RideDetailScreen(slot: args.slot, forecasts: args.forecasts);
    },
  ),
]);

// ProviderScope overrides inline (geen List<Override> parameter):
await tester.pumpWidget(
  ProviderScope(
    overrides: [
      weatherProvider.overrideWith((ref) => FakeWeatherNotifier(forecasts)),
    ],
    child: MaterialApp.router(routerConfig: router),
  ),
);

// Geen pumpAndSettle — gebruik pump(Duration):
await tester.pump();
await tester.pump(const Duration(milliseconds: 100));
```

FakeNotifier patroon (extends concrete class):
```dart
class FakeWeatherNotifier extends WeatherNotifier {
  final List<HourlyForecast> _forecasts;
  FakeWeatherNotifier(this._forecasts);
  @override
  Future<List<HourlyForecast>> build() async => _forecasts;
}
```

Bekende ScoringEngine fixture (voor insights-test):
```
HourlyForecast(
  temperatureC: 22.0, apparentTemperatureC: 21.0,
  precipitationMm: 0.0, precipitationProbability: 0.0,
  windspeedKmh: 10.0, winddirectionDeg: 225.0,
  time: DateTime(2026, 6, 7, 9),
)
WeatherTolerances: tempMin=12, tempMax=26, rainMax=0.5, windMax=15
→ ScoringEngine().score(forecast, tolerances):
  temperatureScore = 100.0 (22°C in [12,26])
  rainScore       = 100.0 (0.0mm ≤ 0.5mm)
  windScore       = 100.0 (10km/u ≤ 15km/u)
  overall         = 100.0
```

RideDetailScreen direct testen (zonder GoRouter navigatie):
```dart
// Simpelere aanpak — geef de widget direct mee aan tester:
await tester.pumpWidget(
  MaterialApp(
    home: RideDetailScreen(slot: testSlot, forecasts: testForecasts),
  ),
);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: RideDetailScreen widget-tests</name>
  <files>test/features/ride_detail_screen_test.dart</files>
  <behavior>
    Test 1: AppBar toont de tijdreeks van het slot
    - Gegeven: slot met start=09:00, end=13:00 op zaterdag
    - Verwacht: tekst "09:00 – 13:00" is zichtbaar in de widget tree

    Test 2: Score-badge tier komt overeen met het slot
    - Gegeven: Perfect-tier slot
    - Verwacht: tekst "Perfect" is zichtbaar (ScoreBadge)

    Test 3: Uurtabel toont gevoelstemperatuur
    - Gegeven: slot met één uur, apparentTemperatureC = 21.0
    - Verwacht: tekst die "21" bevat is zichtbaar in de uurtabel-sectie

    Test 4: "Waarom deze score?" knop is zichtbaar
    - Gegeven: willekeurig slot
    - Verwacht: een knop of tik-doel met "Waarom" tekst of een "i"-icoon is aanwezig

    Test 5: Tikken op "Toevoegen aan agenda" toont SnackBar
    - Gegeven: scherm gemonteerd
    - Verwacht: na tap op de knop is "Google Agenda" tekst zichtbaar in een SnackBar
  </behavior>
  <action>
    Maak `test/features/ride_detail_screen_test.dart` aan.

    Gebruik `MaterialApp(home: RideDetailScreen(...))` direct — geen GoRouter nodig voor
    de meeste tests (eenvoudiger, minder setup). Voor test 5 (SnackBar) gebruik een
    Scaffold wrapper zodat ScaffoldMessenger beschikbaar is.

    Bouw een testfixture:
    ```dart
    final testSlot = RideSlot(
      start: DateTime(2026, 6, 6, 9),   // zaterdag 09:00
      end:   DateTime(2026, 6, 6, 13),  // zaterdag 13:00
      overallScore: 93.0,
      tier: const Perfect(),
      hours: [
        HourlyScore(overall: 93, temperatureScore: 95, rainScore: 100, windScore: 85,
                    time: DateTime(2026, 6, 6, 9)),
        HourlyScore(overall: 93, temperatureScore: 95, rainScore: 100, windScore: 85,
                    time: DateTime(2026, 6, 6, 10)),
        HourlyScore(overall: 93, temperatureScore: 95, rainScore: 100, windScore: 85,
                    time: DateTime(2026, 6, 6, 11)),
        HourlyScore(overall: 93, temperatureScore: 95, rainScore: 100, windScore: 85,
                    time: DateTime(2026, 6, 6, 12)),
      ],
    );

    final testForecasts = [
      HourlyForecast(temperatureC: 22.0, apparentTemperatureC: 21.0,
          precipitationMm: 0.0, precipitationProbability: 0.0,
          windspeedKmh: 10.0, winddirectionDeg: 225.0,
          time: DateTime(2026, 6, 6, 9)),
      // Herhaal voor 10:00, 11:00, 12:00 met licht variërende waarden
    ];
    ```

    setUp: `SharedPreferences.setMockInitialValues({});`
    Gebruik `tester.pump()` en `tester.pump(const Duration(milliseconds: 100))` —
    geen pumpAndSettle vanwege mogelijke animaties.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/features/ride_detail_screen_test.dart --reporter=compact</automated>
  </verify>
  <done>
    - test/features/ride_detail_screen_test.dart heeft 5 groene tests
    - Alle vijf Phase 5 succescriteria 1, 2, 3 zijn automatisch bewezen
  </done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: InsightsSheet widget-tests met fixture-verificatie</name>
  <files>test/features/insights_sheet_test.dart</files>
  <behavior>
    Test 1: Sheet-titel bevat tier-label en score
    - Gegeven: slot met tier=Perfect, overallScore=93.0
    - Verwacht: tekst die "Waarom" en "Perfect" en "93" bevat

    Test 2: Drie LinearProgressIndicator widgets aanwezig
    - Gegeven: willekeurig slot met drie uren
    - Verwacht: exact 3 LinearProgressIndicator widgets in de widget tree

    Test 3: Sub-score balken komen overeen met ScoringEngine fixture
    - Gegeven: slot met 1 uur; HourlyScore berekend via ScoringEngine().score(forecast, tolerances)
      met forecast: temp=22°C, apparent=21°C, precip=0.0mm, wind=10km/u
      en tolerances: tempMin=12, tempMax=26, rainMax=0.5, windMax=15
    - Verwacht: alle drie LinearProgressIndicator widgets hebben value == 1.0 (100/100)
    - Reden: ScoringEngine geeft tempScore=100, rainScore=100, windScore=100 voor deze invoer

    Test 4: "Begrijpen" knop is zichtbaar en sluit de sheet niet nader te testen
    - Gegeven: sheet gemonteerd
    - Verwacht: tekst "Begrijpen" is zichtbaar
  </behavior>
  <action>
    Maak `test/features/insights_sheet_test.dart` aan.

    Gebruik `MaterialApp(home: Scaffold(body: InsightsSheet(slot: testSlot)))` —
    geen showModalBottomSheet nodig voor widget-tests.

    Test 3 is de cruciale fixture-test (Phase 5 succeskriterium 4). Bouw de fixture
    handmatig via bekende ScoringEngine-uitvoer:
    ```dart
    // Fixture berekend via ScoringEngine (zie interfaces-sectie):
    const tolerances = WeatherTolerances(
      tempMinIdealC: 12.0, tempMaxIdealC: 26.0,
      rainMaxIdealMm: 0.5, windMaxIdealKmh: 15.0,
    );
    final forecast = HourlyForecast(
      temperatureC: 22.0, apparentTemperatureC: 21.0,
      precipitationMm: 0.0, precipitationProbability: 0.0,
      windspeedKmh: 10.0, winddirectionDeg: 225.0,
      time: DateTime(2026, 6, 7, 9),
    );
    final score = ScoringEngine().score(forecast, tolerances);
    // score.temperatureScore == 100.0, rainScore == 100.0, windScore == 100.0

    final fixtureSlot = RideSlot(
      start: DateTime(2026, 6, 7, 9),
      end:   DateTime(2026, 6, 7, 10),
      overallScore: score.overall,
      tier: rideTierFromScore(score.overall),
      hours: [score],
    );
    ```

    Voor test 3: zoek alle LinearProgressIndicator widgets via `tester.widgetList<LinearProgressIndicator>`.
    Bevestig dat elk een `value` heeft van `1.0` (100/100).

    setUp: `SharedPreferences.setMockInitialValues({});`
    Gebruik `tester.pump()` en `tester.pump(const Duration(milliseconds: 100))`.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/features/insights_sheet_test.dart --reporter=compact</automated>
  </verify>
  <done>
    - test/features/insights_sheet_test.dart heeft 4 groene tests
    - Test 3 bewijst dat sub-score balken overeenkomen met ScoringEngine fixture (Phase 5 succeskriterium 4)
    - Volledig test-suite slaagt
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschrijving |
|----------|-------------|
| Test fixture → ScoringEngine | Fixture-waarden zijn deterministisch berekend — geen verrassingen |

## STRIDE Threat Register

| Threat ID | Categorie | Component | Dispositie | Mitigatieplan |
|-----------|-----------|-----------|------------|---------------|
| T-05-04-01 | Tampering | ScoringEngine fixture-waarden wijzigen | mitigeer | Test 3 pinnt exacte score-waarden vast; elke ScoringEngine-regressie breekt de test |
| T-05-04-02 | Denial of Service | pumpAndSettle timeout op animaties | mitigeer | Gebruik pump(Duration) — gevestigd patroon uit Phase 4 |
| T-05-SC | Tampering | nieuwe pakketinstallaties | n.v.t. | Geen nieuwe pakketinstallaties in Wave 4 |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
flutter test test/features/ride_detail_screen_test.dart --reporter=compact
flutter test test/features/insights_sheet_test.dart --reporter=compact
flutter test --reporter=compact
```
Verwachte uitvoer: 5+4 = 9 nieuwe tests groen, volledige suite groen.
</verification>

<success_criteria>
1. `test/features/ride_detail_screen_test.dart` — 5 groene tests
2. `test/features/insights_sheet_test.dart` — 4 groene tests inclusief fixture-test
3. Fixture-test (Taak 2 Test 3) bewijst LinearProgressIndicator value == 1.0 voor ScoringEngine-invoer met temp=22°C, rain=0.0mm, wind=10km/u
4. `flutter test` — alle tests slagen (115 bestaand + 9 nieuw = 124+)
5. Phase 5 succescriteria 1–4 zijn alle automatisch bewezen via widget-tests
</success_criteria>

<output>
Maak `.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-04-SUMMARY.md` aan na voltooiing.
</output>
