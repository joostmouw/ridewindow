---
phase: 05-ui-phase-b-ride-detail-insights-sheet
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/domain/models/hourly_row.dart
  - lib/features/detail/detail_args.dart
  - lib/features/shared/score_badge.dart
  - lib/app/router.dart
  - lib/features/home/home_screen.dart
autonomous: true
requirements: [UI-DETAIL-01]

must_haves:
  truths:
    - "Tikken op een ride card navigeert naar /detail en geeft de juiste slot-data mee"
    - "HomeScreen weather chips tonen echte gemiddelde temp/neerslag/wind uit de forecast"
    - "ScoreBadge widget bestaat en wordt gedeeld door HomeScreen en toekomstige detailscherm"
  artifacts:
    - path: lib/domain/models/hourly_row.dart
      provides: "HourlyRow weergave-model (samenvoeging score + forecast per uur)"
    - path: lib/features/detail/detail_args.dart
      provides: "DetailArgs data-transfer object voor navigatie via extra"
    - path: lib/features/shared/score_badge.dart
      provides: "ScoreBadge gedeelde widget (geëxtraheerd uit HomeScreen)"
    - path: lib/app/router.dart
      provides: "/detail GoRoute die DetailArgs via state.extra leest"
    - path: lib/features/home/home_screen.dart
      provides: "Ride card tikt → context.go('/detail', extra: DetailArgs), echte weather chips"
  key_links:
    - from: lib/features/home/home_screen.dart
      to: /detail route
      via: "context.go('/detail', extra: DetailArgs(slot: slot, forecasts: slotForecasts))"
      pattern: "context\\.go.*detail.*DetailArgs"
    - from: lib/app/router.dart
      to: lib/features/detail/ride_detail_screen.dart
      via: "state.extra as DetailArgs"
      pattern: "state\\.extra as DetailArgs"
---

<objective>
Leg de navigatie- en datacontracts vast die alle latere waves nodig hebben.

Doel: Tikken op een ride card in HomeScreen navigeert naar /detail via go_router `extra`,
waarbij een `DetailArgs` object (slot + gefilterde forecasts) wordt meegegeven.
HomeScreen weather chips tonen echte gemiddelde weersdata (was: placeholder "?°C").
`ScoreBadge` wordt geëxtraheerd uit HomeScreen zodat beide screens hem hergebruiken.

Uitvoer:
- `lib/domain/models/hourly_row.dart` — HourlyRow weergave-model
- `lib/features/detail/detail_args.dart` — navigatie DTO
- `lib/features/shared/score_badge.dart` — geëxtraheerde badge-widget
- `lib/app/router.dart` — /detail GoRoute toegevoegd
- `lib/features/home/home_screen.dart` — tap-navigatie + echte weather chips
</objective>

<execution_context>
@/Users/joostmouw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/joostmouw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/joostmouw/ridewindow/.planning/PROJECT.md
@/Users/joostmouw/ridewindow/.planning/ROADMAP.md
@/Users/joostmouw/ridewindow/.planning/STATE.md
@/Users/joostmouw/ridewindow/.planning/phases/04-ui-phase-a-onboarding-home-welcome/04-PATTERNS.md
@/Users/joostmouw/ridewindow/.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-PATTERNS.md
@/Users/joostmouw/ridewindow/.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-CONTEXT.md

<interfaces>
<!-- Bestaande contracten die de uitvoerder direct nodig heeft. -->

Van lib/domain/models/ride_slot.dart:
```dart
@freezed abstract class RideSlot with _$RideSlot {
  const factory RideSlot({
    required DateTime start,
    required DateTime end,
    required double overallScore,
    required RideTier tier,
    required List<HourlyScore> hours,
  }) = _RideSlot;
}
```

Van lib/domain/models/hourly_score.dart:
```dart
@freezed abstract class HourlyScore with _$HourlyScore {
  const factory HourlyScore({
    required double overall,
    required double temperatureScore,
    required double rainScore,
    required double windScore,
    required DateTime time,
  }) = _HourlyScore;
}
```

Van lib/domain/models/hourly_forecast.dart:
```dart
@freezed abstract class HourlyForecast with _$HourlyForecast {
  const factory HourlyForecast({
    @JsonKey(name: 'temperature_2m') required double? temperatureC,
    @JsonKey(name: 'apparent_temperature') required double? apparentTemperatureC,
    @JsonKey(name: 'precipitation') required double? precipitationMm,
    @JsonKey(name: 'precipitation_probability') required double? precipitationProbability,
    @JsonKey(name: 'windspeed_10m') required double? windspeedKmh,
    @JsonKey(name: 'winddirection_10m') required double? winddirectionDeg,
    required DateTime time,
  }) = _HourlyForecast;
}
```

Van lib/domain/models/ride_tier.dart:
```dart
sealed class RideTier { const RideTier(); }
final class Perfect extends RideTier { const Perfect(); }
final class Great extends RideTier { const Great(); }
final class Acceptable extends RideTier { const Acceptable(); }
final class Poor extends RideTier { const Poor(); }
```

Van lib/app/router.dart (huidige toestand, Wave 4):
```dart
@Riverpod(keepAlive: true)
GoRouter router(Ref ref) {
  return GoRouter(
    initialLocation: '/home',
    redirect: (context, state) async { ... },
    routes: [
      GoRoute(path: '/welcome', ...),
      GoRoute(path: '/onboard', ...),
      GoRoute(path: '/home', ...),
      GoRoute(path: '/availability', ...),
      // Wave 5 voegt /detail toe
    ],
  );
}
```

Van lib/features/home/home_screen.dart — relevant fragment:
```dart
// _buildRideCard toont nu placeholder weather chips:
// Phase 5 vervangt dit met echte data vanuit ref.watch(weatherProvider)
Widget _buildWeatherChip(String emoji, String value) { ... }
// _buildBadge wordt ScoreBadge (te extraheren)
Widget _buildBadge(RideTier tier) { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: HourlyRow model + DetailArgs DTO + ScoreBadge widget</name>
  <files>
    lib/domain/models/hourly_row.dart,
    lib/features/detail/detail_args.dart,
    lib/features/shared/score_badge.dart
  </files>
  <behavior>
    - HourlyRow bevat alle velden zoals gedocumenteerd in D-05-01 en 05-PATTERNS.md
    - DetailArgs.forecasts is List&lt;HourlyForecast&gt; (gefilterd op slot-tijdvenster)
    - ScoreBadge toont dezelfde kleuren als de huidige HomeScreen._buildBadge
    - ScoreBadge(tier: Perfect()) toont achtergrond #E8F5E9, tekst #1B5E20, label "Perfect"
    - ScoreBadge(tier: Acceptable()) toont achtergrond #FFF3E0, tekst #E65100, label "Acceptabel"
  </behavior>
  <action>
    Maak drie nieuwe bestanden aan per 05-PATTERNS.md patroonopdrachtkaarten:

    1. `lib/domain/models/hourly_row.dart`: Plain Dart class HourlyRow met negen velden
       (time, temperatureC?, apparentTemperatureC?, precipitationMm?, windspeedKmh?,
       overallScore, temperatureScore, rainScore, windScore). Geen Freezed, geen part-directive.
       Voeg ook een pure hulpfunctie `buildHourlyRows(RideSlot slot, List<HourlyForecast> forecasts)`
       toe die de twee lijsten samenvoegt op basis van gelijke `time`.
       Gebruik `firstWhere` met `orElse` (null-forecast met alle velden null) om ontbrekende
       forecast-uren te overbruggen. Importeer alleen domain-modellen (geen Flutter/Riverpod).

    2. `lib/features/detail/detail_args.dart`: Plain Dart class DetailArgs met twee velden
       (slot: RideSlot, forecasts: List&lt;HourlyForecast&gt;). Const constructor.

    3. `lib/features/shared/score_badge.dart`: Extraheer de logica van HomeScreen._buildBadge
       naar een zelfstandige `ScoreBadge extends StatelessWidget`. De widget ontvangt `RideTier tier`
       als verplichte parameter. Gebruik sealed class switch-expression voor kleuren en labels
       (identiek aan de huidige HomeScreen-logica, zodat het visueel matcht). Labels: "Perfect",
       "Goed", "Acceptabel", "Slecht".

    Schrijf unit-test voor `buildHourlyRows`:
    - Geeft lege lijst terug bij lege score-invoer
    - Vult forecast-velden correct in bij overeenkomende time
    - Gebruikt null-velden als forecast-uur ontbreekt
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/domain/models/hourly_row_test.dart --reporter=compact</automated>
  </verify>
  <done>
    - lib/domain/models/hourly_row.dart bestaat met HourlyRow class + buildHourlyRows functie
    - lib/features/detail/detail_args.dart bestaat met DetailArgs class
    - lib/features/shared/score_badge.dart bestaat met ScoreBadge widget
    - test/domain/models/hourly_row_test.dart groen
    - dart analyze lib/ — geen issues
  </done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: /detail route + HomeScreen tap-navigatie + echte weather chips</name>
  <files>
    lib/app/router.dart,
    lib/features/home/home_screen.dart
  </files>
  <behavior>
    - /detail GoRoute leest state.extra als DetailArgs en geeft deze door aan RideDetailScreen
    - Tikken op een ride card roept context.go('/detail', extra: DetailArgs(...)) aan
    - Gefilterde forecasts omvatten precies de uren binnen [slot.start, slot.end)
    - Weather chips tonen gemiddelde temperatuur (één decimaal), totale neerslag, gemiddelde windsnelheid
    - HomeScreen._buildBadge wordt vervangen door ScoreBadge(tier: slot.tier)
    - dart analyze lib/ geeft geen fouten na de wijziging
  </behavior>
  <action>
    Wijzig twee bestaande bestanden:

    1. `lib/app/router.dart`: Voeg een GoRoute toe voor /detail na de /availability route:
       ```dart
       GoRoute(
         path: '/detail',
         builder: (context, state) {
           final args = state.extra as DetailArgs;
           return RideDetailScreen(slot: args.slot, forecasts: args.forecasts);
         },
       ),
       ```
       Voeg imports toe voor DetailArgs en RideDetailScreen (RideDetailScreen bestaat nog niet —
       maak alvast een stub aan in lib/features/detail/ride_detail_screen.dart als de
       `StatelessWidget` die de slot- en forecast-parameters accepteert maar voorlopig een lege
       Scaffold toont). Dit zorgt dat de router compileert voor Wave 2.

    2. `lib/features/home/home_screen.dart`:
       a. Vervang _buildBadge door ScoreBadge(tier: slot.tier). Verwijder de private _buildBadge
          methode. Voeg import toe voor ScoreBadge.
       b. Voeg GestureDetector onTap toe aan _buildRideCard: lees weatherProvider (al beschikbaar
          via ref.watch op regel 54), filter forecasts op [slot.start, slot.end) via
          `!f.time.isBefore(slot.start) && f.time.isBefore(slot.end)`, en navigeer met
          `context.go('/detail', extra: DetailArgs(...))`.
       c. Vervang de placeholder weather chips ("?°C", "?mm", "?km/u") door echte waarden:
          - Temperatuur: gemiddelde van `temperatureC` over de slot-uren, getoond als "22°C"
            (null-uren overgeslagen bij gemiddelde)
          - Neerslag: som van `precipitationMm` over de slot-uren, getoond als "0.0mm"
          - Wind: gemiddelde van `windspeedKmh` over de slot-uren, getoond als "10km/u"
          - Als alle uren null zijn: toon "—" in plaats van de waarde

    Let op per D-05-06: weatherProvider is al beschikbaar als `AsyncValue<List<HourlyForecast>>`
    via `ref.watch(weatherProvider)`. Controleer op `weatherState.hasValue` voordat je `.requireValue`
    aanroept — toon "—" als het laden nog niet klaar is.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart analyze lib/ && flutter test --reporter=compact</automated>
  </verify>
  <done>
    - router.dart heeft een /detail GoRoute
    - Tikken op een ride card compileert en navigeert (verificatie: geen analyze-fout, widget-test in Wave 4)
    - HomeScreen weather chips tonen echte data (geen "?°C" meer)
    - dart analyze lib/ — geen issues
    - flutter test — alle bestaande tests slagen nog steeds
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschrijving |
|----------|-------------|
| router extra → RideDetailScreen | `state.extra` wordt gecast naar `DetailArgs` zonder runtime type-check |

## STRIDE Threat Register

| Threat ID | Categorie | Component | Dispositie | Mitigatieplan |
|-----------|-----------|-----------|------------|---------------|
| T-05-01 | Tampering | `state.extra as DetailArgs` in router | mitigeer | Voeg null-check toe: `if (state.extra is! DetailArgs) return error-scaffold`; nooit blinde cast in productie |
| T-05-02 | Denial of Service | forecast-filtering O(n×m) per tap | accepteer | Max 168 uren (7×24) — verwaarloosbaar; geen caching nodig |
| T-05-03 | Information Disclosure | slot-data in navigatie-extra (geheugen) | accepteer | Geen PII — weersdata en scores zijn niet privacygevoelig |
| T-05-SC | Tampering | npm/pip/cargo installs | n.v.t. | Geen nieuwe pakketinstallaties in Wave 1 |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
dart analyze lib/
flutter test --reporter=compact
grep -c "context.go.*detail" lib/features/home/home_screen.dart
grep -c "GoRoute.*detail" lib/app/router.dart
grep -c "ScoreBadge" lib/features/home/home_screen.dart
```
Verwachte uitvoer: geen analyze-fouten, alle tests groen, telwaarden > 0.
</verification>

<success_criteria>
1. `lib/domain/models/hourly_row.dart` bestaat — HourlyRow class + buildHourlyRows functie
2. `lib/features/detail/detail_args.dart` bestaat — DetailArgs class
3. `lib/features/shared/score_badge.dart` bestaat — ScoreBadge widget
4. `lib/app/router.dart` bevat een GoRoute voor /detail
5. HomeScreen ride cards navigeren naar /detail bij tikken
6. HomeScreen weather chips tonen echte gemiddelde weerswaarden (geen "?°C" meer)
7. `dart analyze lib/` — geen issues
8. `flutter test` — alle 115+ bestaande tests slagen
</success_criteria>

<output>
Maak `.planning/phases/05-ui-phase-b-ride-detail-insights-sheet/05-01-SUMMARY.md` aan na voltooiing.
</output>
