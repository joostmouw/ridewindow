---
phase: 16-pwa-installability-ios-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/manifest.json
  - web/index.html
  - web/favicon.png
  - web/icons/Icon-192.png
  - web/icons/Icon-512.png
  - web/icons/Icon-maskable-192.png
  - web/icons/Icon-maskable-512.png
  - web/icons/Icon-apple-touch-180.png
  - web/splash/apple-splash-750x1334.png
  - web/splash/apple-splash-1170x2532.png
  - web/splash/apple-splash-1179x2556.png
  - web/splash/apple-splash-1284x2778.png
  - web/splash/apple-splash-1290x2796.png
  - test/web/pwa_install_meta_test.dart
autonomous: true
requirements: [PWA-01, PWA-02]

must_haves:
  truths:
    - "Adding RideWindow to an iPhone Home Screen shows the real black-rounded-square/white-'RW'-monogram icon, not the default Flutter logo placeholder"
    - "Launching the installed app from the Home Screen icon briefly shows a branded splash (RideWindow icon on a flat cycling-green background), not a blank white flash"
    - "manifest.json and index.html advertise RideWindow's real name/description/brand color, not Flutter's template placeholders"
    - "Flutter's Material widgets (AppBar, SafeArea, NavigationBar) can detect real device safe-area insets on iOS Safari, because viewport-fit=cover is present"
  artifacts:
    - path: web/icons/Icon-512.png
      provides: "Branded 512x512 app icon cropped from photos/main app logo real.png (D-01)"
    - path: web/icons/Icon-apple-touch-180.png
      provides: "180x180 apple-touch-icon, branded (D-02)"
    - path: web/manifest.json
      provides: "RideWindow name/short_name/description + theme_color/background_color = #2E7D32 (cycling green, D-03) + icons array"
    - path: web/index.html
      provides: "apple-touch-icon, apple-touch-startup-image (x5), apple-mobile-web-app-capable, theme-color/background-color meta, viewport-fit=cover"
    - path: web/splash/apple-splash-1170x2532.png
      provides: "One of 5 representative iPhone splash screen sizes (D-03 Claude's Discretion: representative subset)"
  key_links:
    - from: web/index.html
      to: web/icons/Icon-apple-touch-180.png
      via: "apple-touch-icon link href"
      pattern: "apple-touch-icon.*Icon-apple-touch-180"
    - from: web/index.html
      to: web/splash/*.png
      via: "apple-touch-startup-image link tags with device media queries"
      pattern: "apple-touch-startup-image"
    - from: web/manifest.json
      to: web/icons/Icon-192.png
      via: "manifest icons array"
      pattern: "Icon-192.png"
---

<objective>
Replace every generic Flutter-placeholder PWA asset (icons, splash screens, manifest metadata) with branded artwork derived from the canonical RideWindow logo, and add the iOS meta tags required for a correct Home Screen icon, splash screen, and safe-area detection.

Purpose: Satisfies PWA-01 (branded icon/splash tags) and the asset/meta half of PWA-02 (standalone display config + the foundational `viewport-fit=cover` tag that makes `env(safe-area-inset-*)` non-zero on iOS Safari — without it, Flutter's own `MediaQuery.padding`-based safe-area logic used across every existing screen's `AppBar`/`SafeArea` silently reports zero insets on web).
Output: Regenerated `web/icons/*.png` + `web/favicon.png`, new `web/splash/*.png` (5 files), updated `web/manifest.json` and `web/index.html`, and a static-content regression test (`test/web/pwa_install_meta_test.dart`) that fails if any placeholder value is reintroduced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-pwa-installability-ios-polish/16-CONTEXT.md
@web/index.html
@web/manifest.json
@test/web/index_html_meta_tag_test.dart
</context>

<interfaces>
<!-- No Dart interfaces involved — this plan only touches static web assets (PNG/HTML/JSON) plus a
     Dart test file that reads them via dart:io File, matching the existing pattern below. -->

From test/web/index_html_meta_tag_test.dart (existing sibling test — follow this exact style,
do not duplicate its CAL-06 assertions, add a new file instead):
```
import 'dart:io';
import 'package:flutter_test/flutter_test.dart';
void main() {
  test('...', () {
    final contents = File('web/index.html').readAsStringSync();
    expect(contents, contains('...'));
  });
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Generate branded icon + splash PNG assets from the canonical logo</name>
  <files>
    web/icons/Icon-192.png, web/icons/Icon-512.png, web/icons/Icon-maskable-192.png, web/icons/Icon-maskable-512.png, web/icons/Icon-apple-touch-180.png, web/favicon.png, web/splash/apple-splash-750x1334.png, web/splash/apple-splash-1170x2532.png, web/splash/apple-splash-1179x2556.png, web/splash/apple-splash-1284x2778.png, web/splash/apple-splash-1290x2796.png
  </files>
  <read_first>
    photos/main app logo real.png
    .planning/phases/16-pwa-installability-ios-polish/16-CONTEXT.md
  </read_first>
  <action>
    Write a one-off python3 script (Pillow is already installed; `python3 -c "import PIL; print(PIL.__version__)"` confirms 11.3.0 is available) that reads `photos/main app logo real.png` (2180x1920 RGBA, per D-01/D-02 the ONLY correct source — never `android/app/src/main/res/mipmap-xxxhdpi/ic_launcher_foreground.png`) and crops it to the pixel box `(483, 222, 1987, 1727)` — this is the tight bounding box of the black rounded-square logo mark (measured directly from the source file; it is 1504x1505px, effectively square), removing the white margin around it. Call this cropped image `base_icon`.

    Generate these outputs from `base_icon` using `Image.resize(..., Image.LANCZOS)`:
    - `web/icons/Icon-192.png`: direct resize of `base_icon` to 192x192.
    - `web/icons/Icon-512.png`: direct resize of `base_icon` to 512x512.
    - `web/icons/Icon-apple-touch-180.png`: direct resize of `base_icon` to 180x180.
    - `web/favicon.png`: direct resize of `base_icon` to 32x32 (upgrade from the current 16x16 default-Flutter placeholder).
    - `web/icons/Icon-maskable-192.png` and `web/icons/Icon-maskable-512.png`: maskable icons need a safe zone — create a new square canvas filled solid `#000000` (matches the logo's own black fill, so the seam is invisible) at the target size (192 or 512), then resize `base_icon` down to 80% of that canvas size and paste it centered. This leaves a 10% margin on every side so OS mask shapes (circle/squircle) never clip the white "RW" monogram.
    - The 5 splash screens under `web/splash/`, each a flat canvas filled solid `#2E7D32` (RideWindow's Material 3 seed / cycling-green brand color — see `lib/theme/app_colors.dart` `AppColors.seed`, NOT the placeholder `#0175C2` currently in manifest.json) at these exact pixel dimensions, with `base_icon` resized to 32% of the canvas WIDTH (square) and centered on the canvas:
      - `apple-splash-750x1334.png` — 750x1334 (iPhone SE/8 class, @2x)
      - `apple-splash-1170x2532.png` — 1170x2532 (iPhone 12/13/14 class, @3x)
      - `apple-splash-1179x2556.png` — 1179x2556 (iPhone 14 Pro/15/16 class, @3x)
      - `apple-splash-1284x2778.png` — 1284x2778 (iPhone 12/13/14 Pro Max class, @3x)
      - `apple-splash-1290x2796.png` — 1290x2796 (iPhone 14/15/16 Pro Max class, @3x)

    This is a deliberately representative subset (5 device-size classes), not an exhaustive matrix, per D-03's "Claude's Discretion" — favor correctness on whatever device is used for the real-iPhone check in Plan 16-04/PWA-05 over covering every historical iPhone model.

    Overwrite the existing placeholder files at these exact paths (same filenames as today so `web/manifest.json`'s existing icon array entries keep working without a path change).
  </action>
  <acceptance_criteria>
    All 11 output files exist at the exact paths listed in `<files>`.
    Each non-splash icon file has the exact pixel dimensions named above (192, 512, 180, 32).
    Each splash file has the exact pixel dimensions named in its filename.
    The center pixel of every icon file is near-black (each RGB channel &lt; 60) — proves it is the branded dark icon, not the default Flutter logo (whose center is not dark).
    The corner pixel (0,0) of every splash file is close to `#2E7D32` (R≈46, G≈125, B≈50, each within ±10) — proves the flat brand-color background was actually applied.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c "
from PIL import Image
icons = {'web/icons/Icon-192.png':192,'web/icons/Icon-512.png':512,'web/icons/Icon-maskable-192.png':192,'web/icons/Icon-maskable-512.png':512,'web/icons/Icon-apple-touch-180.png':180,'web/favicon.png':32}
for path, size in icons.items():
    im = Image.open(path).convert('RGB')
    assert im.size == (size, size), f'{path} wrong size {im.size}'
    r,g,b = im.getpixel((size//2, size//2))
    assert r < 60 and g < 60 and b < 60, f'{path} center not dark: {(r,g,b)}'
splashes = {'web/splash/apple-splash-750x1334.png':(750,1334),'web/splash/apple-splash-1170x2532.png':(1170,2532),'web/splash/apple-splash-1179x2556.png':(1179,2556),'web/splash/apple-splash-1284x2778.png':(1284,2778),'web/splash/apple-splash-1290x2796.png':(1290,2796)}
for path, size in splashes.items():
    im = Image.open(path).convert('RGB')
    assert im.size == size, f'{path} wrong size {im.size}'
    r,g,b = im.getpixel((0,0))
    assert abs(r-46)<=10 and abs(g-125)<=10 and abs(b-50)<=10, f'{path} corner not brand green: {(r,g,b)}'
print('OK: all icon and splash assets correct')
"
    </automated>
  </verify>
  <done>All 11 files exist with correct dimensions; icon centers are dark (branded, not the default Flutter logo); splash corners are the `#2E7D32` brand color.</done>
</task>

<task type="auto">
  <name>Task 2: Wire manifest.json + index.html to the new branded assets and add iOS/safe-area meta tags</name>
  <files>web/manifest.json, web/index.html, test/web/pwa_install_meta_test.dart</files>
  <read_first>
    web/manifest.json
    web/index.html
    test/web/index_html_meta_tag_test.dart
    .planning/phases/16-pwa-installability-ios-polish/16-CONTEXT.md
  </read_first>
  <action>
    In `web/manifest.json`: change `"name"` to `"RideWindow"`, `"short_name"` to `"RideWindow"`, `"description"` to `"Cyclist-specific weather windows and ride planning"` (replacing the Flutter-template placeholders flagged in CONTEXT.md). Change `"theme_color"` and `"background_color"` from `"#0175C2"` to `"#2E7D32"` (the Material 3 seed / cycling-green brand color, per D-03 — confirmed via `lib/theme/app_colors.dart`'s `AppColors.seed = Color(0xFF2E7D32)`, NOT a third new color). Leave `"display": "standalone"` and the existing `icons` array (paths unchanged, Task 1 overwrote the file contents in place) as-is.

    In `web/index.html`'s `<head>`:
    1. Add a new `<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover">` tag (currently there is NO viewport meta tag at all). This is the foundational tag: Safari only exposes non-zero `env(safe-area-inset-*)` values when `viewport-fit=cover` is present, and Flutter Web's `MediaQuery.padding` (already consumed correctly by the existing `AppBar`/`SliverAppBar`/`SafeArea` widgets across `HomeScreen`, `RideDetailScreen`, `WelcomeScreen`, etc.) depends on this same mechanism internally — do not add any competing manual inset-padding CSS to Flutter's own host element, only this meta tag is needed for Flutter's existing widgets to start working correctly.
    2. Add `<meta name="apple-mobile-web-app-capable" content="yes">` (the legacy Apple-prefixed tag; the non-prefixed `mobile-web-app-capable` already exists and should stay).
    3. Add `<meta name="theme-color" content="#2E7D32">` and `<meta name="background-color" content="#2E7D32">` (per ROADMAP Phase 16 success criterion #1's literal wording).
    4. Replace the existing `<link rel="apple-touch-icon" href="icons/Icon-192.png">` with `<link rel="apple-touch-icon" href="icons/Icon-apple-touch-180.png">` (the new dedicated 180x180 asset from Task 1, per D-02).
    5. Add 5 `<link rel="apple-touch-startup-image" href="splash/apple-splash-{W}x{H}.png" media="(device-width: {DW}px) and (device-height: {DH}px) and (-webkit-device-pixel-ratio: {DPR}) and (orientation: portrait)">` tags, one per Task 1 splash file, using these exact media values: 750x1334→(375,667,2); 1170x2532→(390,844,3); 1179x2556→(393,852,3); 1284x2778→(428,926,3); 1290x2796→(430,932,3).
    6. Add a small `<style>` block inside `<head>` setting `html, body { margin: 0; background-color: #2E7D32; padding: env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left); }` — this is real CSS using `env(safe-area-inset-*)` (satisfying REQUIREMENTS.md PWA-02's literal wording) applied to the plain HTML body wrapper (which sits behind Flutter's own fixed-position canvas host and is only ever visible during iOS Safari's elastic overscroll/bounce), so it cannot double-apply padding on top of Flutter's own MediaQuery-driven insets.

    Do not change `apple-mobile-web-app-status-bar-style` (already `"black"`, not flagged as incorrect) or the Google Sign-In meta tag.

    Create `test/web/pwa_install_meta_test.dart` (new file, sibling to `test/web/index_html_meta_tag_test.dart`, same `dart:io` File-read style) asserting: `web/index.html` contains `viewport-fit=cover`, `apple-mobile-web-app-capable`, `Icon-apple-touch-180.png`, all 5 `apple-splash-` filenames, and does NOT contain the old `Icon-192.png` apple-touch-icon reference; and `web/manifest.json` contains `"RideWindow"` and `"#2E7D32"` and does NOT contain `"A new Flutter project."` or `"#0175C2"`.
  </action>
  <acceptance_criteria>
    `web/manifest.json` has `name`/`short_name` = "RideWindow", `theme_color`/`background_color` = "#2E7D32", no remaining Flutter placeholder text.
    `web/index.html` has a `viewport-fit=cover` meta tag, `apple-mobile-web-app-capable`, an `apple-touch-icon` pointing at `Icon-apple-touch-180.png`, and 5 `apple-touch-startup-image` link tags with the exact media-query values listed above.
    `test/web/pwa_install_meta_test.dart` passes and would fail if any of the above were reverted.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/web/pwa_install_meta_test.dart test/web/index_html_meta_tag_test.dart</automated>
  </verify>
  <done>manifest.json and index.html reference only the new branded assets and real iOS meta tags; the regression test passes.</done>
</task>

</tasks>

<verification>
Run `flutter test test/web/` (both meta-tag tests green) and the Task 1 python3 pixel-check script. Then `flutter build web --release` must still complete with zero errors (regression: confirms the new/renamed asset references in index.html/manifest.json don't break the build), and `flutter build apk` must still succeed unchanged (Android is unaffected by this plan — it touches only `web/`).
</verification>

<success_criteria>
`web/index.html` and `web/manifest.json` reference only branded RideWindow assets derived from `photos/main app logo real.png`; no default-Flutter-logo or `#0175C2`/"A new Flutter project." placeholder remains anywhere in the PWA scaffold; `viewport-fit=cover` is present so Flutter's existing safe-area-aware widgets can function correctly on iOS Safari.
</success_criteria>

<output>
Create `.planning/phases/16-pwa-installability-ios-polish/16-01-SUMMARY.md` when done
</output>
