---
phase: 07-location-gps-manual-city-permission-state-machine
plan: 05
type: execute
wave: 5
depends_on:
  - 07-04
files_modified:
  - test/features/profile_screen_location_test.dart
  - test/features/home_screen_location_test.dart
autonomous: true
requirements:
  - LOC-01
  - LOC-02
  - LOC-03
  - LOC-04
  - LOC-05

must_haves:
  truths:
    - "ProfileScreen toont 'GPS (automatisch)' als locationOverride null is"
    - "ProfileScreen toont stadsnaam als locationOverride 'Rotterdam' is"
    - "ProfileScreen toont GPS-geblokkeerd banner als toestemming deniedForever is"
    - "ProfileScreen toont 'Instellingen openen' knop in banner bij deniedForever"
    - "ProfileScreen toont wis-knop (IconButton met Icons.clear) als override actief is"
    - "HomeScreen toont '...' als locationProvider nog laadt"
    - "HomeScreen toont 'Rotterdam' als locationProvider AsyncData met city='Rotterdam' is"
    - "Alle Phase 7 widget-tests slagen in een flutter test run"
  artifacts:
    - path: "test/features/profile_screen_location_test.dart"
      provides: "Widget-tests voor locatie-UI in ProfileScreen"
      contains: "FakeGpsPermissionNotifier"
    - path: "test/features/home_screen_location_test.dart"
      provides: "Widget-tests voor locatienaam in HomeScreen header"
      contains: "FakeLocationNotifier"
  key_links:
    - from: "test/features/profile_screen_location_test.dart"
      to: "lib/features/profile/profile_screen.dart"
      via: "ProviderScope overrides"
      pattern: "profileProvider.overrideWith"
    - from: "test/features/home_screen_location_test.dart"
      to: "lib/features/home/home_screen.dart"
      via: "ProviderScope overrides"
      pattern: "locationProvider.overrideWith"
---

<objective>
Schrijf widget-tests voor de locatie-UI in ProfileScreen en HomeScreen. Tests gebruiken FakeNotifier-subklassen (extends concrete klasse, niet _$abstract) conform het gevestigde patroon uit Phase 3/6.

Purpose: Verifieerbare dekking van alle LOC-vereisten via geautomatiseerde widget-tests. Sluit Phase 7 af met een groen test-suite gate.
Output: test/features/profile_screen_location_test.dart + test/features/home_screen_location_test.dart.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-location-gps-manual-city-permission-state-machine/07-CONTEXT.md
@.planning/phases/07-location-gps-manual-city-permission-state-machine/07-PATTERNS.md
@lib/features/profile/profile_screen.dart
@lib/features/home/home_screen.dart
@lib/providers/gps_permission_notifier.dart
@lib/providers/location_provider.dart
@lib/providers/profile_notifier.dart

<interfaces>
FakeNotifier patronen (uit Phase 6 PATTERNS.md en STATE.md):

```dart
// FakeProfileNotifier (al gebruikt in bestaande tests)
class FakeProfileNotifier extends ProfileNotifier {
  final UserProfile fakeProfile;
  FakeProfileNotifier(this.fakeProfile);
  @override
  Future<UserProfile> build() async => fakeProfile;
}

// Nieuw voor Phase 7:
class FakeGpsPermissionNotifier extends GpsPermissionNotifier {
  final LocationPermission fakePermission;
  FakeGpsPermissionNotifier(this.fakePermission);
  @override
  Future<LocationPermission> build() async => fakePermission;
}

class FakeLocationNotifier extends LocationNotifier {
  final LocationData fakeLocation;
  FakeLocationNotifier(this.fakeLocation);
  @override
  Future<LocationData> build() async => fakeLocation;
}
```

ProviderScope patroon:
```dart
await tester.pumpWidget(
  ProviderScope(
    overrides: [
      profileProvider.overrideWith(() => FakeProfileNotifier(testProfile)),
      gpsPermissionProvider.overrideWith(() => FakeGpsPermissionNotifier(LocationPermission.whileInUse)),
      locationProvider.overrideWith(() => FakeLocationNotifier(testLocation)),
      // Voeg ook weatherProvider + slotsProvider toe voor HomeScreen tests
    ],
    child: MaterialApp(home: const ProfileScreen()),
  ),
);
await tester.pump();
await tester.pump(const Duration(milliseconds: 100));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Taak 1: ProfileScreen locatie widget-tests</name>
  <files>test/features/profile_screen_location_test.dart</files>
  <action>
    Maak `test/features/profile_screen_location_test.dart` aan. Vereis: `SharedPreferences.setMockInitialValues({})` in setUp().

    Importeer: flutter_test, flutter_riverpod, shared_preferences, geolocator, en alle relevante app-providers en schermen.

    Definieer FakeGpsPermissionNotifier en FakeLocationNotifier als lokale klassen in het testbestand (extends concrete notifier, niet _$abstract).

    Definieer ook een minimale FakeWeatherNotifier (extends WeatherNotifier) die `Future<List<HourlyForecast>> build() async => []` retourneert, zodat ProfileScreen via ProviderScope kan bouwen zonder echte HTTP-aanroepen.

    Definieer een helper `UserProfile baseProfile({String? locationOverride})` die een standaard UserProfile retourneert met optionele locationOverride.

    Schrijf de volgende 5 tests:

    TEST 1 — "toont GPS (automatisch) als geen override actief":
    Override profileProvider met locationOverride: null, gpsPermissionProvider met whileInUse.
    Pump ProfileScreen. Verwacht `find.text('GPS (automatisch)')` findsOneWidget.

    TEST 2 — "toont stadsnaam als override actief":
    Override profileProvider met locationOverride: 'Rotterdam'.
    Pump ProfileScreen. Verwacht `find.text('Rotterdam')` findsOneWidget.

    TEST 3 — "toont wis-knop als override actief":
    Override profileProvider met locationOverride: 'Groningen'.
    Pump ProfileScreen. Verwacht `find.byIcon(Icons.clear)` findsOneWidget.

    TEST 4 — "toont geen wis-knop als geen override actief":
    Override profileProvider met locationOverride: null.
    Pump ProfileScreen. Verwacht `find.byIcon(Icons.clear)` findsNothing.

    TEST 5 — "toont GPS-geblokkeerd banner als toestemming deniedForever is":
    Override gpsPermissionProvider met deniedForever.
    Pump ProfileScreen. Verwacht `find.text('Locatie-toegang geblokkeerd')` findsOneWidget.
    Verwacht ook `find.text('Instellingen openen')` findsOneWidget.

    Gebruik `await tester.pump()` gevolgd door `await tester.pump(const Duration(milliseconds: 100))` — niet pumpAndSettle (async providers).
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/features/profile_screen_location_test.dart --reporter=compact</automated>
  </verify>
  <done>Alle 5 ProfileScreen locatie-tests slagen; geen 'pumpAndSettle timeout' fouten.</done>
</task>

<task type="auto">
  <name>Taak 2: HomeScreen locatienaam widget-tests</name>
  <files>test/features/home_screen_location_test.dart</files>
  <action>
    Maak `test/features/home_screen_location_test.dart` aan. Vereis: `SharedPreferences.setMockInitialValues({})` in setUp().

    Definieer dezelfde FakeLocationNotifier (extends LocationNotifier) als in Taak 1, maar nu als lokale klasse in dit testbestand.

    Definieer ook FakeStaticSlotsNotifier (extends SlotsNotifier) die `SlotsLoaded(slots: [], reason: null)` retourneert. Kijk naar de bestaande home_screen_test.dart voor het exacte patroon dat al gebruikt wordt — hergebruik dat.

    Definieer ook FakeWeatherNotifier (extends WeatherNotifier) met lege forecast lijst.

    Schrijf 2 tests:

    TEST 1 — "toont stadsnaam in header als locationProvider data heeft":
    Override locationProvider met FakeLocationNotifier(LocationData(lat: 51.92, lon: 4.48, city: 'Rotterdam')).
    Pump HomeScreen. Verwacht `find.text('Rotterdam')` findsOneWidget.

    TEST 2 — "toont placeholder als locationProvider nog laadt":
    Voor deze test kan FakeLocationNotifier worden gebruikt die een Future retourneert die nooit completeert (of die een vertraging heeft). Alternatief: test dat '...' wordt getoond als locationAsync.valueOrNull is null — dit kan gedaan worden door een Notifier die `async { await Future.delayed(Duration(seconds: 60)); return defaultLocation; }` retourneert en daarna alleen `await tester.pump()` (niet wachten op de future).
    Verwacht `find.text('...')` findsOneWidget na één pump zonder await.

    Alle ProviderScope overrides moeten ook profileProvider, slotsProvider, weatherProvider bevatten (gebruik bestaande Fake-klassen uit home_screen_test.dart of definieer ze lokaal opnieuw).
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/features/home_screen_location_test.dart --reporter=compact && flutter test --reporter=compact 2>&1 | tail -5</automated>
  </verify>
  <done>Beide HomeScreen locatie-tests slagen; volledige test suite slaagt na alle Phase 7 wijzigingen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → Fake providers | FakeNotifiers mogen geen echte GPS of SharedPreferences aanroepen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05-01 | Tampering | Test FakeNotifiers die real services aanroepen | mitigate | Alle FakeNotifiers overschrijven build() en retourneren direct een waarde; geen echte Geolocator of network aanroepen in tests |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
# Alleen de nieuwe testbestanden
flutter test test/features/profile_screen_location_test.dart --reporter=compact
flutter test test/features/home_screen_location_test.dart --reporter=compact
# Volledige suite — alles moet groen zijn
flutter test --reporter=compact 2>&1 | tail -10
```
</verification>

<success_criteria>
1. test/features/profile_screen_location_test.dart bevat 5 tests; alle slagen
2. test/features/home_screen_location_test.dart bevat 2 tests; beide slagen
3. Volledige flutter test suite slaagt — geen regressies door Phase 7 wijzigingen
4. LOC-01 t/m LOC-05 zijn aantoonbaar gedekt door tests:
   - LOC-01: gpsPermissionProvider.requestPermission() aangeroepen in TEST 2 ProfileScreen
   - LOC-02: GPS-locatie retourneer-pad getest in TEST 1 HomeScreen
   - LOC-03: Stad-keuze opgeslagen via setLocationOverride() (TEST 2 ProfileScreen)
   - LOC-04: deniedForever banner getest (TEST 5 ProfileScreen)
   - LOC-05: Override heeft voorrang (TEST 2 ProfileScreen — stadsnaam getoond)
</success_criteria>

<output>
Maak `.planning/phases/07-location-gps-manual-city-permission-state-machine/07-05-SUMMARY.md` aan als klaar.
</output>
