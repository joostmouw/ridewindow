---
phase: 07-location-gps-manual-city-permission-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - android/app/build.gradle.kts
  - android/app/src/main/AndroidManifest.xml
  - lib/core/nl_cities.dart
autonomous: true
requirements:
  - LOC-01
  - LOC-03

must_haves:
  truths:
    - "flutter pub get lost zonder fouten na toevoeging van geolocator + permission_handler"
    - "AndroidManifest.xml bevat ACCESS_FINE_LOCATION en ACCESS_COARSE_LOCATION permissies"
    - "compileSdk is 35 in build.gradle.kts (vereiste permission_handler 12.x)"
    - "kNlCities bevat 12 NL steden met correcte lat/lon"
  artifacts:
    - path: "lib/core/nl_cities.dart"
      provides: "Gecureerde NL steden-lijst als const List<NlCity>"
      contains: "kNlCities"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "Android locatie-permissies"
      contains: "ACCESS_FINE_LOCATION"
    - path: "android/app/build.gradle.kts"
      provides: "compileSdk 35 override"
      contains: "compileSdk = 35"
  key_links:
    - from: "pubspec.yaml"
      to: "geolocator: 14.0.2"
      via: "dependencies sectie"
      pattern: "geolocator"
    - from: "pubspec.yaml"
      to: "permission_handler: 12.0.3"
      via: "dependencies sectie"
      pattern: "permission_handler"
---

<objective>
Voeg geolocator en permission_handler toe aan pubspec.yaml, verhoog compileSdk naar 35 in build.gradle.kts, voeg Android locatie-permissies toe aan AndroidManifest.xml, en maak de NL steden-constante aan in lib/core/nl_cities.dart.

Purpose: Foundation voor alle GPS-logica in Wave 2 en 3. Zonder deze packages en Android-configuratie compileert de GPS-code niet.
Output: Werkende flutter pub get, correcte Android-manifest, NL steden-constante gereed voor gebruik.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-location-gps-manual-city-permission-state-machine/07-CONTEXT.md
@.planning/phases/07-location-gps-manual-city-permission-state-machine/07-PATTERNS.md
@pubspec.yaml
@android/app/build.gradle.kts
@android/app/src/main/AndroidManifest.xml
@lib/core/config.dart
</context>

<tasks>

<task type="auto">
  <name>Taak 1: pubspec.yaml + build.gradle.kts + AndroidManifest.xml</name>
  <files>pubspec.yaml, android/app/build.gradle.kts, android/app/src/main/AndroidManifest.xml</files>
  <action>
    Voer drie wijzigingen door:

    1. pubspec.yaml — voeg toe onder de `# Phase 4 — Routing` blok een nieuw blok:
       ```
       # Phase 7 — GPS + locatie-toestemmingen
       geolocator: ^14.0.2
       permission_handler: ^12.0.3
       ```
       Voeg GEEN andere packages toe. Voer daarna `flutter pub get` uit.

    2. android/app/build.gradle.kts — vervang de regel:
       `compileSdk = flutter.compileSdkVersion`
       door:
       `compileSdk = 35`
       Dit is vereist door permission_handler 12.x (per D-07-11 en CLAUDE.md).

    3. android/app/src/main/AndroidManifest.xml — voeg twee permissie-regels toe DIRECT VOOR de `<application>` tag:
       ```xml
       <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
       <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
       ```
       Voeg deze toe binnen de `<manifest>` root, niet binnen `<application>`.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter pub get 2>&1 | tail -5 && grep -c "geolocator" pubspec.yaml && grep -c "permission_handler" pubspec.yaml && grep "compileSdk = 35" android/app/build.gradle.kts && grep "ACCESS_FINE_LOCATION" android/app/src/main/AndroidManifest.xml</automated>
  </verify>
  <done>flutter pub get slaagt zonder fouten; geolocator en permission_handler aanwezig in pubspec.yaml; compileSdk = 35 in build.gradle.kts; ACCESS_FINE_LOCATION en ACCESS_COARSE_LOCATION in AndroidManifest.xml.</done>
</task>

<task type="auto">
  <name>Taak 2: lib/core/nl_cities.dart — NL steden-constante</name>
  <files>lib/core/nl_cities.dart</files>
  <action>
    Maak een nieuw bestand aan `lib/core/nl_cities.dart` met een simpele `NlCity` data-klasse en de `kNlCities` constante lijst van 12 Nederlandse steden.

    De `NlCity` klasse heeft drie velden: `name` (String), `lat` (double), `lon` (double). Gebruik een `const` constructor.

    Steden met coördinaten (per D-07-05):
    - Amsterdam:   lat 52.3676, lon 4.9041
    - Rotterdam:   lat 51.9225, lon 4.4792
    - Den Haag:    lat 52.0705, lon 4.3007
    - Utrecht:     lat 52.0907, lon 5.1214
    - Eindhoven:   lat 51.4416, lon 5.4697
    - Groningen:   lat 53.2194, lon 6.5665
    - Tilburg:     lat 51.5555, lon 5.0913
    - Almere:      lat 52.3508, lon 5.2647
    - Breda:       lat 51.5719, lon 4.7683
    - Nijmegen:    lat 51.8425, lon 5.8372
    - Leiden:      lat 52.1601, lon 4.4970
    - Haarlem:     lat 52.3874, lon 4.6462

    Geen Riverpod, geen Flutter imports — puur Dart, net als `lib/core/config.dart`.
    Voeg Dutch comment toe bovenaan: `/// Gecureerde lijst van NL steden voor de stad-picker (LOC-03, v1).`
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart analyze lib/core/nl_cities.dart && grep -c "NlCity" lib/core/nl_cities.dart && grep -c "lat:" lib/core/nl_cities.dart</automated>
  </verify>
  <done>dart analyze geeft geen fouten; nl_cities.dart bevat NlCity klasse en kNlCities list met 12 entries; elke entry heeft name, lat en lon.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pubspec.yaml → pub.dev | Pakket-download van externe bron |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | Tampering | pubspec.yaml package versies | accept | geolocator 14.0.2 en permission_handler 12.0.3 zijn vastgelegd in CLAUDE.md als gevalideerde versies; pub.dev packages van baseflow.com (6k+ likes); geen verdere audit nodig voor deze fase |
| T-07-01-SC | Tampering | npm/pip/cargo installs | accept | Flutter pub.dev packages; baseflow.com publisher met hoge reputatie; vastgelegd in CLAUDE.md — geen slopcheck vereist |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
flutter pub get
grep "geolocator" pubspec.yaml
grep "permission_handler" pubspec.yaml
grep "compileSdk = 35" android/app/build.gradle.kts
grep "ACCESS_FINE_LOCATION" android/app/src/main/AndroidManifest.xml
grep "ACCESS_COARSE_LOCATION" android/app/src/main/AndroidManifest.xml
dart analyze lib/core/nl_cities.dart
grep -c "NlCity(" lib/core/nl_cities.dart
```
Alle commando's moeten slagen zonder fouten.
</verification>

<success_criteria>
1. flutter pub get lost op zonder conflicten na toevoeging van geolocator + permission_handler
2. AndroidManifest.xml bevat beide locatie-permissies binnen de manifest root
3. compileSdk = 35 staat in build.gradle.kts (niet flutter.compileSdkVersion)
4. lib/core/nl_cities.dart compileert clean met 12 NlCity entries in kNlCities
</success_criteria>

<output>
Maak `.planning/phases/07-location-gps-manual-city-permission-state-machine/07-01-SUMMARY.md` aan als klaar.
</output>
