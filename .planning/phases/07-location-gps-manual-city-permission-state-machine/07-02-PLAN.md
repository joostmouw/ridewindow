---
phase: 07-location-gps-manual-city-permission-state-machine
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - lib/providers/gps_permission_notifier.dart
  - lib/providers/gps_permission_notifier.g.dart
  - lib/providers/location_provider.dart
  - lib/providers/location_provider.g.dart
autonomous: true
requirements:
  - LOC-01
  - LOC-02
  - LOC-04
  - LOC-05

must_haves:
  truths:
    - "GpsPermissionNotifier laadt initiële toestemmingsstatus via Geolocator.checkPermission()"
    - "GpsPermissionNotifier.requestPermission() roept Geolocator.requestPermission() aan en update state"
    - "GpsPermissionNotifier.openSettings() opent app-instellingen via openAppSettings()"
    - "LocationNotifier levert LocationData vanuit city override als profileProvider.locationOverride != null (LOC-05)"
    - "LocationNotifier levert GPS-coördinaten als toestemming whileInUse of always is (LOC-02)"
    - "LocationNotifier valt terug op Amsterdam defaults als GPS geweigerd en geen override (LOC-04)"
    - "Gegenereerde providernamen zijn gpsPermissionProvider en locationProvider"
  artifacts:
    - path: "lib/providers/gps_permission_notifier.dart"
      provides: "GPS toestemmings-state machine"
      exports: ["GpsPermissionNotifier"]
    - path: "lib/providers/location_provider.dart"
      provides: "Echte LocationNotifier (vervangt stub)"
      exports: ["LocationData", "LocationNotifier"]
    - path: "lib/providers/gps_permission_notifier.g.dart"
      provides: "Gegenereerde code voor gpsPermissionProvider"
      contains: "gpsPermissionProvider"
    - path: "lib/providers/location_provider.g.dart"
      provides: "Gegenereerde code voor locationProvider"
      contains: "locationProvider"
  key_links:
    - from: "lib/providers/location_provider.dart"
      to: "lib/providers/gps_permission_notifier.dart"
      via: "ref.watch(gpsPermissionProvider.future)"
      pattern: "gpsPermissionProvider"
    - from: "lib/providers/location_provider.dart"
      to: "lib/providers/profile_notifier.dart"
      via: "ref.watch(profileProvider.future)"
      pattern: "profileProvider"
    - from: "lib/providers/location_provider.dart"
      to: "lib/core/nl_cities.dart"
      via: "kNlCities.firstWhere()"
      pattern: "kNlCities"
---

<objective>
Vervang de hardcoded location stub door een echte GPS permission state machine (GpsPermissionNotifier) en een echte LocationNotifier die de juiste locatie bepaalt op basis van: (1) city override uit profile, (2) GPS als toestemming verleend, (3) Amsterdam default als fallback.

Purpose: Implementeert LOC-01 (permission request), LOC-02 (GPS gebruik), LOC-04 (permanente weigering fallback), LOC-05 (override persistentie). Na deze wave reageert locationProvider op de echte toestemmingsstatus en profile-override.
Output: gps_permission_notifier.dart + location_provider.dart (beide met gegenereerde .g.dart bestanden).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-location-gps-manual-city-permission-state-machine/07-CONTEXT.md
@.planning/phases/07-location-gps-manual-city-permission-state-machine/07-PATTERNS.md
@lib/providers/location_provider.dart
@lib/providers/profile_notifier.dart
@lib/core/config.dart
@lib/core/nl_cities.dart

<interfaces>
Bestaande interfaces die deze providers consumeren of produceren:

Uit lib/providers/profile_notifier.dart:
```dart
class UserProfile {
  final String? locationOverride; // stadsnaam of null
  // setLocationOverride(String? location) async — persisteert naar SharedPreferences
}
// Gegenereerde naam: profileProvider
// profileProvider.future geeft Future<UserProfile>
```

Uit lib/core/config.dart:
```dart
const double kDefaultLat = 52.3676;
const double kDefaultLon = 4.9041;
const String kDefaultCity = 'Amsterdam';
```

Uit lib/core/nl_cities.dart (gecreëerd in Wave 1):
```dart
class NlCity {
  const NlCity({required this.name, required this.lat, required this.lon});
  final String name;
  final double lat;
  final double lon;
}
const List<NlCity> kNlCities = [ /* 12 steden */ ];
```

Huidige stub die VERVANGEN wordt:
```dart
// lib/providers/location_provider.dart (stub)
class LocationData {
  const LocationData({required this.lat, required this.lon, required this.city});
  final double lat;
  final double lon;
  final String city;
}

@riverpod
LocationData location(Ref ref) {
  return const LocationData(lat: kDefaultLat, lon: kDefaultLon, city: kDefaultCity);
}
```

Consumers die locationProvider gebruiken (mogen NIET aangepast worden in deze wave):
- lib/features/home/home_screen.dart — ref.watch(locationProvider) — verwacht LocationData
- lib/providers/weather_notifier.dart — ref.watch(locationProvider) — verwacht LocationData
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Taak 1: GpsPermissionNotifier — GPS toestemmings-state machine</name>
  <files>lib/providers/gps_permission_notifier.dart, test/providers/gps_permission_notifier_test.dart</files>
  <behavior>
    - build() geeft LocationPermission.denied terug als initiële state bij eerste run
    - requestPermission() update state naar het resultaat van Geolocator.requestPermission()
    - openSettings() roept openAppSettings() aan (geen return value, side effect)
    - FakeGpsPermissionNotifier (extends GpsPermissionNotifier) geeft een gefixte waarde terug voor tests
  </behavior>
  <action>
    Maak `lib/providers/gps_permission_notifier.dart` aan als een Riverpod 3.x `AsyncNotifier<LocationPermission>`.

    Import vereisten:
    - `package:geolocator/geolocator.dart` voor LocationPermission enum en Geolocator.checkPermission() / requestPermission()
    - `package:permission_handler/permission_handler.dart` voor openAppSettings()
    - `package:riverpod_annotation/riverpod_annotation.dart`

    Part directive: `part 'gps_permission_notifier.g.dart';`

    Annoteer de klasse met `@riverpod`. De klasse heet `GpsPermissionNotifier extends _$GpsPermissionNotifier`.

    Methoden:
    1. `build()` — async, roept `Geolocator.checkPermission()` aan en geeft het resultaat terug
    2. `requestPermission()` — async Future<LocationPermission>, roept `Geolocator.requestPermission()` aan, zet `state = AsyncData(result)`, geeft result terug
    3. `openSettings()` — async Future<void>, roept `openAppSettings()` aan (uit permission_handler)

    Schrijf daarna `test/providers/gps_permission_notifier_test.dart` met FakeGpsPermissionNotifier (extends GpsPermissionNotifier):
    - Test: build() geeft de geconfigureerde fakePermission terug
    - Test: gpsPermissionProvider initial state is AsyncData wanneer FakeNotifier gebruikt wordt
    Gebruik `ProviderContainer` + `SharedPreferences.setMockInitialValues({})` in setUp.

    Draai na implementatie: `flutter pub run build_runner build --delete-conflicting-outputs`
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart analyze lib/providers/gps_permission_notifier.dart && flutter test test/providers/gps_permission_notifier_test.dart --reporter=compact</automated>
  </verify>
  <done>dart analyze schoon; flutter test slaagt; gps_permission_notifier.g.dart bestaat en bevat gpsPermissionProvider.</done>
</task>

<task type="auto" tdd="true">
  <name>Taak 2: LocationNotifier — vervangt stub, implementeert prioriteitslogica</name>
  <files>lib/providers/location_provider.dart, test/providers/location_provider_test.dart</files>
  <behavior>
    - Als profileProvider.locationOverride == 'Rotterdam', geeft LocationData(lat: 51.9225, lon: 4.4792, city: 'Rotterdam') terug (LOC-05: override heeft voorrang)
    - Als locationOverride == null en toestemming is whileInUse, geeft GPS-coördinaten terug
    - Als locationOverride == null en toestemming is deniedForever, geeft LocationData(lat: kDefaultLat, lon: kDefaultLon, city: kDefaultCity) terug (LOC-04 fallback)
    - Als stad in override niet in kNlCities staat, valt terug op kNlCities.first (Amsterdam)
    - Gegenereerde naam is locationProvider (Notifier gestript door code-gen)
  </behavior>
  <action>
    Vervang de VOLLEDIGE inhoud van `lib/providers/location_provider.dart`. Behoud de `LocationData` klasse ongewijzigd (zelfde velden: lat, lon, city). Vervang de functionele `location(Ref ref)` provider door `LocationNotifier extends _$LocationNotifier`.

    LocationNotifier is een `AsyncNotifier<LocationData>`. Build-logica in volgorde:

    STAP 1: Wacht op profileProvider: `final profile = await ref.watch(profileProvider.future);`
    STAP 2: Als `profile.locationOverride != null`:
      - Zoek `kNlCities.firstWhere((c) => c.name == override, orElse: () => kNlCities.first)`
      - Return `LocationData(lat: city.lat, lon: city.lon, city: city.name)`
    STAP 3: Wacht op gpsPermissionProvider: `final permission = await ref.watch(gpsPermissionProvider.future);`
    STAP 4: Als toestemming is `whileInUse` of `always`:
      - Probeer `Geolocator.getCurrentPosition(locationSettings: const LocationSettings(accuracy: LocationAccuracy.reduced, timeLimit: Duration(seconds: 30)))`
      - Bij succes: return `LocationData(lat: pos.latitude, lon: pos.longitude, city: 'GPS')`
      - Bij Exception (timeout/fout): val door naar STAP 5
    STAP 5: Return `const LocationData(lat: kDefaultLat, lon: kDefaultLon, city: kDefaultCity)`

    Part directive: `part 'location_provider.g.dart';`

    Imports:
    - `package:geolocator/geolocator.dart`
    - `package:riverpod_annotation/riverpod_annotation.dart`
    - `package:ridewindow/core/config.dart`
    - `package:ridewindow/core/nl_cities.dart`
    - `package:ridewindow/providers/gps_permission_notifier.dart`
    - `package:ridewindow/providers/profile_notifier.dart`

    Schrijf `test/providers/location_provider_test.dart` met drie tests:
    1. City override aanwezig → juiste LocationData
    2. Geen override + permission denied → Amsterdam default
    3. Geen override + permission whileInUse → test dat build() completeert (GPS mock niet nodig; FakeGpsPermissionNotifier + FakeLocationNotifier pattern)

    Gebruik FakeLocationNotifier (extends LocationNotifier) voor de meeste tests.

    Draai na implementatie: `flutter pub run build_runner build --delete-conflicting-outputs`
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart analyze lib/providers/location_provider.dart && flutter test test/providers/location_provider_test.dart --reporter=compact && grep "locationProvider" lib/providers/location_provider.g.dart</automated>
  </verify>
  <done>dart analyze schoon; flutter test slaagt voor alle drie tests; location_provider.g.dart bevat locationProvider; HomeScreen en WeatherNotifier compileren nog steeds (zelfde providernaam en LocationData interface).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → GPS hardware | GPS-coördinaten komen van het OS; kunnen onjuist zijn in emulator |
| App → Android permissions system | Toestemmingsstatus beheerd door Android OS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Information Disclosure | GPS coördinaten | accept | Locatie wordt alleen intern gebruikt voor Open-Meteo API-aanroep; data verlaat het apparaat alleen naar Open-Meteo (privacy policy dekt dit) |
| T-07-02-02 | Denial of Service | Geolocator.getCurrentPosition timeout | mitigate | timeLimit: Duration(seconds: 30) in LocationSettings; try-catch in LocationNotifier.build() valt terug op Amsterdam default bij timeout |
| T-07-02-03 | Elevation of Privilege | Locatie-toestemming | mitigate | Vraag alleen ACCESS_FINE_LOCATION + ACCESS_COARSE_LOCATION; geen background locatie; geen always-toestemming vereist |
| T-07-02-SC | Tampering | geolocator + permission_handler packages | accept | Vastgelegd in CLAUDE.md als gevalideerde versies (baseflow.com, 6k+ likes); reeds geverifieerd in Wave 1 |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
dart analyze lib/providers/gps_permission_notifier.dart
dart analyze lib/providers/location_provider.dart
flutter test test/providers/gps_permission_notifier_test.dart --reporter=compact
flutter test test/providers/location_provider_test.dart --reporter=compact
grep "gpsPermissionProvider" lib/providers/gps_permission_notifier.g.dart
grep "locationProvider" lib/providers/location_provider.g.dart
# Controleer dat bestaande HomeScreen nog steeds compileert
dart analyze lib/features/home/home_screen.dart
```
</verification>

<success_criteria>
1. GpsPermissionNotifier bestaan met build(), requestPermission(), openSettings() methoden
2. LocationNotifier implementeert de drie-stappen prioriteitslogica: override > GPS > default
3. Gegenereerde bestanden gps_permission_notifier.g.dart en location_provider.g.dart bestaan
4. Alle provider-tests slagen
5. dart analyze lib/ rapporteert geen fouten gerelateerd aan de nieuwe providers
</success_criteria>

<output>
Maak `.planning/phases/07-location-gps-manual-city-permission-state-machine/07-02-SUMMARY.md` aan als klaar.
</output>
