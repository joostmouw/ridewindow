---
phase: 07-location-gps-manual-city-permission-state-machine
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - lib/features/home/home_screen.dart
autonomous: true
requirements:
  - LOC-02
  - LOC-03

must_haves:
  truths:
    - "HomeScreen toont de echte locatienaam (GPS of stad-naam) in de header, niet hardcoded 'Amsterdam'"
    - "Als locationProvider AsyncLoading is, toont de header een laad-indicator of placeholder tekst"
    - "Als locationProvider AsyncData is met city='GPS', toont de header 'GPS' of een GPS-icoon"
    - "Als locationProvider AsyncData is met city='Rotterdam', toont de header 'Rotterdam'"
    - "locationProvider is nu AsyncValue<LocationData> — HomeScreen gebruikt .when() of .valueOrNull"
  artifacts:
    - path: "lib/features/home/home_screen.dart"
      provides: "Header met dynamische stadsnaam van locationProvider"
      contains: "locationProvider"
  key_links:
    - from: "lib/features/home/home_screen.dart"
      to: "lib/providers/location_provider.dart"
      via: "ref.watch(locationProvider)"
      pattern: "locationProvider"
---

<objective>
Pas HomeScreen aan zodat de header de echte actieve locatienaam toont vanuit de (nu asynchrone) locationProvider. De huidige HomeScreen roept `location.city` aan op een synchrone LocationData — na Wave 2 is locationProvider een AsyncNotifier, dus de code moet worden bijgewerkt naar AsyncValue-handling.

Purpose: Na deze wave toont HomeScreen de echte locatienaam. Gebruikers zien direct welke stad de voorspelling gebruikt (LOC-02: echte GPS-locatie; LOC-03: gekozen stad).
Output: Bijgewerkte HomeScreen._buildHeader() die locationProvider als AsyncValue<LocationData> consumeert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-location-gps-manual-city-permission-state-machine/07-CONTEXT.md
@.planning/phases/07-location-gps-manual-city-permission-state-machine/07-PATTERNS.md
@lib/features/home/home_screen.dart
@lib/providers/location_provider.dart

<interfaces>
Uit lib/providers/location_provider.dart (Wave 2 implementatie):
```dart
// LocationNotifier is nu AsyncNotifier<LocationData>
// Gegenereerde naam: locationProvider
// ref.watch(locationProvider) geeft AsyncValue<LocationData>
// ref.watch(locationProvider).valueOrNull geeft LocationData? (null tijdens laden)
```

Huidig patroon in HomeScreen (MOET gewijzigd worden):
```dart
// Huidig (werkt niet meer na Wave 2 — locationProvider is nu async):
final location = ref.watch(locationProvider); // was synchrone LocationData
_buildHeader(location.city, weatherState)     // city was String, nu potentieel null
```

Gewenst patroon na Wave 4:
```dart
final locationAsync = ref.watch(locationProvider);
final cityName = locationAsync.valueOrNull?.city ?? '...';
// Of gebruik locationAsync.when() voor loading/error/data states
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Taak 1: HomeScreen header — dynamische locatienaam</name>
  <files>lib/features/home/home_screen.dart</files>
  <action>
    Lees lib/features/home/home_screen.dart volledig. Zoek de locatie waar `ref.watch(locationProvider)` wordt aangeroepen in de `build()` methode (ongeveer regel 59: `final location = ref.watch(locationProvider);`).

    Wijziging 1 — De watch aanroep in build():
    Vervang `final location = ref.watch(locationProvider);` door:
    ```dart
    final locationAsync = ref.watch(locationProvider);
    final cityName = locationAsync.valueOrNull?.city ?? '...';
    ```

    Wijziging 2 — De doorgave aan _buildHeader():
    Vervang `_buildHeader(location.city, weatherState)` door:
    ```dart
    _buildHeader(cityName, weatherState)
    ```

    De `_buildHeader(String city, ...)` methode signature verandert NIET — deze accepteert al een String. Alleen de aanroepsite wordt bijgewerkt.

    Optioneel: als de stad 'GPS' is (echte GPS-locatie actief), kan een klein GPS-icoon naast de naam worden getoond. Dit is optioneel en op eigen discretie van de uitvoerder. De `Row` in `_buildHeader` heeft al een `Icon(Icons.watch_later_outlined)` — dit kan worden vervangen door een voorwaardelijk icoon:
    ```dart
    Icon(
      cityName == 'GPS' ? Icons.gps_fixed : Icons.location_city,
      size: 18,
      color: const Color(0xFF666666),
    ),
    ```

    Controleer na de wijziging dat de rest van HomeScreen ongewijzigd blijft (week strip, ride cards, bottom nav).
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && dart analyze lib/features/home/home_screen.dart && grep "locationAsync" lib/features/home/home_screen.dart && grep "valueOrNull" lib/features/home/home_screen.dart && flutter test test/features/ --reporter=compact 2>&1 | tail -10</automated>
  </verify>
  <done>dart analyze schoon; HomeScreen gebruikt locationAsync.valueOrNull?.city; bestaande widget-tests slagen nog steeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| locationProvider → HomeScreen UI | Locatienaam wordt getoond aan gebruiker; mag niet crashen tijdens laden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04-01 | Denial of Service | locationProvider AsyncLoading → HomeScreen | mitigate | valueOrNull?.city ?? '...' geeft '...' als fallback tijdens laden; geen null-crash mogelijk |
| T-07-04-02 | Information Disclosure | Nauwkeurige GPS-coördinaten getoond | accept | Alleen de stad-naam ('GPS' of stadsnaam) wordt getoond, nooit de exacte coördinaten |
</threat_model>

<verification>
```bash
cd /Users/joostmouw/ridewindow
dart analyze lib/features/home/home_screen.dart
grep "locationAsync" lib/features/home/home_screen.dart
grep "valueOrNull" lib/features/home/home_screen.dart
# Alle bestaande tests moeten nog slagen
flutter test test/features/ --reporter=compact 2>&1 | tail -5
```
</verification>

<success_criteria>
1. HomeScreen._buildHeader() toont cityName uit locationProvider.valueOrNull?.city
2. Tijdens laden (AsyncLoading) toont de header '...' of een vergelijkbare placeholder
3. Na het laden toont de header de echte stadsnaam ('GPS', 'Rotterdam', 'Amsterdam', etc.)
4. Alle bestaande HomeScreen widget-tests slagen nog steeds
5. dart analyze rapporteert geen fouten op home_screen.dart
</success_criteria>

<output>
Maak `.planning/phases/07-location-gps-manual-city-permission-state-machine/07-04-SUMMARY.md` aan als klaar.
</output>
