---
phase: quick-260714-o54
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/features/detail/ride_detail_screen.dart, lib/domain/services/drag_run_counter.dart, test/domain/services/drag_run_counter_test.dart, lib/l10n/app_en.arb, lib/l10n/app_nl.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_en.dart, lib/l10n/app_localizations_nl.dart, lib/features/availability/availability_screen.dart, test/features/availability_screen_test.dart]
autonomous: false
requirements: [O54-01, O54-02]

must_haves:
  truths:
    - "On Ride Detail, the 'Plan ride' button is visible and tappable immediately on screen open, without scrolling past Adjust Time / Weather / Clothing tip / Hourly table"
    - "Tapping 'Plan ride' still adds the effective slot to plannedRidesProvider and shows the existing 'ridePlanned' SnackBar — unchanged behavior, only its position on screen changes"
    - "The other three action buttons (Add to Google Calendar, Remind me the evening before, Share this ride window) remain together in the scrollable content, unchanged in order or behavior"
    - "During an active drag-select gesture on the Availability screen, a live indicator shows the count of distinct contiguous same-day/consecutive-hour runs within the dragged cells, with correct NL singular/plural phrasing ('1 tijdvak geselecteerd' vs 'N losse tijdvakken geselecteerd')"
    - "The run-counting logic is a pure, unit-tested function with no BuildContext/widget dependency, and makes no claim about ride-slot/scoring-engine output"
    - "The indicator disappears as soon as the drag gesture ends"
  artifacts:
    - path: "lib/features/detail/ride_detail_screen.dart"
      provides: "_buildPlanRideBar pinned via Scaffold.bottomNavigationBar + _buildSecondaryActions holding the remaining 3 scrollable buttons"
      contains: "_buildPlanRideBar"
    - path: "lib/domain/services/drag_run_counter.dart"
      provides: "countSelectionRuns(Set<DateTime>) pure function"
      contains: "int countSelectionRuns"
    - path: "test/domain/services/drag_run_counter_test.dart"
      provides: "unit tests for countSelectionRuns covering empty/single/consecutive/gapped/cross-day cases"
      contains: "countSelectionRuns"
    - path: "lib/l10n/app_nl.arb"
      provides: "dragRunsSelected ICU plural string (NL, template file)"
      contains: "dragRunsSelected"
    - path: "lib/l10n/app_en.arb"
      provides: "dragRunsSelected ICU plural string (EN)"
      contains: "dragRunsSelected"
    - path: "lib/features/availability/availability_screen.dart"
      provides: "drag-run indicator wired above the grid using countSelectionRuns(_draggedCells)"
      contains: "countSelectionRuns(_draggedCells)"
  key_links:
    - from: "lib/features/detail/ride_detail_screen.dart build()"
      to: "Scaffold.bottomNavigationBar"
      via: "_buildPlanRideBar(context) assigned to bottomNavigationBar, outside the SingleChildScrollView"
      pattern: "bottomNavigationBar:\\s*_buildPlanRideBar"
    - from: "lib/features/availability/availability_screen.dart _buildGrid"
      to: "lib/domain/services/drag_run_counter.dart"
      via: "import + countSelectionRuns(_draggedCells) call while _isDragging"
      pattern: "countSelectionRuns\\(_draggedCells\\)"
    - from: "lib/features/availability/availability_screen.dart indicator Text"
      to: "S.of(context).dragRunsSelected"
      via: "ICU-plural localized string call"
      pattern: "dragRunsSelected\\("
---

<objective>
Bundle two corrected/scoped-down backlog items into one quick plan:

**Task A (backlog #34):** Make the real "Plan ride" button on the Ride Detail screen (`lib/features/detail/ride_detail_screen.dart`) sticky/always-visible at the bottom of the screen, instead of buried at the end of a `SingleChildScrollView` after Adjust Time, Weather, Clothing tip, and the Hourly table. The other three action buttons (Add to Google Calendar, Remind me the evening before, Share this ride window) stay in the scrollable content, unchanged.

**Task B (backlog #35, scoped down):** Add a live "N losse tijdvakken geselecteerd" indicator to the Availability screen (`lib/features/availability/availability_screen.dart`) during an active drag-select gesture, showing how many separate contiguous runs of same-day/consecutive-hour cells exist in the current drag selection. This is a pure selection-count indicator — it does NOT claim any relationship to the ride-slot/scoring engine (explicitly out of scope). The run-counting logic is a pure, unit-tested helper function.

Purpose: Reduce friction on the two highest-touch screens — get the primary CTA in front of the user without scrolling, and give immediate feedback about what a multi-region drag-select actually produced (discontiguous blocks are easy to create by accident on a 7×24 grid).
Output: Updated `ride_detail_screen.dart` with a pinned "Plan ride" bar; new `lib/domain/services/drag_run_counter.dart` pure function + unit tests; new NL/EN ARB strings; updated `availability_screen.dart` with a wired drag-run indicator + widget test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<interfaces>
<!-- Confirmed by reading the actual files (not assumed). -->

**Ride Detail current structure** (`lib/features/detail/ride_detail_screen.dart`): `build()` returns a `Scaffold` with `appBar: _buildAppBar(context)` and `body: SafeArea(child: Column(children: [Expanded(child: SingleChildScrollView(child: Column(children: [_buildTimeAdjuster(), _buildInfoCard(weather), _buildClothingTip(), _buildInfoCard(hourly), _buildActions(context)])))]))`. `_buildActions` (currently ~line 741) is a `Padding` wrapping a `Column` of 4 buttons in this order: `FilledButton.icon` "Plan ride" (calls `ref.read(plannedRidesProvider.notifier).add(PlannedRide(start: slot.start, end: slot.end, plannedScore: slot.overallScore))` then shows `SnackBar(content: Text(S.of(context).ridePlanned))`), `OutlinedButton.icon` "Add to Google Calendar" (`onPressed: _isLoading ? null : _addToCalendar`), `FilledButton.tonalIcon` "Remind me the evening before", `OutlinedButton.icon` "Share this ride window" (`onPressed: _shareSlot`). Each button (after the first) is preceded by `const SizedBox(height: 10)`.

**No existing `bottomNavigationBar` usage in this file.** The app's main shell (`lib/app/scaffold_with_nav.dart`) uses `bottomNavigationBar: NavigationBar(...)` for the primary nav — confirms `Scaffold.bottomNavigationBar` is an established, idiomatic slot in this codebase for pinned bottom UI, safe to reuse here without inventing a new pattern.

**Top-border divider convention** — `_buildWeatherRow` and `_buildHourlyRowWidget` in this same file use `Container(decoration: BoxDecoration(border: Border(top: BorderSide(color: rw.borderDim))))` as the visual separator between stacked sections. Reuse this exact token (`rw.borderDim`, from `context.rw` / `RideWindowTheme`) for the pinned bar's top divider so it reads as part of the same design system, not a bespoke shadow.

**Pre-existing broken test files (confirmed by running, unrelated to this plan — do not fix here):** `test/features/ride_detail_screen_test.dart` (13 tests) and `test/features/detail/ride_detail_screen_calendar_test.dart` (4 tests) both currently FAIL 100% (`flutter test` on either file shows all tests erroring) because they pump `RideDetailScreen` inside a bare `MaterialApp` with no `ProviderScope` ancestor and no `localizationsDelegates` — `RideDetailScreen` is a `ConsumerStatefulWidget` that needs a `ProviderScope`, and calls `S.of(context)` which needs the `S` localization delegate. This is pre-existing repo debt, not something Task A introduces or should attempt to fix. Do NOT extend or "fix" these files as part of this plan — verification for Task A is `flutter analyze` plus the human-verify checkpoint at the end of this plan (visual confirmation is the correct tool here since building working provider-override test infra for this screen is a separate, larger undertaking, out of scope for this bundle).

**Availability screen drag state** (`lib/features/availability/availability_screen.dart`): `_isDragging` (bool), `_draggedCells` (`Set<DateTime>`) are mutated in `_onDragStart`/`_onDragUpdate`/`_onDragEnd` (~lines 505-560), each wrapped in `setState`. `_cellKey(weekStart, dayIndex, hour)` returns `DateTime.utc(weekStart.year, weekStart.month, weekStart.day + dayIndex, hour)` — so distinct calendar days always have distinct `.day` values (never wraps within a displayed week), and the "hour" component is the literal 0-23 hour. `_buildGrid`'s `LayoutBuilder` computes `_cellWidth`/`_cellHeight` and currently does `final availableHeight = constraints.maxHeight - 140;` (140 = heuristic budget for the presets/legend/rider-profile rows below the grid) then `_cellHeight = (availableHeight - _headerHeight) / 24;` with a `_cellHeight < 16` clamp to 16. Private consts: `_headerWidth = 36`, `_headerHeight = 28`.

**l10n pattern** — `S.of(context)` from `package:ridewindow/l10n/app_localizations.dart`, generated by `flutter gen-l10n` (NOT hand-edited). `l10n.yaml`: `template-arb-file: app_nl.arb` (NL is the template/reference locale in this repo, carries the `@key` metadata blocks; `app_en.arb` mirrors keys without metadata), `output-class: S`. Every key must exist in BOTH `app_en.arb` and `app_nl.arb`. ICU plural syntax is already an established, working pattern in this repo — e.g. `app_nl.arb`: `"rideWindowCount": "{count} {count, plural, =1{rijvenster} other{rijvensters}} deze week"` with a matching `"@rideWindowCount": {"placeholders": {"count": {"type": "int"}}}` metadata block, and a plain-count (no plural variants needed) example `"agendaHoursSelected": "{count} uur geselecteerd"` used in `lib/features/agenda/week_agenda_screen.dart`. After editing the ARB files, run `flutter gen-l10n` to regenerate `lib/l10n/app_localizations.dart`, `app_localizations_en.dart`, `app_localizations_nl.dart` (all three are committed to git).

**Similar "N selected" indicator precedent** — `lib/features/agenda/week_agenda_screen.dart` (~line 285-310) shows an established convention for a live selection-count indicator during a drag/selection gesture: a `Row` containing `Icon(Icons.touch_app, size: 14, color: theme.colorScheme.primary)` + `SizedBox(width: 4)` + `Text(S.of(context).agendaHoursSelected(_selection!.count), style: TextStyle(fontSize: 12, fontWeight: FontWeight.w600, color: theme.colorScheme.primary))`, swapped in above the grid in place of the normal legend row while a selection is active. Mirror this icon+text visual style for the new Availability drag-run indicator (same icon, same font weight/size/color convention) for consistency across the two "live selection count" features in this app.

**No existing pure "run counting" or grouping helper anywhere in `lib/domain/services/`** (confirmed via directory listing: only `availability_filter.dart`, `scoring_engine.dart`, `slot_generator.dart` exist there) — this is a new file, not a refactor of an existing one.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin "Plan ride" to a bottomNavigationBar bar on Ride Detail</name>
  <files>lib/features/detail/ride_detail_screen.dart</files>
  <action>
  Rename the existing `_buildActions(BuildContext context)` method to `_buildSecondaryActions(BuildContext context)` and remove the first button from its `Column` — the `FilledButton.icon` "Plan ride" block (icon `Icons.directions_bike`, label `S.of(context).planRide`) together with the `const SizedBox(height: 10)` spacer that immediately follows it. `_buildSecondaryActions` should now start directly with the "Add to Google Calendar" `OutlinedButton.icon`, followed by the existing spacers/buttons for "Remind me the evening before" and "Share this ride window", unchanged.

  Add a new method `Widget _buildPlanRideBar(BuildContext context)` that returns a `Container` decorated with `color: context.rw.surface` and `decoration`'s `border: Border(top: BorderSide(color: context.rw.borderDim))` (reuse the same top-divider convention as `_buildWeatherRow`/`_buildHourlyRowWidget` in this file — combine border and color via a single `BoxDecoration`), `padding: const EdgeInsets.fromLTRB(16, 12, 16, 12)`, wrapping a `FilledButton.icon` that is functionally IDENTICAL to the removed one: same `icon: const Icon(Icons.directions_bike)`, same `label: Text(S.of(context).planRide)`, and the exact same `onPressed` body (read `_effectiveSlot`, call `ref.read(plannedRidesProvider.notifier).add(PlannedRide(start: slot.start, end: slot.end, plannedScore: slot.overallScore))`, then `ScaffoldMessenger.of(context).showSnackBar(SnackBar(content: Text(S.of(context).ridePlanned)))`) — copy this logic verbatim, do not alter it.

  In `build()`, add `bottomNavigationBar: SafeArea(top: false, child: _buildPlanRideBar(context))` to the `Scaffold` (as a sibling of `appBar:` and `body:`) — this pins the button below the scrollable body, always visible, using Material's built-in bottom-bar slot rather than restructuring the existing `body: SafeArea(child: Column(children: [Expanded(child: SingleChildScrollView(...))]))`, which stays exactly as-is except its trailing call changes from `_buildActions(context)` to `_buildSecondaryActions(context)`.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter analyze lib/features/detail/ride_detail_screen.dart</automated>
  </verify>
  <done>
  `flutter analyze` reports zero new errors/warnings on `ride_detail_screen.dart`. `_buildSecondaryActions` contains exactly 3 buttons (Add to Google Calendar, Remind me the evening before, Share this ride window). `_buildPlanRideBar` exists and is wired to `Scaffold.bottomNavigationBar`. The `onPressed` logic for "Plan ride" is unchanged (same provider call, same SnackBar).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure run-counting helper for drag-select (backlog #35 logic)</name>
  <files>lib/domain/services/drag_run_counter.dart, test/domain/services/drag_run_counter_test.dart</files>
  <behavior>
    - Test 1: `countSelectionRuns({})` (empty set) returns `0`.
    - Test 2: a single cell returns `1`.
    - Test 3: three consecutive hours on the same day (e.g. hours 9, 10, 11) return `1` run.
    - Test 4 (the literal backlog example): the same hour (17) on three different days returns `3` runs — one run per day, since hours are never "consecutive" across different calendar days.
    - Test 5: two separate runs on the same day with a gap in hours (e.g. hours 9, 10, then 14) return `2`.
    - Test 6: a mixed case — 2 runs on one day (hours 9, 10, then 14) plus 1 run on a different day (hour 8) — returns `3` total.
    - Test 7: constructing the input `Set<DateTime>` in a different insertion order than sorted-hour order still produces the same correct count (proves the function sorts internally rather than relying on `Set` iteration order).
  </behavior>
  <action>
  Create `lib/domain/services/drag_run_counter.dart` with a single top-level pure function `int countSelectionRuns(Set<DateTime> cells)` — no `BuildContext`, no widget/provider dependency, so it is directly unit-testable and carries no claim about the ride-slot/scoring engine (per locked scope: this is a selection-count indicator only). Implementation: if `cells` is empty return `0`. Group the cells by calendar day using a `Map<DateTime, List<int>>` keyed by `DateTime(cell.year, cell.month, cell.day)` (day-only key, ignoring hour), collecting each cell's `.hour` into that day's list. For each day's hour list: sort it ascending, then walk it tracking `previousHour` (start at a sentinel like `-2`, guaranteed non-adjacent to any real hour 0-23) — increment a running `runCount` whenever `hour != previousHour + 1` (i.e. a gap or the start of a new day's list), otherwise the hour extends the current run. Sum `runCount` across all days and return it. Add a doc comment on the function explaining "run" = maximal set of same-day, consecutive-hour cells, and explicitly noting this has no relationship to ride-slot/scoring-engine output.

  Create `test/domain/services/drag_run_counter_test.dart` implementing the 7 behaviors above using plain `DateTime.utc(year, month, day, hour)` values (matching `_cellKey`'s construction in `availability_screen.dart`) grouped in a `group('countSelectionRuns', ...)` block, importing `package:ridewindow/domain/services/drag_run_counter.dart`.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter test test/domain/services/drag_run_counter_test.dart && flutter analyze lib/domain/services/drag_run_counter.dart</automated>
  </verify>
  <done>
  `flutter test test/domain/services/drag_run_counter_test.dart` passes all 7 tests. `countSelectionRuns` is a pure top-level function with no `BuildContext`/widget/provider imports. `flutter analyze` reports zero new errors/warnings.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire live run-count indicator into Availability drag-select</name>
  <files>lib/l10n/app_en.arb, lib/l10n/app_nl.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_en.dart, lib/l10n/app_localizations_nl.dart, lib/features/availability/availability_screen.dart, test/features/availability_screen_test.dart</files>
  <action>
  In `lib/l10n/app_nl.arb` (the template file), insert immediately after the existing `"agendaHoursSelected"` entry (and its `@agendaHoursSelected` metadata block): `"dragRunsSelected": "{count} {count, plural, =1{tijdvak} other{losse tijdvakken}} geselecteerd"` followed by a `"@dragRunsSelected": {"placeholders": {"count": {"type": "int"}}}` metadata block (mirroring the `@rideWindowCount` metadata shape). This produces "1 tijdvak geselecteerd" for count=1 and "N losse tijdvakken geselecteerd" for count>1, matching the backlog example exactly.

  In `lib/l10n/app_en.arb`, insert the matching key in the same relative position (after `"agendaHoursSelected"`, no metadata block needed to match this file's convention): `"dragRunsSelected": "{count} {count, plural, =1{separate time block} other{separate time blocks}} selected"`.

  Run `flutter gen-l10n` from the repo root to regenerate `lib/l10n/app_localizations.dart`, `app_localizations_en.dart`, `app_localizations_nl.dart` with the new `dragRunsSelected(int count)` getter/method. Do not hand-edit the generated files.

  In `lib/features/availability/availability_screen.dart`, add `import 'package:ridewindow/domain/services/drag_run_counter.dart';`. Add a private static constant `static const double _dragIndicatorHeight = 32;` alongside the existing `_headerWidth`/`_headerHeight` consts. Add a new method `Widget _buildDragIndicatorBar(BuildContext context)` returning a `SizedBox(height: _dragIndicatorHeight, child: ...)` whose child is `const SizedBox.shrink()` when `!_isDragging`, or — when `_isDragging` — a `Center(child: Row(mainAxisSize: MainAxisSize.min, children: [Icon(Icons.touch_app, size: 14, color: Theme.of(context).colorScheme.primary), const SizedBox(width: 6), Text(S.of(context).dragRunsSelected(countSelectionRuns(_draggedCells)), style: TextStyle(fontSize: 12, fontWeight: FontWeight.w600, color: Theme.of(context).colorScheme.primary))]))` — mirroring `week_agenda_screen.dart`'s existing "N selected" icon+text visual convention.

  In `_buildGrid`'s returned `Column` (inside the `LayoutBuilder`'s `builder`), insert `_buildDragIndicatorBar(context)` as the FIRST child, before the `Expanded(child: GestureDetector(...))` grid. This row is always present at a fixed height (32px) whether or not a drag is active — content is empty when not dragging — so the grid's available height never shifts when a drag starts or ends. Update the existing `final availableHeight = constraints.maxHeight - 140;` line to `final availableHeight = constraints.maxHeight - 140 - _dragIndicatorHeight;` so the cell-height calculation accounts for the new fixed-height row.

  In `test/features/availability_screen_test.dart`, add `import 'package:ridewindow/l10n/app_localizations.dart';` to the imports, then add a new test group `group('BACKLOG-35: drag run indicator', ...)` with a `setUp`/test that: sets `tester.view.physicalSize = const Size(800, 1400);` and `tester.view.devicePixelRatio = 1.0;`, with `addTearDown(tester.view.reset)` (or `tester.view.resetPhysicalSize()` + `tester.view.resetDevicePixelRatio()` in an `addTearDown`) for determinism. Pump `AvailabilityScreen` wrapped in `ProviderScope(overrides: [availabilityProvider.overrideWith(() => FakeEmptyAvailabilityNotifier())], child: MaterialApp(locale: const Locale('nl'), localizationsDelegates: S.localizationsDelegates, supportedLocales: S.supportedLocales, home: const AvailabilityScreen()))`, then `await tester.pump(); await tester.pump(const Duration(milliseconds: 100));`.

  With this fixed viewport (physical size 800x1400, devicePixelRatio 1.0, default AppBar `toolbarHeight` of 56.0, no status-bar padding in the test environment), the grid's `LayoutBuilder` constraints resolve to a body height of 1400 - 56 = 1344 logical pixels, giving `cellWidth = (800 - 36) / 7 ≈ 109.14` and `cellHeight = (1344 - 140 - 32 - 28) / 24 ≈ 47.67`. Compute cell-center global coordinates using `x = 36 + (dayIndex + 0.5) * cellWidth` and `y = 56 + 32 + 28 + (hour + 0.5) * cellHeight` (56 = AppBar height, 32 = the new drag-indicator bar, 28 = `_headerHeight`, 36 = `_headerWidth`) for `dayIndex` 0, 1, 2 at `hour = 1` (chosen to stay comfortably inside the visible viewport near the top, avoiding hour-0 boundary rounding). Use `final gesture = await tester.startGesture(day0Hour1Center);` then `await gesture.moveTo(day1Hour1Center); await tester.pump();` then `await gesture.moveTo(day2Hour1Center); await tester.pump();` — this simulates one continuous horizontal drag across 3 day-columns at the same hour, exactly matching the backlog's Monday/Tuesday/Wednesday 17:00 example (3 separate 1-hour runs). Assert `find.text('3 losse tijdvakken geselecteerd')` finds one widget. Then `await gesture.up(); await tester.pump();` and assert the indicator text is gone (e.g. `find.textContaining('geselecteerd')` finds nothing, or `find.byIcon(Icons.touch_app)` finds nothing) confirming the indicator disappears when the drag ends.
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter gen-l10n && flutter analyze lib/features/availability/availability_screen.dart lib/l10n/app_localizations.dart && flutter test test/features/availability_screen_test.dart</automated>
  </verify>
  <done>
  `flutter gen-l10n` runs clean and `S` exposes `dragRunsSelected(int count)` in both `app_localizations_en.dart` and `app_localizations_nl.dart`. `flutter analyze` reports zero new errors/warnings. `flutter test test/features/availability_screen_test.dart` passes, including the new drag-indicator test showing "3 losse tijdvakken geselecteerd" mid-drag and no indicator text after the drag ends. The grid's cell sizing does not jump when a drag starts/ends (fixed-height indicator row reserved either way).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
  Task A: "Plan ride" is now pinned via `Scaffold.bottomNavigationBar` on the Ride Detail screen — always visible without scrolling, while the other 3 action buttons stay in the scrollable content below Adjust Time / Weather / Clothing tip / Hourly table.
  Task B: The Availability screen now shows a live "N losse tijdvakken geselecteerd" / "1 tijdvak geselecteerd" indicator above the grid while actively drag-selecting cells, counting distinct same-day/consecutive-hour runs.
  </what-built>
  <how-to-verify>
  Run the app (`flutter run` on a connected device/emulator, or the release APK per usual workflow) and check:
  1. Open any Ride Detail screen (tap a ride window from Home). Confirm "Plan ride" is visible immediately, pinned at the bottom, without scrolling. Scroll the content above it — confirm it stays fixed and the other 3 buttons (Add to Google Calendar / Remind me the evening before / Share this ride window) are still reachable by scrolling, in their original order. Tap "Plan ride" and confirm the existing SnackBar confirmation still appears.
  2. Open the Availability screen. Start a drag (long-press/drag) across three cells in different day columns at the same hour (e.g. Monday, Tuesday, Wednesday at the same hour) in one continuous gesture. Confirm a live indicator reading "3 losse tijdvakken geselecteerd" appears above the grid while dragging. Release the drag and confirm the indicator disappears. Try a drag across 3 consecutive hours in the SAME day column and confirm the indicator reads "1 tijdvak geselecteerd" (one contiguous run).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | Both changes are local UI/layout restructuring and a pure client-side counting function operating on data the user already entered via on-device drag-select; no new external input, network call, or persisted data format changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-quick260714o54-01 | Information Disclosure | `dragRunsSelected` indicator text | accept | Displays only a locally-computed count derived from the user's own in-progress drag selection on their own device; nothing leaves the device, nothing is persisted. |
| T-quick260714o54-02 | Tampering | `countSelectionRuns` pure function | accept | Pure, side-effect-free function operating on `Set<DateTime>` already produced by the existing (unmodified) `_onDragStart`/`_onDragUpdate` handlers — no new untrusted input path introduced. |
| T-quick260714o54-03 | Denial of Service | Layout shift / cell-size miscalculation from the new fixed-height indicator row | mitigate | Row height is a fixed constant (`_dragIndicatorHeight = 32`) always reserved whether or not a drag is active, and `availableHeight` calculation explicitly subtracts it — verified in Task 3 that cell sizing is stable across drag start/end. |
</threat_model>

<verification>
Run, in order: `flutter analyze` (whole repo, to catch any cross-file issues), `flutter test test/domain/services/drag_run_counter_test.dart`, `flutter test test/features/availability_screen_test.dart`. Then complete the human-verify checkpoint above for the two visual/interaction behaviors that automated widget tests can't cheaply cover for `RideDetailScreen` (pre-existing broken provider/localization test infra in this repo, unrelated to this plan — see `<interfaces>` note).
</verification>

<success_criteria>
- "Plan ride" button visible and tappable on Ride Detail without any scrolling; unchanged add-to-plannedRidesProvider + SnackBar behavior
- Add to Google Calendar / Remind me the evening before / Share this ride window remain together, unchanged, in the scrollable content
- `countSelectionRuns` is a pure, unit-tested function (7 passing tests) with zero UI/scoring-engine dependency
- Availability screen shows a correctly-pluralized NL indicator ("1 tijdvak geselecteerd" / "N losse tijdvakken geselecteerd") during an active drag, and it disappears when the drag ends
- No layout shift/jump in the Availability grid when a drag starts or ends
- No new pubspec dependency added
- `flutter analyze` and both `flutter test` commands pass; `flutter gen-l10n` runs clean
</success_criteria>

<output>
Create `.planning/quick/260714-o54-bundle-backlog-34-sticky-plan-ride-butto/260714-o54-SUMMARY.md` when done
</output>
