---
phase: quick-260717-no1
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260717-no1-backlog-31-google-oauth-consent-screen-p/OAUTH-PUBLISH-CHECKLIST.md
  - .planning/BACKLOG.md
autonomous: true
requirements: [BACKLOG-31]

must_haves:
  truths:
    - "A precise, step-by-step checklist exists telling Joost exactly how to publish the OAuth consent screen via the fast route in Google Cloud Console"
    - "The checklist explains what end users will see (the 'Google hasn't verified this app' warning) and confirms this is expected, not a bug"
    - "The checklist confirms the privacy policy URL field (https://joostmouw.github.io/ridewindow/privacy-policy.html) is a required field to check/fill before publishing"
    - "BACKLOG.md item #31's Status column reflects that the fast-publish route was chosen and instructions were handed off, without claiming the action itself is Done"
  artifacts:
    - path: ".planning/quick/260717-no1-backlog-31-google-oauth-consent-screen-p/OAUTH-PUBLISH-CHECKLIST.md"
      provides: "Step-by-step Google Cloud Console instructions for publishing the OAuth consent screen"
    - path: ".planning/BACKLOG.md"
      provides: "Updated Status column for item #31"
  key_links:
    - from: ".planning/BACKLOG.md item #31"
      to: "OAUTH-PUBLISH-CHECKLIST.md"
      via: "Status column references where the instructions live"
      pattern: "OAUTH-PUBLISH-CHECKLIST"
---

<objective>
Produce a precise, human-executable checklist for publishing the Google OAuth consent screen via the FAST "Publish app" route (not full Google verification) in Cloud Console project `my-project-joost`, and update BACKLOG.md item #31 to reflect that this route was chosen and instructions are ready for the user to act on.

Purpose: The app's only OAuth scope (`https://www.googleapis.com/auth/calendar.events`, used in `lib/services/calendar_service.dart`) is a Google "Sensitive" scope, not "Restricted" — so publishing does not require Google's formal verification/security-assessment process. Moving the consent screen from Testing to Production removes the 100-test-user cap, which currently blocks public launch of the Google Calendar feature (CAL-06/07). The actual button click is a human-only action in Cloud Console requiring Joost's authenticated Google session — no CLI or browser automation is available in this environment, so this plan documents the exact steps rather than attempting to perform them.

Output:
- `.planning/quick/260717-no1-backlog-31-google-oauth-consent-screen-p/OAUTH-PUBLISH-CHECKLIST.md` — step-by-step instructions
- Updated `.planning/BACKLOG.md` item #31 Status column
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/BACKLOG.md
@lib/services/calendar_service.dart

Confirmed technical facts (do not re-derive):
- Only scope in use: `CalendarApi.calendarEventsScope` = `https://www.googleapis.com/auth/calendar.events`, referenced at 4 call sites in `lib/services/calendar_service.dart` (`addRideSlotToCalendar`, `getEvents`, `isCalendarConnected`, `disconnectCalendar`).
- This is a Google "Sensitive" scope, not "Restricted" — fast publish is valid without formal verification.
- Privacy policy already live at: https://joostmouw.github.io/ridewindow/privacy-policy.html
- Google Cloud project: `my-project-joost`
- No `gcloud` CLI and no browser automation available in this environment — the Cloud Console click is a human-only action.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the OAuth consent screen publish checklist</name>
  <files>.planning/quick/260717-no1-backlog-31-google-oauth-consent-screen-p/OAUTH-PUBLISH-CHECKLIST.md</files>
  <action>
Create a markdown checklist document for Joost to follow manually in Google Cloud Console. Structure it with these sections:

1. **Context** — one paragraph: current state is Testing mode (max 100 manually-added test users), blocking public launch of Google Calendar linking (CAL-06/07, backlog #31). Fast "Publish app" route is valid here because the only scope (`https://www.googleapis.com/auth/calendar.events`) is a Sensitive scope, not Restricted — no Google security assessment required.

2. **Before you publish — pre-flight check** — a checklist of items to confirm are already filled in on the OAuth consent screen configuration page before clicking Publish:
   - App name is set
   - User support email is set
   - App logo (optional but recommended)
   - Application home page URL
   - **Privacy policy URL is set to `https://joostmouw.github.io/ridewindow/privacy-policy.html`** (call out explicitly that this is a required field and this exact URL should be present)
   - Authorized domains include `joostmouw.github.io` (the domain hosting the privacy policy) and any other domains used
   - Scopes list shows only `.../auth/calendar.events`

3. **Step-by-step: Publish app** — exact navigation path and clicks:
   - Go to https://console.cloud.google.com/
   - Select project `my-project-joost` from the project switcher (top bar)
   - Navigate: left sidebar hamburger menu -> "APIs & Services" -> "OAuth consent screen"
   - Confirm "Publishing status" currently shows "Testing"
   - Click the "PUBLISH APP" button
   - A confirmation dialog will appear warning that the app will be available to any user with a Google account, and that if the app uses sensitive/restricted scopes it may require verification. Since only a Sensitive scope is used (not Restricted), click "CONFIRM" to proceed anyway — this is expected and safe per the pre-confirmed technical context.
   - Publishing status should now show "In production"

4. **What end users will see** — describe the one-time unverified-app warning screen: when a user first signs in and grants Calendar access, Google shows an interstitial titled "Google hasn't verified this app" with a "Advanced" link that must be clicked to reveal "Go to RideWindow (unsafe)" — the user must click through this once per Google account to proceed. Explicitly state: this is EXPECTED and ACCEPTED behavior for this fast-publish route, not a defect. It does not block sign-in, it is just an extra confirmation click. Full formal Google verification (which would remove this warning) is NOT being pursued per the user's decision — the effort/timeline tradeoff was judged not worth it for a small personal app.

5. **After publishing — verification steps** — how Joost can confirm it worked:
   - OAuth consent screen page shows "In production" status
   - Test on a Google account that was never added as a test user in Testing mode — sign-in + Calendar authorization should succeed after clicking through the unverified-app warning
   - No 100-user cap applies anymore

6. **Rollback note** — mention that Google allows returning an app to Testing status from the same OAuth consent screen page if ever needed, and this is non-destructive to existing user grants.

Write this as clean, scannable markdown with headers and checkboxes (`- [ ]`) for the pre-flight and step-by-step sections so Joost can tick items off while executing.
  </action>
  <verify>
    <automated>test -f ".planning/quick/260717-no1-backlog-31-google-oauth-consent-screen-p/OAUTH-PUBLISH-CHECKLIST.md" && grep -c "privacy-policy.html" ".planning/quick/260717-no1-backlog-31-google-oauth-consent-screen-p/OAUTH-PUBLISH-CHECKLIST.md"</automated>
  </verify>
  <done>OAUTH-PUBLISH-CHECKLIST.md exists, contains the exact privacy policy URL, the exact Cloud Console navigation path, an explanation of the one-time unverified-app warning users will see, and a pre-flight checklist confirming the privacy policy field.</done>
</task>

<task type="auto">
  <name>Task 2: Update BACKLOG.md item #31 status</name>
  <files>.planning/BACKLOG.md</files>
  <action>
Open .planning/BACKLOG.md and locate row 32 (item `# 31`, the Google OAuth consent screen row). Change the Status column value from "Backlog" to "Ready for user action — fast-publish route chosen, see .planning/quick/260717-no1-backlog-31-google-oauth-consent-screen-p/OAUTH-PUBLISH-CHECKLIST.md". Do not mark it "Done" — the actual Cloud Console click has not happened yet and can only be performed by the user in their own authenticated browser session.

Also update the "Laatst bijgewerkt" footer line at the bottom of the file to add a brief note that item #31's fast-publish decision was made and instructions were handed off on 2026-07-17, following the existing style of prior footer updates in that file (append to the existing sentence, do not remove prior content).
  </action>
  <verify>
    <automated>grep -c "Ready for user action" /Users/joostmouw/ridewindow/.planning/BACKLOG.md</automated>
  </verify>
  <done>BACKLOG.md item #31 Status column reads "Ready for user action" (not "Done", not "Backlog") and references the checklist file path; footer updated-date line mentions the #31 decision.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None — no code changes | This plan only writes documentation and updates a tracking file; no application code, network calls, or trust boundaries are touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-quick-260717-no1-01 | Tampering | Cloud Console OAuth consent screen publish action | accept | This is a manual, human-authenticated action outside this plan's scope; Google's own confirmation dialog is the safeguard. No code path in this repo is affected. |
</threat_model>

<verification>
- OAUTH-PUBLISH-CHECKLIST.md exists and contains the confirmed privacy policy URL, exact Cloud Console navigation steps, and the unverified-app warning explanation.
- BACKLOG.md item #31 Status column no longer reads "Backlog" and does not read "Done".
</verification>

<success_criteria>
Joost has an unambiguous, click-by-click checklist to follow himself in Google Cloud Console to publish the OAuth consent screen via the fast route, and BACKLOG.md accurately tracks that the decision was made and instructions are ready, without falsely claiming the publish action itself is complete.
</success_criteria>

<output>
Create `.planning/quick/260717-no1-backlog-31-google-oauth-consent-screen-p/260717-no1-SUMMARY.md` when done
</output>
