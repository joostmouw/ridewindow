---
phase: quick-260714-r8u
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/shared/screen_hint_overlay.dart
autonomous: false
requirements: []

must_haves:
  truths:
    - "Hint overlay on Home, Agenda, and Planned Rides screens shows a compact 'N/M' step counter and a text-only action button instead of the dot-progress row and filled pill button"
    - "Tapping the text button (or the overlay) still advances through hints and dismisses on the last step, identical to current behavior"
  artifacts:
    - path: "lib/features/shared/screen_hint_overlay.dart"
      provides: "ScreenHintOverlay widget with restyled counter+button row, dot-indicator Positioned block removed"
  key_links:
    - from: "lib/features/shared/screen_hint_overlay.dart"
      to: "_next()"
      via: "TextButton onPressed"
      pattern: "TextButton\\(\\s*onPressed:\\s*_next"
---

<objective>
Restyle `ScreenHintOverlay`'s bottom-of-card call-to-action: replace the current bottom dot-indicator row and the filled pill "Next"/"Sluiten" button with a compact inline `{step}/{total}` muted-text counter + a Material 3 `TextButton`, matching the layout of a React/shadcn Popover demo the user reviewed as *visual reference only* (not literal code — this stays pure Flutter/MD3).

User decisions locked for this task:
1. Replace (not supplement) the bottom dot-indicator row and the filled pill CTA with an inline `Row`: left = small muted `'${step+1}/${total}'` text (MD3 `onSurfaceVariant` color role, label-scale typography per the material-3 skill), right = a standard MD3 `TextButton` (the low-emphasis action per the skill's Button Quick Reference) using the existing `s.hintNext` / `s.hintDismiss` labels.
2. Last-step behavior is UNCHANGED: last step still shows `s.hintDismiss` and calls `_next()`, which invokes `widget.onDismiss()` when `_currentStep == widget.hints.length - 1`. Do NOT add any "start over"/loop-to-first-step behavior — the demo's loop-back was explicitly rejected by the user.

Purpose: Bring the coach-mark overlay's CTA in line with this app's Material 3 conventions (proper color roles instead of `Colors.white.withAlpha(...)`, standard TextButton instead of a hand-rolled filled pill Container) while keeping the spotlight/glass-morphism visual language and existing hint content/API untouched.

Output: Updated `lib/features/shared/screen_hint_overlay.dart` — dot-indicator block removed, CTA row restyled. No other files change (3 call sites in home/agenda/planned_rides screens are unaffected since `HintItem` and the widget's public constructor are untouched).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@lib/features/shared/screen_hint_overlay.dart

<user_decision_verbatim>
User was shown a React/shadcn/Radix Popover demo ("tooltip with steps") as a *visual reference only* — NOT literal code to port (this is a Flutter app, no React/Tailwind/shadcn exists or should be introduced). The demo's relevant layout: a compact card showing `{currentIndex + 1}/{total}` as small muted text on the left, and a subtle text-only link button ("Next" / "Start over") on the right — no dot-progress row, no filled pill button.

Explicit decisions:
1. Adopt the compact inline "N/M" counter + text-link button pattern, styled per this app's own Material 3 conventions (not raw-copied Tailwind/shadcn styling) — replace, not supplement, the existing bottom dot-indicator row and the filled pill CTA button.
2. Last-step behavior: unchanged — last step still shows "Sluiten"/dismiss and closes the overlay (`widget.onDismiss()`). Do NOT add the demo's "Start over"/loop-back-to-step-1 behavior.
</user_decision_verbatim>

<interfaces>
Existing l10n strings to reuse (do not rename, no new strings needed):
- `app_en.arb`: `"hintDismiss": "Tap to close"`, `"hintNext": "Tap for next"`
- `app_nl.arb`: `"hintDismiss": "Tik om te sluiten"`, `"hintNext": "Tik voor volgende"`
Accessed in the widget via `final s = S.of(context);` then `s.hintNext` / `s.hintDismiss` (already in scope inside `build()` and passed into `_buildTooltip`).

Current structure of `lib/features/shared/screen_hint_overlay.dart` (line numbers as of this read; may drift slightly if you re-open the file, use as landmarks not exact anchors):
- Lines 137-160: `Positioned(bottom: 48, left: 24, right: 24, child: SafeArea(child: Row(...for i in widget.hints.length -> Container dot...)))` — the dot-indicator block to DELETE ENTIRELY from the `Stack` in `build()`.
- Lines 226-295: the tooltip `Container` card inside `_buildTooltip`, containing icon+title Row, description Text, then at lines 273-292 the `Align(alignment: Alignment.centerRight, child: Container(...filled pill...))` CTA — REPLACE this `Align(...)` block only.
- `isLast` (bool) and `s` (S instance) are already parameters of `_buildTooltip(BuildContext, HintItem, Rect, bool isLast, S s)` — reuse directly, do not re-derive.
- `_next()` (existing, untouched): advances `_currentStep` with fade animation, or calls `widget.onDismiss()` on last step.

MD3 conventions to apply (per `.claude/skills/material-3/SKILL.md`):
- Muted/low-emphasis text → `Theme.of(context).colorScheme.onSurfaceVariant` (never a raw `Colors.white.withAlpha(...)` value for new elements).
- Label-scale typography for the counter text (small caption-like size, e.g. `Theme.of(context).textTheme.labelMedium?.copyWith(color: colorScheme.onSurfaceVariant)` or equivalent — pick whichever existing text-style helper pattern is used elsewhere in this codebase if one exists; otherwise construct a small `TextStyle` using the theme's `onSurfaceVariant` color).
- Low-emphasis action → `TextButton` (per skill's Button Quick Reference: Text is the correct variant for a subordinate/dismiss-style action, not Filled).
- Do NOT touch the glass-morphism card decoration (`Colors.white.withAlpha(30)` background/border), the icon container, title/description text colors, or arrow painters — those are explicitly out of scope and stay as `Colors.white.withAlpha(...)` per current design.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace dot-indicator + filled pill CTA with N/M counter + TextButton</name>
  <files>lib/features/shared/screen_hint_overlay.dart</files>
  <action>
  In `build()`: delete the entire `Positioned(bottom: 48, left: 24, right: 24, child: SafeArea(child: Row(mainAxisAlignment: MainAxisAlignment.center, children: [for (var i = 0; i < widget.hints.length; i++) Container(...)])))` block (the dot-indicator step row) from the `Stack`'s children. The `Stack` should end up with just the `Positioned.fill` spotlight painter and the `_buildTooltip(...)` call (guarded by `if (targetRect != null)`).

  In `_buildTooltip()`: replace the `Align(alignment: Alignment.centerRight, child: Container(...))` filled-pill CTA block (the one containing `isLast ? s.hintDismiss : s.hintNext` in a `Container` with `color: Theme.of(context).colorScheme.primary` and `borderRadius: BorderRadius.circular(20)`) with a `Row` that has `mainAxisAlignment: MainAxisAlignment.spaceBetween` and two children:
  - Left: a `Text('${_currentStep + 1}/${widget.hints.length}', style: ...)` using `Theme.of(context).colorScheme.onSurfaceVariant` for color and a small/label-scale font size (e.g. via `Theme.of(context).textTheme.labelMedium` merged with the `onSurfaceVariant` color — match whatever text-styling approach keeps this legible against the translucent glass card; a plain `TextStyle(color: ..., fontSize: 12)` is acceptable if no theme labelMedium override exists for this dark-glass context).
  - Right: `TextButton(onPressed: _next, child: Text(isLast ? s.hintDismiss : s.hintNext))`. Do not set a custom `style:` unless needed for contrast against the translucent card — if default `TextButton` foreground color is too low-contrast against the glass background, override `style: TextButton.styleFrom(foregroundColor: Colors.white)` to preserve current on-dark-card readability (the card background remains `Colors.white.withAlpha(30)`, untouched).

  Do not modify: `_SpotlightPainter`, `_ArrowPainter`, `_getTargetRect`, `_next()`'s internals, the icon+title+description block, the card's `BoxDecoration`, `HintItem`, `shouldShowHint`/`markHintSeen`, or the `minTop`/`maxBottom` clamp constants in `_buildTooltip` (leave `maxBottom = safeBottom + 80` as-is even though the comment mentions "step dots" — out of scope for this task).
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter analyze lib/features/shared/screen_hint_overlay.dart</automated>
  </verify>
  <done>`flutter analyze` reports no new errors/warnings on this file; the dot-indicator `Positioned` block is gone from `build()`; the CTA in `_buildTooltip` is a `Row` with `onSurfaceVariant`-colored '{n}/{total}' text on the left and a `TextButton` (using `_next` and `s.hintNext`/`s.hintDismiss`) on the right; no new l10n keys were added.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Restyled `ScreenHintOverlay` CTA: bottom dot-progress row removed; tooltip card now shows a small muted "N/M" counter on the left and an MD3 `TextButton` ("Tik voor volgende" / "Tik om te sluiten") on the right, replacing the old filled pill button. This overlay is shared by Home, Week Agenda, and Planned Rides screens.</what-built>
  <how-to-verify>
    1. Run `flutter run` (or install a fresh debug/release APK) on a device/emulator.
    2. Clear hint-seen state if needed so the overlay reappears: uninstall+reinstall, or clear app storage (hints are gated by `shared_preferences` keys `hint_seen_*`).
    3. Open the Home screen — confirm the hint overlay appears with: no dot row at the bottom, and inside the tooltip card a small muted "1/N" style counter on the left and a text-only "Tik voor volgende" button on the right (no filled pill).
    4. Tap the text button repeatedly through all steps — confirm counter increments (e.g. "1/3" -> "2/3" -> "3/3") and the spotlight/arrow/card visuals still animate correctly between steps.
    5. On the last step, confirm the button reads "Tik om te sluiten" and tapping it dismisses the overlay (no loop back to step 1).
    6. Repeat steps 3-5 on the Week Agenda screen and the Planned Rides screen (same overlay, different `HintItem` lists) to confirm no screen-specific breakage.
  </how-to-verify>
  <resume-signal>Type "approved" once verified on all 3 screens, or describe any visual/behavioral issue found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Purely local UI restyle — no new data input, network calls, or trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-r8u-01 | N/A | `screen_hint_overlay.dart` | accept | Cosmetic-only change to a local coach-mark widget; no new inputs, no external data, no security-relevant surface introduced. |
</threat_model>

<verification>
- `flutter analyze lib/features/shared/screen_hint_overlay.dart` passes with no new issues.
- Manual checkpoint confirms correct visual/behavioral output on Home, Week Agenda, and Planned Rides screens.
</verification>

<success_criteria>
- Dot-indicator `Positioned` block fully removed from `screen_hint_overlay.dart`.
- Filled pill CTA replaced with `{n}/{total}` muted counter + MD3 `TextButton`, using `onSurfaceVariant` color role instead of ad-hoc `Colors.white.withAlpha(...)` for the new elements.
- Last-step dismiss behavior unchanged (`_next()` untouched); no "start over" loop introduced.
- No new l10n strings added; existing `s.hintNext`/`s.hintDismiss` reused.
- All 3 call sites (home, agenda, planned rides) unaffected since `HintItem` API and `ScreenHintOverlay` constructor are untouched.
</success_criteria>

<output>
Create `.planning/quick/260714-r8u-herstijl-screenhintoverlay-dot-indicator/260714-r8u-SUMMARY.md` when done
</output>
</content>
