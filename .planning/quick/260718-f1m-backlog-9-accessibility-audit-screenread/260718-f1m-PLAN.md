---
phase: quick-260718-f1m
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [lib/l10n/app_en.arb, lib/l10n/app_nl.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_en.dart, lib/l10n/app_localizations_nl.dart, lib/features/home/home_screen.dart, lib/features/detail/ride_detail_screen.dart, lib/features/profile/profile_screen.dart, lib/features/onboarding/onboarding_screen.dart, lib/features/profile/feedback_dialog.dart, lib/features/shared/weather_indicator_bar.dart, lib/features/shared/weather_icon.dart, lib/features/availability/availability_screen.dart, lib/features/agenda/week_agenda_screen.dart, lib/theme/app_colors.dart, test/features/availability_screen_test.dart]
autonomous: true
requirements: [F1M-01, F1M-02, F1M-03]

must_haves:
  truths:
    - "TalkBack/VoiceOver announces a meaningful, distinct label (not just 'button') for every icon-only button audited: home delete, ride-detail info + 4 time adjusters, profile clear-location, onboarding back, feedback 5 stars"
    - "TalkBack announces day, hour, and blocked/available/tier/planned/selected status when a user explores an Availability or Agenda grid cell, or the weather-icon glyph, or a weather metric's info icon"
    - "The Availability grid's hour cells and header cells, the Agenda grid's hour rows, the Profile info button, and the Onboarding back button meet the 48dp minimum tap target, or a narrower-but-improved target is used with the tradeoff documented in code comments"
    - "The 'Acceptable' tier ScoreBadge text meets >=4.5:1 contrast against its background in light mode (was 3.46:1)"
    - "Secondary/hint-styled label text (rw.textHint) meets >=4.5:1 contrast against its surface in both light mode (was 2.85:1) and dark mode (was 3.62:1)"
    - "All new user-facing strings (tooltips, semantic labels, hints) are available in both English and Dutch via S.of(context) / generated S class"
  artifacts:
    - path: "lib/l10n/app_en.arb"
      provides: "16 new EN accessibility strings (tooltips, semantic labels, hints)"
      contains: "removePlannedRideTooltip"
    - path: "lib/l10n/app_nl.arb"
      provides: "16 new NL accessibility strings"
      contains: "removePlannedRideTooltip"
    - path: "lib/features/availability/availability_screen.dart"
      provides: "Semantics-wrapped grid cells/headers + 48dp cell/header floor"
      contains: "Semantics("
    - path: "lib/features/agenda/week_agenda_screen.dart"
      provides: "Semantics-wrapped grid cells + scrollable 48dp-row layout"
      contains: "Semantics("
    - path: "lib/theme/app_colors.dart"
      provides: "WCAG AA-passing acceptableFg, lightTextHint, darkTextHint values"
      contains: "0xFFBF360C"
  key_links:
    - from: "lib/features/availability/availability_screen.dart _buildCell"
      to: "_cellSemanticLabel helper (shared with _showCellInfo)"
      via: "Semantics(label: _cellSemanticLabel(...))"
      pattern: "Semantics\\(.*label: _cellSemanticLabel"
    - from: "lib/features/shared/weather_icon.dart build()"
      to: "S.of(context).weatherTierDescription"
      via: "Semantics(label:) wrapping the tier-switch SizedBox"
      pattern: "Semantics\\(.*label:.*weatherTierDescription"
    - from: "lib/theme/app_theme.dart RideWindowTheme.light/dark"
      to: "lib/theme/app_colors.dart AppColors.lightTextHint/darkTextHint, TierColors.light.acceptableFg"
      via: "direct constant pass-through (confirmed no other file needs changes for the contrast fix)"
      pattern: "textHint: AppColors\\.(light|dark)TextHint"
---

<objective>
Close backlog #9 (accessibility audit) for the three research-confirmed, high-confidence gap categories: (A) icon-only buttons and custom-gesture grid cells with zero screen-reader label, (B) touch targets explicitly sized below the 48dp Material minimum, and (C) the one confirmed WCAG AA contrast failure (ScoreBadge "Acceptable" tier) plus the systemic `textHint` contrast issue. Scope is fixed by `.planning/quick/260718-f1m-backlog-9-accessibility-audit-screenread/260718-f1m-RESEARCH.md` — do not redesign the Availability/Agenda grid interaction model; per-cell `Semantics` labels only.

Purpose: RideWindow currently has zero `Semantics(` widgets anywhere in `lib/` — roughly half of icon-only controls are unlabeled to TalkBack, and the two custom hour grids are 100% unusable via screen reader. This plan makes every audited interactive element operable and legible per Material/WCAG AA guidance.
Output: New EN/NL ARB strings; tooltips + `Semantics` wrapping on all Priority-A elements from the research; 48dp-compliant (or documented-tradeoff) sizing on all Priority-B elements; corrected `AppColors` contrast tokens for Priority-C.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260718-f1m-backlog-9-accessibility-audit-screenread/260718-f1m-RESEARCH.md

<interfaces>
**l10n pattern** — `S.of(context)` from `package:ridewindow/l10n/app_localizations.dart`, generated by `flutter gen-l10n` (NOT hand-edited) from `lib/l10n/app_en.arb` / `lib/l10n/app_nl.arb`. `l10n.yaml` sets `template-arb-file: app_nl.arb`, `output-class: S`, `nullable-getter: false` — every key MUST exist in BOTH arb files. ICU plurals need no `@key` metadata block in this codebase's convention (see existing `rideWindowCount`, `dragRunsSelected`); simple `{placeholder}` substitution also needs no metadata block (see `greetingWithName`: `"{greeting}, {name}"`). After editing ARBs, run `flutter gen-l10n` to regenerate `lib/l10n/app_localizations*.dart` (these generated files are committed to git in this repo).

**Theme access** — `context.rw` extension (`lib/theme/app_theme.dart`) exposes `RideWindowTheme` fields that are a direct 1:1 pass-through of `lib/theme/app_colors.dart` `AppColors`/`TierColors` constants (e.g. `textHint: AppColors.lightTextHint`). Fixing a value in `app_colors.dart` propagates everywhere the token is used — no other file needs to change for the Priority C fix.

**Computed WCAG replacement values (relative-luminance formula, verified locally against the exact hex values in `app_colors.dart`):**
- `TierColors.light.acceptableFg`: `0xFFE65100` (3.46:1 on `acceptableBg` 0xFFFFF3E0, FAILS 4.5:1) → `0xFFBF360C` (5.11:1 on same bg, PASSES with margin). This is Material Design's own "Deep Orange 900" swatch — not an invented value. `TierColors.dark.acceptableFg` (0xFFFFCC80 on 0xFF3E2723) already passes at 9.35:1 — leave dark unchanged.
- `AppColors.lightTextHint`: `0xFF999999` (2.85:1 on white, 2.61:1 on `lightSurface` 0xFFF5F5F5 — FAILS both) → `0xFF6B6B6B` (5.33:1 on white, 4.89:1 on `lightSurface` — PASSES).
- `AppColors.darkTextHint`: `0xFF757575` (3.62:1 on `darkSurface` 0xFF1E1E1E, 3.03:1 on `darkSurfaceDim` 0xFF2C2C2C — FAILS 4.5:1 normal-text threshold despite research flagging it as a "separate/passing pair"; this plan's own recomputation shows it does not clear 4.5:1, and this token is used for real label text, not decorative fills, at multiple call sites) → `0xFF949494` (5.5:1 on `darkSurface`, 4.6:1 on `darkSurfaceDim` — PASSES). Document this correction to the research's assumption inline where the value is changed.

**Existing reusable ARB keys** (do NOT duplicate): `dayShortMon..Sun` (short day labels), `cellInfoStatusWork/Calendar/Custom/Free` (Availability grid status text), `tierPerfect/Great/Acceptable/Poor` (tier labels), `tierPerfectAgenda/GreatAgenda/AcceptableAgenda/PoorAgenda` (Agenda's tier labels), `feedbackCommentHint`, `navHome`.

**Test-file coupling** — `test/features/availability_screen_test.dart` duplicates the Availability grid's cell-sizing formula in three places (BACKLOG groups at ~line 446, ~490, ~747) to compute tap coordinates: `cellWidth = (800 - 36) / 7`, `cellHeight = (1344 - 140 - 32 - 28) / 24` (≈47.67, currently just under the old 16px floor check so it's NOT hitting the floor today), and `cellCenter` offsets using literal `36` (x baseline) and `56 + 32 + 28` (y baseline, where `28` is the old `_headerHeight`). Raising the production floor to 48 makes the effective cellHeight in this exact test viewport become `48.0` (since the raw computed 46.83 — after `_headerHeight` also becomes 48 — is still below 48). These three test blocks MUST be updated in the same task that changes the production constants, or the drag/tap gesture tests will silently target the wrong pixel and start failing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Tooltips, Semantics(selected), and a TextField label for standard icon-only controls (Priority A1-A6, D2, D5)</name>
  <files>lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/features/home/home_screen.dart, lib/features/detail/ride_detail_screen.dart, lib/features/profile/profile_screen.dart, lib/features/onboarding/onboarding_screen.dart, lib/features/profile/feedback_dialog.dart</files>
  <action>
Add 9 new ARB key pairs to BOTH `lib/l10n/app_nl.arb` (template file — edit first) and `lib/l10n/app_en.arb`, inserted near their thematically related existing keys (valid JSON, comma-separated):
`removePlannedRideTooltip` (EN "Remove planned ride" / NL "Verwijder geplande rit"), `showScoreDetails` (EN "Why this score" / NL "Waarom deze score"), `decreaseStartTime` (EN "Decrease start time" / NL "Starttijd vroeger zetten"), `increaseStartTime` (EN "Increase start time" / NL "Starttijd later zetten"), `decreaseEndTime` (EN "Decrease end time" / NL "Eindtijd vroeger zetten"), `increaseEndTime` (EN "Increase end time" / NL "Eindtijd later zetten"), `clearLocationOverride` (EN "Clear location override" / NL "Locatie-override wissen"), `feedbackStarRating` as an ICU plural with a bare `{count}` placeholder matching the existing `dragRunsSelected` pattern (EN `"{count, plural, =1{{count} star} other{{count} stars}}"` / NL `"{count, plural, =1{{count} ster} other{{count} sterren}}"`), `feedbackCommentLabel` (EN "Comment" / NL "Opmerking"). Run `flutter gen-l10n` to regenerate `lib/l10n/app_localizations.dart`, `app_localizations_en.dart`, `app_localizations_nl.dart`.

In `lib/features/home/home_screen.dart`: in `_buildPlannedRidesSliver` (the `final s = S.of(context);` local is already in scope at line ~525), add `tooltip: s.removePlannedRideTooltip` to the `IconButton(icon: const Icon(Icons.delete_outline), ...)` at line ~580 (A1). In the day-chip builder method (the one computing `chipBg`/`chipFg` from `isSelected` around line ~450-463), wrap the `return GestureDetector(...)` at line ~465 with `Semantics(selected: isSelected, button: true, child: <the existing GestureDetector>)` so the chip's selected state reaches the accessibility tree (D5).

In `lib/features/detail/ride_detail_screen.dart`: add `tooltip: S.of(context).showScoreDetails` to the `IconButton(icon: const Icon(Icons.info_outline), ...)` at line ~317 (A2, opens the Insights sheet). In the time-adjuster build method (the `final s = S.of(context);` local is already in scope there, used for `s.adjustTime`/`s.startLabel`/`s.endLabel`), add `tooltip: s.decreaseStartTime` to the `remove_circle_outline` IconButton at line ~691, `tooltip: s.increaseStartTime` to the `add_circle_outline` IconButton at line ~704, `tooltip: s.decreaseEndTime` to the `remove_circle_outline` IconButton at line ~722, `tooltip: s.increaseEndTime` to the `add_circle_outline` IconButton at line ~735 (A3).

In `lib/features/profile/profile_screen.dart`: add `tooltip: s.clearLocationOverride` to the `IconButton(icon: const Icon(Icons.clear), ...)` at line ~376-381 (`final s = S.of(context);` already in scope, used nearby for `s.useGpsLocation` etc.) (A4).

In `lib/features/onboarding/onboarding_screen.dart`: add `tooltip: MaterialLocalizations.of(context).backButtonTooltip` to the back `IconButton` at line ~103-108 — no new ARB key, matches the existing `SafeBackButton` pattern's use of Flutter's built-in localization for this exact string (A5). Do not touch the `padding`/`visualDensity` overrides here — those are fixed in Task 3.

In `lib/features/profile/feedback_dialog.dart`: in the 5-star `List.generate(5, (i) => ...)` loop at lines ~83-94 (`final s = S.of(context);` already in scope at line 74), add `tooltip: s.feedbackStarRating(n)` to each `IconButton`, and wrap each returned `IconButton` in `Semantics(selected: selected, button: true, child: <the IconButton>)` so the currently-selected star count reaches the accessibility tree (A6). On the `TextField` at lines ~96-103, add `labelText: s.feedbackCommentLabel` inside the existing `InputDecoration` alongside the existing `hintText: s.feedbackCommentHint` (Material's `InputDecoration` supports both simultaneously — hint remains a placeholder, label persists once text is entered) (D2).
  </action>
  <verify>
    <automated>flutter gen-l10n && dart analyze lib/features/home/home_screen.dart lib/features/detail/ride_detail_screen.dart lib/features/profile/profile_screen.dart lib/features/onboarding/onboarding_screen.dart lib/features/profile/feedback_dialog.dart lib/l10n/app_localizations.dart && flutter test test/features/feedback_dialog_test.dart</automated>
  </verify>
  <done>All 6 tooltip/Semantics/label additions are present, `flutter gen-l10n` succeeds with no missing-key errors, `dart analyze` reports zero new issues on the 5 touched lib files, and `test/features/feedback_dialog_test.dart` passes.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Semantics for custom-gesture widgets — weather icon, weather metric info icon, Availability grid, Agenda grid (Priority A7-A10, D1)</name>
  <files>lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/features/shared/weather_indicator_bar.dart, lib/features/shared/weather_icon.dart, lib/features/availability/availability_screen.dart, lib/features/agenda/week_agenda_screen.dart</files>
  <action>
Add 7 new ARB key pairs to BOTH `lib/l10n/app_nl.arb` and `lib/l10n/app_en.arb`: `weatherMetricInfoTooltip` with a bare `{label}` placeholder (EN `"{label} info"` / NL `"{label} info"`), `weatherTierDescription` with a bare `{tier}` placeholder (EN `"{tier} riding weather"` / NL `"{tier} fietsweer"`), `cellStatusPlanned` (EN "Planned" / NL "Gepland"), `cellStatusSelected` (EN "Selected" / NL "Geselecteerd"), `cellStatusBlocked` (EN "Blocked" / NL "Geblokkeerd"), `dayHeaderToggleHint` (EN "Double tap to toggle all hours for this day" / NL "Dubbeltik om alle uren voor deze dag te wisselen"), `hourHeaderToggleHint` (EN "Double tap to toggle this hour across all days" / NL "Dubbeltik om dit uur voor alle dagen te wisselen"). Run `flutter gen-l10n` to regenerate the localization files.

In `lib/features/shared/weather_indicator_bar.dart`: add the import `package:ridewindow/l10n/app_localizations.dart` (not currently imported). Replace the `GestureDetector(onTap: () => _showInfo(context), child: Icon(Icons.info_outline, size: 14, color: rw.textHint))` block at lines ~111-114 with a `Semantics(button: true, label: S.of(context).weatherMetricInfoTooltip(label), child: GestureDetector(onTap: () => _showInfo(context), behavior: HitTestBehavior.opaque, child: SizedBox(width: 48, height: 48, child: Center(child: Icon(Icons.info_outline, size: 14, color: rw.textHint)))))`. This widens the info-icon element from 14px to 48px, which is safe: the enclosing `Row` has an `Expanded` bar segment (the `CustomPaint` between the value and the ideal-range label) that absorbs the extra width automatically — verified by reading the full `Row`'s children list, no overflow risk (A7 + B6).

In `lib/features/shared/weather_icon.dart`: add the import `package:ridewindow/l10n/app_localizations.dart`. In `build()`, compute a local `tierLabel` via the same tier switch already used elsewhere (`Perfect() => S.of(context).tierPerfect`, `Great() => S.of(context).tierGreat`, `Acceptable() => S.of(context).tierAcceptable`, `Poor() => S.of(context).tierPoor`), then wrap the existing `return SizedBox(width: widget.size, height: widget.size, child: switch (widget.tier) {...})` in `Semantics(label: S.of(context).weatherTierDescription(tierLabel), child: <existing SizedBox>)` (D1).

In `lib/features/availability/availability_screen.dart`: extract the day-name-formatting + status-switch logic currently inlined in `_showCellInfo` (lines ~605-616: `DateFormat('EEEE', locale).format(key)` capitalized, and the `blockedHours[key]` switch over `BlockType.work/calendar/custom/null` mapping to `S.of(context).cellInfoStatusWork/cellInfoStatusCalendar/cellInfoStatusCustom/cellInfoStatusFree`) into a new private method `String _cellSemanticLabel(BuildContext context, DateTime key, Map<DateTime, BlockType> blockedHours, int hour)` returning `'$dayName ${hour.toString().padLeft(2, '0')}:00, $status'`. Call this new helper from `_showCellInfo` (replacing its duplicated inline logic — the bottom-sheet header text stays identical) AND wrap `_buildCell`'s `GestureDetector(onTap: ..., onLongPress: ..., child: Container(...))` at lines ~236-250 in `Semantics(label: _cellSemanticLabel(context, key, blockedHours, hour), button: true, child: <existing GestureDetector>)` (A8, highest-value fix in this audit — the core 7x24 grid currently has zero accessibility info). Also wrap the day-header `GestureDetector` at lines ~145-147 in `Semantics(hint: S.of(context).dayHeaderToggleHint, button: true, child: ...)` and the hour-header `GestureDetector` at lines ~168-170 in `Semantics(hint: S.of(context).hourHeaderToggleHint, button: true, child: ...)` (A10).

In `lib/features/agenda/week_agenda_screen.dart`: in `_CellWidget.build` (a `ConsumerWidget`, has `context`/`ref`), compute a semantic label using the pattern already established in `_showDetail` (same file: `Localizations.localeOf(context).languageCode == 'en' ? 'en_US' : 'nl_NL'`, `DateFormat('EEEE', locale)`) combined with the already-computed `score`/`blocked`/`isPlanned`/`isSelected` local variables: build `'$dayName $hour:00'`, then if `score != null` append `', ${S.of(context).weatherTierDescription(_tierLabel(score.overall, context))}'`, then conditionally append `', ${S.of(context).cellStatusBlocked}'` if `blocked`, `', ${S.of(context).cellStatusPlanned}'` if `isPlanned`, `', ${S.of(context).cellStatusSelected}'` if `isSelected`. Wrap the `GestureDetector(onTap: onTap, onLongPress: () => _showDetail(...), child: Container(...))` at lines ~515-540 in `Semantics(label: <the composed label>, button: true, child: <existing GestureDetector>)` (A9).
  </action>
  <verify>
    <automated>flutter gen-l10n && dart analyze lib/features/shared/weather_indicator_bar.dart lib/features/shared/weather_icon.dart lib/features/availability/availability_screen.dart lib/features/agenda/week_agenda_screen.dart && grep -c "Semantics(" lib/features/availability/availability_screen.dart lib/features/agenda/week_agenda_screen.dart lib/features/shared/weather_indicator_bar.dart lib/features/shared/weather_icon.dart</automated>
  </verify>
  <done>All 4 Semantics wraps (weather metric info icon, weather tier icon, Availability grid cell, Agenda grid cell) plus the 2 Availability header hints are present with correct labels sourced from `S.of(context)`, `flutter gen-l10n` succeeds, `dart analyze` reports zero new issues on the 4 touched lib files, and each file contains at least one `Semantics(` call (grep count >=1 per file).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Touch-target sizing (Priority B) and contrast fixes (Priority C), plus dependent test-file updates</name>
  <files>lib/theme/app_colors.dart, lib/features/availability/availability_screen.dart, lib/features/agenda/week_agenda_screen.dart, lib/features/profile/profile_screen.dart, lib/features/onboarding/onboarding_screen.dart, test/features/availability_screen_test.dart</files>
  <action>
**Priority C — contrast (`lib/theme/app_colors.dart`), do this first since Task 1/2 already reference these tokens indirectly via `context.rw`:**
Change `TierColors.light.acceptableFg` from `0xFFE65100` to `0xFFBF360C` (5.11:1 on `acceptableBg` 0xFFFFF3E0, was 3.46:1 — FAIL to PASS). Leave `TierColors.dark.acceptableFg` unchanged (already 9.35:1, passes). Change `AppColors.lightTextHint` from `0xFF999999` to `0xFF6B6B6B` (5.33:1 on white / 4.89:1 on `lightSurface`, was 2.85:1/2.61:1 — FAIL to PASS). Change `AppColors.darkTextHint` from `0xFF757575` to `0xFF949494` (5.5:1 on `darkSurface` / 4.6:1 on `darkSurfaceDim`, was 3.62:1/3.03:1 — FAIL to PASS; this plan's own WCAG recomputation found the token used for real label text at multiple sites, not just decorative fills, so both light and dark variants are fixed). Add a one-line code comment above each changed constant citing the new contrast ratio, so a future contributor doesn't accidentally "fix" it back to the old value.

**Priority B — touch targets:**

`lib/features/availability/availability_screen.dart`: raise the `_cellHeight` floor from `if (_cellHeight < 16) _cellHeight = 16;` to `if (_cellHeight < 48) _cellHeight = 48;` (B1). Raise `_headerHeight` from the hardcoded `28` to `48` (B2, day-header row — only 1 row tall, no width budget conflict). Raise `_headerWidth` from the hardcoded `36` to `44` — NOT the full 48dp, because this is the single hour-label column on the left shared across all 24 rows and every extra dp here is subtracted from all 7 day columns' width on narrow phones (360-390dp wide); document this as an inline comment: `// 44dp not 48dp: narrower-but-improved tradeoff to preserve day-column width on narrow phones (see backlog #9 research)`. This satisfies the plan's own must_haves allowance for a documented narrower-but-improved target.

`lib/features/agenda/week_agenda_screen.dart`: convert the hour-row layout from fixed `Expanded` rows inside a `Column` (which computes to ~30-40dp per row on typical phones, B3) to a `SingleChildScrollView` wrapping a `Column` of fixed-height `SizedBox(height: 48, child: <existing row content>)` rows — read the existing `_kHours` iteration and row-building code first to preserve the exact per-cell content/gesture wiring, only change the outer layout mechanism from "fill available height via Expanded" to "fixed 48dp per row, scrollable if it overflows the viewport." This is a visual behavior change (the grid may now require vertical scrolling on shorter screens instead of always fitting exactly) — acceptable per this plan's must_haves, since a 48dp minimum tap target takes priority over guaranteed no-scroll fit.

`lib/features/profile/profile_screen.dart`: change `constraints: const BoxConstraints(minWidth: 28, minHeight: 28)` to `constraints: const BoxConstraints(minWidth: 48, minHeight: 48)` on the tier/tolerance-explainer `IconButton` at line ~748-749 (B4, clearest single violation in the codebase).

`lib/features/onboarding/onboarding_screen.dart`: remove `padding: EdgeInsets.zero` and `visualDensity: VisualDensity.compact` from the back `IconButton` at line ~103-107 (B5) — Flutter's Material 3 `IconButton` default constraints already provide a ≥48dp tap target once these overrides are removed; no explicit `constraints:` needed.

**Dependent test-file update (MUST be in this same task, per the plan's own interfaces note on test/production coupling):**
`test/features/availability_screen_test.dart` duplicates the cell-sizing formula in 3 places (~line 446, ~490, ~747) to compute tap coordinates. Update each occurrence's `cellHeight`/`cellCenter` computation to use the new `_headerHeight = 48` and `_headerWidth = 44` baselines (replacing the old literal `36`/`28` values used in the y/x-offset math) and confirm the resulting effective `cellHeight` for the test's fixed viewport is `>= 48.0` given the new floor — if the test's own computed raw cellHeight (before flooring) is still below 48 at its fixed viewport size, the floor now applies and the test's tap-coordinate math must use `48.0` directly rather than the raw formula result, mirroring what production code now does.
  </action>
  <verify>
    <automated>dart analyze lib/theme/app_colors.dart lib/features/availability/availability_screen.dart lib/features/agenda/week_agenda_screen.dart lib/features/profile/profile_screen.dart lib/features/onboarding/onboarding_screen.dart && flutter test test/features/availability_screen_test.dart test/features/onboarding_screen_test.dart test/features/profile_screen_test.dart</automated>
  </verify>
  <done>All 3 `app_colors.dart` contrast tokens are updated to the computed passing values with inline comments; Availability grid cell/header floors are raised to 48dp (44dp for the hour-label column, documented tradeoff); Agenda grid rows are 48dp fixed-height and scrollable; the Profile info button and Onboarding back button meet the 48dp default; `test/features/availability_screen_test.dart`'s 3 duplicated coordinate-math blocks are updated to match the new dimensions and the full test suite for the 3 touched test files passes.</done>
</task>

</tasks>

<threat_model>
Security enforcement is disabled for this project (`workflow.security_enforcement: false` in `.planning/config.json`). This plan touches only client-side Flutter UI code (widget tree, theme constants, l10n strings) — no network calls, no data persistence changes, no new dependencies, no user input handling changes beyond adding accessibility metadata to existing interactive elements. No new trust boundaries are introduced.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-f1m-01 | Tampering | Availability/Agenda grid layout change (touch-target enlargement) | accept | Purely visual/layout change to first-party widget code; no data or security boundary affected. Regression risk is covered by the existing widget test suites for both screens, updated in Task 3 |
| T-f1m-02 | Information Disclosure | New Semantics labels expose day/hour/status text to accessibility tree | accept | This is the intended fix — the same information is already visible on-screen to sighted users via the existing `Text`/`Container` styling; Semantics labels only make already-visible information available to screen readers, disclosing nothing new |

</threat_model>

<verification>
1. `flutter gen-l10n` succeeds with no missing-key errors across both ARB files after all 3 tasks.
2. `dart analyze` reports zero new issues across all touched `lib/` files.
3. `grep -c "Semantics(" lib/` returns >=13 (was 0 before this plan) across the touched files.
4. `flutter test` for the 3 directly-touched test files (`availability_screen_test.dart`, `onboarding_screen_test.dart`, `profile_screen_test.dart`) plus `feedback_dialog_test.dart` all pass — no regressions from the layout/constant changes.
5. A full `flutter test` run afterward shows no NEW failures versus the 282/2 baseline established in the prior `test-binding-state-leakage` debug session (the 2 pre-existing out-of-scope failures in `providers/slots_notifier_test.dart`/`providers/integration_test.dart` are unrelated to this plan and may remain).
</verification>

<success_criteria>
- Every icon-only button and both custom grid cell widgets audited in Priority A now expose a meaningful, distinct `Semantics`/`tooltip` label in both English and Dutch.
- Every touch target audited in Priority B meets the 48dp minimum, or uses a narrower-but-improved size with the tradeoff documented inline (Availability's hour-label column only).
- The one confirmed WCAG AA contrast failure (`acceptableFg`) and the systemic `textHint` near-fail (both light and dark) are corrected to pass 4.5:1.
- No existing test regresses; the 3 test files with production-code-coupled coordinate math are updated in the same task as their corresponding production change.
- Scope stays within the research's Priority A/B/C findings — no redesign of the grid interaction model, no new dependencies, no changes outside the files listed in this plan's frontmatter.
</success_criteria>

<output>
Create `.planning/quick/260718-f1m-backlog-9-accessibility-audit-screenread/260718-f1m-SUMMARY.md` when done
</output>