---
phase: 16-pwa-installability-ios-polish
plan: 04
type: execute
wave: 2
depends_on: ["16-01", "16-02", "16-03"]
files_modified: []
autonomous: false
requirements: [PWA-05]

must_haves:
  truths:
    - "On a real iPhone in Safari, adding RideWindow to the Home Screen shows the correct branded icon and a branded splash on launch (not the Flutter default, not a blank white flash)"
    - "The installed app launches standalone (no Safari browser chrome) and the 'Add to Home Screen' banner is gone once installed"
    - "In-app back/close navigation has no dead ends anywhere in the installed standalone app"
    - "Data written to Drift before leaving the installed app is confirmed present (or the eviction outcome is explicitly documented) after leaving it untouched for several days"
  artifacts: []
  key_links: []
---

<objective>
Deploy the Plan 16-01/02/03 changes to the live Firebase Hosting domain and manually verify the full PWA-05 install + standalone + storage-durability story on a real iPhone in Safari — this cannot be automated (no simulator/desktop dev-tools substitute is accepted per REQUIREMENTS.md PWA-05 and ROADMAP Phase 16 success criterion #5).

Purpose: Confirms the icon/splash/meta-tag work (16-01), the Add-to-Home-Screen overlay (16-02), and the back/close navigation fixes (16-03) actually work together on the one platform that matters for this entire phase — real iOS Safari, not desktop Chrome or a simulator.
Output: A deployed build on the existing production URL, plus a documented, dated real-device verification result (including the deliberately delayed multi-day storage-eviction check).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-pwa-installability-ios-polish/16-CONTEXT.md
@.planning/phases/15-google-calendar-web-integration/15-02-SUMMARY.md
@firebase.json
@.firebaserc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and deploy the Phase 16 changes to the existing Firebase Hosting domain</name>
  <files>None (deploy-only; firebase.json/.firebaserc already exist from Phase 15-02, unchanged here)</files>
  <read_first>
    firebase.json
    .firebaserc
    .planning/phases/15-google-calendar-web-integration/15-02-SUMMARY.md
  </read_first>
  <action>
    Run `flutter build web --release` (CanvasKit renderer — do not pass `--wasm`, per WEB-02/the phase's established convention) from the repo root, then `firebase deploy --only hosting` using the existing `my-project-joost` project (`.firebaserc` default, already configured in Phase 15-02 — no new Firebase/GCP setup needed). This publishes Plan 16-01's branded icons/splash/manifest, Plan 16-02's Add-to-Home-Screen overlay, and Plan 16-03's navigation fixes to the live URL `https://my-project-joost.web.app`.
  </action>
  <acceptance_criteria>
    `flutter build web --release` completes with zero errors.
    `firebase deploy --only hosting` completes and reports the live hosting URL.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && flutter build web --release && firebase deploy --only hosting 2>&1 | tail -20</automated>
  </verify>
  <done>The latest build (including all Phase 16 changes) is live at https://my-project-joost.web.app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real iPhone Safari verification of install, standalone mode, navigation, safe-area, and storage durability (PWA-05)</name>
  <files>none</files>
  <read_first>.planning/phases/16-pwa-installability-ios-polish/16-CONTEXT.md (D-04, D-05, PWA-05's explicit "real iPhone Safari, not simulator/desktop dev tools" wording)</read_first>
  <action>
    Branded Home Screen icon + splash screens (16-01), a persistent "Add to Home Screen" banner shown only in iOS Safari browser mode (16-02), and fixed in-app back/close navigation with no dead ends (16-03) are all now live at https://my-project-joost.web.app following Task 1's deploy. Walk the user through the real-device verification below — there is no automatable or simulator-based substitute for any of these steps.
  </action>
  <verify>
    <human-check>
      On a real iPhone, in Safari (not a simulator, not desktop Chrome dev tools):

      1. Open https://my-project-joost.web.app in Safari. Confirm the "Tap the Share icon, then 'Add to Home Screen'" banner appears at the top of every screen you visit (Home, Profile, Availability, Ride Detail, etc.).
      2. Tap Share → "Add to Home Screen" → Add. Confirm the Home Screen icon shown is the black-rounded-square white-"RW"-monogram logo (not the default Flutter logo).
      3. Open the app from the new Home Screen icon. Confirm: (a) a branded green splash screen briefly appears (not a blank white flash), (b) the app opens with NO Safari address bar/browser chrome (standalone mode), (c) the "Add to Home Screen" banner is now gone on every screen.
      4. With the app open in standalone mode, navigate: Onboarding → "Set my own schedule" (if reachable) or directly to the Availability screen, and confirm the back button in the top-left actually navigates you somewhere (not inert). Navigate to a ride's Detail screen from Home and confirm its back button works. Force-quit and relaunch the app directly (simulating a cold launch) and confirm you are not stuck on any screen with no way to get back to Home.
      5. Check that content near the top of the screen (e.g. the Home screen's greeting header) is not obscured by the notch/Dynamic Island, and that the bottom NavigationBar tabs are not obscured by the Home indicator area.
      6. Toggle at least one Availability cell (write test data) and note the time. Leave the installed app untouched (do not reopen it) for several days, then reopen it and confirm whether that change is still present. This step confirms or refutes whether ITP's storage-eviction policy exempts installed PWAs (an explicitly unconfirmed research gap) — document the actual outcome (data survived / data was evicted) whenever it is completed, even if that is after this session ends.

      Reply "approved" if steps 1-5 all pass. If step 6's multi-day check hasn't happened yet, say so explicitly — note it as a pending follow-up rather than blocking the rest of the phase. Describe any failures for steps 1-5 (they must be fixed before this phase can close).
    </human-check>
  </verify>
  <acceptance_criteria>
    User confirms steps 1-5 all pass on a real iPhone in Safari (not a simulator or desktop browser).
    Step 6's outcome (data survived / data evicted / not yet checked) is explicitly recorded in the plan's SUMMARY, even if the multi-day wait hasn't elapsed yet at execution time.
  </acceptance_criteria>
  <done>All 5 immediate verification steps pass on a real iPhone in Safari; step 6's storage-durability outcome is documented (or explicitly flagged as a pending follow-up if the multi-day wait hasn't elapsed).</done>
</task>

</tasks>

<verification>
Steps 1-5 of the checkpoint are the phase's hard gate (cannot be automated — real iPhone Safari only, per PWA-05). Step 6 (multi-day storage-eviction check) may be reported after this plan's execution session ends; document its outcome in the SUMMARY once known, and carry it forward as a noted follow-up if not yet available.
</verification>

<success_criteria>
Real iPhone Safari confirms: correct branded icon + splash, standalone launch with the install banner gone, no in-app navigation dead ends, correct safe-area layout, and a documented (even if pending) answer on whether written data survives several days of the installed app being untouched.
</success_criteria>

<output>
Create `.planning/phases/16-pwa-installability-ios-polish/16-04-SUMMARY.md` when done
</output>
