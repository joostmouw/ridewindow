---
phase: 33-datamodel-en-rechten
plan: 02
type: execute
wave: 2
depends_on: [33-01]
files_modified: [supabase/tests/clubs_deny_test.sql]
autonomous: true
requirements: [CLUB-17, CLUB-18, CLUB-10, CLUB-19]

must_haves:
  truths:
    - "Eén SQL-bestand, te plakken in de Supabase SQL Editor, draait alles in één transactie die altijd eindigt met rollback — er blijft niets achter in de live database"
    - "Het resultaat is een zichtbare tabel (check_name, want, got, ok), geen raise notice; bij één mislukte check stopt het script met een exception die de namen van de mislukte checks noemt"
    - "De test bewijst: buitenstaander en ex-lid lezen niets van groep, leden, links, groepsrit, deelnemers, opties, stemmen (CLUB-17)"
    - "De test bewijst: gewoon lid kan geen beheerdershandeling; beheerder wel; lid kan wel een groepsrit maken (ook met RETURNING) en zijn eigen antwoord geven (CLUB-17)"
    - "De test bewijst: group_full bij lid 31, too_many_groups bij groep 11, en create_group is atomisch (CLUB-18)"
    - "De test bewijst: opvolging bij vertrek en bij delete_own_account, last_admin bij zelf-degraderen, lege groep verdwijnt, groepsritten blijven met group_id null en de deelnemer ziet ze weer (CLUB-10, CLUB-19, CLUB-11)"
  artifacts:
    - path: "supabase/tests/clubs_deny_test.sql"
      provides: "Deny- en positieve tests voor 0012, als SQL-Editor-script"
      contains: "rollback;"
      min_lines: 250
  key_links:
    - from: "supabase/tests/clubs_deny_test.sql"
      to: "public.delete_own_account()"
      via: "aanroep als gesimuleerde user Q (CLUB-19 via het echte pad)"
      pattern: "delete_own_account\\(\\)"
    - from: "supabase/tests/clubs_deny_test.sql"
      to: "public.redeem_group_invite / public.create_group"
      via: "rpc-aanroepen onder set local role authenticated"
      pattern: "redeem_group_invite|create_group"
---

<objective>
Schrijf `supabase/tests/clubs_deny_test.sql`: het geautomatiseerde bewijs voor fase 33 (roadmap-criteria 2–5). Joost plakt het na 0012 in de SQL Editor; het resultaat is één tabel waarin elke regel `ok = true` heeft.

Purpose: CLUB-17 eist dat deny-tests de rechten bewijzen; CLUB-10/18/19 zijn gedrag van triggers dat alleen in een echte database te zien is. Er is lokaal geen Postgres (geen psql, docker of supabase CLI), dus dit script ís de test.
Output: één SQL-bestand in het patroon van supabase/tests/rls_deny_test.sql, uitgebreid met een expect-helper en een eindcontrole.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/clubs/phases/33-datamodel-en-rechten/33-CONTEXT.md
@.planning/workstreams/clubs/phases/33-datamodel-en-rechten/33-01-PLAN.md
@supabase/migrations/0012_groups.sql
@supabase/tests/rls_deny_test.sql
@supabase/migrations/0002_peloton.sql
@supabase/migrations/0010_group_ride_options.sql

<interfaces>
Uit 0012 (plan 01) — namen exact:
- groups(id, name, created_by, created_at, updated_at); group_members(group_id, user_id, role, display_name, joined_at); group_invites(code, group_id, created_by, created_at, expires_at); group_rides.group_id
- is_group_member(uuid, boolean default false); create_group(p_name text) returns uuid; redeem_group_invite(p_code text) returns table(group_id uuid, group_name text)
- Fouten (P0001): not_authenticated, group_name_invalid, invite_invalid, group_full, too_many_groups, last_admin. RLS-weigering en kolomrecht-weigering: SQLSTATE 42501. Check-constraint: 23514.
- Client-grants: groups select/delete/update(name); group_members select/delete/insert(group_id,user_id)/update(role); group_invites select/delete/insert(code,group_id,created_by,expires_at)
Bestaand: profiles-kolommen (zie rls_deny_test.sql seed), friendships(user_a < user_b), group_rides(owner_id,start_at,end_at,planned_score,...), group_ride_participants(ride_id,user_id,status,display_name), group_ride_options(ride_id,start_at,end_at,planned_score), group_ride_option_votes(option_id,user_id,can_ride), delete_own_account().
Codes moeten voldoen aan ^[A-HJKMNP-TW-Z2-9]{8}$ — gebruik GRPTESTA (geldig, G), GRPXPRD2 (verlopen, G), GRPFZZZ3 (groep GF), GRPNEWA2 (door A aan te maken). Let op: I, L, O, U, V zijn niet toegestaan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Raamwerk, gebruikers en seed, plus buitenstaander-, lid- en beheerderschecks</name>
  <files>supabase/tests/clubs_deny_test.sql</files>
  <read_first>supabase/tests/rls_deny_test.sql (hele bestand: kop, replica-seed, set local role/claims, temp-tabel, eind-select, rollback); supabase/migrations/0012_groups.sql (werkelijke namen van policies, functies, fouten — volg het bestand als het afwijkt van dit plan en noteer dat in de SUMMARY); supabase/migrations/0002_peloton.sql (kolommen group_rides, participants, friendships)</read_first>
  <action>
Kop (Nederlands, zelfde toon als rls_deny_test.sql): wat het bewijst (CLUB-17/18/10/19/11), hoe te draaien (SQL Editor, ná 0012, nieuwe query), dat alles in één transactie zit die met rollback eindigt en dus veilig herhaalbaar is op de live database, en hoe het resultaat te lezen (elke rij ok = true; een fout noemt de mislukte check-namen). Leg de techniek uit: echte rijen in auth.users voor de hoofdpersonen (nodig omdat FK's, triggers en delete_own_account echt moeten afgaan — replica schakelt triggers uit), replica alleen voor bulk-seed waar triggers juist níét mogen lopen (vaste joined_at, 30 opvulleden).

Raamwerk:
- `begin;` Temp-tabel club_test_results (seq serial, check_name text, want text, got text, ok boolean) `on commit drop`; `grant select, insert on` die tabel en `grant usage on` zijn sequence aan authenticated (de checks draaien onder die rol).
- Helper `pg_temp.expect(p_name text, p_want text, p_got text)` (language plpgsql) die een rij invoegt met ok = (p_want is not distinct from p_got) en NIET raiset — zo verschijnen alle resultaten. Aanroepen altijd gekwalificeerd als pg_temp.expect. Voor verwachte fouten: in elk DO-blok de handeling in een `begin ... exception when others then` zetten en SQLERRM (voor P0001-sleutels) of SQLSTATE (voor 42501/23514) als got vastleggen; slaagt de handeling onverwacht, dan got = 'ok'.
- Wisselen van gebruiker: `reset role;` (terug naar postgres voor seed/controle), dan `set local role authenticated;` en `set local request.jwt.claims = '{"sub":"<uuid>","role":"authenticated"}';`.

Vaste ids (hex-geldig): gebruikers A c1000000-0000-0000-0000-00000000000a (beheerder G), B ...0b (lid G), C ...0c (buitenstaander), D ...0d (maatje van A, geen lid), E ...0e (lid G dat vertrekt), P ...01, Q ...02, R ...03 (opvolgingsgroep S). Groepen G c2000000-0000-0000-0000-000000000001, S ...02, GF ...03. Ritten R0 c3000000-0000-0000-0000-000000000001 (eigenaar A, groep G), RS ...02 (eigenaar R, groep S). Optie O0 c4000000-0000-0000-0000-000000000001 op R0.

Seed als postgres, triggers AAN: auth.users-rijen voor A,B,C,D,E,P,Q,R met alleen (id, aud 'authenticated', role 'authenticated', email '<letter>@clubs-test.invalid'); profiles voor D met user_name 'Dirk' (kolommen zoals in rls_deny_test.sql); friendship A–D via least/greatest.
Seed onder `set local session_replication_role = replica` (daarna `reset session_replication_role`): groep G (naam 'Testgroep', created_by A) met leden A admin joined_at now()-3d, B member -2d, E member -1d; groep S (created_by Q) met P admin -3d, Q member -2d, R member -1d; invites GRPTESTA (G, geldig 7 dagen) en GRPXPRD2 (G, expires_at in het verleden, created_at daarvóór); R0 (group_id G, eigenaar A) met optie O0 en een stem van A (can_ride true) en een participant-rij voor E (accepted); RS (group_id S, eigenaar R) met participant-rij voor P (accepted).

Checks sectie 1 — buitenstaander C (want tussen haakjes): 1.1 select groups G (0); 1.2 group_members van G (0); 1.3 group_invites van G (0); 1.4 group_rides R0 (0); 1.5 participants R0 (0); 1.6 options R0 (0); 1.7 votes op O0 (0); 1.8 insert group_rides met group_id G en owner C (42501); 1.9 insert eigen participant-rij op R0 (42501); 1.10 update groups.name van G (0 rijen); 1.11 delete group G (0 rijen).

Sectie 2 — gewoon lid B: 2.1 select G (1); 2.2 leden G (3); 2.3 R0 (1); 2.4 stemmen O0 (1); 2.5 invites G (0); 2.6 insert invite voor G (42501); 2.7 delete GRPTESTA (0 rijen); 2.8 insert lid D in G (42501); 2.9 update rol E naar admin (0 rijen); 2.10 update eigen rol naar admin (0 rijen); 2.11 delete lidmaatschap A (0 rijen); 2.12 rename G (0 rijen); 2.13 delete G (0 rijen); 2.14 insert groepsrit R1 (group_id G, owner B) met `returning id into` — de 0003-valkuil — (ok); 2.15 insert eigen participant-rij op R0 status accepted (ok); 2.16 insert participant-rij voor C op R0 (42501); 2.17 stem op O0 (ok); 2.18 insert groepsrit met group_id S (42501, niet zijn groep).

Sectie 3 — beheerder A: 3.1 select invites G (2); 3.2 insert invite GRPNEWA2 (ok); 3.3 insert invite code 'ABC' (23514); 3.4 delete GRPNEWA2 (1 rij); 3.5 insert lid D in G mét joined_at '2000-01-01' (42501 — kolomrecht; opvolging draait op joined_at); 3.6 insert lid D in G met alleen group_id en user_id (ok); 3.7 als postgres: display_name van D in G ('Dirk') en joined_at van D ≥ transactiestart (want 'true'); 3.8 insert lid C in G — geen maatje (42501); 3.9 update rol B → admin (1 rij); 3.10 update rol B → member (1 rij); 3.11 update eigen rol → member als enige admin (last_admin); 3.12 rename G naar 'Testgroep 2' (1 rij); 3.13 delete lidmaatschap D (1 rij); 3.14 D select G (0 — verwijderd lid ziet niets meer).

Plaats na sectie 3 nog GEEN eind-select; Task 2 vult aan.
  </action>
  <verify>
    <automated>f=supabase/tests/clubs_deny_test.sql; test -f $f && grep -q "^begin;" $f && grep -q "session_replication_role = replica" $f && grep -q "set local role authenticated" $f && grep -q "request.jwt.claims" $f && grep -q "pg_temp.expect" $f && grep -q "insert into auth.users" $f && grep -q "GRPTESTA" $f && grep -q "returning id into" $f && grep -q "last_admin" $f && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Checks 1.1–1.11, 2.1–2.18 en 3.1–3.14 bestaan elk als aparte pg_temp.expect-aanroep; check_name begint met het nummer (bv. '2.14 lid maakt groepsrit met returning'), zodat `grep -c "pg_temp.expect('[123]\." supabase/tests/clubs_deny_test.sql` ≥ 43
    - Alle codes in het bestand voldoen aan de regex: `grep -o "GRP[A-Z0-9]*" supabase/tests/clubs_deny_test.sql | sort -u | grep -v -E '^[A-HJKMNP-TW-Z2-9]{8}$'` levert niets op
    - Geen `raise notice` in het bestand
    - De temp-tabel krijgt expliciete grants aan authenticated (`grep -q "to authenticated"`)
  </acceptance_criteria>
  <done>Raamwerk, seed en de 43 checks voor buitenstaander, lid en beheerder staan in het bestand.</done>
</task>

<task type="auto">
  <name>Task 2: Ex-lid, grenzen, opvolging, account verwijderen, opheffen en eindcontrole</name>
  <files>supabase/tests/clubs_deny_test.sql</files>
  <read_first>supabase/tests/clubs_deny_test.sql (zoals Task 1 hem achterliet); supabase/migrations/0012_groups.sql secties guard_group_member_insert, ensure_group_admin, create_group, redeem_group_invite; supabase/migrations/0001_accounts_sync.sql regels 232–257 (delete_own_account)</read_first>
  <action>
Vul het bestand aan met onderstaande secties, elke check via pg_temp.expect, genummerd.

Sectie 4 — inwisselen en ex-lid: 4.1 C redeem GRPXPRD2 (invite_invalid); 4.2 C redeem 'grptesta' in kleine letters met spaties eromheen (ok, geeft G terug — want de groepsnaam 'Testgroep 2'); 4.3 C select G (1); 4.4 C select R0 (1 — nieuw lid ziet een rit van vóór zijn lidmaatschap, CLUB-13); 4.5 C redeem GRPTESTA opnieuw (ok) en als postgres: aantal rijen van C in G (1); 4.6 E verlaat G: delete eigen rij (1 rij); 4.7–4.12 E select groups G, leden G, R0, participants R0, options R0, votes O0 (elk 0 — ook zijn eigen participant-rij en stem geven geen toegang meer); 4.13 E insert eigen participant-rij op R1 (42501).

Sectie 5 — grenzen (CLUB-18). Seed als postgres onder replica: groep GF met 30 opvulleden (gen_random_uuid()-users via generate_series, role member; eerste rij admin) en invite GRPFZZZ3 voor GF; negen extra groepen met B als lid (B zit dan in 10: G + 9). Reset replica. 5.1 C redeem GRPFZZZ3 (group_full); 5.2 als postgres: aantal leden GF (30); 5.3 B create_group('Elfde') (too_many_groups); 5.4 als postgres: aantal groepen met naam 'Elfde' (0 — atomisch); 5.5 C create_group('  Clubtest  ') (ok, uuid terug); 5.6 als postgres: C is admin van die groep en de naam is 'Clubtest' (want 'admin|Clubtest'); 5.7 C create_group('   ') (group_name_invalid).

Sectie 6 — opvolging en account verwijderen (CLUB-10, CLUB-19), groep S: 6.1 P verlaat S (1 rij); 6.2 als postgres: rol Q in S ('admin' — langst zittende); 6.3 P select RS (0 — ex-lid, participant-rij telt niet); 6.4 als Q (set local role authenticated + claims Q): `perform public.delete_own_account()` (ok); 6.5 als postgres: auth.users-rij Q bestaat niet meer (0); 6.6 rol R in S ('admin'); 6.7 groep S bestaat nog (1) en created_by is null (want 'true'); 6.8 R degradeert zichzelf als enige admin (last_admin); 6.9 R verlaat S (1 rij); 6.10 als postgres: groep S bestaat niet meer (0); 6.11 RS bestaat nog met group_id null (want 'true' — CLUB-11); 6.12 P select RS (1 — wie geantwoord heeft ziet de rit weer als gewone gedeelde rit).

Sectie 7 — beheerder heft op (CLUB-11): 7.1 A delete G (1 rij); 7.2 als postgres: group_members van G (0) en group_invites van G (0); 7.3 R0 en R1 bestaan met group_id null (want '2'); 7.4 B select R0 (1 — B antwoordde in 2.15); 7.5 C select R0 (0 — lid geweest zonder antwoord).

Eindcontrole als postgres (reset role): een DO-blok dat, als er rijen met ok = false zijn, `raise exception 'CLUBS DENY-TEST FAILED: %'` doet met string_agg van de mislukte check_names (plus want/got); anders niets. Daarna `select seq, check_name, want, got, ok from club_test_results order by seq;` en als laatste regel `rollback;`. Werk de kop bij met het verwachte aantal rijen (tel ze: 43 uit Task 1 + deze) en de melding dat elke rij ok = true moet zijn.
  </action>
  <verify>
    <automated>f=supabase/tests/clubs_deny_test.sql; tail -3 $f | grep -q "^rollback;" && grep -q "CLUBS DENY-TEST FAILED" $f && grep -q "delete_own_account()" $f && grep -q "group_full" $f && grep -q "too_many_groups" $f && grep -q "group_name_invalid" $f && grep -q "invite_invalid" $f && grep -q "generate_series" $f && test "$(grep -c "^begin;" $f)" -eq 1 && ! grep -q "^commit;" $f && ! grep -q "raise notice" $f && test $(( $(grep -o '\$\$' $f | wc -l) % 2 )) -eq 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "pg_temp.expect('[4567]\." supabase/tests/clubs_deny_test.sql` ≥ 36 (4.1–4.13, 5.1–5.7, 6.1–6.12, 7.1–7.5 — samengevoegde postgres-controles mogen één expect delen als de check_name het zegt)
    - Het bestand eindigt met `rollback;` en bevat geen `commit;` — niets blijft achter
    - De eind-select selecteert check_name, want, got, ok uit club_test_results, gesorteerd op seq
    - De kop noemt het verwachte aantal rijen en dat dat getal gelijk is aan het aantal expect-aanroepen: `grep -c "pg_temp.expect(" supabase/tests/clubs_deny_test.sql` (min de definitie) komt overeen met het in de kop genoemde getal
    - delete_own_account wordt aangeroepen onder `set local role authenticated` met claims van Q, niet als postgres
  </acceptance_criteria>
  <done>Het script bewijst alle vijf roadmap-criteria 2–5 in één run, toont een volledige resultaattabel en rolt alles terug.</done>
</task>

</tasks>

<verification>
- Beide automated-checks slagen.
- Het echte resultaat komt uit plan 03 (Joost draait het in de SQL Editor na 0012).
- Mislukt een check daar, dan is óf 0012 óf de test fout: de check-naam en want/got in de foutmelding wijzen welke; herstel in 0012 moet herdraaibaar blijven.
</verification>

<success_criteria>
supabase/tests/clubs_deny_test.sql bestaat, volgt het rls_deny_test.sql-patroon, dekt elke scenario-regel uit dit plan, en is veilig op de live database te draaien.
</success_criteria>

<output>
Create `.planning/workstreams/clubs/phases/33-datamodel-en-rechten/33-02-SUMMARY.md` when done
</output>
