---
phase: 33-datamodel-en-rechten
plan: 03
type: execute
wave: 3
depends_on: [33-01, 33-02]
files_modified: [CLAUDE.md, AGENTS.md, .planning/PROJECT.md, .planning/workstreams/clubs/REQUIREMENTS.md, .planning/workstreams/clubs/ROADMAP.md]
autonomous: false
requirements: [CLUB-20, CLUB-17, CLUB-18, CLUB-10, CLUB-19]

must_haves:
  truths:
    - "CLAUDE.md en AGENTS.md noemen hetzelfde aantal server-functies als het schema werkelijk heeft (elf na 0012), met alle namen in CLAUDE.md"
    - "De afwijking van 'acht' uit CLUB-20 is traceerbaar vastgelegd in REQUIREMENTS.md, ROADMAP.md en PROJECT.md — geen stille wijziging"
    - "Migratie 0012 is toegepast op de live database (project hcdrydlgqpnmumfupgcx) — door Joost, in de SQL Editor"
    - "clubs_deny_test.sql is op de live database gedraaid en elke resultaatrij heeft ok = true"
  artifacts:
    - path: "CLAUDE.md"
      provides: "No backend-alinea met elf functies"
      contains: "eleven of them today"
    - path: "AGENTS.md"
      provides: "Constraint-regel met elf functies"
      contains: "Elf vandaag"
  key_links:
    - from: "CLAUDE.md"
      to: "supabase/migrations/0012_groups.sql"
      via: "zelfde functienamen als in de kop van 0012"
      pattern: "redeem_group_invite"
---

<objective>
Breng de projectdocumentatie in lijn met het schema (CLUB-20) en laat Joost 0012 plus de deny-test op de live database draaien — het blokkerende bewijs voor roadmap-criteria 1–5.

Purpose: CONTEXT "Server-functies": "De telling volgt het schema, niet de roadmap". 0012 brengt het totaal op elf (zie kop van 0012), niet acht; dat moet in CLAUDE.md, AGENTS.md en de plannen van deze workstream kloppen. Er is geen Supabase CLI en geen databasewachtwoord op deze machine: alleen Joost kan de migratie toepassen.
Output: bijgewerkte docs; live toegepaste migratie; een door Joost gerapporteerde resultaattabel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md
@supabase/migrations/0012_groups.sql
@supabase/tests/clubs_deny_test.sql
@.planning/workstreams/clubs/REQUIREMENTS.md
@.planning/workstreams/clubs/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Functietelling bijwerken in CLAUDE.md, AGENTS.md en de workstream-docs</name>
  <files>CLAUDE.md, AGENTS.md, .planning/PROJECT.md, .planning/workstreams/clubs/REQUIREMENTS.md, .planning/workstreams/clubs/ROADMAP.md</files>
  <read_first>CLAUDE.md (de bullet "No backend (revised in v3.0)" in Constraints); AGENTS.md regels ~90–105 (bullet "Geen server-side code behalve plpgsql-functies"); supabase/migrations/0012_groups.sql (kop, sectie FUNCTIETELLING — de bron van waarheid voor namen en indeling); .planning/PROJECT.md regel ~56 (v4.2 Key context: "van zes naar acht"); .planning/workstreams/clubs/REQUIREMENTS.md (CLUB-20); .planning/workstreams/clubs/ROADMAP.md (fase 33, criterium 5)</read_first>
  <action>
Tel eerst de werkelijke functies: de unieke namen achter `create or replace function public.` over alle bestanden in supabase/migrations. Verwacht 11. Wijkt het af van wat 0012 in zijn kop noemt, stop en meld het — dan klopt plan 01 niet.

CLAUDE.md, bullet "No backend (revised in v3.0)", alleen de zin over server-side functies vervangen (Engels, zelfde toon, rest van de bullet ongemoeid): "`plpgsql` functions only, eleven of them today, each earning its place by doing something the client provably cannot: six are called via `rpc()` because they must be a single transaction or must read rows the caller may not select directly (`migrate_account_data` for first-login migration, `delete_own_account`, `friend_profiles`, `redeem_friend_invite`, `create_group`, `redeem_group_invite`), two are `security definer` helpers that RLS policies call (`is_ride_member`, `is_group_member`), and three are trigger functions (`set_updated_at` stamps `updated_at`; `guard_group_member_insert` enforces the 30-member / 10-group limits; `ensure_group_admin` keeps every group with at least one admin and removes an empty group)." De zinnen "New functions are justified in the migration that adds them. No Edge Functions, no other server-side code." blijven staan. Raak de rest van CLAUDE.md niet aan (privacy van groepsleden is fase 36, CLUB-21).

AGENTS.md: in de bullet "Geen server-side code behalve plpgsql-functies" wordt "Zes vandaag" → "Elf vandaag"; de rest van die bullet blijft (CLAUDE.md blijft de bron van waarheid, AGENTS.md verwijst ernaar).

.planning/PROJECT.md, v4.2 Key context: vervang "Twee nieuwe plpgsql-functies (`redeem_group_invite`, `is_group_member`) brengen het totaal van zes naar acht" door een zin die zegt dat 0012 vijf functies toevoegt (redeem_group_invite, is_group_member, create_group, guard_group_member_insert, ensure_group_admin) en het totaal op elf brengt, omdat grenzen en opvolging alleen met triggers in de database af te dwingen zijn en atomisch aanmaken een rpc vraagt. Raak regel ~175 (v3.0-historie) niet aan.

.planning/workstreams/clubs/REQUIREMENTS.md CLUB-20 en ROADMAP.md fase 33 criterium 5: laat de oorspronkelijke tekst staan en voeg er direct achter een korte Nederlandse noot aan toe: "(bijgesteld 2026-09-23 per 33-CONTEXT 'De telling volgt het schema': 0012 voegt vijf functies toe — ook create_group en de triggerfuncties guard_group_member_insert en ensure_group_admin — en de telling wordt elf; zie de kop van 0012)". Wijzig .planning/STATE.md, .planning/ROADMAP.md en .planning/REQUIREMENTS.md op topniveau NIET (die horen bij v4.1).
  </action>
  <verify>
    <automated>cd /Users/joostmouw/ridewindow && test "$(grep -ho 'create or replace function public\.[a-z_]*' supabase/migrations/*.sql | sort -u | wc -l | tr -d ' ')" = "11" && grep -q "eleven of them today" CLAUDE.md && ! grep -q "six of them today" CLAUDE.md && grep -q "Elf vandaag" AGENTS.md && ! grep -q "Zes vandaag" AGENTS.md && for n in create_group redeem_group_invite is_group_member guard_group_member_insert ensure_group_admin; do grep -q "$n" CLAUDE.md || { echo "CLAUDE.md mist $n"; exit 1; }; done && grep -q "telling volgt het schema" .planning/workstreams/clubs/REQUIREMENTS.md && grep -q "telling volgt het schema" .planning/workstreams/clubs/ROADMAP.md && ! grep -q "van zes naar acht" .planning/PROJECT.md && git diff --quiet -- .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - Het aantal unieke `create or replace function public.<naam>` over supabase/migrations is 11 en gelijk aan het getal in CLAUDE.md ("eleven") en AGENTS.md ("Elf")
    - CLAUDE.md noemt alle elf functienamen in de No backend-bullet
    - REQUIREMENTS.md (workstream) en ROADMAP.md (workstream) bevatten de noot met "telling volgt het schema"; de oorspronkelijke tekst "acht" staat er nog
    - `git diff --quiet -- .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md` slaagt (v4.1-bestanden onaangeroerd)
  </acceptance_criteria>
  <done>Documentatie en schema noemen hetzelfde aantal functies; de afwijking van "acht" is verklaard op elke plek waar "acht" stond.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Joost past 0012 toe en draait de deny-test op de live database</name>
  <files>supabase/migrations/0012_groups.sql, supabase/tests/clubs_deny_test.sql</files>
  <read_first>supabase/migrations/0012_groups.sql (controle-footer); supabase/tests/clubs_deny_test.sql (kop: verwacht aantal rijen)</read_first>
  <action>
Waarom Joost: er is geen Supabase CLI en geen databasewachtwoord op deze machine; migraties gaan via de SQL Editor (zoals 0010 en 0011). Claude bereidt alles voor en zet het klaar; Joost plakt en rapporteert.

Claude doet vooraf: kopieer de volledige inhoud van supabase/migrations/0012_groups.sql naar het klembord met `pbcopy < supabase/migrations/0012_groups.sql` en noem in het bericht aan Joost het verwachte aantal resultaatrijen uit de kop van clubs_deny_test.sql.

Stappen voor Joost:
1. Open https://supabase.com/dashboard/project/hcdrydlgqpnmumfupgcx/sql/new
2. Plak (0012 staat op het klembord) en klik Run. Verwacht: "Success. No rows returned". Een fout rolt alles terug (begin/commit) — plak dan de foutmelding terug; herdraaien na een fix is veilig.
3. Optioneel: draai de pg_proc-query uit de controle-footer van 0012 — verwacht 11 rijen.
4. Nieuwe query. Claude zet dan clubs_deny_test.sql op het klembord (`pbcopy < supabase/tests/clubs_deny_test.sql`). Plak en Run. Verwacht: een tabel met het genoemde aantal rijen en in de kolom ok overal true. Het script eindigt met rollback — er blijft niets achter.
5. Meld het resultaat: "approved" als alle rijen ok = true zijn, anders de foutmelding (die noemt de mislukte checks met want/got) of een screenshot van de tabel.

Bij een mislukte check: Claude herstelt 0012 (herdraaibaar) of de test — afhankelijk van wat de want/got aanwijst — en de stappen 2–5 worden herhaald. Leg in 33-03-SUMMARY.md vast: datum van toepassen, aantal rijen, en eventuele herstelrondes.
  </action>
  <what-built>Migratie 0012_groups.sql (groepen, lidmaatschap, RLS, vijf nieuwe functies) en de deny-test clubs_deny_test.sql, plus bijgewerkte functietelling in CLAUDE.md/AGENTS.md.</what-built>
  <how-to-verify>Zie stappen 1–5 in de action: 0012 draait zonder fout, en clubs_deny_test.sql geeft een tabel waarin elke rij ok = true heeft.</how-to-verify>
  <verify>
    <human-check>Joost bevestigt: 0012 toegepast zonder fout op hcdrydlgqpnmumfupgcx, en clubs_deny_test.sql toont het verwachte aantal rijen met overal ok = true.</human-check>
  </verify>
  <acceptance_criteria>
    - Joost meldt "approved" of levert een resultaattabel waarin elke rij ok = true heeft
    - Het aantal rijen in Joosts resultaat is gelijk aan het in de kop van clubs_deny_test.sql genoemde aantal
    - 33-03-SUMMARY.md noteert de datum van toepassen en het rijenaantal
  </acceptance_criteria>
  <resume-signal>Typ "approved" als alle rijen ok = true zijn, of plak de foutmelding / resultaattabel</resume-signal>
  <done>0012 staat live en de deny-test is groen op de live database: roadmap-criteria 1–5 van fase 33 zijn bewezen.</done>
</task>

</tasks>

<verification>
- Task 1 automated-check slaagt.
- Task 2: Joosts bevestiging met volledige groene tabel.
- Samen: criterium 1 (live toegepast), 2–4 (deny-test rijen secties 1–7), 5 (delete_own_account-sectie 6 + docs-telling).
</verification>

<success_criteria>
Fase 33 is af: het schema staat live, de rechten zijn bewezen door een groene deny-test op de echte database, en de documentatie telt dezelfde elf functies als het schema.
</success_criteria>

<output>
Create `.planning/workstreams/clubs/phases/33-datamodel-en-rechten/33-03-SUMMARY.md` when done
</output>
