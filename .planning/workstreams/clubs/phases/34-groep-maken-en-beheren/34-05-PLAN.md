---
phase: 34-groep-maken-en-beheren
plan: 05
type: execute
wave: 5
depends_on: [34-04]
files_modified:
  - lib/features/peloton/group_screen.dart
  - lib/features/peloton/group_propose_sheet.dart
  - lib/features/peloton/groups_section.dart
  - lib/l10n/app_nl.arb
  - lib/l10n/app_en.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_nl.dart
  - lib/l10n/app_localizations_en.dart
  - test/features/group_members_admin_test.dart
  - test/features/group_requests_test.dart
autonomous: true
requirements: [CLUB-03, CLUB-05, CLUB-07, CLUB-08, CLUB-27]

must_haves:
  truths:
    - "Alleen een beheerder ziet een ⋮ achter de leden; een gewoon lid ziet geen ⋮ en geen sectie Aanvragen"
    - "Een beheerder maakt een ander lid beheerder of neemt die rol af; de enige beheerder die zichzelf wil degraderen krijgt 'Maak eerst iemand anders beheerder'"
    - "Een beheerder haalt een lid uit de groep met een snackbar 'Ongedaan maken'; ongedaan maken laat het lid staan zonder dat er iets naar de database ging"
    - "Een beheerder ziet bovenaan het groepsscherm de open aanvragen met 'via de groepslink' of 'voorgedragen door X' en kan elke aanvraag accepteren of afwijzen; bij een volle groep of een aanvrager in 10 groepen komt een gewone zin"
    - "Ieder lid kan een maatje voordragen; een beheerder voegt een maatje direct toe; wie al lid is of al een aanvraag heeft staat erbij maar is niet aan te tikken"
    - "Op de Peloton-tab ziet een beheerder op de groepskaart hoeveel aanvragen er openstaan"
  artifacts:
    - path: "lib/features/peloton/group_propose_sheet.dart"
      provides: "showGroupProposeSheet: maatjes voordragen (lid) of toevoegen (beheerder)"
      contains: "proposeGroupMember"
    - path: "lib/features/peloton/group_screen.dart"
      provides: "⋮ per lid voor beheerders, sectie Aanvragen, uitgestelde verwijdering met ongedaan maken"
      contains: "SnackBarClosedReason.action"
  key_links:
    - from: "lib/features/peloton/group_screen.dart"
      to: "PelotonGateway.acceptGroupRequest / deleteGroupRequest / setGroupMemberRole / removeGroupMember"
      via: "ref.read(pelotonGatewayProvider)"
      pattern: "acceptGroupRequest\\(|setGroupMemberRole\\(|removeGroupMember\\("
    - from: "lib/features/peloton/group_propose_sheet.dart"
      to: "friendsProvider + pelotonGroupProvider"
      via: "ref.watch in de sheet, zodat de lijst meebeweegt na elke voordracht"
      pattern: "friendsProvider"
---

<objective>
Het ledenbeheer op het groepsscherm: ⋮ per lid voor beheerders (schets 015 vraag 2 variant A), eruit halen met ongedaan maken, beheerder maken en afnemen met de last_admin-uitleg, de sectie Aanvragen met accepteren/afwijzen (CLUB-27) en voordragen of toevoegen van een maatje (CLUB-03 herzien).

Purpose: CLUB-03, CLUB-07, CLUB-08 en CLUB-27. Volgt de HERZIENING in 34-CONTEXT: ieder lid draagt voor, alleen beheerders maken lid; een gewoon lid ziet nog steeds geen ⋮ en geen Aanvragen-sectie.
Output: uitgebreid group_screen.dart, group_propose_sheet.dart, teller op de groepskaart, twee testbestanden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-CONTEXT.md
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-04-SUMMARY.md
@lib/features/peloton/group_screen.dart
@lib/features/peloton/groups_section.dart
@test/helpers/fake_group_gateway.dart
@test/features/peloton_withdraw_test.dart

<interfaces>
Uit plan 03/04:
- PelotonGroup: members, requests, isAdmin(uid), adminCount, memberFor(uid), openRequests(uid), isFull, name, memberCount
- GroupJoinRequest: id, userId, proposedBy, displayName, proposedByName, isViaLink, label(fallback)
- Gateway: setGroupMemberRole({groupId, userId, role}), removeGroupMember({groupId, userId}), acceptGroupRequest(requestId), deleteGroupRequest(requestId), proposeGroupMember({groupId, userId}) -> GroupJoinStatus
- groupErrorTextOf(s, error, {groupName, personName}); s.groupErrorLastAdmin, s.groupErrorFullNamed(group)
- GroupScreen: ConsumerStatefulWidget; _MemberRow heeft `Widget? trailingAction` (nu null); SectionCard(action:) bestaat; GroupAdminChip bestaat
- AppIcons.dotsThreeVertical, AppIcons.userPlus, AppIcons.crown
- friendsProvider (List<Friend>{userId, displayName, label(fallback)})
- s.pelotonUndo = "Ongedaan maken", s.pelotonUnnamedFriend = "Fietser"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ⋮ per lid voor beheerders: rol wijzigen, last_admin, eruit halen met ongedaan maken</name>
  <files>lib/features/peloton/group_screen.dart, lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_nl.dart, lib/l10n/app_localizations_en.dart, test/features/group_members_admin_test.dart</files>
  <read_first>lib/features/peloton/group_screen.dart (uit plan 04); lib/features/planned/planned_rides_screen.dart (zoek de afzeg-snackbar met pelotonUndo: hoe de bestaande ongedaan-maken-snackbar eruitziet); test/features/peloton_withdraw_test.dart; .planning/sketches/015-clubs-groepen/index.html (vraag 2 variant A: menu "Beheerder maken / Uit de groep halen"; last_admin-regel)</read_first>
  <behavior>
    - Gewoon lid: geen enkel dotsThreeVertical-icoon in de ledenlijst
    - Beheerder: een ⋮ achter elk lid, ook achter zichzelf
    - Menu achter een gewoon lid: 'Beheerder maken' en 'Uit de groep halen'; achter een andere beheerder: 'Beheerder af' en 'Uit de groep halen'; achter jezelf alleen 'Beheerder af'
    - 'Beheerder maken' logt setGroupMemberRole:g1:uid-x:admin en daarna staat de chip bij dat lid
    - Enige beheerder kiest 'Beheerder af' bij zichzelf: de zin 'Je bent de enige beheerder. Maak eerst iemand anders beheerder, dan kun je dit afgeven.' verschijnt en er gaat niets naar de gateway
    - Gooit de gateway last_admin (race), dan verschijnt dezelfde zin
    - 'Uit de groep halen': het lid verdwijnt meteen uit de lijst en een snackbar '<naam> is uit de groep gehaald' met 'Ongedaan maken' verschijnt; tik op ongedaan maken -> het lid staat er weer en de log bevat geen removeGroupMember
    - Sluit de snackbar zonder ongedaan maken (hideCurrentSnackBar of verlopen), dan logt de gateway removeGroupMember:g1:uid-x
    - Faalt die verwijdering, dan komt het lid terug en verschijnt de generieke foutzin
  </behavior>
  <action>
In group_screen.dart (per 34-CONTEXT: ⋮ alleen voor beheerders, verwijderen met snackbar-undo zoals afzeggen):
- Bepaal isAdmin = group.isAdmin(me). Alleen dan krijgt elke _MemberRow een trailingAction: een PopupMenuButton<_MemberAction> met Icon(AppIcons.dotsThreeVertical) en tooltip s.groupMemberMenuTooltip. Items: voor jezelf alleen demote; voor een ander lid promote of demote naar zijn rol, plus remove (tekst in colorScheme.error).
- Promote/demote: _run-achtige busy-vlag; bij demote van jezelf terwijl group.adminCount == 1: snackbar s.groupErrorLastAdmin en stop (voorcontrole voor een duidelijke melding; de database weigert het ook). Anders gateway.setGroupMemberRole(...), ref.invalidate(visibleGroupsProvider), snackbar s.groupMadeAdmin(naam) of s.groupAdminRemoved(naam). catch: snackbar groupErrorTextOf(s, e). Demote van jezelf zet het scherm na verversen vanzelf in de ledenweergave.
- Remove: uitgestelde verwijdering. Houd in de State een Set<String> _pendingRemovals bij; filter die userIds uit de getoonde leden (en uit het getoonde ledental in de hero). Pak vóór alles de gateway (ref.read(pelotonGatewayProvider)), de ProviderContainer (ProviderScope.containerOf(context, listen: false)) en de ScaffoldMessenger, zodat de afhandeling ook werkt als het scherm intussen gesloten is. Toon SnackBar(content: s.groupMemberRemoved(naam), action: SnackBarAction(label: s.pelotonUndo)). Op controller.closed: reden SnackBarClosedReason.action -> haal de userId uit _pendingRemovals (setState alleen als mounted); elke andere reden -> gateway.removeGroupMember(groupId, userId), dan container.invalidate(visibleGroupsProvider) en de userId uit de set; bij een fout de userId uit de set, container.invalidate en via de bewaarde messenger groupErrorTextOf. Leg in een doccomment vast waarom uitgesteld: opnieuw toevoegen na verwijderen kan alleen voor maatjes van de beheerder en zet joined_at en rol opnieuw, dus echt ongedaan maken kan alleen door de verwijdering nog niet te versturen.
ARB (NL / EN): groupMemberMenuTooltip "Opties voor dit lid" / "Options for this member"; groupMakeAdmin "Beheerder maken" / "Make admin"; groupRemoveAdmin "Beheerder af" / "Remove as admin"; groupRemoveMember "Uit de groep halen" / "Remove from group"; groupMadeAdmin "{name} is nu beheerder" / "{name} is now an admin"; groupAdminRemoved "{name} is geen beheerder meer" / "{name} is no longer an admin"; groupMemberRemoved "{name} is uit de groep gehaald" / "{name} was removed from the group". flutter gen-l10n.

test/features/group_members_admin_test.dart: GoRouter-opzet zoals group_screen_test.dart, FakeGroupGateway met groep g1 (jij beheerder + gewoon lid Ingrid + tweede beheerder Anna, en een variant waarin jij de enige beheerder bent), en een variant waarin jij gewoon lid bent. Alle behavior-cases; voor 'snackbar sluiten' gebruik ScaffoldMessenger.of(tester.element(find.byType(GroupScreen))).hideCurrentSnackBar() en pumpAndSettle.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/group_members_admin_test.dart test/features/group_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "SnackBarClosedReason.action" lib/features/peloton/group_screen.dart geeft minstens 1
    - grep -c "_pendingRemovals" lib/features/peloton/group_screen.dart geeft minstens 3
    - grep -c "AppIcons.dotsThreeVertical" lib/features/peloton/group_screen.dart geeft minstens 1
    - flutter test test/features/group_members_admin_test.dart slaagt met minstens 9 tests; group_screen_test.dart blijft groen
  </acceptance_criteria>
  <done>Beheerders beheren rollen en leden via ⋮, de laatste beheerder krijgt uitleg in plaats van een fout, en eruit halen is echt ongedaan te maken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Aanvragen accepteren of afwijzen, maatje voordragen of toevoegen, teller op de groepskaart</name>
  <files>lib/features/peloton/group_screen.dart, lib/features/peloton/group_propose_sheet.dart, lib/features/peloton/groups_section.dart, lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_nl.dart, lib/l10n/app_localizations_en.dart, test/features/group_requests_test.dart</files>
  <read_first>lib/features/peloton/group_screen.dart (na Task 1); lib/features/peloton/groups_section.dart; lib/features/peloton/invite_buddies_sheet.dart (bestaande maatjeslijst in een sheet: opbouw, lege staat pelotonNeedFriendsFirst); .planning/sketches/015-clubs-groepen/index.html (sheet "Maatje toevoegen": banner bij vol, "Ingrid al lid", "Jacco Toevoegen", "Jacco zit al in 10 groepen"); 34-CONTEXT sectie HERZIENING (Aanvragen bovenaan, alleen beheerders; "+ Maatje voordragen" voor ieder lid, bij een beheerder "+ Maatje toevoegen")</read_first>
  <behavior>
    - Beheerder met twee open aanvragen: sectie 'Aanvragen' staat boven 'Leden'; de ene rij zegt 'via de groepslink', de andere 'voorgedragen door Anna'
    - Gewoon lid (ook als hij zelf iemand voordroeg): geen sectie 'Aanvragen'
    - 'Accepteren' logt acceptGroupRequest:r1; daarna staat die persoon in de ledenlijst en is de aanvraag weg, met snackbar '<naam> is nu lid'
    - Accepteren met group_full geeft een zin met de groepsnaam en 30; met too_many_groups een zin met de naam van de aanvrager en 10
    - 'Afwijzen' logt deleteGroupRequest:r2 en de rij verdwijnt
    - Sectiekop Leden: gewoon lid ziet 'Maatje voordragen', beheerder ziet 'Maatje toevoegen'
    - In de sheet: een maatje dat al lid is toont 'al lid' zonder knop; een maatje met een open aanvraag toont 'aanvraag loopt' zonder knop; de rest heeft 'Voordragen' (lid) of 'Toevoegen' (beheerder)
    - Lid draagt Jacco voor: log proposeGroupMember:g1:uid-jacco, Jacco toont daarna 'aanvraag loopt' en er staat 'Jacco is voorgedragen. Een beheerder beslist.'
    - Beheerder voegt Jacco toe: Jacco toont daarna 'al lid'
    - Volle groep: de sheet toont bovenaan de zin van groupErrorFullNamed en geen knoppen
    - Geen maatjes: de sheet zegt hoe je een maatje uitnodigt
    - Op de Peloton-tab toont de kaart van een groep waar jij beheerder bent '2 aanvragen' als er twee openstaan; bij een gewoon lid niet
  </behavior>
  <action>
group_screen.dart:
- Als isAdmin en group.openRequests(me) niet leeg: SectionCard(title: s.groupRequestsSection) direct onder de hero en boven Leden. Per aanvraag een rij: CircleAvatar met initiaal, titel request.label(s.pelotonUnnamedFriend), subtitel request.isViaLink ? s.groupRequestViaLink : s.groupRequestProposedBy(request.proposedByName ?? s.pelotonUnnamedFriend), rechts FilledButton.tonal s.groupAccept en TextButton s.groupReject. Accept: gateway.acceptGroupRequest(id) -> invalidate visibleGroupsProvider -> snackbar s.groupRequestAccepted(naam); catch -> groupErrorTextOf(s, e, groupName: group.name, personName: naam). Reject: gateway.deleteGroupRequest(id) -> invalidate -> snackbar s.groupRequestRejected(naam); catch -> groupErrorTextOf. Busy-vlag per aanvraag zodat dubbel tikken niets dubbel doet.
- Leden-SectionCard krijgt action: TextButton.icon(AppIcons.userPlus, isAdmin ? s.groupAddFriend : s.groupProposeFriend) die showGroupProposeSheet(context, groupId: group.id) opent. Voor ieder lid (CLUB-03 herzien).

group_propose_sheet.dart: Future<void> showGroupProposeSheet(BuildContext context, {required String groupId}). Scrollbare sheet (isScrollControlled, showDragHandle, useSafeArea) met een Consumer die friendsProvider en pelotonGroupProvider(groupId) bekijkt (live, zodat een voordracht meteen zichtbaar wordt). Titel isAdmin ? s.groupAddFriend : s.groupProposeFriend, uitleg isAdmin ? s.groupAddFriendHint ("Wie je toevoegt is meteen lid.") : s.groupProposeFriendHint ("Een beheerder beslist of je maatje lid wordt."). Als group.isFull: banner (errorContainer-achtige kaart, zoals .banner in de schets; gebruik colorScheme.errorContainer/onErrorContainer zodat licht en donker kloppen) met s.groupErrorFullNamed(group.name) en alle knoppen uit. Geen maatjes: tekst s.groupNoFriendsToPropose. Per maatje een rij: naam; als group.isMember(friend.userId): trailing tekst s.groupAlreadyMember in onSurfaceVariant; anders als er in group.requests een aanvraag met die userId staat: s.groupRequestPendingShort; anders een FilledButton.tonal isAdmin ? s.groupAddAction : s.groupProposeAction. Tik: gateway.proposeGroupMember(groupId, userId) -> invalidate visibleGroupsProvider -> bij GroupJoinStatus.member tekst s.groupFriendAddedMember(naam), bij requested s.groupFriendProposed(naam), getoond als regel of banner binnen de sheet (niet als snackbar achter de sheet). catch -> dezelfde bannerplek met groupErrorTextOf(s, e, groupName: group.name, personName: naam).

groups_section.dart: toon op de kaart van een groep waar jij beheerder bent en openRequests(me).length > 0 een tweede chip s.groupOpenRequests(count) (plural) in colorScheme.tertiaryContainer/onTertiaryContainer, zodat een beheerder aanvragen ziet zonder de groep te openen (er zijn geen pushmeldingen: REQUIREMENTS Out of Scope).

ARB (NL / EN): groupRequestsSection "Aanvragen" / "Requests"; groupRequestViaLink "via de groepslink" / "via the group link"; groupRequestProposedBy "voorgedragen door {name}" / "proposed by {name}"; groupAccept "Accepteren" / "Accept"; groupReject "Afwijzen" / "Decline"; groupRequestAccepted "{name} is nu lid" / "{name} is now a member"; groupRequestRejected "Aanvraag van {name} afgewezen" / "Declined {name}'s request"; groupAddFriend "Maatje toevoegen" / "Add a buddy"; groupProposeFriend "Maatje voordragen" / "Propose a buddy"; groupAddFriendHint "Wie je toevoegt is meteen lid." / "Anyone you add is a member right away."; groupProposeFriendHint "Een beheerder beslist of je maatje lid wordt." / "An admin decides whether your buddy joins."; groupAlreadyMember "al lid" / "already a member"; groupAddAction "Toevoegen" / "Add"; groupProposeAction "Voordragen" / "Propose"; groupFriendAddedMember "{name} is nu lid" / "{name} is now a member"; groupFriendProposed "{name} is voorgedragen. Een beheerder beslist." / "{name} has been proposed. An admin decides."; groupNoFriendsToPropose "Je hebt nog geen maatjes. Nodig er een uit onder Maatjes op de Peloton-tab." / EN-equivalent; groupOpenRequests "{count, plural, =1{1 aanvraag} other{{count} aanvragen}}" / "{count, plural, =1{1 request} other{{count} requests}}". flutter gen-l10n.

test/features/group_requests_test.dart: alle behavior-cases met FakeGroupGateway (requests met isViaLink en met proposedByName 'Anna'; friends Ingrid (lid), Jacco (geen lid), Mark (open aanvraag); failWith voor acceptGroupRequest). Voor de tellerchip een aparte pump van BuddiesTab zoals in peloton_groups_section_test.dart.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/group_requests_test.dart test/features/group_members_admin_test.dart test/features/peloton_groups_section_test.dart && flutter analyze && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "acceptGroupRequest(" lib/features/peloton/group_screen.dart geeft minstens 1 en grep -c "deleteGroupRequest(" minstens 1
    - grep -c "proposeGroupMember(" lib/features/peloton/group_propose_sheet.dart geeft 1
    - grep -c "groupOpenRequests" lib/features/peloton/groups_section.dart geeft minstens 1
    - flutter test test/features/group_requests_test.dart slaagt met minstens 12 tests
    - flutter analyze schoon; volledige suite groen, aantal in de SUMMARY
  </acceptance_criteria>
  <done>Beheerders beslissen over aanvragen op het groepsscherm en zien ze al op de tab; ieder lid draagt een maatje voor en een beheerder voegt direct toe, met gewone zinnen bij elke grens.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI-verberging vs database | ⋮ en Aanvragen zijn alleen voor beheerders zichtbaar; de echte grens is RLS + rpc (0012/0013, bewezen in plan 02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-18 | Elevation of privilege | beheerknoppen voor gewone leden | transfer | Database weigert (not_allowed / RLS 0 rijen); UI verbergt alleen; test bewijst dat een lid geen ⋮ ziet |
| T-34-19 | Repudiation | uitgestelde verwijdering | accept | Wordt de app binnen de snackbartijd hard gesloten, dan blijft het lid staan; zichtbaar na verversen, geen stille schade |
| T-34-20 | Spoofing | namen in aanvragen | transfer | display_name en proposed_by_name komen uit de database (0013 keuze a) |
</threat_model>

<verification>
- flutter analyze schoon; flutter test volledig groen (aantal in SUMMARY)
</verification>

<success_criteria>
Een beheerder beheert leden, rollen en aanvragen vanaf het groepsscherm; ieder lid draagt voor; een gewoon lid ziet een rustig scherm.
</success_criteria>

<output>
Create `.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-05-SUMMARY.md` when done
</output>
