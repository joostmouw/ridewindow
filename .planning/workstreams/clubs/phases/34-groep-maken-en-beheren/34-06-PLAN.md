---
phase: 34-groep-maken-en-beheren
plan: 06
type: execute
wave: 6
depends_on: [34-05]
files_modified:
  - lib/features/peloton/group_link.dart
  - lib/features/peloton/group_dialogs.dart
  - lib/features/peloton/group_screen.dart
  - lib/l10n/app_nl.arb
  - lib/l10n/app_en.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_nl.dart
  - lib/l10n/app_localizations_en.dart
  - test/features/group_menu_test.dart
autonomous: true
requirements: [CLUB-02, CLUB-06, CLUB-09, CLUB-11]

must_haves:
  truths:
    - "Ieder lid ziet in de hero 'Deel de groepslink' en deelt tekst + link + code via het deelmenu"
    - "Het appbar-menu van een beheerder heeft Naam wijzigen, Link vervangen, Groep verlaten en Groep opheffen; van een gewoon lid alleen Groep verlaten"
    - "Naam wijzigen gebruikt dezelfde sheet als maken (max 40, teller), met de huidige naam ingevuld"
    - "Link vervangen maakt de oude link ongeldig en biedt meteen 'Delen' voor de nieuwe"
    - "Verlaten vraagt eerst bevestiging; de tekst zegt wie beheerder wordt als jij de enige beheerder bent, en dat de groep verdwijnt als jij het laatste lid bent"
    - "Opheffen vraagt bevestiging met het aantal leden en dat ritten met antwoorden blijven zonder groepslabel; het is de enige onomkeerbare handeling"
  artifacts:
    - path: "lib/features/peloton/group_link.dart"
      provides: "kGroupLinkBase + groupLinkFor(code)"
      contains: "/#/group"
    - path: "lib/features/peloton/group_dialogs.dart"
      provides: "showLeaveGroupDialog, showDisbandGroupDialog"
      contains: "showDisbandGroupDialog"
    - path: "lib/features/peloton/group_screen.dart"
      provides: "deelknop in de hero en het appbar-menu"
      contains: "groupInviteCode("
  key_links:
    - from: "lib/features/peloton/group_screen.dart"
      to: "Share.share"
      via: "s.groupShareText(naam, groupLinkFor(code), code)"
      pattern: "groupLinkFor\\("
    - from: "lib/features/peloton/group_screen.dart"
      to: "PelotonGateway.leaveGroup / deleteGroup / renameGroup / replaceGroupInvite"
      via: "appbar-PopupMenuButton"
      pattern: "leaveGroup\\(|deleteGroup\\(|renameGroup\\(|replaceGroupInvite\\("
---

<objective>
De groepsbrede handelingen: de groepslink delen (ieder lid, per HERZIENING), en het appbar-menu met naam wijzigen, link vervangen, verlaten en opheffen (schets 015 vraag 2 variant A).

Purpose: CLUB-02 (delen), CLUB-06, CLUB-09, CLUB-11. Vastgelegde keuze (34-CONTEXT "kies en leg vast"): **verlaten krijgt een bevestiging, geen ongedaan-maken**, want terugkomen kan alleen met een nieuwe aanvraag die een beheerder moet goedkeuren; **eruit halen** heeft wel ongedaan maken (plan 05); **opheffen** heeft een bevestiging omdat het onomkeerbaar is.
Output: group_link.dart, group_dialogs.dart, uitbreiding van group_screen.dart, een testbestand.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-CONTEXT.md
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-05-SUMMARY.md
@lib/features/peloton/group_screen.dart
@lib/features/peloton/invite_landing_screen.dart
@lib/features/peloton/buddies_tab.dart
@lib/features/shared/unplan_confirm_dialog.dart

<interfaces>
- Gateway (plan 03): groupInviteCode(groupId) -> code (hergebruik of nieuw; ieder lid), replaceGroupInvite(groupId) -> code (beheerder), renameGroup({groupId, name}), leaveGroup(groupId), deleteGroup(groupId)
- PelotonGroup: name, memberCount, isAdmin(uid), isLastMember(uid), successorIfLeaving(uid) -> GroupMember?
- showGroupNameSheet(context, {initialName, title, hint, actionLabel}) -> Future<String?> (plan 04)
- groupErrorTextOf(s, e, {groupName}); s.cancel bestaat (gebruikt in unplan_confirm_dialog.dart)
- kInviteLinkBase = 'https://my-project-joost.web.app/#/invite' en inviteLinkFor(code) in invite_landing_screen.dart; de '/#/' is nodig (hash-routing)
- Deelpatroon in buddies_tab._shareInvite: messenger vóór await, trackEvent(ref, kEvPelotonInvite, props: {'kind': ...}) zonder code, Share.share(tekst), catch -> debugPrint + snackbar
- share_plus 10: MethodChannel('dev.fluttercommunity.plus/share'); in tests mocken via TestDefaultBinaryMessengerBinding.instance.defaultBinaryMessenger.setMockMethodCallHandler (voorbeeld: test/features/profile_screen_links_test.dart regel ~95)
- AppIcons.shareNetwork, pencilSimple, arrowsCounterClockwise, signOut, trash, dotsThreeVertical
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Groepslink delen (ieder lid) en link vervangen (beheerder)</name>
  <files>lib/features/peloton/group_link.dart, lib/features/peloton/group_screen.dart, lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_nl.dart, lib/l10n/app_localizations_en.dart, test/features/group_menu_test.dart</files>
  <read_first>lib/features/peloton/invite_landing_screen.dart (kInviteLinkBase-doccomment over /#/ en het domein); lib/features/peloton/buddies_tab.dart (_shareInvite); lib/features/peloton/group_screen.dart; test/features/profile_screen_links_test.dart (mock van een platformkanaal)</read_first>
  <behavior>
    - Zowel een gewoon lid als een beheerder ziet 'Deel de groepslink' in de hero; een aanvrager niet
    - Tik logt groupInviteCode:g1 en roept het deelkanaal aan met een tekst die de groepsnaam, 'https://my-project-joost.web.app/#/group/<code>' en de code bevat
    - Faalt groupInviteCode, dan verschijnt de generieke foutzin
    - Link vervangen (beheerdersmenu) logt replaceGroupInvite:g1, toont 'Nieuwe link gemaakt. De oude werkt niet meer.' met actie 'Delen', en 'Delen' deelt de nieuwe code
  </behavior>
  <action>
group_link.dart: const kGroupLinkBase = 'https://my-project-joost.web.app/#/group' en String groupLinkFor(String code), met een doccomment die naar kInviteLinkBase verwijst (zelfde domein, zelfde /#/-reden; verandert het domein, dan beide) en noteert dat de live PWA de route /group pas kent na de uitrol in fase 36.

group_screen.dart:
- In _GroupHero, alleen voor leden (niet voor de aanvraagstaat): FilledButton.tonalIcon(AppIcons.shareNetwork, s.groupShareLink) full width. _shareLink(group): messenger vóór await; code = await gateway.groupInviteCode(group.id); trackEvent(ref, kEvPelotonInvite, props: {'kind': 'group_link_created'}) (nooit code of naam); await Share.share(s.groupShareText(group.name, groupLinkFor(code), code)); catch -> debugPrint + snackbar groupErrorTextOf.
- Voeg aan de appbar-actions (na GroupRulesButton) een PopupMenuButton<_GroupAction> toe (Icon(AppIcons.dotsThreeVertical), tooltip s.groupMenuTooltip). In deze taak alleen het item replaceLink (alleen beheerders, icoon arrowsCounterClockwise, s.groupReplaceLink); Task 2 voegt de andere toe. replaceLink: code = await gateway.replaceGroupInvite(group.id) -> snackbar s.groupLinkReplaced met SnackBarAction(s.groupShareAction) die met die code deelt; catch -> groupErrorTextOf.

ARB (NL / EN, geen em-dash): groupShareLink "Deel de groepslink" / "Share the group link"; groupShareText "Fiets mee met {group} in Ridewindow: {link}\n\nNog geen app? De link werkt gewoon in je browser. Of vul code {code} in onder Ritten, tab Peloton. Een beheerder van de groep laat je erin." / EN-equivalent, placeholders group, link, code; groupMenuTooltip "Groepsopties" / "Group options"; groupReplaceLink "Link vervangen" / "Replace link"; groupLinkReplaced "Nieuwe link gemaakt. De oude werkt niet meer." / "New link created. The old one no longer works."; groupShareAction "Delen" / "Share". flutter gen-l10n.

test/features/group_menu_test.dart (deel 1): mock het kanaal 'dev.fluttercommunity.plus/share' en leg de argumenten vast (tekst zit in arguments['text']); behavior-cases 1-4.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/group_menu_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "kGroupLinkBase = 'https://my-project-joost.web.app/#/group'" lib/features/peloton/group_link.dart geeft 1
    - grep -c "groupLinkFor(" lib/features/peloton/group_screen.dart geeft minstens 1
    - grep -c "'kind': 'group_link_created'" lib/features/peloton/group_screen.dart geeft 1
    - flutter test test/features/group_menu_test.dart slaagt met minstens 4 tests
  </acceptance_criteria>
  <done>Ieder lid deelt de groepslink met code, een beheerder vervangt hem en deelt meteen de nieuwe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Appbar-menu: naam wijzigen, verlaten met bevestiging, opheffen met bevestiging</name>
  <files>lib/features/peloton/group_dialogs.dart, lib/features/peloton/group_screen.dart, lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_nl.dart, lib/l10n/app_localizations_en.dart, test/features/group_menu_test.dart</files>
  <read_first>lib/features/shared/unplan_confirm_dialog.dart (dialoogvorm: builder-context popt, rode bevestigknop); lib/features/profile/profile_screen.dart regels 1366-1373 (noot over dialogContext en de shell-navigator); lib/features/peloton/group_screen.dart (na Task 1); .planning/sketches/015-clubs-groepen/index.html (dialoog "Dinsdagclub opheffen?")</read_first>
  <behavior>
    - Gewoon lid: het menu bevat alleen 'Groep verlaten'; beheerder: 'Naam wijzigen', 'Link vervangen', 'Groep verlaten', 'Groep opheffen' in die volgorde
    - Naam wijzigen: de sheet staat open met de huidige naam; 'Nieuw' opslaan logt renameGroup:g1:Nieuw en de appbar toont 'Nieuw'; dezelfde naam opslaan logt niets
    - Verlaten als gewoon lid: dialoog '<groep> verlaten?' met de tekst dat terugkomen een nieuwe aanvraag vraagt; Annuleren logt niets; Verlaten logt leaveGroup:g1, gaat terug en toont '<groep> verlaten'
    - Verlaten als enige beheerder met andere leden: de dialoog noemt het langst zittende andere lid als nieuwe beheerder
    - Verlaten als laatste lid: de dialoog zegt dat de groep en de link verdwijnen
    - Opheffen: dialoog '<groep> opheffen?' noemt '3 leden', dat ritten met antwoorden blijven zonder groepslabel en dat het niet terug te draaien is; Annuleren logt niets; Opheffen logt deleteGroup:g1, gaat terug en toont '<groep> is opgeheven'
  </behavior>
  <action>
group_dialogs.dart: Future<bool> showLeaveGroupDialog(BuildContext context, {required PelotonGroup group, required String? me}) en Future<bool> showDisbandGroupDialog(BuildContext context, {required PelotonGroup group}), beide als AlertDialog naar het model van unplan_confirm_dialog.dart (pop met de dialoogcontext, bevestigknop in colorScheme.error, s.cancel als tweede knop). Leave-body: isLastMember(me) -> s.groupLeaveBodyLast; anders successorIfLeaving(me) != null -> s.groupLeaveBodySuccessor(opvolger.label(s.pelotonUnnamedFriend)); anders s.groupLeaveBody. Disband-body: s.groupDisbandBody(group.memberCount). Leg bovenin in een doccomment de keuze vast: verlaten = bevestiging zonder ongedaan maken (terugkomen = nieuwe aanvraag), opheffen = bevestiging (onomkeerbaar), eruit halen = ongedaan maken (plan 05).

group_screen.dart, het appbar-menu uit Task 1 uitbreiden:
- rename (beheerder, pencilSimple): showGroupNameSheet(initialName: group.name, title: s.groupRename, actionLabel: s.groupSave); als de naam veranderde: gateway.renameGroup -> invalidate visibleGroupsProvider; catch -> groupErrorTextOf.
- leave (iedereen, signOut): showLeaveGroupDialog; bij ja: messenger en router vóór await pakken, gateway.leaveGroup(group.id) -> invalidate visibleGroupsProvider en groupRidesProvider -> terug (pop als dat kan, anders go('/rides')) -> snackbar s.groupLeft(naam); catch -> groupErrorTextOf (last_admin kan hier niet voorkomen, opvolging regelt de database).
- disband (beheerder, trash, tekst in colorScheme.error): showDisbandGroupDialog; bij ja: gateway.deleteGroup(group.id) -> invalidate visibleGroupsProvider en groupRidesProvider -> terug -> snackbar s.groupDisbanded(naam); catch -> groupErrorTextOf.
Volgorde van items voor beheerders: rename, replaceLink, leave, disband. Gewoon lid: alleen leave. Aanvraagstaat: geen menu (intrekken staat al op het scherm).

ARB (NL / EN): groupRename "Naam wijzigen" / "Rename"; groupSave "Opslaan" / "Save"; groupLeave "Groep verlaten" / "Leave group"; groupLeaveTitle "{group} verlaten?" / "Leave {group}?"; groupLeaveBody "Je ziet de groep en zijn ritten dan niet meer. Terugkomen gaat met een nieuwe aanvraag." / EN; groupLeaveBodySuccessor "Je bent de enige beheerder. {name} zit er het langst in en wordt dan beheerder. Terugkomen gaat met een nieuwe aanvraag." / EN; groupLeaveBodyLast "Je bent het laatste lid. De groep en de link verdwijnen dan." / EN; groupLeaveAction "Verlaten" / "Leave"; groupLeft "{group} verlaten" / "You left {group}"; groupDisband "Groep opheffen" / "Disband group"; groupDisbandTitle "{group} opheffen?" / "Disband {group}?"; groupDisbandBody "{count, plural, =1{De groep en de link verdwijnen voor het enige lid.} other{De groep en de link verdwijnen voor alle {count} leden.}} Ritten waarop iemand al geantwoord heeft blijven staan, zonder groepslabel. Dit kun je niet terugdraaien." / EN-equivalent met plural; groupDisbandAction "Opheffen" / "Disband"; groupDisbanded "{group} is opgeheven" / "{group} has been disbanded". flutter gen-l10n.

test/features/group_menu_test.dart (deel 2): de behavior-cases met GoRouter-opzet (start op '/', push naar de groep, zodat 'gaat terug' te toetsen is).
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/group_menu_test.dart test/features/group_members_admin_test.dart test/features/group_requests_test.dart test/structure/ && flutter analyze && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "Future<bool> showLeaveGroupDialog\|Future<bool> showDisbandGroupDialog" lib/features/peloton/group_dialogs.dart geeft 2
    - grep -c "leaveGroup(\|deleteGroup(\|renameGroup(\|replaceGroupInvite(" lib/features/peloton/group_screen.dart geeft minstens 4
    - grep -c "groupRidesProvider" lib/features/peloton/group_screen.dart geeft minstens 1 (ritten verversen na verlaten/opheffen)
    - flutter test test/features/group_menu_test.dart slaagt met minstens 11 tests in totaal
    - flutter analyze schoon; volledige suite groen, aantal in de SUMMARY
  </acceptance_criteria>
  <done>Alle groepsbrede handelingen staan in het appbar-menu, met bevestiging waar er geen weg terug is, en een gewoon lid ziet alleen verlaten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> deelmenu (buiten de app) | De groepslink met code verlaat de app; de code is de capability, maar geeft sinds 0013 alleen een aanvraag |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-21 | Information disclosure | gedeelde groepslink | mitigate | Link geeft alleen een aanvraag; beheerder beslist (0013); link vervangen trekt alle oude codes in |
| T-34-22 | Information disclosure | analytics bij delen | mitigate | Alleen {'kind': 'group_link_created'}; nooit code, naam of id |
| T-34-23 | Tampering | opheffen/verlaten door een verkeerde tik | mitigate | Bevestigingsdialoog met gevolgen in gewone taal |
| T-34-24 | Elevation of privilege | beheerdersmenu voor een lid | transfer | RLS weigert rename/delete/replace (0012); menu toont het niet |
</threat_model>

<verification>
- flutter analyze schoon; flutter test volledig groen (aantal in SUMMARY)
</verification>

<success_criteria>
Een lid deelt de groepslink en kan verlaten; een beheerder hernoemt, vervangt de link en heft op, met de gevolgen vooraf in gewone taal.
</success_criteria>

<output>
Create `.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-06-SUMMARY.md` when done
</output>
