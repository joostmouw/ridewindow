---
phase: 34-groep-maken-en-beheren
plan: 03
type: execute
wave: 3
depends_on: [34-02]
files_modified:
  - lib/domain/models/peloton_group.dart
  - lib/data/remote/supabase_tables.dart
  - lib/services/peloton_gateway.dart
  - lib/providers/peloton_providers.dart
  - lib/providers/peloton_providers.g.dart
  - lib/features/peloton/group_error_text.dart
  - lib/l10n/app_nl.arb
  - lib/l10n/app_en.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_nl.dart
  - lib/l10n/app_localizations_en.dart
  - test/helpers/fake_group_gateway.dart
  - test/domain/models/peloton_group_test.dart
  - test/providers/peloton_groups_providers_test.dart
  - test/features/group_error_text_test.dart
autonomous: true
requirements: [CLUB-01, CLUB-02, CLUB-03, CLUB-04, CLUB-05, CLUB-06, CLUB-07, CLUB-08, CLUB-09, CLUB-11, CLUB-27]

must_haves:
  truths:
    - "De app haalt alle zichtbare groepen op en splitst ze in groepen waar je lid bent en groepen waar je aanvraag loopt"
    - "Per groep kent de app naam, aanmaakdatum, ledenlijst (naam, rol, joined_at) en de aanvragen die RLS jou laat zien, en niets anders van een lid"
    - "Uitgelogd leveren de groepenproviders lege lijsten en raken ze de gateway niet aan"
    - "Elke databasefoutsleutel uit 0012 en 0013 wordt een GroupError en daarna een gewone NL/EN-zin, nooit een foutcode"
    - "Elke groepshandeling (maken, hernoemen, opheffen, verlaten, rol wijzigen, eruit halen, voordragen, accepteren, afwijzen/intrekken, link delen/vervangen, inwisselen) heeft een gatewaymethode volgens 0012 + 0013"
  artifacts:
    - path: "lib/domain/models/peloton_group.dart"
      provides: "PelotonGroup, GroupMember, GroupJoinRequest, GroupRole, GroupJoinStatus, GroupError, GroupException, groupInitials, grensconstanten"
      contains: "class GroupJoinRequest"
    - path: "lib/services/peloton_gateway.dart"
      provides: "groepsmethoden op PelotonGateway + SupabasePelotonGateway"
      contains: "Future<List<PelotonGroup>> listGroups()"
    - path: "lib/providers/peloton_providers.dart"
      provides: "visibleGroupsProvider, myGroupsProvider, myPendingGroupsProvider, pelotonGroupProvider(groupId)"
      contains: "myPendingGroups("
    - path: "lib/features/peloton/group_error_text.dart"
      provides: "groupErrorText + groupErrorTextOf"
      contains: "String groupErrorText("
    - path: "test/helpers/fake_group_gateway.dart"
      provides: "FakeGroupGateway voor alle widgettests van fase 34"
      contains: "class FakeGroupGateway implements PelotonGateway"
  key_links:
    - from: "lib/services/peloton_gateway.dart"
      to: "supabase/migrations/0013_group_join_requests.sql"
      via: "kProposeGroupMemberRpc / kAcceptGroupRequestRpc / kRedeemGroupInviteRpc (status-kolom) / kGroupJoinRequestsTable"
      pattern: "kProposeGroupMemberRpc|kAcceptGroupRequestRpc"
    - from: "lib/services/peloton_gateway.dart"
      to: "GroupException"
      via: "PostgrestException wordt vertaald via GroupError.fromPostgres"
      pattern: "GroupError\\.fromPostgres"
---

<objective>
Leg de datalaag voor Clubs in de app: modellen, gatewaymethoden, providers en de vertaling van databasefouten naar gewone zinnen. Geen UI.

Purpose: plannen 04..07 bouwen tegen deze contracten, zodat geen scherm zelf naar Supabase grijpt of zelf een foutcode leest. Het schema staat live na plan 02 (0012 + 0013). Blijkt tijdens uitvoering dat het schema iets niet kan, stop dan en meld het; voeg nooit stil SQL toe.
Output: peloton_group.dart, uitgebreide gateway + providers, group_error_text.dart, foutteksten in beide ARB-bestanden, een gedeelde FakeGroupGateway en drie testbestanden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-CONTEXT.md
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-01-SUMMARY.md
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-02-SUMMARY.md
@supabase/migrations/0012_groups.sql
@supabase/migrations/0013_group_join_requests.sql
@lib/services/peloton_gateway.dart
@lib/providers/peloton_providers.dart
@lib/domain/models/peloton.dart
@lib/data/remote/supabase_tables.dart
@test/providers/peloton_providers_test.dart

<interfaces>
Schema na 0012 + 0013 (bron van waarheid; lees de migraties zelf na op afwijkingen die 34-01/02-SUMMARY noemen):
- groups(id, name, created_by, created_at, updated_at). authenticated: select (lid, of open aanvraag voor jou), delete (beheerder), update(name) (beheerder). Geen insert: create_group.
- group_members(group_id, user_id, role 'admin'|'member', display_name, joined_at). authenticated: select (leden), delete (jezelf of beheerder), update(role) (beheerder), insert(group_id, user_id) (beheerder + maatje; de app gebruikt dit pad NIET meer, zie propose).
- group_invites(code, group_id, created_by, created_at, expires_at). select + insert(code, group_id, created_by, expires_at): ieder lid; delete: beheerder.
- group_join_requests(id, group_id, user_id, proposed_by (null = via link), display_name, proposed_by_name, created_at). select + delete: aanvrager, voordrager, beheerder van de groep. Geen insert/update voor de client.
- rpc create_group(p_name text) returns uuid.
- rpc redeem_group_invite(p_code text) returns table(group_id uuid, group_name text, status text) met status 'member' | 'requested'.
- rpc propose_group_member(p_group_id uuid, p_user_id uuid) returns text 'member' | 'requested' (beheerder: direct lid).
- rpc accept_group_request(p_request_id uuid) returns void.
- Foutsleutels (P0001): not_authenticated, group_name_invalid, invite_invalid, group_full, too_many_groups, last_admin, not_member, not_friend, not_allowed, too_many_requests. Hernoemen via update geeft bij een ongeldige naam een check violation 23514.

Bestaand (lib/services/peloton_gateway.dart): abstract class PelotonGateway met friends/rides/options-methoden; SupabasePelotonGateway met getter _client => Supabase.instance.client en getter _uid (StateError als uitgelogd). Alle fakes in test/ gebruiken noSuchMethod, dus nieuwe abstracte methoden breken ze niet.
Bestaand (lib/domain/services/invite_code.dart): generateInviteCode({Random? random}), normalizeInviteCode(String).
Bestaand (lib/providers/peloton_providers.dart): pelotonGatewayProvider (keepAlive), friendsProvider, groupRidesProvider; patroon `final userId = ref.watch(currentUserIdProvider); if (userId == null) return const [];`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Groepsmodellen, aanvragen, grenzen, initialen en foutvertaling (pure Dart, TDD)</name>
  <files>lib/domain/models/peloton_group.dart, test/domain/models/peloton_group_test.dart</files>
  <read_first>lib/domain/models/peloton.dart (stijl: plain Dart met fromRow, geen Freezed; Friend.label(fallback); .toLocal() op tijden); supabase/migrations/0012_groups.sql secties 2, 9, 10 (joined_at, opvolging: order by joined_at, user_id); supabase/migrations/0013_group_join_requests.sql (tabel group_join_requests, statuswaarden, foutsleutels)</read_first>
  <behavior>
    - GroupRole.fromRow('admin') is admin, elke andere waarde (ook null) is member; GroupRole.admin.row == 'admin'
    - GroupJoinStatus.fromRow('member') is member, elke andere waarde is requested
    - GroupMember.fromRow leest group_id, user_id, role, display_name, joined_at (parse + toLocal); label('Fietser') geeft de terugval bij null of alleen spaties
    - GroupJoinRequest.fromRow leest id, group_id, user_id, proposed_by, display_name, proposed_by_name, created_at; isViaLink is true als proposed_by null is
    - PelotonGroup.fromRows(groupRow, memberRows, requestRows) sorteert leden: beheerders eerst, dan joinedAt oplopend, dan userId; aanvragen op createdAt oplopend
    - isMember(uid), isAdmin(uid), memberFor(uid), isPendingFor(uid) (geen lid maar wel een aanvraag met userId == uid), requestFor(uid); alle vier false/null bij uid null
    - openRequests geeft de aanvragen van anderen (niet die van jezelf) voor de beheerdersweergave
    - successorIfLeaving(uid): null als uid geen beheerder is, null als er nog een andere beheerder is, null als uid het enige lid is; anders het andere lid met de vroegste joinedAt, bij gelijkspel de kleinste userId
    - isLastMember(uid) true als uid het enige lid is; isFull bij memberCount >= kGroupMaxMembers (30)
    - groupInitials('Dinsdagclub') == 'DI', ('Buren') == 'BU', ('De Zaterdagclub') == 'DZ', ('  x ') == 'X', ('') == '?'; grafeem-veilig
    - GroupError.fromPostgres(code: 'P0001', message: m) geeft de juiste waarde voor alle tien sleutels (group_full, too_many_groups, invite_invalid, last_admin, group_name_invalid, not_authenticated, not_member, not_friend, not_allowed, too_many_requests); code '23514' geeft groupNameInvalid; al het andere geeft unknown
  </behavior>
  <action>
Maak lib/domain/models/peloton_group.dart met een library-doccomment die verwijst naar 0012 en 0013 en uitlegt waarom een lid niets anders draagt dan naam, rol en joined_at (CLUB-05; groeit deze klasse, controleer dan eerst of de database die velden mag teruggeven, zelfde waarschuwing als bij Friend).

Inhoud: constanten kGroupMaxMembers = 30, kMaxGroupsPerAccount = 10, kMaxOpenRequestsPerGroup = 30 (spiegels van 0012/0013 met de regel dat de database de echte grens is en de app alleen vooraf waarschuwt), kGroupNameMaxLength = 40 (UI-grens uit schets 015; de database staat 60 toe). enum GroupRole { admin, member } met fromRow/row. enum GroupJoinStatus { member, requested } met fromRow. class GroupMember (groupId, userId, role, displayName, joinedAt; fromRow; isAdmin; label). class GroupJoinRequest (id, groupId, userId, proposedBy, displayName, proposedByName, createdAt; fromRow; isViaLink; label(fallback)). class PelotonGroup (id, name, createdAt, members, requests; factory fromRows(Map groupRow, Iterable<Map> memberRows, Iterable<Map> requestRows) die zelf sorteert; getters memberCount, adminCount, isFull, initials; methoden isMember, isAdmin, memberFor, isPendingFor, requestFor, openRequests(String? uid), isLastMember, successorIfLeaving). Top-level groupInitials(String name) met package:characters (transitief aanwezig via Flutter; importeer package:characters/characters.dart, geen pubspec-wijziging; lukt dat niet zonder pubspec, gebruik runes en noteer het in de SUMMARY).

enum GroupError { groupFull, tooManyGroups, inviteInvalid, lastAdmin, groupNameInvalid, notAuthenticated, notMember, notFriend, notAllowed, tooManyRequests, unknown } met static fromPostgres({String? code, String? message}). class GroupException implements Exception { final GroupError error; } met toString die de enumnaam geeft (voor debugPrint, nooit voor de gebruiker).

Eerst de test (RED), dan de implementatie (GREEN). Geen Flutter-imports in lib/domain (test/structure/no_flutter_imports_test.dart bewaakt dat).
  </action>
  <verify>
    <automated>flutter test test/domain/models/peloton_group_test.dart test/structure/no_flutter_imports_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - flutter test test/domain/models/peloton_group_test.dart slaagt met minstens 15 tests
    - grep -c "class PelotonGroup\|class GroupMember\|class GroupJoinRequest\|enum GroupRole\|enum GroupJoinStatus\|enum GroupError\|class GroupException" lib/domain/models/peloton_group.dart geeft 7
    - grep -v '^\s*//' lib/domain/models/peloton_group.dart | grep -c "package:flutter/" geeft 0
  </acceptance_criteria>
  <done>Modellen, aanvragen en foutvertaling bestaan als puur Dart, en alle gedragsregels zijn door tests vastgepind.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gatewaymethoden, tabelconstanten, providers en een gedeelde fake</name>
  <files>lib/data/remote/supabase_tables.dart, lib/services/peloton_gateway.dart, lib/providers/peloton_providers.dart, lib/providers/peloton_providers.g.dart, test/helpers/fake_group_gateway.dart, test/providers/peloton_groups_providers_test.dart</files>
  <read_first>lib/services/peloton_gateway.dart (hele bestand); lib/data/remote/supabase_tables.dart; lib/providers/peloton_providers.dart; test/providers/peloton_providers_test.dart (containerpatroon, noSuchMethod-fake); supabase/migrations/0013_group_join_requests.sql (rpc-signaturen en returntypes)</read_first>
  <behavior>
    - visibleGroupsProvider, myGroupsProvider en myPendingGroupsProvider geven lege lijsten en roepen listGroups niet aan als currentUserIdProvider null is
    - myGroupsProvider geeft ingelogd alleen groepen waar jij lid bent, gesorteerd op naam (hoofdletterongevoelig)
    - myPendingGroupsProvider geeft alleen groepen waar jouw aanvraag loopt
    - pelotonGroupProvider('g1') geeft de groep g1 (lid of aanvraag), en null voor een onbekend id
  </behavior>
  <action>
supabase_tables.dart: voeg onder een kop "Clubs (v4.2), migraties 0012 en 0013" toe: kGroupsTable, kGroupMembersTable, kGroupInvitesTable, kGroupJoinRequestsTable, kCreateGroupRpc = 'create_group', kRedeemGroupInviteRpc = 'redeem_group_invite', kProposeGroupMemberRpc = 'propose_group_member', kAcceptGroupRequestRpc = 'accept_group_request'.

peloton_gateway.dart: breid PelotonGateway uit, elk met een korte doccomment die de schemaregel noemt:
- Future<List<PelotonGroup>> listGroups(): drie vaste rondgangen, geen lus per groep: select id, name, created_at op kGroupsTable; select group_id, user_id, role, display_name, joined_at op kGroupMembersTable; select id, group_id, user_id, proposed_by, display_name, proposed_by_name, created_at op kGroupJoinRequestsTable. Expliciete kolommen, nooit '*' (CLUB-05). Groeperen per group_id, PelotonGroup.fromRows.
- Future<String> createGroup(String name): rpc kCreateGroupRpc, p_name = name.trim(), scalar uuid.
- Future<void> renameGroup({required String groupId, required String name}): update alleen {'name': name.trim()}.
- Future<void> deleteGroup(String groupId).
- Future<void> setGroupMemberRole({required String groupId, required String userId, required GroupRole role}): update alleen {'role': role.row}.
- Future<void> removeGroupMember({required String groupId, required String userId}).
- Future<void> leaveGroup(String groupId): removeGroupMember met userId = _uid.
- Future<GroupJoinStatus> proposeGroupMember({required String groupId, required String userId}): rpc kProposeGroupMemberRpc (p_group_id, p_user_id). Dit is ook het pad voor een beheerder die direct toevoegt (0013 keuze e); er komt GEEN addGroupMember via client-insert.
- Future<void> acceptGroupRequest(String requestId): rpc kAcceptGroupRequestRpc (p_request_id).
- Future<void> deleteGroupRequest(String requestId): delete op kGroupJoinRequestsTable eq id (afwijzen door beheerder, intrekken door aanvrager of voordrager; RLS bepaalt wie).
- Future<String> groupInviteCode(String groupId): hergebruik een bestaande link die nog minstens kGroupInviteMinRemaining (7 dagen) geldig is; anders nieuw met generateInviteCode(), created_by = _uid, expires_at = nu UTC + kGroupInviteValidity (30 dagen), zonder .select(). Ieder lid mag dit na 0013. Constanten top-level in dit bestand met een regel uitleg (een clublink staat een tijd in een WhatsApp-groep; intrekken kan altijd via replaceGroupInvite).
- Future<String> replaceGroupInvite(String groupId): delete alle links eq group_id (alleen beheerder na RLS), dan nieuw zoals boven; code teruggeven.
- Future<({String groupId, String? groupName, GroupJoinStatus status})> redeemGroupInvite(String code): rpc met p_code = normalizeInviteCode(code); lege lijst geeft GroupException(GroupError.inviteInvalid).
Tijden altijd expliciet UTC (les 21-13).

In SupabasePelotonGateway een private helper _guard<T>(Future<T> Function() body): vangt PostgrestException en gooit GroupException(GroupError.fromPostgres(code: e.code, message: e.message)) tenzij dat unknown oplevert; dan rethrow. Alle groepsmethoden lopen door _guard; bestaande methoden niet wijzigen.

peloton_providers.dart (Riverpod 3 codegen, zelfde stijl en doccomments):
- @riverpod Future<List<PelotonGroup>> visibleGroups(Ref ref): uitgelogd lege lijst zonder gateway-aanroep; anders listGroups, gesorteerd op name.toLowerCase().
- @riverpod Future<List<PelotonGroup>> myGroups(Ref ref): afgeleid, g.isMember(userId).
- @riverpod Future<List<PelotonGroup>> myPendingGroups(Ref ref): afgeleid, g.isPendingFor(userId).
- @riverpod Future<PelotonGroup?> pelotonGroup(Ref ref, String groupId): afgeleid uit visibleGroups, geen tweede netwerkronde.
Draai dart run build_runner build --delete-conflicting-outputs.

test/helpers/fake_group_gateway.dart: class FakeGroupGateway implements PelotonGateway met muteerbare Map<String, PelotonGroup> groups, List<Friend> friends, String me, en een calls-log (List<String>, leesbare regels zoals 'createGroup:Dinsdagclub', 'proposeGroupMember:g1:uid-x', 'acceptGroupRequest:r1', 'deleteGroupRequest:r1', 'removeGroupMember:g1:uid-x', 'setGroupMemberRole:g1:uid-x:admin', 'deleteGroup:g1', 'leaveGroup:g1', 'groupInviteCode:g1', 'replaceGroupInvite:g1', 'renameGroup:g1:Nieuw', 'redeemGroupInvite:CODE'). Elke methode past de in-memory staat aan zoals de database: create maakt jou admin; propose door een beheerder maakt direct lid, door een lid een aanvraag met proposedByName; accept maakt de aanvrager lid en haalt de aanvraag weg; leave/remove passen opvolging toe; redeem geeft een instelbare status (redeemStatus, standaard requested). Map<String, GroupError> failWith laat een methode (op naam) een GroupException gooien. listFriends geeft friends, listGroupRides een lege lijst, rest via noSuchMethod met UnimplementedError. Statische bouwhelpers FakeGroupGateway.member(...), .request(...), .group(...) met vaste datums.

test/providers/peloton_groups_providers_test.dart: de vier behavior-cases met ProviderContainer en FakeGroupGateway.
  </action>
  <verify>
    <automated>dart run build_runner build --delete-conflicting-outputs && flutter test test/providers/</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "kGroupsTable\|kGroupMembersTable\|kGroupInvitesTable\|kGroupJoinRequestsTable\|kCreateGroupRpc\|kRedeemGroupInviteRpc\|kProposeGroupMemberRpc\|kAcceptGroupRequestRpc" lib/data/remote/supabase_tables.dart geeft 8
    - grep -c "listGroups()\|createGroup(\|renameGroup(\|deleteGroup(\|setGroupMemberRole(\|removeGroupMember(\|leaveGroup(\|proposeGroupMember(\|acceptGroupRequest(\|deleteGroupRequest(\|groupInviteCode(\|replaceGroupInvite(\|redeemGroupInvite(" lib/services/peloton_gateway.dart geeft minstens 26 (declaratie + implementatie)
    - grep -c "kGroupMembersTable).insert" lib/services/peloton_gateway.dart geeft 0 (geen client-insert in group_members)
    - grep -c "GroupError.fromPostgres" lib/services/peloton_gateway.dart geeft minstens 1
    - grep -c "visibleGroupsProvider\|myGroupsProvider\|myPendingGroupsProvider\|pelotonGroupProvider" lib/providers/peloton_providers.g.dart geeft minstens 4
    - flutter test test/providers/ slaagt
  </acceptance_criteria>
  <done>De gateway kent elke groepshandeling uit 0012 + 0013, de providers splitsen lid en aanvraag en leveren niets aan uitgelogde gebruikers, en er is een fake voor de widgettests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Foutsleutels worden gewone zinnen in NL en EN</name>
  <files>lib/features/peloton/group_error_text.dart, lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_nl.dart, lib/l10n/app_localizations_en.dart, test/features/group_error_text_test.dart</files>
  <read_first>lib/l10n/app_nl.arb regels 495-600 (peloton-keys en "@key"-metadata voor placeholders); lib/l10n/app_en.arb; test/structure/no_em_dash_test.dart; .planning/sketches/015-clubs-groepen/index.html (sectie "Grenzen" en de last_admin-zin)</read_first>
  <behavior>
    - Voor elke GroupError-waarde, in 'nl' en 'en', geeft groupErrorText een niet-lege zin zonder underscore en zonder ruwe sleutel
    - groupFull met groupName 'Dinsdagclub' noemt 'Dinsdagclub' en '30'; zonder groupName noemt hij '30'
    - tooManyGroups met personName 'Jacco' noemt 'Jacco' en '10'; zonder personName spreekt hij de gebruiker aan
    - lastAdmin geeft in NL exact "Je bent de enige beheerder. Maak eerst iemand anders beheerder, dan kun je dit afgeven."
    - groupErrorTextOf(s, Exception('x')) geeft de generieke zin
  </behavior>
  <action>
Voeg aan app_nl.arb en app_en.arb toe (met "@"-metadata voor placeholders; nergens een em-dash):
- groupErrorFull: NL "Deze groep heeft al 30 leden, het maximum. Een beheerder kan eerst iemand uit de groep halen." / EN "This group already has 30 members, the maximum. An admin can remove someone first."
- groupErrorFullNamed {group}: NL "{group} heeft al 30 leden, het maximum. Haal eerst iemand uit de groep." / EN "{group} already has 30 members, the maximum. Remove someone first."
- groupErrorTooManyGroupsSelf: NL "Je zit al in 10 groepen, het maximum. Verlaat eerst een andere groep." / EN "You're already in 10 groups, the maximum. Leave another group first."
- groupErrorTooManyGroupsOther {name}: NL "{name} zit al in 10 groepen, het maximum." / EN "{name} is already in 10 groups, the maximum."
- groupErrorInviteInvalid: NL "Deze groepslink werkt niet meer. Vraag iemand uit de groep om een nieuwe." / EN "This group link no longer works. Ask someone in the group for a new one."
- groupErrorLastAdmin: NL "Je bent de enige beheerder. Maak eerst iemand anders beheerder, dan kun je dit afgeven." / EN "You're the only admin. Make someone else admin first, then you can hand this over."
- groupErrorNameInvalid: NL "Geef de groep een naam van 1 tot 40 tekens." / EN "Give the group a name of 1 to 40 characters."
- groupErrorSignedOut: NL "Log eerst in om met groepen te werken." / EN "Sign in first to use groups."
- groupErrorNotMember: NL "Je bent geen lid meer van deze groep." / EN "You're no longer a member of this group."
- groupErrorNotFriend: NL "Je kunt alleen je eigen maatjes voordragen." / EN "You can only propose your own buddies."
- groupErrorNotAllowed: NL "Alleen een beheerder van de groep kan dit doen." / EN "Only a group admin can do this."
- groupErrorTooManyRequests: NL "Er liggen al 30 aanvragen bij de beheerders. Probeer het later opnieuw." / EN "There are already 30 requests waiting for the admins. Try again later."
- groupErrorGeneric: NL "Dat is niet gelukt. Probeer het opnieuw." / EN "That didn't work. Please try again."
Draai flutter gen-l10n.

lib/features/peloton/group_error_text.dart: String groupErrorText(S s, GroupError error, {String? groupName, String? personName}) als exhaustieve switch zonder default (een nieuwe sleutel geeft een compileerfout), en String groupErrorTextOf(S s, Object error, {String? groupName, String? personName}) die voor GroupException de error gebruikt en anders groupErrorGeneric. Elke catch-tak in de UI van fase 34 gebruikt groupErrorTextOf (sweep "stille takken").

test/features/group_error_text_test.dart laadt S via S.delegate.load(Locale('nl')) en Locale('en') en toetst de behavior, inclusief een lus over GroupError.values.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/group_error_text_test.dart test/structure/no_em_dash_test.dart && flutter analyze && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "^  \"groupError" lib/l10n/app_nl.arb geeft 13 en hetzelfde voor app_en.arb
    - grep -c "default:" lib/features/peloton/group_error_text.dart geeft 0
    - flutter test test/features/group_error_text_test.dart en test/structure/no_em_dash_test.dart slagen
    - flutter analyze meldt geen issues
    - flutter test (volledige suite) is groen; aantal in de SUMMARY
  </acceptance_criteria>
  <done>Elke foutsleutel uit 0012 en 0013 heeft een geteste NL- en EN-zin met wat de gebruiker kan doen, via een enkele functie.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app -> Supabase PostgREST | De client stuurt groeps-id's, user-id's, namen en codes; RLS, kolomgrants en definer-functies uit 0012/0013 zijn de echte grens |
| database -> app (foutmeldingen) | Alleen bekende sleutels worden als eigen zin getoond |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-10 | Tampering | lid maken | mitigate | Geen client-insert in group_members; voordragen en accepteren alleen via rpc (0013) |
| T-34-11 | Information disclosure | listGroups | mitigate | Expliciete kolomselectie; GroupMember draagt alleen naam, rol, joined_at (CLUB-05) |
| T-34-12 | Information disclosure | groupErrorText | mitigate | Ruwe servertekst wordt nooit getoond; onbekend wordt groupErrorGeneric |
| T-34-13 | Spoofing | groupInviteCode | mitigate | generateInviteCode (Random.secure, 8 uit 29), created_by = eigen uid; database-check op het patroon |
| T-34-14 | Elevation of privilege | beheerdershandelingen | transfer | Afgedwongen door RLS en rpc-controles (bewezen in plan 02); de app verbergt knoppen alleen voor rust |
</threat_model>

<verification>
- flutter analyze schoon; flutter test volledig groen (aantal in SUMMARY)
- Geen wijziging in supabase/
</verification>

<success_criteria>
De datalaag voor groepen en aanvragen staat, getest met een fake, en elke databasefout heeft een gewone zin.
</success_criteria>

<output>
Create `.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-03-SUMMARY.md` when done
</output>
