---
phase: 34-groep-maken-en-beheren
plan: 02
type: execute
wave: 2
depends_on: [34-01]
files_modified:
  - supabase/tests/clubs_requests_deny_test.sql
  - supabase/tests/clubs_deny_test.sql
autonomous: false
requirements: [CLUB-27, CLUB-02, CLUB-03]

must_haves:
  truths:
    - "Een geautomatiseerde deny-test bewijst: een gewoon lid kan niemand lid maken en geen aanvraag accepteren of afwijzen; een buitenstaander ziet geen aanvragen; de link maakt een aanvraag en geen lid; accepteren respecteert 30 leden en 10 groepen; meer dan 30 open aanvragen wordt geweigerd"
    - "De bestaande clubs_deny_test.sql klopt weer met het schema na 0013 en blijft alles uit fase 33 bewijzen"
    - "0013 is door Joost op de live database toegepast en beide tests geven op elke rij ok = true"
  artifacts:
    - path: "supabase/tests/clubs_requests_deny_test.sql"
      provides: "deny-test voor aanvragen, voordragen, accepteren, links voor leden"
      contains: "CLUBS REQUESTS DENY-TEST FAILED"
    - path: "supabase/tests/clubs_deny_test.sql"
      provides: "0012-bewijs bijgewerkt voor 0013 (links voor leden, inwisselen = aanvraag + accepteren)"
      contains: "accept_group_request"
  key_links:
    - from: "supabase/tests/clubs_requests_deny_test.sql"
      to: "supabase/migrations/0013_group_join_requests.sql"
      via: "exacte foutsleutels en functienamen"
      pattern: "propose_group_member|accept_group_request|too_many_requests"
---

<objective>
Bewijs 0013 met deny-tests in dezelfde vorm als fase 33, werk de bestaande clubs-test bij waar 0013 bewust gedrag wijzigt, en laat Joost 0013 plus beide tests op de live database draaien. BLOCKING: de UI-plannen (03 en verder) gaan pas tegen echte data als dit groen is.

Purpose: CLUB-27 eist dat "een gewoon lid niemand zelf lid kan maken", afgedwongen in de database en bewezen met deny-tests. Op deze machine is geen Postgres of Supabase CLI; alleen Joost kan toepassen (zie 33-03-SUMMARY).
Output: supabase/tests/clubs_requests_deny_test.sql, bijgewerkte supabase/tests/clubs_deny_test.sql, en de resultaten van Joost.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@supabase/migrations/0013_group_join_requests.sql
@supabase/migrations/0012_groups.sql
@supabase/tests/clubs_deny_test.sql
@.planning/workstreams/clubs/phases/33-datamodel-en-rechten/33-03-SUMMARY.md
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: clubs_requests_deny_test.sql schrijven en clubs_deny_test.sql bijwerken</name>
  <files>supabase/tests/clubs_requests_deny_test.sql, supabase/tests/clubs_deny_test.sql</files>
  <read_first>supabase/tests/clubs_deny_test.sql (helemaal: kop "WAT HET BEWIJST / HOE TE DRAAIEN / HOE HET RESULTAAT TE LEZEN / TECHNIEK / DE CAST", raamwerk pg_temp.expect/try_value/try_exec/try_rows, seed onder replica, gebruiker nadoen met set local role + request.jwt.claims, eindcontrole met telling, rollback); supabase/migrations/0013_group_join_requests.sql (functienamen, returntypes, foutsleutels, policies); 34-01-SUMMARY.md (eventuele afwijkingen)</read_first>
  <action>
Deel A, nieuw bestand supabase/tests/clubs_requests_deny_test.sql, zelfstandig te draaien NA 0012 en 0013. Kopieer het raamwerk en de kopstructuur van clubs_deny_test.sql (eigen tijdelijke tabel club_request_results, eigen foutprefix "CLUBS REQUESTS DENY-TEST FAILED", een transactie die met rollback eindigt, eindcontrole die ook het aantal checks telt). Eigen cast met uuid-prefixen die niet botsen met de 0012-test (bijv. d1.../d2...), e-mails op @clubs-req-test.invalid. Seed: beheerder A en lid B van groep G (vaste joined_at onder replica), maatjes B-X en A-Y en B-Y, buitenstaander C, profielnamen voor X en C (om de naamvulling te toetsen), een geldige link voor G, een volle groep GF (30 opvulleden onder replica) met een geldige link, een persoon T die al in 10 groepen zit (onder replica), en een groep H met 30 open aanvragen (onder replica, opvulgebruikers zonder auth-rij).

Checks, genummerd per sectie (elk via pg_temp.expect; de namen vertellen wat er bewezen wordt):
1. Buitenstaander C: ziet 0 aanvragen van G; propose_group_member in G geeft not_member; accept_group_request op een bestaande aanvraag geeft not_allowed; direct insert in group_join_requests geeft 42501.
2. Lid B: ziet de link van G (telling >= 1); maakt een link voor G ('ok'); trekt de link van A niet in (0 rijen); draagt X voor -> 'requested'; X is daarna geen lid (0 rijen in group_members, gelezen als postgres); de aanvraag heeft display_name uit X's profiel en proposed_by_name 'B'; B draagt X nog eens voor -> 'requested' en er staat 1 aanvraag; B draagt C (geen maatje) voor -> not_friend; B accepteert de aanvraag van X -> not_allowed; B verwijdert de aanvraag van C (via link, zie 3) -> 0 rijen; B voegt X direct toe via insert in group_members -> 42501; B update of insert in group_join_requests -> 42501.
3. Link: C wisselt de link van G in -> status 'requested' (select status from redeem_group_invite(...)); C staat niet in group_members; C ziet zijn eigen aanvraag (1); C ziet de groepsrij van G (1) maar 0 leden en 0 groepsritten van G; C wisselt nog eens in -> 'requested' en er is 1 aanvraag; verlopen link -> invite_invalid.
4. Beheerder A: ziet beide aanvragen (2); accepteert C -> 'ok'; C is lid, de aanvraag is weg; C wisselt de link nog eens in -> 'member'; A wijst X af (delete) -> 1 rij; X ziet daarna 0 aanvragen en 0 groepen; A draagt maatje Y voor -> 'member' en Y is direct lid met rol member.
5. Grenzen: aanvraag voor GF (via link, door iemand anders) -> group_full; een aanvraag die onder replica in GF is gezet en door de beheerder van GF wordt geaccepteerd -> group_full en de aanvraag blijft staan; T wisselt een link in -> too_many_groups; een onder replica gezette aanvraag van T geaccepteerd -> too_many_groups; een 31e aanvraag voor H -> too_many_requests.
6. Intrekken en opruimen: aanvrager trekt eigen aanvraag in (1 rij) en ziet de groep daarna niet meer (0); voordrager trekt eigen voordracht in (1 rij); groep opheffen verwijdert zijn aanvragen (cascade, gelezen als postgres).
Werk de kop bij met het exacte aantal checks en zet dat getal ook in de eindcontrole.

Deel B, bijwerken van supabase/tests/clubs_deny_test.sql waar 0013 bewust gedrag wijzigt, zonder iets uit fase 33 minder te bewijzen:
- Checks 2.5 en 2.6 (lid ziet geen link / maakt geen link) worden: lid ziet de links van G (tel de werkelijke rijen op dat moment, inclusief de verlopen link) en lid maakt een link ('ok'), met namen die de 0013-regel noemen. 2.7 (lid trekt link niet in) blijft.
- Sectie 4: na 4.2 (inwisselen geeft nog steeds de groepsnaam terug) een check dat C na inwisselen nog geen lid is, dan als A (claims wisselen) accept_group_request op de aanvraag van C ('ok'), en daarna terug naar C voor 4.3 en verder, zodat alle latere checks die C als lid veronderstellen blijven kloppen. 4.5 (nog eens inwisselen) blijft 'ok'.
- Loop alle checks na 2.6 na die group_invites van G tellen (o.a. rond regels 374 en 714): de extra link van B uit 2.6 bestaat nu; pas het verwachte getal aan of ruim de link direct na 2.6 op als postgres, en leg de keuze in een commentaarregel vast.
- 5.1 (lid 31 via link -> group_full) blijft geldig omdat redeem in 0013 vooraf op 30 toetst; controleer de volgorde group_full voor too_many_groups.
- Kop: voeg "Bijgewerkt voor 0013 (fase 34)" toe met de gewijzigde checknummers, en werk het totaal (81) overal bij naar het nieuwe aantal.

Beide bestanden zijn alleen door lezen te controleren (geen Postgres hier); lees elke check na tegen 0013 en noteer twijfelgevallen in de SUMMARY zodat Joost weet waar hij moet kijken als er iets faalt.
  </action>
  <verify>
    <automated>grep -c "pg_temp.expect(" supabase/tests/clubs_requests_deny_test.sql && grep -c "rollback;" supabase/tests/clubs_requests_deny_test.sql supabase/tests/clubs_deny_test.sql && grep -c "accept_group_request" supabase/tests/clubs_deny_test.sql</automated>
  </verify>
  <acceptance_criteria>
    - supabase/tests/clubs_requests_deny_test.sql bestaat, eindigt met rollback; en bevat "CLUBS REQUESTS DENY-TEST FAILED"
    - Het aantal `perform pg_temp.expect(` in clubs_requests_deny_test.sql is gelijk aan het getal in zijn eigen eindcontrole (`if v_total <> N`) en in de kop ("Verwacht: N rijen")
    - Idem voor clubs_deny_test.sql: het getal 81 komt niet meer voor tenzij het nieuwe aantal toevallig 81 is; telling expect-aanroepen = getal in eindcontrole
    - grep -c "not_allowed\|not_friend\|not_member\|too_many_requests\|group_full\|too_many_groups" supabase/tests/clubs_requests_deny_test.sql geeft minstens 8
    - grep -c "accept_group_request" supabase/tests/clubs_deny_test.sql geeft minstens 1
    - Geen wijziging aan supabase/migrations in deze taak (git diff --stat supabase/migrations leeg ten opzichte van 34-01)
  </acceptance_criteria>
  <done>Er ligt een deny-test voor alle nieuwe rechten uit 0013, en de fase-33-test is in lijn gebracht met het nieuwe gedrag en telt klopt met zichzelf.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: [BLOCKING] Joost past 0013 toe en draait beide deny-tests live</name>
  <files>supabase/migrations/0013_group_join_requests.sql, supabase/tests/clubs_requests_deny_test.sql, supabase/tests/clubs_deny_test.sql</files>
  <read_first>supabase/migrations/0013_group_join_requests.sql (controle-footer); de koppen van beide tests (verwacht aantal rijen); .planning/workstreams/clubs/phases/33-datamodel-en-rechten/33-03-PLAN.md Task 2 (zelfde route voor 0012)</read_first>
  <action>
Waarom Joost: er is geen Supabase CLI en geen databasewachtwoord op deze machine; migraties gaan via de SQL Editor (zoals 0010, 0011, 0012). Claude zet alles klaar: kopieer 0013 naar het klembord met pbcopy < supabase/migrations/0013_group_join_requests.sql, open https://supabase.com/dashboard/project/hcdrydlgqpnmumfupgcx/sql/new, en noem in het bericht aan Joost het verwachte aantal resultaatrijen van beide tests. Na elke stap zet Claude het volgende bestand op het klembord. Faalt iets, dan herstelt Claude de migratie of de test (0013 is herdraaibaar; de tests rollen altijd terug) en begint de ronde opnieuw.
  </action>
  <verify>
    <automated>MISSING: live database alleen via de SQL Editor bereikbaar; Joost rapporteert de resultaattabellen</automated>
  </verify>
  <acceptance_criteria>
    - Joost meldt dat 0013 zonder fout liep en dat de pg_proc-query 13 rijen gaf
    - Joost meldt voor clubs_requests_deny_test.sql en clubs_deny_test.sql elk "N/N ok = true" met N gelijk aan de kop van het bestand
  </acceptance_criteria>
  <done>0013 staat live en beide deny-tests zijn op de live database groen; de SUMMARY noemt de aantallen.</done>
  <what-built>Migratie supabase/migrations/0013_group_join_requests.sql en de twee deny-tests. Er is geen Supabase CLI en geen databasewachtwoord op deze machine; toepassen kan alleen via de SQL Editor in het dashboard (zelfde route als 0012 in 33-03).</what-built>
  <how-to-verify>
    1. Supabase Dashboard -> project hcdrydlgqpnmumfupgcx -> SQL Editor -> New query. Plak supabase/migrations/0013_group_join_requests.sql en draai hem ("Run without RLS" als de editor waarschuwt; 0013 zet RLS zelf aan op group_join_requests).
    2. Draai de controlequery's uit de footer van 0013: pg_proc-query geeft 13 rijen; policies groups 3, group_members 4, group_invites 3, group_join_requests 2.
    3. New query: plak supabase/tests/clubs_requests_deny_test.sql en draai hem. Verwacht een tabel waarin elke rij ok = true is en het aantal rijen gelijk is aan de kop van het bestand. Zie je "CLUBS REQUESTS DENY-TEST FAILED", kopieer dan de hele melding.
    4. New query: plak supabase/tests/clubs_deny_test.sql en draai hem. Zelfde verwachting (nieuw aantal uit de kop).
    5. Beide tests eindigen met rollback: er blijft niets achter.
  </how-to-verify>
  <resume-signal>Typ "0013 live, N/N en M/M groen", of plak de foutmelding. Bij een fout herstelt Claude de migratie of de test en draai je opnieuw (0013 is herdraaibaar).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SQL Editor (postgres) -> live database | Test draait als postgres en speelt gebruikers na met set local role; alles in een transactie met rollback |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-08 | Tampering | testdata in de live database | mitigate | Beide tests eindigen met rollback; e-mails op .invalid-domein; eigen uuid-prefixen |
| T-34-09 | Elevation of privilege | regressie van 0012-rechten door 0013 | mitigate | Volledige 0012-test opnieuw gedraaid na 0013, met bijgewerkte verwachtingen alleen waar 0013 bewust wijzigt |
</threat_model>

<verification>
- Beide tests: elke rij ok = true op de live database (gerapporteerd door Joost)
- pg_proc-telling 13
</verification>

<success_criteria>
0013 staat live en is bewezen; de UI-plannen kunnen tegen echte data.
</success_criteria>

<output>
Create `.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-02-SUMMARY.md` when done
</output>
