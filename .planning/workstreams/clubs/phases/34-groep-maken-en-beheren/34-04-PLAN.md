---
phase: 34-groep-maken-en-beheren
plan: 04
type: execute
wave: 4
depends_on: [34-03]
files_modified:
  - lib/theme/app_icons.dart
  - lib/features/shared/section_card.dart
  - lib/features/peloton/group_crest.dart
  - lib/features/peloton/group_name_sheet.dart
  - lib/features/peloton/group_rules_sheet.dart
  - lib/features/peloton/groups_section.dart
  - lib/features/peloton/buddies_tab.dart
  - lib/features/peloton/group_screen.dart
  - lib/app/router.dart
  - lib/l10n/app_nl.arb
  - lib/l10n/app_en.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_nl.dart
  - lib/l10n/app_localizations_en.dart
  - test/features/group_widgets_test.dart
  - test/features/peloton_groups_section_test.dart
  - test/features/group_screen_test.dart
autonomous: true
requirements: [CLUB-01, CLUB-04, CLUB-05, CLUB-28, CLUB-02]

must_haves:
  truths:
    - "Op de Peloton-tab staat een sectie Groepen boven Maatjes; per groep een kenteken, naam, 'N leden' en een chip 'beheerder' als jij beheerder bent"
    - "Zonder groepen staat er een uitnodigende kaart met 'Fiets je met een vaste club?' en een knop 'Groep maken'"
    - "Een groep waar jouw aanvraag loopt staat erbij als kaart met 'aanvraag loopt'"
    - "Een ingelogde gebruiker maakt via een sheet met naamveld (max 40, teller) een groep en komt daarna op het groepsscherm"
    - "Het groepsscherm toont kenteken, naam, 'N leden · sinds <datum>' en de ledenlijst met '(jij)' en de chip 'beheerder', en niets anders van een lid"
    - "Wie alleen een aanvraag heeft ziet op het groepsscherm 'Je aanvraag ligt bij de beheerders' en kan hem intrekken"
    - "Een info-knop naast 'Groepen' en op het groepsscherm opent de groepsregels in NL en EN"
    - "Uitgelogd verandert er niets op de Peloton-tab"
  artifacts:
    - path: "lib/features/peloton/groups_section.dart"
      provides: "GroupsSection: kaarten, lege staat, aanvraagkaarten, info-knop, + Nieuwe groep"
      contains: "class GroupsSection"
    - path: "lib/features/peloton/group_screen.dart"
      provides: "GroupScreen(groupId): hero, ledenlijst, aanvraagstaat, niet-gevonden-staat"
      contains: "SafeBackButton("
    - path: "lib/features/peloton/group_rules_sheet.dart"
      provides: "showGroupRulesSheet (CLUB-28)"
      contains: "showGroupRulesSheet"
    - path: "lib/features/peloton/group_name_sheet.dart"
      provides: "showGroupNameSheet voor maken en (plan 06) hernoemen"
      contains: "kGroupNameMaxLength"
    - path: "lib/app/router.dart"
      provides: "route /peloton/group/:groupId"
      contains: "/peloton/group/:groupId"
  key_links:
    - from: "lib/features/peloton/buddies_tab.dart"
      to: "GroupsSection"
      via: "eerste kind in de ListView, boven de SectionCard Maatjes"
      pattern: "GroupsSection\\("
    - from: "lib/features/peloton/groups_section.dart"
      to: "/peloton/group/"
      via: "context.push na tik op een kaart en na createGroup"
      pattern: "context\\.push\\('/peloton/group/"
    - from: "lib/features/peloton/group_screen.dart"
      to: "pelotonGroupProvider"
      via: "ref.watch(pelotonGroupProvider(groupId))"
      pattern: "pelotonGroupProvider\\("
---

<objective>
Maak de groepen zichtbaar: de sectie Groepen op de Peloton-tab (schets 015 vraag 1, variant A), de sheet om een groep te maken, het groepsscherm in leesstand (vraag 2, variant A, zonder de beheerknoppen), de aanvraagstaat en de info-knop met de regels (CLUB-28).

Purpose: CLUB-01, CLUB-04, CLUB-05 en CLUB-28, plus het zichtbare deel van de aanvraag uit CLUB-02 (je ziet dat je aanvraag loopt). Beheer (plan 05), groepsmenu en link delen (plan 06) en de landing (plan 07) bouwen op dit scherm.
Output: drie gedeelde widgets, GroupsSection in BuddiesTab, GroupScreen + route, ARB-teksten, drie testbestanden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-CONTEXT.md
@.planning/sketches/015-clubs-groepen/README.md
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-03-SUMMARY.md
@lib/features/peloton/buddies_tab.dart
@lib/features/shared/section_card.dart
@lib/app/router.dart
@test/features/peloton_withdraw_test.dart

<interfaces>
Uit plan 03 (lib/domain/models/peloton_group.dart, lib/providers/peloton_providers.dart, lib/features/peloton/group_error_text.dart, test/helpers/fake_group_gateway.dart):
- PelotonGroup { id, name, createdAt, members, requests; memberCount, isFull, initials; isMember(uid), isAdmin(uid), memberFor(uid), isPendingFor(uid), requestFor(uid), openRequests(uid), isLastMember(uid), successorIfLeaving(uid) }
- GroupMember { groupId, userId, role, displayName, joinedAt; isAdmin; label(fallback) }
- GroupJoinRequest { id, groupId, userId, proposedBy, displayName, proposedByName, createdAt; isViaLink; label(fallback) }
- kGroupNameMaxLength = 40, kMaxGroupsPerAccount = 10
- providers: myGroupsProvider, myPendingGroupsProvider, visibleGroupsProvider, pelotonGroupProvider(groupId), pelotonGatewayProvider, currentUserIdProvider (auth_notifier.dart)
- gateway: createGroup(name) -> Future<String> groupId; deleteGroupRequest(requestId)
- groupErrorTextOf(S s, Object error, {groupName, personName}); s.groupErrorTooManyGroupsSelf
- FakeGroupGateway(groups:, friends:, me:), calls-log, failWith

Bestaand:
- SectionCard({title, children}) in lib/features/shared/section_card.dart: kop in hoofdletters boven een kaart.
- BuddiesTab: ConsumerStatefulWidget met _invalidateAll(), _SignedOutState als userId null, ListView met SectionCard(title: s.pelotonFriends).
- SafeBackButton in lib/core/safe_back_button.dart (verplicht op elk scherm buiten de shell; test/structure/back_affordance_test.dart controleert 'appBar: AppBar(' + 'SafeBackButton(').
- router.dart: GoRoute('/invite/:code') buiten de shell met _fadeTransition; _slideUpTransition voor detailschermen.
- Info-knoppatroon: IconButton met Icon(AppIcons.info, size 16..20, color colorScheme.onSurfaceVariant), minimaal 48x48, tooltip (profile_screen.dart _infoButton, planned_rides_screen.dart appbar). Dialoog/sheet popt met zijn eigen context (zie de noot bij _infoButton over de shell-navigator).
- Kleuren: AppColors.brandLight (0xFFC5D4B6) en AppColors.brandDark (0xFF234934) in lib/theme/app_colors.dart; context.rw voor RideWindowTheme-tokens.
- Phosphor-codepunten (gecontroleerd in assets/fonts/Phosphor.ttf): dotsThreeVertical 0xe208, pencilSimple 0xe3b4, arrowsCounterClockwise 0xe096, signOut 0xe42a, crown 0xe614, linkSimple 0xe2e6.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gedeelde bouwstenen: iconen, SectionCard-actie, kenteken, naamsheet en regelsheet (CLUB-28)</name>
  <files>lib/theme/app_icons.dart, lib/features/shared/section_card.dart, lib/features/peloton/group_crest.dart, lib/features/peloton/group_name_sheet.dart, lib/features/peloton/group_rules_sheet.dart, lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_nl.dart, lib/l10n/app_localizations_en.dart, test/features/group_widgets_test.dart</files>
  <read_first>lib/theme/app_icons.dart (kop: codepunten altijd als constante, tree-shake-noot); lib/features/shared/section_card.dart; lib/features/profile/profile_screen.dart regels 1355-1410 (_infoButton en de dialogContext-noot); lib/features/shared/feels_like_bar.dart rond regel 220 (showModalBottomSheet met isScrollControlled); lib/theme/app_colors.dart (brandLight/brandDark); .planning/sketches/015-clubs-groepen/index.html (sheet "Nieuwe groep": titel, hint, veld Naam, teller 11/40)</read_first>
  <behavior>
    - showGroupNameSheet toont een veld met teller '0/40'; na 'Dinsdagclub' typen staat er '11/40'; meer dan 40 tekens wordt niet geaccepteerd
    - De actieknop is uit bij een lege of alleen-spaties-naam en geeft bij tikken de getrimde naam terug
    - Met initialName 'Buren' staat die naam al in het veld (voor hernoemen in plan 06)
    - showGroupRulesSheet toont de titel en zeven regels; in NL komen '30' en '10' en 'beheerder' voor
    - GroupCrest toont groupInitials(name) en heeft in licht en donker een leesbaar contrast (vaste voor- en achtergrond)
  </behavior>
  <action>
app_icons.dart: voeg in AppIcons (alfabetisch) toe: arrowsCounterClockwise 0xe096, crown 0xe614, dotsThreeVertical 0xe208, linkSimple 0xe2e6, pencilSimple 0xe3b4, signOut 0xe42a (fontFamily 'Phosphor'). Plannen 05 en 06 gebruiken ze; hier alleen de constanten.

section_card.dart: optionele parameter `Widget? action` die rechts in de kopregel staat (Row met de kop in Expanded). Zonder action verandert er niets aan bestaande schermen; zet een korte noot in de doccomment.

group_crest.dart: GroupCrest({required String name, double size = 42}): afgeronde rechthoek (radius size/3), initialen via groupInitials. Licht: achtergrond AppColors.brandLight, tekst AppColors.brandDark. Donker: achtergrond AppColors.brandDark, tekst AppColors.brandLight (schets 015 dark; zelfde contrastpaar, dus in beide helderheden leesbaar; een scherm dat zijn achtergrond vastzet, zet ook zijn voorgrond vast, zie buildAppTheme-noot). Tekstgrootte schaalt mee (size * 0.36), fontWeight w600.

group_name_sheet.dart: Future<String?> showGroupNameSheet(BuildContext context, {String? initialName, required String title, String? hint, required String actionLabel}). showModalBottomSheet met isScrollControlled: true, showDragHandle: true, useSafeArea: true, padding onderaan MediaQuery.viewInsetsOf(context).bottom zodat het toetsenbord het veld niet bedekt. TextField met autofocus, labelText s.groupNameLabel, maxLength kGroupNameMaxLength (Flutter toont zelf de teller 'n/40'), textCapitalization.sentences, onSubmitted = actie. FilledButton full width, disabled als naam.trim() leeg is. Pop met de getrimde naam via de context van de sheet (niet die van het aanroepende scherm).

group_rules_sheet.dart: Future<void> showGroupRulesSheet(BuildContext context): scrollbare sheet (isScrollControlled, showDragHandle) met titel s.groupRulesTitle en zeven regels als rijen met een klein icoon of bolletje: groupRuleProposing, groupRuleLimits, groupRuleVisibility, groupRuleRides, groupRuleLeaving, groupRuleLastAdmin, groupRuleDisband. Plus een publieke widget GroupRulesButton() die het info-knoppatroon volgt (Icon(AppIcons.info, size 20, color onSurfaceVariant), 48x48, tooltip s.groupRulesTitle) en de sheet opent; plannen 04 en later gebruiken alleen deze knop, zodat hij overal gelijk is.

ARB (NL / EN, geen em-dash):
- groupNameLabel "Naam" / "Name"
- groupCreateTitle "Nieuwe groep" / "New group"; groupCreateHint "Geef hem een naam die je club herkent. Je kunt hem later wijzigen." / "Give it a name your club will recognise. You can change it later."; groupsCreateAction "Groep maken" / "Create group"
- groupRulesTitle "Zo werken groepen" / "How groups work"
- groupRuleProposing "Ieder lid kan mensen voordragen: met de groepslink of door een maatje voor te dragen. Een beheerder beslist wie er lid wordt. Draagt een beheerder iemand voor, dan is die meteen lid." / EN-equivalent
- groupRuleLimits "Een groep heeft maximaal 30 leden, en je zit in maximaal 10 groepen." / EN
- groupRuleVisibility "Leden zien elkaars naam, wie beheerder is en elkaars antwoord op groepsritten. Je rooster, je instellingen en je e-mailadres zien ze niet." / EN
- groupRuleRides "Een groepsrit zien alle huidige leden, ook wie later lid werd. Wie de groep verlaat, ziet de groepsritten niet meer." / EN
- groupRuleLeaving "Je kunt een groep altijd zelf verlaten. Terugkomen gaat met een nieuwe aanvraag." / EN
- groupRuleLastAdmin "Vertrekt de laatste beheerder, dan wordt het lid dat er het langst in zit beheerder. Vertrekt het laatste lid, dan verdwijnt de groep." / EN
- groupRuleDisband "Een beheerder kan de groep opheffen. Ritten waarop al iemand geantwoord heeft, blijven staan zonder groepslabel." / EN
flutter gen-l10n.

test/features/group_widgets_test.dart: de behavior-cases (pump een knop die de sheet opent; lees de terugkeerwaarde uit via een variabele). Voor de crest: pump in ThemeData licht en donker en controleer dat voor- en achtergrond de verwachte AppColors zijn.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/group_widgets_test.dart test/structure/no_em_dash_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "dotsThreeVertical\|pencilSimple\|arrowsCounterClockwise\|signOut\|crown\|linkSimple" lib/theme/app_icons.dart geeft minstens 6
    - grep -c "Widget? action" lib/features/shared/section_card.dart geeft 1
    - grep -c "maxLength: kGroupNameMaxLength" lib/features/peloton/group_name_sheet.dart geeft 1
    - grep -c "groupRule" lib/features/peloton/group_rules_sheet.dart geeft minstens 7 en grep -c "\"groupRule" lib/l10n/app_nl.arb geeft 7 (zonder @-regels) en idem app_en.arb
    - flutter test test/features/group_widgets_test.dart slaagt; bestaande tests met SectionCard (profile_screen_test.dart) blijven groen
  </acceptance_criteria>
  <done>Kenteken, naamsheet, regelsheet met info-knop en de actieplek in SectionCard bestaan en zijn getest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sectie Groepen op de Peloton-tab met maken, lege staat, aanvraagkaarten en info-knop</name>
  <files>lib/features/peloton/groups_section.dart, lib/features/peloton/buddies_tab.dart, lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_nl.dart, lib/l10n/app_localizations_en.dart, test/features/peloton_groups_section_test.dart</files>
  <read_first>lib/features/peloton/buddies_tab.dart (hele bestand: _run, _busy, _invalidateAll, foutpatroon met messenger vóór await, trackEvent); test/features/peloton_withdraw_test.dart (ProviderScope-overrides, locale nl, RideWindowTheme.light); test/helpers/fake_group_gateway.dart; lib/core/analytics_events.dart (kEvPelotonInvite, props 'kind'); .planning/sketches/015-clubs-groepen/index.html (vraag 1 variant A: kaart met kenteken, "9 leden", chip beheerder; lege staat)</read_first>
  <behavior>
    - Uitgelogd: geen tekst 'Groepen' of 'Groep maken' op de tab; de bestaande uitgelogde staat blijft
    - Geen groepen: de kaart met 'Fiets je met een vaste club?' en de knop 'Groep maken'; geen '+ Nieuwe groep' in de kop
    - Twee groepen: beide namen, '3 leden' en '1 lid' (meervoud klopt), en de chip 'beheerder' alleen bij de groep waar jij beheerder bent; de kop heeft 'Nieuwe groep'
    - Een groep met jouw aanvraag staat erbij met 'aanvraag loopt' en zonder ledental
    - Groep maken: sheet, 'Dinsdagclub' invullen, knop -> gateway-log bevat 'createGroup:Dinsdagclub' en de app staat op /peloton/group/<id>
    - Met 10 groepen opent 'Groep maken' geen sheet maar toont de zin van groupErrorTooManyGroupsSelf, zonder gateway-aanroep
    - Faalt createGroup met too_many_groups, dan verschijnt dezelfde zin in een snackbar
    - De info-knop naast 'Groepen' opent 'Zo werken groepen'
    - De sectie Maatjes staat er nog, onder Groepen
  </behavior>
  <action>
groups_section.dart: GroupsSection (ConsumerWidget of ConsumerStatefulWidget voor een busy-vlag) die myGroupsProvider en myPendingGroupsProvider bekijkt en een SectionCard(title: s.groupsSection, action: Row[GroupRulesButton(), en alleen als er groepen zijn een TextButton.icon(AppIcons.plus, s.groupsNew)]) teruggeeft.
- Laden: kleine spinner in de kaart; fout: rij met AppIcons.cloudSlash en 'Opnieuw proberen' (s.pelotonRetry) die myGroupsProvider invalideert (zelfde vorm als _ErrorRow in buddies_tab).
- Geen groepen en geen aanvragen: lege staat binnen de kaart: GroupCrest-achtig icoon AppIcons.usersThree, titel s.groupsEmptyTitle, tekst s.groupsEmptyHint, FilledButton.tonal s.groupsCreateAction.
- Kaarten: ListTile-vorm met GroupCrest(name), titel naam, subtitel s.groupMemberCount(count), trailing een chip s.groupAdminChip als g.isAdmin(me) (outlined, rand colorScheme.outline, tekst onSurfaceVariant, labelSmall/w600, zoals .chip.admin in de schets), onTap context.push('/peloton/group/${g.id}').
- Aanvraagkaarten (myPendingGroups) onder de eigen groepen: GroupCrest, naam, subtitel s.groupRequestPendingShort ("aanvraag loopt"), onTap naar hetzelfde groepsscherm.
- Maken (_create): als myGroups.length >= kMaxGroupsPerAccount -> snackbar s.groupErrorTooManyGroupsSelf en stop. Anders showGroupNameSheet(title s.groupCreateTitle, hint s.groupCreateHint, actionLabel s.groupsCreateAction). Bij een naam: gateway.createGroup(naam) -> ref.invalidate(visibleGroupsProvider) -> trackEvent(ref, kEvPelotonInvite, props: {'kind': 'group_created'}) (nooit naam of id meesturen) -> context.push('/peloton/group/$id'). catch: debugPrint + snackbar groupErrorTextOf(s, e). Pak ScaffoldMessenger en router vóór de eerste await (patroon uit buddies_tab).

buddies_tab.dart: zet GroupsSection() als eerste kind in de ListView, boven de SectionCard Maatjes; voeg ref.invalidate(visibleGroupsProvider) toe aan _invalidateAll zodat pull-to-refresh ook de groepen ververst. Verder niets aan de maatjesstroom wijzigen (CLUB-23 in fase 36 bewaakt die).

ARB (NL / EN): groupsSection "Groepen" / "Groups"; groupsNew "Nieuwe groep" / "New group"; groupsEmptyTitle "Fiets je met een vaste club?" / "Do you ride with a regular club?"; groupsEmptyHint "Maak een groep. Dan zet je een rit in één keer uit voor iedereen." / "Create a group. Then you can put out a ride to everyone at once."; groupMemberCount "{count, plural, =1{1 lid} other{{count} leden}}" / "{count, plural, =1{1 member} other{{count} members}}"; groupAdminChip "beheerder" / "admin"; groupRequestPendingShort "aanvraag loopt" / "request pending". flutter gen-l10n.

test/features/peloton_groups_section_test.dart: pump MaterialApp.router met een GoRouter met twee routes: '/' -> Scaffold(body: BuddiesTab()) en '/peloton/group/:groupId' -> Text('groep-${groupId}'); overrides pelotonGatewayProvider (FakeGroupGateway), currentUserIdProvider, en wat BuddiesTab verder nodig heeft (friendsProvider leest listFriends uit de fake). Locale nl. Alle behavior-cases.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/peloton_groups_section_test.dart test/features/peloton_withdraw_test.dart test/features/peloton_options_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "GroupsSection()" lib/features/peloton/buddies_tab.dart geeft 1 en staat vóór "title: s.pelotonFriends" (grep -n bevestigt de volgorde)
    - grep -c "visibleGroupsProvider" lib/features/peloton/buddies_tab.dart geeft minstens 1
    - grep -c "GroupRulesButton()" lib/features/peloton/groups_section.dart geeft 1
    - grep -c "kMaxGroupsPerAccount" lib/features/peloton/groups_section.dart geeft minstens 1
    - flutter test test/features/peloton_groups_section_test.dart slaagt met minstens 9 tests; de bestaande peloton-tests blijven groen
  </acceptance_criteria>
  <done>De Peloton-tab toont groepen en aanvragen boven de maatjes, een nieuwe groep maken werkt tot op het groepsscherm, en de regels zijn een tik weg.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Groepsscherm in leesstand, aanvraagstaat en de route</name>
  <files>lib/features/peloton/group_screen.dart, lib/app/router.dart, lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_nl.dart, lib/l10n/app_localizations_en.dart, test/features/group_screen_test.dart</files>
  <read_first>lib/app/router.dart (routes buiten de shell, _slideUpTransition); lib/features/peloton/invite_landing_screen.dart (Scaffold met SafeBackButton, _Message-vorm); lib/core/safe_back_button.dart; test/structure/back_affordance_test.dart; .planning/sketches/015-clubs-groepen/index.html (vraag 2 variant A: hero met kenteken 64, naam, "9 leden · sinds 23 sep"; ledenrijen "Joost (jij)" + chip beheerder; ledenweergave "Ingrid (jij)")</read_first>
  <behavior>
    - Lid-weergave: appbar-titel is de groepsnaam; hero toont naam, '3 leden · sinds <datum>'; de ledenlijst toont alle leden in de volgorde van het model; alleen bij jezelf staat '(jij)'; het aantal 'beheerder'-chips is gelijk aan het aantal beheerders
    - Er staat per lid alleen naam en eventueel de chip; een lid zonder naam heet 'Fietser'
    - Ben je het enige lid, dan staat onder de hero de hint om de link te delen of een maatje voor te dragen
    - Aanvraagstaat: hero met naam, de tekst 'Je aanvraag ligt bij de beheerders', geen ledenlijst; 'Aanvraag intrekken' roept deleteGroupRequest met het id van jouw aanvraag aan, gaat terug en toont 'Aanvraag ingetrokken'
    - Onbekend groeps-id: 'Deze groep bestaat niet meer, of je bent er geen lid meer van.' en een knop naar Peloton
    - De appbar heeft een SafeBackButton en de info-knop met de groepsregels
  </behavior>
  <action>
group_screen.dart: GroupScreen({required String groupId}) als ConsumerStatefulWidget (plannen 05 en 06 voegen lokale staat toe). Scaffold met appBar: AppBar(leading: const SafeBackButton(), title: Text(naam of leeg tijdens laden), actions: [GroupRulesButton()]). Body via ref.watch(pelotonGroupProvider(widget.groupId)).when:
- loading: spinner; error: _Message-achtige staat met AppIcons.cloudSlash en s.pelotonRetry die visibleGroupsProvider invalideert.
- null: centrale staat met AppIcons.linkBreak, s.groupNotFound, FilledButton s.pelotonGoToPeloton -> context.go('/rides').
- isPendingFor(me): _GroupHero(group, showCount: false) en daaronder een kaart met AppIcons.hourglass, s.groupRequestPending en OutlinedButton s.groupWithdrawRequest -> gateway.deleteGroupRequest(group.requestFor(me)!.id) -> invalidate visibleGroupsProvider -> pop (of go('/rides') als er niets te poppen valt) -> snackbar s.groupRequestWithdrawn; catch -> snackbar groupErrorTextOf.
- lid: RefreshIndicator(onRefresh: invalidate visibleGroupsProvider) met ListView: _GroupHero (GroupCrest size 64, naam titleLarge, regel s.groupHeroSubtitle(count-tekst, datum) met datum via DateFormat.MMMd(localeName) uit package:intl; als memberCount == 1 de hint s.groupOnlyYouHint in onSurfaceVariant). Daaronder SectionCard(title: s.groupMembersSection, children: leden als _MemberRow). _MemberRow: CircleAvatar met de eerste letter van het label, titel label(s.pelotonUnnamedFriend) en bij jezelf s.groupYou(label) ("{name} (jij)"), trailing de beheerder-chip als member.isAdmin (zelfde chipstijl als in groups_section; zet de chip in een kleine gedeelde widget GroupAdminChip in group_crest.dart, zodat tab en scherm dezelfde hebben; groups_section.dart gebruikt hem daarna ook). Laat in _MemberRow een parameter `Widget? trailingAction` open voor plan 05 (⋮), nu altijd null. Toon niets anders van een lid (CLUB-05).
Zet een doccomment bovenaan die naar schets 015 vraag 2 variant A verwijst en zegt dat beheerknoppen in plan 05/06 komen.

router.dart: GoRoute(path: '/peloton/group/:groupId', pageBuilder: _slideUpTransition(state, GroupScreen(groupId: state.pathParameters['groupId'] ?? ''))) buiten de shell, naast /invite/:code, met een commentaarregel waarom buiten de shell (eigen scherm met terugknop, net als detail).

ARB (NL / EN): groupHeroSubtitle "{count} · sinds {date}" / "{count} · since {date}" (count is de al vertaalde groupMemberCount-tekst, beide String-placeholders); groupMembersSection "Leden" / "Members"; groupYou "{name} (jij)" / "{name} (you)"; groupOnlyYouHint "Je bent nog alleen. Deel de groepslink of draag een maatje voor." / "It's just you so far. Share the group link or propose a buddy."; groupNotFound "Deze groep bestaat niet meer, of je bent er geen lid meer van." / "This group no longer exists, or you're no longer a member."; groupRequestPending "Je aanvraag ligt bij de beheerders" / "Your request is with the admins"; groupWithdrawRequest "Aanvraag intrekken" / "Withdraw request"; groupRequestWithdrawn "Aanvraag ingetrokken" / "Request withdrawn". flutter gen-l10n.

test/features/group_screen_test.dart: MaterialApp.router met routes '/' (Text('peloton')), '/rides' (Text('rides')) en '/peloton/group/:groupId' (GroupScreen); start op '/' en push naar de groep zodat terug werkt. FakeGroupGateway met een groep van drie leden (twee beheerders, waaronder jij niet, of wel, per test) en een groep waarvoor jij een aanvraag hebt. Alle behavior-cases.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/group_screen_test.dart test/structure/back_affordance_test.dart && flutter analyze && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "'/peloton/group/:groupId'" lib/app/router.dart geeft 1
    - grep -c "SafeBackButton(" lib/features/peloton/group_screen.dart geeft minstens 1 en flutter test test/structure/back_affordance_test.dart slaagt
    - grep -c "GroupRulesButton()" lib/features/peloton/group_screen.dart geeft 1
    - grep -c "email\|e-mail" lib/features/peloton/group_screen.dart geeft 0 (buiten commentaar)
    - flutter test test/features/group_screen_test.dart slaagt met minstens 6 tests
    - flutter analyze schoon; volledige suite groen, aantal in de SUMMARY
  </acceptance_criteria>
  <done>Het groepsscherm toont de groep en zijn leden zoals in schets 015, wie een aanvraag heeft ziet dat en kan hem intrekken, en elke weg erheen heeft een weg terug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gebruiker -> naamveld | Vrije tekst gaat naar create_group; de database trimt en toetst 1..60 |
| gateway -> scherm | Alleen velden uit het model worden getoond |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-15 | Information disclosure | GroupScreen ledenrijen | mitigate | Alleen label en rolchip; GroupMember heeft geen andere velden (CLUB-05) |
| T-34-16 | Information disclosure | analytics bij groep maken | mitigate | Alleen props {'kind': 'group_created'}; nooit naam, id of code |
| T-34-17 | Tampering | groepsnaam | transfer | Database toetst lengte en trimt (create_group); UI begrenst op 40 voor rust |
</threat_model>

<verification>
- flutter analyze schoon; flutter test volledig groen (aantal in SUMMARY)
- back_affordance_test en no_em_dash_test groen
</verification>

<success_criteria>
Een ingelogde gebruiker ziet zijn groepen en aanvragen op de Peloton-tab, maakt een groep, komt op het groepsscherm met naam en ledenlijst, en kan de regels lezen.
</success_criteria>

<output>
Create `.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-04-SUMMARY.md` when done
</output>
