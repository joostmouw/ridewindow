---
phase: 34-groep-maken-en-beheren
plan: 07
type: execute
wave: 7
depends_on: [34-06]
files_modified:
  - lib/services/pending_invite_store.dart
  - lib/services/pending_group_join.dart
  - lib/features/peloton/group_landing_screen.dart
  - lib/features/profile/account_section.dart
  - lib/features/peloton/buddies_tab.dart
  - lib/app/router.dart
  - lib/l10n/app_nl.arb
  - lib/l10n/app_en.arb
  - lib/l10n/app_localizations.dart
  - lib/l10n/app_localizations_nl.dart
  - lib/l10n/app_localizations_en.dart
  - test/features/group_landing_test.dart
  - test/services/pending_group_join_test.dart
  - test/app/router_pending_code_test.dart
  - test/features/peloton_code_field_test.dart
autonomous: true
requirements: [CLUB-02]

must_haves:
  truths:
    - "Wie uitgelogd /group/<code> opent ziet 'Log in en doe mee' en 'Niet nu'; de code wordt bewaard"
    - "Na inloggen wordt de bewaarde code vanzelf ingewisseld: bij een aanvraag verschijnt 'Je aanvraag voor <groep> ligt bij de beheerders', ben je al lid dan sta je op het groepsscherm"
    - "Ingelogd /group/<code> openen wisselt meteen in en toont de aanvraagstaat of opent het groepsscherm"
    - "Een nieuwe gebruiker die via een groepslink binnenkomt en nog door de welkomstschermen moet, verliest de code niet"
    - "De code uit de gedeelde tekst werkt ook in het bestaande codeveld op de Peloton-tab"
    - "Een verlopen of ingetrokken link, een volle groep of 10 groepen geeft een gewone zin"
  artifacts:
    - path: "lib/features/peloton/group_landing_screen.dart"
      provides: "GroupLandingScreen(code) voor /group/:code"
      contains: "SafeBackButton("
    - path: "lib/services/pending_group_join.dart"
      provides: "redeemPendingGroupCode(gateway) -> resultaat of null, altijd de code wissen"
      contains: "Future<"
    - path: "lib/app/router.dart"
      provides: "route /group/:code en code bewaren bij de onboarding-redirect"
      contains: "/group/:code"
  key_links:
    - from: "lib/features/profile/account_section.dart"
      to: "lib/services/pending_group_join.dart"
      via: "na _redeemPendingInvite bij inloggen"
      pattern: "redeemPendingGroupCode\\("
    - from: "lib/app/router.dart redirect"
      to: "PendingInviteStore.groupKey"
      via: "prefs.setString vóór de redirect naar /welcome"
      pattern: "PendingInviteStore\\.(groupKey|friendKey)"
    - from: "lib/features/peloton/buddies_tab.dart _redeemCode"
      to: "PelotonGateway.redeemGroupInvite"
      via: "tweede poging als de maatjescode niet bestaat"
      pattern: "redeemGroupInvite\\("
---

<objective>
Maak de groepslink af: de landing /group/:code naar het model van /invite/:code, de code die het inloggen en de welkomstschermen overleeft, en de code die ook in het bestaande codeveld werkt.

Purpose: CLUB-02 (herzien): wie de link opent en inlogt dient een aanvraag in en ziet "wacht op goedkeuring". Zonder de bewaarde code moet een nieuwe gebruiker na het inloggen zelf de weg terug vinden; dat is precies wat Jacco op 2026-09-07 bij de maatjeslink overkwam (PendingInviteStore-doccomment).
Output: GroupLandingScreen, pending_group_join.dart, uitbreidingen in PendingInviteStore, account_section, router en buddies_tab, vier testbestanden.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-CONTEXT.md
@.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-06-SUMMARY.md
@lib/features/peloton/invite_landing_screen.dart
@lib/services/pending_invite_store.dart
@lib/app/router.dart
@firebase.json

<interfaces>
- PendingInviteStore (abstract final class): static save/read/clear op key 'peloton.pendingInviteCode' (private _key).
- account_section.dart: na _ensureCloudProfileRow() roept de inlogstroom _redeemPendingInvite() aan (regels ~255-330): leest, wist, verzilvert, toont snackbar pelotonAutoJoined; fouten alleen debugPrint.
- InviteLandingScreen: initState verzilvert als ingelogd, anders PendingInviteStore.save; ref.listen(currentUserIdProvider) verzilvert als iemand inlogt terwijl het scherm openstaat; uitgelogd: FilledButton naar /profile; SafeBackButton in de appbar.
- router.dart redirect: als onboarding_complete niet true is en loc niet /welcome, /onboard of /availability -> '/welcome'. prefs via ref.read(sharedPrefsProvider) (synchroon beschikbaar). Onboarding eindigt op /home (onboarding_screen.dart) of /profile (welcome_screen.dart "al een account").
- Gateway (plan 03): redeemGroupInvite(code) -> ({groupId, groupName, status: GroupJoinStatus.member|requested}); redeemFriendInvite(code) bestaand.
- groupErrorTextOf(s, e, {groupName})
- firebase.json: rewrite { "source": "**", "destination": "/index.html" } dekt alles; bovendien draait de app op hash-routing (/#/group/...), dus de server ziet het pad niet eens. Geen wijziging nodig.
- De landing kan uitgelogd geen groepsnaam tonen: anon heeft in 0012/0013 op geen enkele groepstabel rechten, en de rpc eist auth.uid(). Schets 015 toonde "Joost nodigt je uit voor Dinsdagclub, 9 fietsers"; dat kan niet zonder nieuwe serverfunctie. De gedeelde tekst noemt de groepsnaam al.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Bewaarde groepscode, inwisselen na inloggen en de onboarding-redirect</name>
  <files>lib/services/pending_invite_store.dart, lib/services/pending_group_join.dart, lib/features/profile/account_section.dart, lib/app/router.dart, lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_nl.dart, lib/l10n/app_localizations_en.dart, test/services/pending_group_join_test.dart, test/app/router_pending_code_test.dart</files>
  <read_first>lib/services/pending_invite_store.dart (hele bestand en de waarom-doccomment); lib/features/profile/account_section.dart regels 240-330 (_redeemPendingInvite en waar hij aangeroepen wordt); lib/app/router.dart (redirect); ls test/app (bestaande routertests als voorbeeld)</read_first>
  <behavior>
    - PendingInviteStore.saveGroup/readGroup/clearGroup werken los van de maatjescode (twee sleutels naast elkaar)
    - redeemPendingGroupCode(gateway) geeft null zonder bewaarde code en roept de gateway dan niet aan
    - Met code 'ABCDEFGH' logt hij redeemGroupInvite:ABCDEFGH en geeft hij groupId, naam en status terug; de code is daarna gewist
    - Faalt het inwisselen, dan is de code toch gewist en komt de fout terug als GroupException (voor een melding)
    - Router: onboarding niet af en navigeren naar /group/ABCDEFGH -> locatie /welcome en SharedPreferences bevat de groepscode; idem /invite/ABCDEFGH -> maatjescode bewaard
    - Router: onboarding af -> /group/ABCDEFGH blijft /group/ABCDEFGH
  </behavior>
  <action>
pending_invite_store.dart: maak de sleutels publiek als static const friendKey = 'peloton.pendingInviteCode' (zelfde waarde als nu, zodat bestaande bewaarde codes blijven werken) en groupKey = 'peloton.pendingGroupCode', en voeg saveGroup/readGroup/clearGroup toe met dezelfde regels (lege waarde is null, overschrijven bij een tweede link). Vul de doccomment aan: groepslinks hebben een eigen sleutel omdat iemand een maatjeslink en een groepslink tegelijk kan hebben openstaan.

pending_group_join.dart: Future<({String groupId, String? groupName, GroupJoinStatus status})?> redeemPendingGroupCode(PelotonGateway gateway): leest readGroup, null -> null; anders eerst clearGroup (zelfde reden als _redeemPendingInvite: een kapotte code mag niet bij elke login terugkomen), dan gateway.redeemGroupInvite(code) en het resultaat teruggeven; fouten doorgooien. Pure logica zonder BuildContext, zodat hij te toetsen is.

account_section.dart: voeg direct na await _redeemPendingInvite() een await _redeemPendingGroupInvite() toe. Die pakt GoRouter en ScaffoldMessenger vóór de eerste await, roept redeemPendingGroupCode(ref.read(pelotonGatewayProvider)) aan, invalideert visibleGroupsProvider, en: status member -> router.push('/peloton/group/$groupId'); status requested -> snackbar s.groupJoinRequested(naam ?? s.groupUnnamed) met SnackBarAction(s.groupOpen) die naar het groepsscherm gaat. Fout -> snackbar groupErrorTextOf (anders dan bij maatjes niet stil: een volle groep of 10 groepen moet de gebruiker horen) plus debugPrint. Trek geen andere code in dit bestand mee.

router.dart:
- (De route /group/:code zelf komt in Task 2, samen met het scherm, zodat elke commit analyse-groen is.)
- In redirect, in de tak die naar '/welcome' stuurt: als de locatie met '/group/' of '/invite/' begint, schrijf het laatste padsegment via prefs.setString(PendingInviteStore.groupKey of friendKey, code) weg vóór de redirect. Commentaar: zonder dit verliest een nieuwe gebruiker (nog niet door de welkomstschermen) de code, en dat is juist wie een clublink het vaakst opent. De code wordt na inloggen ingewisseld via account_section.

ARB (NL / EN): groupJoinRequested "Je aanvraag voor {group} ligt bij de beheerders" / "Your request to join {group} is with the admins"; groupOpen "Bekijk" / "View"; groupUnnamed "de groep" / "the group". flutter gen-l10n.

Tests: test/services/pending_group_join_test.dart met SharedPreferences.setMockInitialValues en FakeGroupGateway; test/app/router_pending_code_test.dart met een ProviderContainer waarin sharedPrefsProvider overschreven is (onboarding_complete false resp. true), routerProvider lezen, router.go(...) en de huidige locatie plus de prefs controleren (volg de vorm van bestaande tests in test/app als die er zijn; anders MaterialApp.router in een widgettest).
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/services/pending_group_join_test.dart test/app/router_pending_code_test.dart test/features/profile_account_section_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "static const friendKey = 'peloton.pendingInviteCode'" lib/services/pending_invite_store.dart geeft 1 en grep -c "groupKey" geeft minstens 2
    - grep -c "redeemPendingGroupCode(" lib/features/profile/account_section.dart geeft 1
    - grep -c "PendingInviteStore.groupKey\|PendingInviteStore.friendKey" lib/app/router.dart geeft minstens 2
    - De nieuwe tests slagen; test/features/profile_account_section_test.dart blijft groen
  </acceptance_criteria>
  <done>Een groepscode overleeft de welkomstschermen en het inloggen, en wordt daarna vanzelf ingewisseld met een duidelijke melding of een directe sprong naar de groep.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GroupLandingScreen en de groepscode in het bestaande codeveld</name>
  <files>lib/features/peloton/group_landing_screen.dart, lib/app/router.dart, lib/features/peloton/buddies_tab.dart, lib/l10n/app_nl.arb, lib/l10n/app_en.arb, lib/l10n/app_localizations.dart, lib/l10n/app_localizations_nl.dart, lib/l10n/app_localizations_en.dart, test/features/group_landing_test.dart, test/features/peloton_code_field_test.dart</files>
  <read_first>lib/features/peloton/invite_landing_screen.dart (hele bestand: dit is het model); lib/features/peloton/buddies_tab.dart (_redeemCode); .planning/sketches/015-clubs-groepen/index.html (landing: "Log in en doe mee", "Niet nu")</read_first>
  <behavior>
    - Uitgelogd: titel 'Uitnodiging voor een groep', de tekst dat inloggen je aanvraag naar de beheerders stuurt, knoppen 'Log in en doe mee' (naar /profile) en 'Niet nu' (naar /home); de code staat daarna in PendingInviteStore.readGroup
    - Ingelogd, status requested: log redeemGroupInvite:<code>, tekst 'Je aanvraag voor Dinsdagclub ligt bij de beheerders' en een knop 'Naar de groep'
    - Ingelogd, status member: de app gaat naar /peloton/group/<id>
    - invite_invalid: de zin 'Deze groepslink werkt niet meer...' en 'Opnieuw proberen'; too_many_groups en group_full geven hun eigen zin
    - Inloggen terwijl het scherm uitgelogd openstaat: het inwisselen start vanzelf en de bewaarde code wordt gewist
    - Codeveld op de Peloton-tab: een code die geen maatjescode is maar wel een groepscode, geeft 'Je aanvraag voor <groep> ligt bij de beheerders' (of opent de groep bij status member); een code die geen van beide is geeft nog steeds 'Die code werkt niet. Hij kan verlopen zijn.'; een geldige maatjescode werkt precies als voorheen en raakt redeemGroupInvite niet aan
  </behavior>
  <action>
group_landing_screen.dart: GroupLandingScreen({required String code}) naar het model van InviteLandingScreen (ConsumerStatefulWidget; initState: ingelogd -> _start(), anders PendingInviteStore.saveGroup(code); ref.listen(currentUserIdProvider) -> bij inloggen clearGroup + _start()). Scaffold met appBar: AppBar(leading: const SafeBackButton(), title: Text(s.groupJoinTitle)). _start: gateway.redeemGroupInvite(code) -> invalidate visibleGroupsProvider -> trackEvent(ref, kEvPelotonInvite, props: {'kind': 'group_redeemed_link'}); bij status member meteen context.go('/peloton/group/$groupId') (schets: "daarna sta je op het groepsscherm"; de SafeBackButton daar valt terug op Home). Weergaven (hergebruik de _Message-vorm: icoon, titel, detail):
- uitgelogd: AppIcons.usersThree, s.groupJoinSignedOutTitle, s.groupJoinSignedOutBody, FilledButton s.groupJoinSignIn -> context.go('/profile'), TextButton s.groupNotNow -> context.go('/home'). Geen groepsnaam: die is uitgelogd niet op te vragen (zie interfaces); doccomment legt uit waarom de schets hier afwijkt.
- bezig: AppIcons.hourglass, s.groupJoining.
- aanvraag: AppIcons.hourglass, s.groupJoinRequested(naam ?? s.groupUnnamed), detail s.groupJoinRequestedHint ("Zodra een beheerder je accepteert, staat de groep op je Peloton-tab."), FilledButton s.groupOpenGroup -> context.go('/peloton/group/$groupId') (daar staat de aanvraagstaat uit plan 04).
- fout: AppIcons.linkBreak, groupErrorTextOf(s, error), TextButton s.pelotonRetry -> _start().

router.dart: GoRoute(path: '/group/:code', pageBuilder: (context, state) => _fadeTransition(state, GroupLandingScreen(code: state.pathParameters['code'] ?? ''))) naast /invite/:code, buiten de shell, met een commentaarregel naar het model van de /invite-route.

buddies_tab.dart _redeemCode: behoud de maatjespoging ongewijzigd als eerste. Faalt die, probeer dan redeemGroupInvite(raw). Succes: _invalidateAll(); status member -> context.push('/peloton/group/$groupId'); requested -> snackbar s.groupJoinRequested(naam ?? s.groupUnnamed); _codeController.clear(); trackEvent kind 'group_redeemed_code'. Faalt ook die met GroupError.inviteInvalid of een andere fout, toon dan de bestaande s.pelotonCodeInvalid (verklapt niet welke soort code bestaat, zelfde reden als het commentaar daar); alleen group_full en too_many_groups krijgen hun eigen zin, omdat de code dan aantoonbaar een geldige groepscode was. Leg in een commentaarregel vast waarom: de gedeelde groepstekst zegt "vul code in onder Ritten, tab Peloton", en op Android opent de link de PWA en niet de app.

ARB (NL / EN): groupJoinTitle "Uitnodiging voor een groep" / "Group invitation"; groupJoinSignedOutTitle "Je bent uitgenodigd voor een groep" / "You've been invited to a group"; groupJoinSignedOutBody "Log in, dan gaat je aanvraag naar de beheerders van de groep." / "Sign in and your request goes to the group's admins."; groupJoinSignIn "Log in en doe mee" / "Sign in and join"; groupNotNow "Niet nu" / "Not now"; groupJoining "Je aanvraag wordt verstuurd…" / "Sending your request…" (ellipsis-teken, geen em-dash); groupJoinRequestedHint "Zodra een beheerder je accepteert, staat de groep op je Peloton-tab." / EN; groupOpenGroup "Naar de groep" / "Go to the group". flutter gen-l10n.

Tests: test/features/group_landing_test.dart (GoRouter met '/group/:code', '/profile', '/home', '/peloton/group/:groupId' als tekst-stubs; SharedPreferences mock; currentUserIdProvider overschreven met null resp. een id; voor 'inloggen terwijl open' een StateProvider-achtige override of een ProviderContainer waarin je currentUserIdProvider wisselt, zoals de bestaande tests van InviteLandingScreen dat doen als ze bestaan). test/features/peloton_code_field_test.dart: FakeGroupGateway met failWith voor redeemFriendInvite (of een subklasse die gooit) en een instelbare redeemGroupInvite-uitkomst; de vier codeveld-cases.
  </action>
  <verify>
    <automated>flutter gen-l10n && flutter test test/features/group_landing_test.dart test/features/peloton_code_field_test.dart test/structure/ && flutter analyze && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "SafeBackButton(" lib/features/peloton/group_landing_screen.dart geeft minstens 1; back_affordance_test slaagt
    - grep -c "PendingInviteStore.saveGroup" lib/features/peloton/group_landing_screen.dart geeft 1
    - grep -c "'/group/:code'" lib/app/router.dart geeft 1
    - grep -c "redeemGroupInvite(" lib/features/peloton/buddies_tab.dart geeft 1 en grep -c "redeemFriendInvite(" blijft 1
    - flutter test test/features/group_landing_test.dart (minstens 6 tests) en peloton_code_field_test.dart (minstens 4 tests) slagen
    - firebase.json ongewijzigd (git diff --stat firebase.json leeg); in de SUMMARY staat dat de rewrite "**" en hash-routing /group/ al dekken
    - flutter analyze schoon; volledige suite groen, aantal in de SUMMARY
  </acceptance_criteria>
  <done>De groepslink werkt uitgelogd, ingelogd, na de welkomstschermen en als overgetypte code, en eindigt altijd in een aanvraag of op het groepsscherm.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL (buiten de app) -> router | De code in /group/:code is onbetrouwbare invoer |
| SharedPreferences | Bewaarde code op het toestel tot na inloggen |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-25 | Tampering | code uit de URL | mitigate | normalizeInviteCode in de gateway; database-check op het patroon; foutpad geeft een gewone zin |
| T-34-26 | Information disclosure | codeveld (maatjes vs groep) | mitigate | Onbekende code geeft altijd dezelfde zin; alleen group_full/too_many_groups (dus een geldige code) krijgen een eigen zin |
| T-34-27 | Information disclosure | bewaarde code op het toestel | accept | Zelfde model als de bestaande maatjescode; wordt gewist na elke poging |
| T-34-28 | Information disclosure | analytics bij inwisselen | mitigate | Alleen 'kind'; nooit de code |
</threat_model>

<verification>
- flutter analyze schoon; flutter test volledig groen (aantal in SUMMARY)
- firebase.json ongewijzigd, met uitleg in de SUMMARY
</verification>

<success_criteria>
Iedereen die een groepslink krijgt, komt met zo min mogelijk stappen tot een aanvraag of in de groep, ook als hij de app nog nooit geopend had.
</success_criteria>

<output>
Create `.planning/workstreams/clubs/phases/34-groep-maken-en-beheren/34-07-SUMMARY.md` when done
</output>
